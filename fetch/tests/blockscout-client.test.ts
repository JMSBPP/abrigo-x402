// fetch/tests/blockscout-client.test.ts — Plan 01-04 lands the real assertions.
import { describe } from 'vitest';

describe.todo('Blockscout v1 etherscan-compat getLogs client (Plan 01-04)', () => {
  // Will assert: topic-filtered swap-event pull against captured fixture
  // (554-log cKES/USDT response), keyset pagination, zod-validated row shape.
});
