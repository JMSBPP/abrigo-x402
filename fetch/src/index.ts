// fetch/src/index.ts
// Phase 1 skeleton entrypoint. Wave 1 plans (01-01..01-08) fill the
// implementation modules and re-export them through this barrel.
//
// Until Wave 1 lands, this file exists solely to give `tsc --noEmit`
// at least one input to type-check against (rootDir = "src" is
// declared in fetch/tsconfig.json per Plan 01-00 frontmatter
// key_links assertion).

export const FETCH_PHASE = '01-l1-data-fetch-skeleton-free-tier-discipline' as const;
