---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
current_plan: 5
status: executing
last_updated: "2026-05-27T00:37:18.198Z"
progress:
  total_phases: 8
  completed_phases: 3
  total_plans: 36
  completed_plans: 30
  percent: 78
---

# State: abrigo-x402

**Last updated:** 2026-05-25

## Project Reference

**Core value:** A pipeline that produces a calibrated joint cashflow function `C(t)` and a falsifiable DGP estimate (NHPP vs Hawkes) for a real Celo LP-aggregator protocol — using only free-tier data resources, with null results explicitly publishable.

**Current focus:** Iteration 1 = ICHI on cKES/USDT anchor pool (locked 2026-05-25 post scope-correction). Iteration 2 = Steer on cCOP/USDT (gated on Iteration 1 PDF deliverable shipping + Steer cost-leg empirical lower-bound check).

**Pipeline shape:** Seven-layer architecture (L0 protocol-spec TOML → L1 TypeScript x402-aware fetch → L2 content-addressed Parquet cache → L3 Python panel → L4 NHPP+Hawkes DGP → L5 cross-leg dependence → L6 Carr–Madan + falsification → L7 PDF report).

## Current Position

**Phase:** 3 — DGP Estimation (L4) with Boundary-Correct LR Test — **IN PROGRESS (1/9 plans landed; Wave-0 scaffold complete)**
**Current Plan:** 5
**Total Plans in Phase:** 9
**Last completed:** Plan 03-00 (Wave 0 scaffold; commits `b091e3e` + `874b140` + `6abe1e8`). Produced 9 `analysis/src/abrigo_x402/dgp/*` module stubs with canonical Wave-1 symbol names locked (`wall_clock_split`, `compute_held_out_loglik_hawkes`, `compute_held_out_loglik_nhpp`, `compute_compensator_exp_kernel`, `Q9_CI_WIDTH_THRESHOLD`, `STATIONARITY_RATIO_THRESHOLD`), `WallClockSplit` frozen dataclass (consumed by 03-07 orchestrator via `split.t_split` / `split.to_metadata()`), `InsufficientEvaluationError` exception; 9 skip-marked `analysis/tests/test_*.py` stubs (21 functions, pytest --collect-only = 102 tests across Phase 2 + Phase 3); 2 deterministic synthetic Parquet fixtures via `tick.SimuHawkesExpKernels` (HAWKES_SEED=20260526, NHPP_SEED=20260527, baseline=0.00013 events/sec/leg, 30-day end_time): `synthetic_hawkes_eta_05.parquet` (354+332 events, sha256 `3bc7eb46…`) + `synthetic_nhpp_baseline_only.parquet` (337+316 events, sha256 `5897776b…`); `synthetic_fixtures_manifest.json` records adjacency/baseline/seed/sha256/tick+polars versions; `scripts/capture_synthetic_fixtures.py` reproducible (Pitfall 9 honoured — `force_simulation=True` NOT passed); `scripts/lint_artifacts.py` extended with `FIT_REPORT_REQUIRED_KEYS` + dormant `data/fits/**/fit_report.json` walker (Phase 2 PANEL-02 still PASS). 1 Rule-1 deviation auto-fixed (reworded `lr_test.py` docstring to honour SC-3 grep gate — plan-supplied docstring literally contained the prohibited names `likelihood_ratio_test` / `chi2(1).sf` which tripped the plan's own acceptance grep). Pre-commit hooks AF-01..AF-12 PASS on all 3 commits. (Phase 2 closure detail: `02-10` real-data driver landed `data/raw/ichi/<pool>/<from>_<to>.parquet` with PANEL-02 metadata header; reality-checker note re V3 math vault_liquidity computation lives in commit `6ab4a87`.)

(Original last-completed Plan 01-08 block retained below for full chain context.)

Plan 01-08 detail: Plan 01-08 (Wave 3 — Phase 1 acceptance gate; commit `f5e3d52`). Produced `.planning/phases/01-l1-.../01-VERIFICATION-pre.md` (frontmatter `verification_pass: true`; full acceptance grid mapping FETCH-01..04 + ROADMAP SC-1..SC-6 to commands + exit codes + verdicts; SC-4 Parquet → JSONL pivot acknowledged per checker I6; library-drift v1/v2 inventory carried from Plan 01-07; deferred-items rolled up — single 01-03→01-05 entry resolved) + `01-08-SUMMARY.md` thin pointer per checker M15. Full Phase-1 suite at end of 01-08: **11 passed / 0 skipped / 80 tests passing / 0 fail**; `tsc --noEmit` clean; `pnpm install --frozen-lockfile` clean; `make leak-check` / `make schema-frozen-check` / `make schema-probe` all PASS. CLI dry-run JSON: `total_queries=795 < earmark=30000`, `exceeds_earmark=false`, `head_source="snapshot"` (proves dry-run mechanically cannot escalate to live). One acceptance-cmd substitution: `make verify-cache-idempotency` network leg → `tests/cache.test.ts` unit-level byte-identity (18/18 PASS) per plan body line 158 Tier-1 substitute (pnpm fetch ↔ pnpm built-in collision documented; Makefile rewrite deferred to Phase 2). I11 regex acceptance: `grep -cE "FETCH-0[1234]|SC-[1-6]"` = 33 hits (>= 6 required). No outstanding gaps. Previously completed: Plan 01-07 (Wave 2 parallel — x402 mock server + client bridge + integration test: `fetch/src/x402-mock/server.ts` + `fetch/src/x402-mock/client-bridge.ts` + `fetch/tests/x402_mock.test.ts`; commits `4718946`+`27176ce`+`e62b694`).

(Original last-completed Plan 01-07 block retained below for full chain context).

Plan 01-07 detail: Three atomic 01-07 commits: `4718946` (feat — node:http 402 mock with x402 v2 PaymentRequirements + structural X-PAYMENT validation + X-PAYMENT-RESPONSE settlement echo) / `27176ce` (fix — v1-protocol named-network drift adaptation; STACK drift discovered via live probe: @x402/evm v2.13 ExactEvmSchemeV1 registers on NAMED networks like 'base-sepolia', NOT CAIP-2 'eip155:84532' — plan-body proposal overridden; added `extra: {name: 'USDC', version: '2'}` to PaymentRequirements for EIP-712 domain, load-bearing for ExactEvmScheme.createPaymentPayload) / `e62b694` (feat — client bridge wires @x402/fetch x402Client + @x402/evm/exact/client registerExactEvmScheme with viem privateKeyToAccount; 3-test integration suite: bare 402 → PaymentRequirements / wrapFetchWithPayment round-trip → 200 + 32-byte tx hash / malformed-network rejection with errorReason; cost-ledger row written for x402-mock-sepolia endpoint with paid_real=false, chain='base-sepolia'). 3 new vitest tests green; full Phase-1 suite at end of 01-07: **11 passed / 0 skipped / 80 tests passing / 0 fail**; `tsc --noEmit` clean. 2 Rule-3 deviations auto-fixed (both library wire-format drifts the plan body explicitly authorized adapting). Header-only mode is the Phase-1 default; X402_MOCK_REAL_SETTLE=1 env-toggle reserved for manual developer-machine validation against a faucet-funded Base Sepolia wallet (not exercised in CI). Previously completed: Plan 01-06 (Wave 2 — FETCH-02 SC-6 dry-run budget estimator + CLI ichi/steer subcommand wiring; commits `f8b7df8`+`388379f`+`1d8c515`+`1321d11`); Plan 01-05 (FETCH-01 blockscout + decoder + dormant subgraph + SC-5 leak gate; commits `3d7bb04`+`b24f8d5`+`969676d`+`d3f1c64`); Plan 01-03 (FETCH-03 freshness wrappers; commits `d48b51f`+`ceec9c4`+`92eab70`+`c477604`); Plan 01-04 (FETCH-04 content-addressed cache; commits `e394cf1`+`5129ef7`+`a1daf97`+`0496df8`); Plan 01-02 (cost-ledger + 90k budget gate; commits `b951b22`+`e569d2f`+`729a17e`); Plan 01-01 (stack-pins + protocol-spec + viem-clients); Plan 01-00 (workspace bootstrap).
**Status:** Ready to execute

```
Progress: [████████░░] 78% (28/36 plans complete; 9/9 Phase-1 + 10/10 Phase-2 + 1/9 Phase-3 + 7/7 Phase-0 + 1/1 Phase-init; 3/8 phases complete; Phase 3 Wave-1 unblocked)
```

## Performance Metrics

| Metric | Target | Current |
|--------|--------|---------|
| Phases complete | 8/8 | 1/8 |
| Iteration 1 PDF deliverable | `reports/ichi.pdf` shipped | Not started |
| Iteration 2 swap-surface invariant | `grep -r "ichi" fetch/src analysis/src` returns 0 hits | N/A (no source code yet) |
| Free-tier query budget | < 90k Graph queries/mo (soft cap) | 0 consumed |
| v1 requirements complete | 32/32 | 9/32 (GOV-01, GOV-02, GOV-03, DEMAND-01, FETCH-01, FETCH-02, FETCH-03, FETCH-04, REPRO-04) |
| Phase 00-candidate-eligibility-pre-registration P03 | 15min | 1 tasks | 1 files |
| Phase 00 P04 | 4min | 1 tasks | 1 files |
| Phase 00-candidate-eligibility-pre-registration P01 | 30min | 1 tasks | 2 files |
| Phase 00-candidate-eligibility-pre-registration P02 | 4min | 2 tasks | 1 files |
| Phase 00-candidate-eligibility-pre-registration P05 | 5min | 3 tasks | 2 files |
| Phase 00 P06 | 5min | 3 tasks | 27 files |
| Phase 00 P07 | 30min | 3 tasks | 4 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P00 | 7min | 4 tasks | 23 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P02 | 3min | 2 tasks | 4 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P04 | 3min | 2 tasks | 4 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P01 | 7min | 3 tasks | 9 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P03 | 4min | 2 tasks | 3 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P05 | 6min | 2 tasks | 6 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P06 | 3min | 2 tasks | 4 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P07 | 6min | 2 tasks | 3 files |
| Phase 01-l1-data-fetch-skeleton-free-tier-discipline P08 | 4min | 1 tasks | 2 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P00 | 9min | 3 tasks | 23 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P03 | 3min | 1 tasks | 2 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P01 | 9min | 2 tasks | 5 files |
| Phase 02 P05 | 3min | 1 tasks | 2 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P07 | 3min | 2 tasks | 4 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P04 | 5min | 2 tasks | 4 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P02 | 3min | 1 tasks | 4 files |
| Phase 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor P06 | 8min | 2 tasks | 5 files |
| Phase 02 P08 | 4min | 2 tasks | 7 files |
| Phase 02 P09 | 333s | 1 tasks | 2 files |
| Phase 03 P00 | 5min | 3 tasks | 24 files |
| Phase 03 P01 | 3min | 1 tasks | 2 files |
| Phase 03 P02 | 7min | 1 tasks | 4 files |

## Accumulated Context

### Key Decisions Locked (from PROJECT.md scope-correction block)

- Iteration 1 = ICHI on cKES/USDT anchor pool
- Iteration 2 = Steer Protocol on cCOP/USDT (conditional on cost-leg empirical lower bound)
- Tail-risk reframe: USDT depeg + USDT/USDC basis risk (NOT USDC depeg)
- Minteo COPM allowed into scope under controlled-broadening flag
- Cost-leg = indexer-backed analytics/UI queries only (Forno RPC keeper polling excluded)
- MiniPay-by-preference filter retired (anti-correlated with FX-hedge thesis)
- Myriad / Halo excluded (primary-source disqualification)
- **Plan 00-04 SCHEMA-FROZEN BASELINE** [LOCKED 2026-05-25, commit `e9b214d` (full SHA `e9b214dcb26d7a6085aa98765a3f8816950495eb`)]: `protocols/_schema.toml` is the frozen baseline for `make schema-frozen-check`. `data_cost_class` enum = `[indexer-analytics-queries, per-event-oracle-stretch, per-scan-ocr-stretch]`. `mixing_class` enum = `[mento-native, minteo-fintech, mento-bridged]`. Demand-window scope (DEMAND-01) and AF-12 silent re-scope defense encoded in schema. Closes DEMAND-01 + GOV-03.
- **Plan 00-01 PRE-REGISTRATION LOCKED** [LOCKED 2026-05-25, commit `6cd61ed`]: `notes/PRE_REGISTRATION.md` (160 lines) commits all pre-fit thresholds — kernel forms (Kirchner INAR(p) NHPP + bivariate HawkesExpKern off-diagonal); priors (α=0.01, η≥0.2, rate_per_event grid (1,5,10), USD_per_query $5e-6 ±50%); bootstrap-LR 50:50 χ²(0):χ²(1) mixture; PITFALLS §4 four-criterion gate; REPRO-03 two-tier threshold (PASS ≥100k/mo, STRADDLE 30-100k marginal-demand, FAIL <30k below-window); Q-9 V3-only-primary + V3+V4+Broker-unified-fallback (trigger: sample<300 OR CI>0.4, AND cross-class permutation p>0.05); Q-7 floor (TVL<$10k OR events<30/30d); deferred substrate (COPM, cXOF/USDm, BRLm/EURm, ~38 non-anchor ICHI vaults); condition-4 reparam to USDT depeg + USDT/USDC basis (NOT USDC depeg) per CLAUDE.md. AF-03 audit anchor. Closes GOV-01 + REPRO-04.
- **Plan 00-02 PHASE_0_GATE LOCKED** [LOCKED 2026-05-25, commit `a669d37`]: `notes/PHASE_0_GATE.md` (117 lines) commits five-check eligibility outcome per candidate. **ICHI on cKES/USDT = PASS verbatim per CANDIDATES §4.1** (with §7.3 thinness retraction upgrading check 4 from BORDERLINE to PASS) — eligible for Iteration 1 anchor. **Steer on cCOP/USDT REPRO-03 verdict = STRADDLE** (30k–100k Celo-attributable Graph queries/mo per Phase-0 three-channel triangulation: Blockscout factory enumeration (6 active in-scope vaults), Steer architecture (Gelato keeper-RPC class excluded per DEMAND-01), DefiLlama TVL extrapolation ($855.65 = 0.0041% of $20.64M multi-chain across 42 chains)). Fires HEDGE-05 memo-only null with `marginal-demand` flag at Phase 0; Iteration 2 does NOT run Phase 1–5 cycle. `<SCHEMA_BASELINE_COMMIT>` placeholder embedded for Plan 07 substitution (target: schema baseline `e9b214dcb26d7a6085aa98765a3f8816950495eb`). Closes GOV-02 + DEMAND-01 (verify component).
- **Plan 00-05 PROTOCOL-SPEC TOMLS LOCKED** [LOCKED 2026-05-25, commits `aa2fcc8` (ichi.toml) + `24d054b` (steer.toml)]: `protocols/ichi.toml` (299 lines, 27 vault rows — 10 verified + 17 pending zero-address placeholders; 1 active anchor cKES_USDT_anchor at `0xe304b9...4176F` mento-native; 2 COPM Minteo vaults at `0x9F2bB8...FFce8` + `0xB52CfF...3FBF5` v2-deferred minteo-fintech; full ICHI Celo footprint enumeration deferred to Phase-1 factory-log pagination per [enumeration_status] block). `protocols/steer.toml` (166 lines, 6 vaults all verified) — Iter-2 stub; cCOP/USDT anchor `0x2AC5ba...17B0`; factory `0x116Dba...014C`; `panel_construction = "v3-anchor-only"`; `phase_0_repro_03_verdict = "STRADDLE"`; `phase_0_repro_03_flag = "marginal-demand"`; `hedge_05_fires = true`; Q-9 fallback metadata embedded verbatim from Q9_DECISION.md (sample_floor=300, ci_width_floor=0.4, permutation_reps=1000, p_threshold=0.05; V4 PoolManager `0x288dc8...87BC`; Mento V2 Broker `0x777A82...4CaD`). AF-12 silent re-scope defense active at L0; M12 verified-before-fetch invariant enforced. Closes GOV-03 + REPRO-04 at the L0 protocol-spec layer.
- **Plan 00-07 PRE-COMMIT HOOKS INSTALLED + VALIDATED** [LOCKED 2026-05-25, commits `59f43f7` + `b68cefa` + `13ccdf6` + `09e9b1a` + `d87abef` + `3d2af6b`]: pre-commit v4.6.0 installed via `uv tool install pre-commit`; `.git/hooks/pre-commit` dispatcher active; `notes/PHASE_0_GATE.md` schema-baseline placeholder substituted with `e9b214dcb26d7a6085aa98765a3f8816950495eb` (full SHA of `protocols/_schema.toml` Phase-0 commit); `make schema-frozen-check` exits 0 with PASS. 9 negative-case hook validations passed (Tests 1-9 in 00-07-SUMMARY.md): all 7 active AF fixtures (AF-01 mock-data, AF-03 structural-passthrough, AF-04 invalid-mixing-class, AF-06 strip-without-gate, AF-08 dashboard-dir, AF-10 Dune-Plus-permanently-active, AF-12 silent-re-scope) plus review-trail + schema-frozen demonstrably trigger exit-nonzero on their violating fixtures and exit-0 after cleanup. **3 Rule-1 hook auto-fixes applied during validation:** (1) AF-01 filename-pattern check added (`find -name "*fake_panel*"` etc.) so binary parquet fixtures are detected — original content-grep was blind to binary files; (2) AF-03 empty-operand guard added (`:-0` defaults on PRE_REG_TS / ANALYSIS_FIRST_TS) to prevent `integer expected` stderr noise when `analysis/src` exists without git history; (3) `review_trail.sh` + `.pre-commit-config.yaml` regex broadened from `^.planning/(.*/)?PLAN.md$` to `^.planning/.*PLAN.md$` so GSD convention `00-NN-PLAN.md` filenames are matched (original regex would have silently skipped enforcement on all actual plan files). **AF-10 `.env.violating` fixture is permanently active in repo by design (C2);** workflow for future commits: temporarily remove fixture + commit + `--no-verify` restore, OR `--no-verify` with documented rationale. Closes GOV-03 enforcement at the runtime pre-commit-gate layer. **Phase 0 complete: 7/7 plans landed.**
- **Plan 00-06 PRE-COMMIT INFRASTRUCTURE LOCKED** [LOCKED 2026-05-25, commits `fc653e8` (Makefile) + `ec5c492` (scripts/pre-commit/) + `13a7c99` (.pre-commit-config.yaml + 12 AF fixtures)]: Three-layer hook scaffolding for GOV-03 + review-trail + schema-frozen invariant. `.pre-commit-config.yaml` deploys 3 local-repo hooks via `pre-commit install` (Plan 00-07). `Makefile` with `schema-frozen-check` target reads `<SCHEMA_BASELINE_COMMIT>` from notes/PHASE_0_GATE.md and runs `git diff <baseline> -- protocols/_schema.toml`; defers to no-op while placeholder unsubstituted. `scripts/pre-commit/af_lint.sh` (154 lines) covers all 12 AFs — 7 active (AF-01, AF-03, AF-04, AF-06, AF-08, AF-10, AF-12) + 5 Phase-3+ deferred passthroughs (AF-02, AF-05, AF-07, AF-09, AF-11). C2 fix: AF-10 grep excludes `tests/unit` not `tests/`, so af_10_dune_plus/.env.violating IS detected by design. C3 fix: AF-12 uses `git cat-file -e HEAD:$f` for initial-commit handling. M13 fix: AF-08 find excludes `./tests/*`. `review_trail.sh` (76 lines) enforces paired `_reality_checker.md` + `_code_reviewer.md` with `## VERDICT` first H2; `--allow-revision` overrides NEEDS REVISION but never BLOCKER. `schema_frozen.sh` (4 lines) wraps `make schema-frozen-check`. 12 AF fixture dirs + 2 auxiliary (af_review_trail_missing + af_schema_frozen_diff). `tests/fixtures/README.md` (70 lines) resolves the AF-04 label drift: FEATURES.md "Hand-tuned bin width for INAR(p)" is canonical for *labels*; active hook check enforces REQUIREMENTS.md GOV-03 mixing_class enum interpretation since INAR(p) code is Phase-3+ deferred. Hooks NOT installed in this plan; Plan 00-07 runs install + validates each hook via negative-case tests. Closes GOV-03 operationalization at the pre-commit layer.
- **Plan 01-05 FETCH-01 BLOCKSCOUT V1 CLIENT + SWAP DECODER + DORMANT SUBGRAPH + SC-5 LEAK GATE LOCKED** [LOCKED 2026-05-26, commits `3d7bb04` (test RED blockscout+decoder) + `b24f8d5` (feat GREEN — 9/9 tests; v1 etherscan-compat URL pinned; auto-pagination via block-cursor keyset; viem decodeSwap typed; topic0 = 0xc42079...cca67 verbatim) + `969676d` (test RED protocol-agnostic+subgraph dormant) + `d3f1c64` (feat GREEN subgraph client — read GRAPH_API_KEY at call time per checker I8; MissingGraphApiKeyError on unset)]: `fetch/src/blockscout/v1-getlogs.ts` uses v1 etherscan-compat `/api?module=logs&action=getLogs` (NOT v2 — v2 rejects topic0 with HTTP 422 per RESEARCH §C); auto-pagination via block-cursor keyset (`fromBlock = parseInt(lastLog.blockNumber, 16) + 1`) on 1000-row cap; zod-validated response shape; fetcher override for offline tests. `fetch/src/decoders/uniswap-v3-swap.ts` UniswapV3SwapTopic0 verified against RESEARCH §D; viem `decodeEventLog` wrapper returning typed args `{sender, recipient, amount0, amount1, sqrtPriceX96, liquidity, tick}`. `fetch/src/subgraph/client.ts` DORMANT BY DEFAULT per orchestrator finding #2 + RESEARCH §A — `getSubgraphClient(deploymentId)` throws `MissingGraphApiKeyError` when `GRAPH_API_KEY` unset/empty; reads env at CALL TIME (NOT module-load) so `vi.stubEnv` tests work with natural static-import pattern. `fetch/tests/protocol-agnostic.test.ts` FETCH-01 SC-5 LEAK GATE: recursive grep over `fetch/src/` rejects protocol-name branches (`config.name === ichi/steer`), factory addresses (0x9FAb…418F ICHI + 0x116Dba…014C Steer), magic fee-tier literals (`fee=0.0001|100|500|3000|10000`) — offender count = 0; synthetic `test_fixture.toml` (name=synthetic-test, fee_tier=7777) loads via SAME `loadProtocol` code path as `ichi.toml` (proves loader has no name-based or magic-number branches). 1 Rule-1 deviation auto-fixed: corrected plan-supplied test assertion `fromBlock=67_502_983` → `67_508_615` (0x4061986 = 67_508_614, not 67_502_982). 15 new vitest tests added (9 blockscout+decoder + 6 protocol-agnostic+subgraph); full Phase-1 suite at end of 01-05: **8 passed / 3 skipped / 69 tests passing / 0 fail**. `pnpm -C fetch exec tsc --noEmit` clean; `make leak-check` PASS. **Wave 2 unblocked**: 01-06 imports `{ getLogsV1, BlockscoutLog }` + `{ UniswapV3SwapTopic0, decodeSwap }` + `{ getSubgraphClient, MissingGraphApiKeyError }` (kept dormant); CLI gates subgraph calls behind env check. **Wave 3 unblocked**: 01-08 inherits SC-5 leak-gate as defense-in-depth complement to `make leak-check`. Phase 1.5 enrichment path: provision `GRAPH_API_KEY` + add `[subgraphs.uniswap_v3]` block to `protocols/ichi.toml` to flip subgraph leg live (no code change in 01-05 modules). Closes FETCH-01 SC-5 acceptance criterion at unit-test level; CLI-level integration deferred to 01-06+01-08.
- **Plan 01-03 FETCH-03 FRESHNESS WRAPPERS LOCKED** [LOCKED 2026-05-26, commits `d48b51f` (test RED subgraph) + `ceec9c4` (feat GREEN subgraph) + `92eab70` (test RED blockscout) + `c477604` (feat GREEN blockscout)]: `fetch/src/subgraph/freshness.ts` (72 lines) exports `subgraphFreshness<T>({response, forno}, threshold=100)` + `SubgraphLagError` — endpoint-agnostic generic-T response shape compares `response._meta.block.number` vs `await forno.getBlockNumber()` and throws on lag > threshold OR missing `_meta` (sentinel subgraph_block = -1). `fetch/src/blockscout/freshness.ts` (60 lines) exports `blockscoutFreshness({most_recent_log_block, forno, endpoint, threshold=100})` + `BlockscoutFreshnessError` — checks lag-vs-Forno ONLY (NO `block_consensus` field per RESEARCH §H + orchestrator finding #1; the per-log consensus field is ABSENT from both v1 `/api?module=logs&action=getLogs` AND v2 `/api/v2/addresses/{addr}/logs` per live API probe in RESEARCH §C). Both wrappers default threshold = 100 blocks ≈ 100s at Celo 1s/block (REPRO-02-compatible margin over typical 10-30-block indexer reorg buffers). `fetch/tests/freshness.test.ts` rewritten from Plan 01-00 describe.todo placeholder to 109 lines with BOTH `describe('subgraphFreshness …')` (6 tests) AND `describe('blockscoutFreshness …')` (3 tests) blocks in a SINGLE file (checker C4 invariant), `fornoMock` imported from `./_helpers` (Plan 01-01) and NEVER redeclared locally. 9/9 vitest tests green; `pnpm -C fetch exec tsc --noEmit` exits 0; `grep -r "block_consensus" fetch/src fetch/tests` returns 0 hits. 0 plan deviations. One out-of-scope item logged to `.planning/phases/01-l1-…/deferred-items.md`: pre-existing failure in `fetch/tests/blockscout-client.test.ts:150` (cursor-advance expects `fromBlock=67502983` receives `67508615`) — Plan 01-05's territory, not 01-03's. **Wave 1b complete for 01-03 leg**: 01-05 imports `{ blockscoutFreshness, BlockscoutFreshnessError }` to wrap every `/api?module=logs&action=getLogs` response; 01-06 threads each response through `blockscoutFreshness({ most_recent_log_block: response.result.at(-1).blockNumber, forno: celoClient, endpoint })` before handing off to parser; subgraph leg wired but Phase-1-disabled by default (GRAPH_API_KEY not provisioned; Phase 1.5 retroactive enrichment activates). Closes FETCH-03 SC-3.
- **Plan 01-04 FETCH-04 CONTENT-ADDRESSED CACHE LOCKED** [LOCKED 2026-05-26, commits `e394cf1` (test RED key) + `5129ef7` (feat GREEN key) + `a1daf97` (test RED manifest+payload) + `0496df8` (feat GREEN manifest+payload)]: `fetch/src/cache/key.ts` exports `canonicalize()` via explicit string template literal (NOT `JSON.stringify` with property-filter array — **checker C2 mechanical byte-stability**) returning `{"blockRange":[lo,hi],"chainId":N,"contractAddress":"lc-addr"}` and `cacheKeyHash()` = sha256(canonical) as 64-char lowercase hex. `fetch/src/cache/manifest.ts` ships zod `ManifestEntrySchema` (`EndpointEnum`: graph-mainnet / graph-sepolia / blockscout / forno / x402-mock-sepolia; `usdc_cost` as decimal-string never JS-number), atomic rename-from-tmp write, idempotent `appendManifestIfNew()` on `cache_key_hash` collision, `findByCacheKey()` for Plan 01-06 cache short-circuit. `fetch/src/cache/parquet-writer.ts` ships deterministic JSONL `writeCachePayload()` to `data/raw/<protocol>/<hash[0:2]>/<hash>.jsonl` (256-way prefix fanout) returning `dataHash = sha256(file content)` for Plan 01-08 `make verify-cache-idempotency` sha256sum cmp. **`fetchTimestamp` is MANIFEST METADATA only (NEVER in canonical key)** per FETCH-04 paid-step-is-idempotent invariant. 18 vitest tests green (7 key + 7 manifest + 4 payload) including literal-byte-content reference-string assertion (checker C2) and schema-level proof that `canonicalize()` output excludes `fetchTimestamp` / `dataHash` / `gitCommit`. `pnpm -C fetch test tests/cache.test.ts --run` = 18 PASS; `pnpm -C fetch exec tsc --noEmit` exits 0. JSONL-now / Parquet-later (Phase-2 polars batch-converts at ingest) consistent with cost-ledger Plan 01-02 choice — isolates binary-format risk to Python. 0 plan deviations. **Wave 2 unblocked**: 01-06 imports `{ cacheKeyHash, canonicalize }` + `{ appendManifestIfNew, findByCacheKey, type ManifestEntry }` + `{ writeCachePayload }`; cache short-circuit pattern = `findByCacheKey(hash)` BEFORE network call. **Wave 3 unblocked**: 01-08 sha256sum cmp uses dataHash field returned by writeCachePayload as the byte-identity oracle. **Known artifact**: commit `a1daf97` swept in unstaged parallel-wave files (01-01 stack-pins, 01-02 cli.ts stub + _helpers.ts) via the pre-commit hook's stash-restore-then-add cycle — these are pre-existing parallel-wave content with no impact on FETCH-04 correctness; 01-01 / 01-02 executors will re-commit them in their own scopes.
- **Plan 01-02 COST-LEDGER + 90K BUDGET GATE LOCKED** [LOCKED 2026-05-26, commits `b951b22` (test RED) + `e569d2f` (feat GREEN) + `729a17e` (test Task 2 + .gitignore)]: `fetch/src/cost-ledger.ts` (179 lines) exports `appendLedger(row, path?)`, `readLedger(path?)`, `checkBudget(opts)`, `GraphBudgetExceededError`, `CostLedgerRowSchema` + types, `EndpointEnum` + `ChainEnum`. Endpoint enum = `[graph-mainnet, graph-sepolia, blockscout, forno, x402-mock-sepolia]` (CONTEXT.md FETCH-02 schema). 90k/mo soft cap counts only `endpoint='graph-mainnet'` rows in current UTC month; blockscout/forno/x402-mock-sepolia/graph-sepolia uncapped. `--force` via `checkBudget({force:true})` returns `{would_exceed:true}` without throwing. JSONL on-disk format (one JSON object per line via `fs.appendFile`, POSIX-atomic for <PIPE_BUF payloads ~400B/row) — chosen over Parquet per orchestrator finding #5 (hyparquet-writer small-write profile adds binary-format risk without empirical benefit at Phase-1 volumes; Phase 2/5 may batch-convert via polars). Month-boundary uses explicit `Date.UTC(year, month, 1)` construction per checker M12 (avoids `setUTCMonth(-1)` January-rollover ambiguity). Default ledger path = `data/raw/_cost_ledger.jsonl`; auto-creates parent dir on first append. Default cap = 90_000 (10k headroom from Graph's 100k/mo free tier for ad-hoc Subgraph Studio probes). `fetch/tests/cost-ledger.test.ts` (205 lines, 9 tests passing): 4 append/read round-trip + 5 budget-gate (empty-ledger pass, 85k+6k throws GraphBudgetExceededError, --force bypass, blockscout uncapped, last-month rows excluded). Day-15 clamp in graphRow test helper avoids May 31 + monthOffset=-1 normalizing to May 1 ("April 31" → May 1) which would land back in current month. 2 Rule-3 deviations: removed empty `fetch/src/cost-ledger/` dir to disambiguate ESM resolution between `cost-ledger.ts` file vs `cost-ledger/index.ts`; added `fetch/data/` to `.gitignore` because vitest cwd resolves test paths `data/raw/*.test.jsonl` to `fetch/data/raw/` rather than repo-root `data/raw/`. `pnpm -C fetch tsc --noEmit` exits 0. Closes FETCH-02. Downstream unblocked: 01-04 Blockscout client logs blockscout rows; 01-06 CLI calls `checkBudget` before any Graph fetch + surfaces `--force` flag; 01-07 x402 mock logs `paid_real=false chain='base-sepolia' endpoint='x402-mock-sepolia'` rows for each round-trip (counts toward audit trail but NOT toward 90k cap).
- **Plan 01-01 STACK-PINS + PROTOCOL-SPEC + VIEM-CLIENTS LOCKED** [LOCKED 2026-05-26, commits `a1daf97` (Task 1 — content correct, scope-label drifted to `test(01-04):` due to parallel-execution race) + `6f0d500` (feat protocol-spec + ichi.toml I5-gate field) + `c799049` (feat viem-clients)]: `fetch/src/cli.ts` Phase-1 entrypoint stub (`--version` prints 0.1.0 exit 0; `ichi|steer` exits 2 stub; usage exits 1); `fetch/src/protocol-spec.ts` `loadProtocol(path)` via @iarna/toml parse + zod ProtocolSpecSchema validate (mirrors `_schema.toml` enums: data_cost_class / mixing_class / address_resolution_status; HexAddr regex; cold_backfill_from_block optional non-negative int; VaultSchema.pool_address OPTIONAL per Rule 1 fix — COPM Minteo vaults wrap underlying_token without V3 pool; M12 verified-before-fetch is runtime guarantee for active=true rows); `fetch/src/viem-clients.ts` cached module-level singletons `celoClient` (chain 42220 Forno) + `baseSepoliaClient` (chain 84532) + factory aliases createCeloClient / createBaseSepoliaClient (type annotations dropped per Rule 1 fix — viem chain-narrows return type; annotating as `PublicClient` triggers TS2719); `fetch/tests/_helpers.ts` exports `testDirname(import.meta.url)` (ESM __dirname) + `fornoMock(head)` (PublicClient mock factory) — load-bearing for Wave 1b plans 01-03 + 01-05; `fetch/tests/stack-pins.test.ts` (9 tests pass — 8 pinned-version contracts + semver check, rejects ^/~ prefixes); `fetch/tests/protocol-spec.test.ts` (4 tests pass — ichi load + synthetic fee_tier=7777 load + invalid-enum + missing-file); `fetch/tests/viem-clients.test.ts` (5 tests pass — chain.id + singleton identity + factory == singleton); `fetch/tests/fixtures/test_fixture.toml` + `test_fixture_bad.toml` synthetic fixtures (test_fixture.toml also consumed by Plan 01-05 protocol-agnosticism scan). **I5 gate resolution**: `cold_backfill_from_block = 60000000` added to `protocols/ichi.toml` `[protocol]` table (per-protocol field, NOT in `_schema.toml`); `make schema-frozen-check` PASS verified after edit (baseline `e9b214d` unchanged); NOT a Phase-0-style schema increment — schema-frozen baseline intact; value is conservative placeholder (~90 days before Forno head 67,896,653 at Celo 1s/block), Plan 01-04 may refine downward to exact ICHI factory deployment block. Total tests added: 18 (9+4+5). Full suite at end of plan: 45 pass / 6 skipped / 0 fail. `pnpm -C fetch exec tsc --noEmit` exits 0. 4 Rule-1/2/3 deviations auto-fixed (VaultSchema.pool_address optional; viem type annotation drop; I5-gate field add; frontmatter files_modified incomplete vs plan body). **Wave 1b unblocked**: 01-03 imports `{ testDirname, fornoMock }` + `{ celoClient }`; 01-04 imports `{ loadProtocol, ProtocolSpec }` + reads `cold_backfill_from_block` from ichi.toml; 01-05 imports `{ loadProtocol, testDirname }` + uses test_fixture.toml; 01-06 reads `cold_backfill_from_block` for dry-run; 01-07 imports `{ baseSepoliaClient }`. Closes FETCH-01 SC-1.
- **Plan 01-06 FETCH-02 SC-6 DRY-RUN BUDGET ESTIMATOR + CLI WIRING LOCKED** [LOCKED 2026-05-26, commits `f8b7df8` (test RED budget) + `388379f` (feat GREEN budget) + `1d8c515` (test RED CLI) + `1321d11` (feat GREEN CLI)]: `fetch/src/budget.ts` (87 lines) exports pure `estimateBudget(spec, fornoHead, earmark=30_000) -> BudgetEstimate` (RESEARCH §M cold-backfill formula: blocks_per_vault = max(0, head - cold_backfill_from_block); subgraph = ceil(blocks/10k); blockscout = ceil(swaps_per_30d/1000); total = vault_count * (sub+bs)/vault). `exceeds_earmark` gates ONLY the graph leg (blockscout uncapped per FETCH-02); `recommended_reallocation` mentions BOTH "reserve" (45k reserve) AND "force" (CLI --force flag) so operator knows both operational levers. `fetch/src/cli.ts` (158 lines, REWRITTEN from 01-01 stub) implements full ichi|steer subcommand with parseArgs / 4-tier resolveHead / cache short-circuit / budget gate / exit-code conventions (0=ok, 1=usage, 2=GraphBudgetExceededError, 99=fatal). **Checker C3 head-derivation precedence** (NEVER touches network on dry-run): (1) FETCH_HEAD_OVERRIDE env > (2) loadFornoHeadSnapshot() from notes/forno_head_snapshot.json > (3) DRY_RUN_FALLBACK_HEAD from fetch/src/constants.ts > (4) celoClient.getBlockNumber() — `head_source` field surfaced in every JSON emit ('env_override'|'snapshot'|'fallback'|'live'); dry-run mechanically cannot escalate to 'live'. JSON output augmented with cache_hit + dry_run + head_block + head_source. **Verbatim ICHI Iter-1 dry-run output** at head=67_896_653: `{protocol:"ichi", iteration:1, vault_count:1, blocks_per_vault:7_896_653, queries_per_vault:795, total_queries:795, exceeds_earmark:false, earmark:30_000, recommended_reallocation:null, cache_hit:false, dry_run:true, head_block:67_896_653, head_source:"snapshot"}`. Per checker I9: 50-vault synthetic test round-trips through `ProtocolSpecSchema.parse(rawSynth)` BEFORE `estimateBudget(synth, ...)` consumes it — catches schema regressions in tests themselves. 8 vitest tests added (3 unit + 5 process-level CLI integration); full Phase-1 suite at end of 01-06: **10 passed / 1 skipped / 77 tests passing / 0 fail**. `pnpm -C fetch exec tsc --noEmit` exits 0. 0 plan deviations (implementation was correct verbatim from plan; budget.ts + cli.ts compiled clean on first GREEN). **pnpm command-name collision** documented: `pnpm -C fetch fetch ichi ...` triggers pnpm's built-in `fetch` (unknown-options); invokers MUST use `pnpm -s -C fetch run fetch ichi ...` or spawn `tsx src/cli.ts` directly. **Wave 3 unblocked**: Plan 01-08 extends `cli.ts` non-dry-run path with full Blockscout v1 getLogs + cache write + cost-ledger append; the 4-tier head resolver, parseArgs, and exit-code conventions established here remain intact; sha256sum cmp uses `dataHash` field from 01-04 writeCachePayload as byte-identity oracle. **Phase 2+ inherits**: `head_source` field is now part of CLI contract → propagate onto Parquet rows as `data_source` provenance; `cold_backfill_from_block` is required for any protocol shipped to 01-06 dry-run (`protocols/ichi.toml` has 60_000_000; Iter-2 Steer stub does NOT — Plan 01-08 will surface error). Closes FETCH-02 SC-6 (the dry-run estimator deliverable on top of Plan 01-02's cost-ledger).
- **Plan 01-07 X402 MOCK + CLIENT BRIDGE + ROUND-TRIP INTEGRATION TEST LOCKED** [LOCKED 2026-05-26, commits `4718946` (feat — node:http 402 mock with x402 v2 PaymentRequirements + structural X-PAYMENT validation + X-PAYMENT-RESPONSE settlement echo, ISO-8601 timestamp body) + `27176ce` (fix — v1-protocol named-network drift adaptation; live probe of @x402/evm v2.13 `client.createPaymentPayload(requirements)` threw `"No network/scheme registered for x402 version: 1"` on CAIP-2 'eip155:84532' because ExactEvmSchemeV1 registers on NAMED networks ('base-sepolia', 'base', 'ethereum', …); plan-body CAIP-2 form overridden; added `extra: {name: 'USDC', version: '2'}` to PaymentRequirements for EIP-712 domain — load-bearing for ExactEvmScheme.createPaymentPayload TransferWithAuthorization signing) + `e62b694` (feat — client bridge: `makeFetchWithPayment(opts)` wires `@x402/fetch` x402Client + `@x402/evm/exact/client` registerExactEvmScheme with viem `privateKeyToAccount(pk)` signer; TEST_DEFAULT_PK = 0x{31 zero bytes}01 deterministic test key (address 0x7E5F4552091A69125d5DfCb7b8C2659029395Bdf, no funds); 3-test integration suite green: (1) bare 402 → PaymentRequirements (network='base-sepolia', asset=Base Sepolia USDC, extra={name:'USDC',version:'2'}); (2) wrapFetchWithPayment round-trip → 200 + X-PAYMENT-RESPONSE base64 decoded to {success:true, transaction:/^0x[0-9a-f]{64}$/, network:'base-sepolia', payer:0x…} + cost-ledger row written for x402-mock-sepolia endpoint (chain='base-sepolia', paid_real=false, cost_usdc='0.001'); (3) malformed X-PAYMENT (wrong named network 'ethereum') → 402 with X-PAYMENT-RESPONSE.errorReason containing 'network')]: `fetch/src/x402-mock/server.ts` (162 lines, node:http) exports `startMockServer(port=0)` (ephemeral port via `server.listen(0)`; no port collision with parallel test files), `validateXPaymentHeader(b64)` (structural validator: scheme==='exact' + network==='base-sepolia' + sig matches /^0x[0-9a-fA-F]{130}$/ + payer matches /^0x[0-9a-fA-F]{40}$/), `PAYMENT_REQUIREMENTS` (x402 v1 envelope). `fetch/src/x402-mock/client-bridge.ts` (56 lines) exports `makeFetchWithPayment(opts)` + lazy `fetchWithPayment` (constructed at first call so vi.stubEnv-set PRIVATE_KEY overrides apply in tests). `fetch/tests/x402_mock.test.ts` rewritten from describe.todo stub. 3 new vitest tests; full Phase-1 suite at end of 01-07: **11 passed / 0 skipped / 80 tests passing / 0 fail**; `tsc --noEmit` clean. 2 Rule-3 deviations auto-fixed (both library-wire-format drift adaptations the plan body explicitly authorized: "If `@x402/fetch` 2.13's exact API surface differs from RESEARCH §E… adapt imports to the actual surface, document the discrepancy in 01-07-SUMMARY.md"). **STACK DRIFT INVENTORY** (load-bearing for downstream plans): @x402/evm v2.13 v1-protocol ExactEvmSchemeV1 → named networks ('base-sepolia', 'base', 'ethereum', 'sepolia', 'abstract', 'abstract-testnet', 'avalanche-fuji', 'avalanche', 'iotex', 'sei', 'sei-testnet', 'polygon', 'polygon-amoy', 'peaq', 'story', 'educhain', 'skale-base-sepolia', 'megaeth', 'monad', 'stable', 'stable-testnet') + header=X-PAYMENT + envelope={x402Version, scheme, network, payload:{authorization:{from,to,value,validAfter,validBefore,nonce}, signature}}. v2-protocol ExactEvmScheme → CAIP-2 ('eip155:*') + header=PAYMENT-SIGNATURE + envelope={x402Version:2, resource:{url,method}, accepted:PaymentRequirements, payload:{authorization, signature}, extensions?}. v2 PaymentRequirements uses `amount` (not `maxAmountRequired`) + REMOVES `resource`/`description`/`mimeType` from `accepts[]` items. **Header-only mode default**; `X402_MOCK_REAL_SETTLE=1` env-toggle reserved for manual developer-machine validation against a faucet-funded Base Sepolia wallet (NOT exercised in CI; implementation deferred until Phase-1.5 enrichment if needed). Honors RESEARCH §F functional contract: 402 → sign → retry → 200 → settlement-header-echo. **Wave 3 unblocked**: Plan 01-08 may invoke startMockServer for end-to-end CLI product test if elected; mock binds ephemeral port so multiple parallel suites don't collide. **Phase 5 PDF deliverable** has a working code-path for the real-mainnet x402 footnote (deferred decision); only `network` field flips from 'base-sepolia' → 'base' and signer must hold real USDC. Closes the partial relaxation of PROJECT.md's cost-leg-modeled-not-paid stance: x402 plumbing is now exercised end-to-end against a representative payload through real @x402/fetch + @x402/evm code, not just stubbed.
- **Plan 01-08 PHASE 1 ACCEPTANCE GATE LOCKED** [LOCKED 2026-05-26, commit `f5e3d52`]: `.planning/phases/01-l1-.../01-VERIFICATION-pre.md` (the substantive Phase-1 acceptance artifact, ~200 lines, frontmatter `verification_pass: true`) + `.planning/phases/01-l1-.../01-08-SUMMARY.md` (thin pointer per checker M15 so orchestrator phase-globbing of `*-SUMMARY.md` finds Plan 01-08's output). **Full Phase-1 acceptance grid PASS**: pnpm install --frozen-lockfile (exit 0); pnpm -C fetch exec tsc --noEmit (exit 0); pnpm -C fetch test --run = 80/80 tests across 11 files (3.65s wall-clock); make leak-check PASS; make schema-frozen-check PASS (baseline `e9b214d` unchanged); make schema-probe PROBE_PASS; CLI dry-run JSON emit `{protocol:"ichi", iteration:1, vault_count:1, blocks_per_vault:7896653, queries_per_vault:795, total_queries:795, exceeds_earmark:false, earmark:30000, cache_hit:false, dry_run:true, head_block:67896653, head_source:"snapshot"}` — `head_source: "snapshot"` proves dry-run mechanically cannot escalate to live network call (per Plan 01-06 4-tier resolver). **SC-4 Parquet → JSONL pivot acknowledged** explicitly in 01-VERIFICATION-pre.md per checker I6: ROADMAP SC-4 verbiage says "Parquet" but Phase 1 ships JSONL append (hyparquet-writer high-frequency-small-write risk per RESEARCH §J fallback); byte-identity invariant HOLDS for JSONL (`sha256sum` on the `.jsonl` file is byte-stable across reruns; Makefile target already globs `*.parquet -o -name "*.jsonl"`); Phase 2 / Phase 5 batch-converts JSONL → Parquet via polars. **One acceptance-cmd substitution documented**: `make verify-cache-idempotency` Makefile target invokes `pnpm -C fetch fetch ichi ...` which collides with pnpm's built-in `fetch` (rejected as "Unknown options: 'pool', 'from', 'to'"); per plan body line 158 Tier-1 substitute clause, the network leg was replaced by `pnpm -C fetch test tests/cache.test.ts --run` = 18/18 PASS (covers SAME byte-identity invariant at unit-test level including literal-byte-content reference-string assertion per checker C2); Makefile rewrite (use `pnpm -s -C fetch run fetch ...` or `pnpm -s -C fetch exec tsx src/cli.ts ...`) deferred to Phase 2 cosmetic touch. **Library-drift inventory carried from Plan 01-07**: v1 protocol (current `@x402/evm` v2.13 `ExactEvmSchemeV1`) uses named networks ('base-sepolia', 'base', 'ethereum', 'sepolia', 'abstract', 'abstract-testnet', 'avalanche-fuji', 'avalanche', 'iotex', 'sei', 'sei-testnet', 'polygon', 'polygon-amoy', 'peaq', 'story', 'educhain', 'skale-base-sepolia', 'megaeth', 'monad', 'stable', 'stable-testnet') + X-PAYMENT header + `maxAmountRequired`; v2 protocol uses CAIP-2 networks + PAYMENT-SIGNATURE header + `accepted` envelope + `amount`. Selection for Phase 1 / Phase 5: v1. **Subgraph hunt verdict** (orchestrator finding #2): Phase 1 default Blockscout-only — no `[subgraphs.uniswap_v3]` block added to `protocols/ichi.toml`; `getSubgraphClient` throws `MissingGraphApiKeyError` without `GRAPH_API_KEY` env; Phase 1.5 enrichment task pending (provision `GRAPH_API_KEY` + add block to ichi.toml; `make schema-probe` already confirmed adding the block does NOT trip schema-frozen hook). **Deferred items rolled up**: single entry from Plan 01-03 (pre-existing blockscout-client cursor-advance test failure) already RESOLVED in Plan 01-05 via Rule-1 fix. **Phase goal-backward checks all PASS**: FETCH-01..04 + SC-1..SC-6 covered with primary evidence; no commits under `analysis/src/` or `data/raw/` (Phase 2 territory); USDT framing preserved. **I11 regex acceptance check**: `grep -cE "FETCH-0[1234]|SC-[1-6]" 01-VERIFICATION-pre.md` = 33 hits (>= 6 required). **No outstanding gaps; Phase 1 closed at 9/9 plans**; `/gsd:verify-work 01-l1-data-fetch-skeleton-free-tier-discipline` produces canonical 01-VERIFICATION.md.
- **Plan 01-00 WORKSPACE BOOTSTRAP LOCKED** [LOCKED 2026-05-26, commits `c46cbb7` (feat workspace + fetch scaffold) + `682ca13` (chore Python pin) + `eaf7de3` (feat Makefile + schema-probe) + `36dc615` (feat x402 exports + Forno snapshot + constants.ts)]: pnpm workspace bootstrapped (pnpm-workspace.yaml lists fetch/ + analysis/); fetch/package.json pins exact-version deps per STACK.md (viem@2.51.0 / @x402/fetch@2.13.0 / @x402/evm@2.13.0 / @x402/core@2.13.0 / @graphprotocol/client-x402@1.0.0 / @graphprotocol/client-cli@3.0.7 dev-only / graphql-request@7.4.0 / @mento-protocol/mento-sdk@3.2.8 / zod@4.4.3); analysis/uv.lock pins tick==0.8.0.2 / statsmodels==0.14.6 / polars==1.41.0 / numpy==2.4.6 / scipy==1.17.1 (24 transitive packages against Python 3.13.5 via `uv lock`); fetch/src skeleton dirs (cost-ledger/, cache/, freshness/, endpoints/{blockscout,forno,graph}/, x402-mock/) + fetch/tests 10 describe.todo files (vitest run reports 10 skipped); Makefile Phase-1 targets (fetch-ichi / lint-artifacts / verify-cache-idempotency / schema-probe / full Phase-1 leak-check replacing Phase-0 stub); scripts/schema_probe.sh returns PROBE_PASS (schema-frozen hook scans _schema.toml only; [subgraphs.uniswap_v3] block deferred to Phase 1.5 retroactive enrichment); notes/forno_head_snapshot.json {head: 67896653, snapshotted_at: 2026-05-26T11:01:00Z} (Forno eth_blockNumber 0x40bf54d live pull); .planning/phases/01-l1-.../01-00-x402-exports.txt enumerates x402 v2.13 exports — x402Client + wrapFetchWithPayment + wrapFetchWithPaymentFromConfig + decodePaymentResponseHeader + x402HTTPClient in `@x402/fetch`; registerExactEvmScheme + ExactEvmScheme + createPermit2ApprovalTx in `@x402/evm/exact/client` subpath; `@x402/core` only exports x402Version (Plan 01-07 import resolution); fetch/src/constants.ts exports loadFornoHeadSnapshot + DRY_RUN_FALLBACK_HEAD (67896653) + CELO_CHAIN_ID (42220) + BASE_SEPOLIA_CHAIN_ID (84532) + CELO_USDT_ADDRESS (per _schema.toml canonical_celo_usdt) + BASE_SEPOLIA_USDC_ADDRESS; .env.example with PRIVATE_KEY / CELO_RPC_URL / BASE_SEPOLIA_RPC_URL / BLOCKSCOUT_API_KEY / GRAPH_API_KEY (empty/public defaults). 5 Rule-1/2/3 deviations auto-fixed (tsconfig rootDir/include narrowing; src/index.ts stub for TS18003; .gitignore /cache/ anchor fix to unmask fetch/src/cache/; extra constants for Wave 1 single-source-of-truth; Phase-0 leak-check stub replaced by Phase-1 three-class gate). pnpm install clean from fresh state; pnpm -C fetch exec tsc --noEmit exits 0; make schema-frozen-check PASS (baseline e9b214d unchanged); make leak-check PASS. **Wave 1 unblocked**: 01-01..01-05 parallelizable; 01-06 dry-run reads forno_head_snapshot.json; 01-07 x402 mock imports per 01-00-x402-exports.txt; 01-08 CLI integration uses `make fetch-ichi ARGS=...` pass-through.
- **Plan 02-02 PANEL-01 DECODERS LOCKED** [LOCKED 2026-05-26, commits `b4c0e0a` (test RED + deps add) + `705769c` (feat GREEN decoders.py) + `ea3ab0a` (chore deferred-items)]: `analysis/src/abrigo_x402/decoders.py` (240 lines, replaces 28-line skeleton) exports `decode_all(df)` + `SWAP_TOPIC0` / `MINT_TOPIC0` / `BURN_TOPIC0` (canonical hex verbatim from Phase 1 `fetch/src/constants.ts`) + `DEPOSIT_TOPIC0` / `WITHDRAW_TOPIC0` (computed at module-load via `_compute_topic0_from_abi(ICHI_VAULT_ABI, name)` → `keccak256(canonical_signature)` parsed from `analysis/tests/fixtures/ichi_vault_abi.json` Plan 02-00 capture — drift-proof against future ICHI vault versions) + `ICHI_VAULT_ABI` (re-exported for Plan 02-04 vault_state.py reads) + `TOPIC0_TO_EVENT` registry mapping all 5 topic0s → names + per-event decoders `decode_swap/mint/burn/deposit/withdraw`. **Canonical ICHI topic0s recorded**: DEPOSIT_TOPIC0 = `0x4e2ca0515ed1aef1395f66b5303bb5d6f1bf9d61a353fa53f73f8ac9973fa9f6` (sig: `Deposit(address,address,uint256,uint256,uint256)`); WITHDRAW_TOPIC0 = `0xebff2602b3f468259e1e99f613fed6691f3a6526effe6ef3e768ba7ae7a36c4f` (sig: `Withdraw(address,address,uint256,uint256,uint256)`). **Plan-body fallback hex was WRONG** (paste from different ICHI variant: DEPOSIT_TOPIC0=0xdcbc1c... ≠ correct 0x4e2ca0...; WITHDRAW_TOPIC0=0xf279e6... ≠ correct 0xebff26...); Rule-1 fix REMOVED the try/except FileNotFoundError fallback entirely so module fails loudly on missing fixture per canonical-fixture invariant from Plan 02-00. **Plan-body fixture path Rule-1 corrected**: `Path(__file__).resolve().parents[2] / "analysis" / "tests" / "fixtures"` → `Path(__file__).resolve().parents[2] / "tests" / "fixtures"` (from `analysis/src/abrigo_x402/decoders.py`, parents[2] is `analysis/`; original would resolve to `analysis/analysis/tests/...`). **Identifier naming**: plan-body skeleton used `UNISWAP_V3_SWAP_TOPIC0` / `UNISWAP_V3_MINT_TOPIC0` / `UNISWAP_V3_BURN_TOPIC0` but body code AND success criteria use `SWAP_TOPIC0` / `MINT_TOPIC0` / `BURN_TOPIC0` — resolved to shorter form. **Deps added (Rule-3)**: `eth-abi==5.2.0` (Python ABI decoder), `eth-utils==6.0.0` (keccak), `eth-hash[pycryptodome]==0.8.0` + `pycryptodome==3.23.0` (keccak backend — first call raised `ImportError: None of these hashing backends are installed`; chose pycryptodome over unmaintained pysha3). Amount fields emitted as decimal-strings (polars String dtype) to preserve uint256 precision; Plan 02-05 casts to Decimal[38,18] for Q96 arithmetic. `decode_all` preserves rows on Unknown topic0 (`event_name='Unknown'`) instead of raising or dropping; per-row decode failures emit `decode_error` column (Plan 02-08 audits). **10/10 decoder tests pass**: topic0 registry completeness (1) + 5 per-event round-trips with hand-crafted hex payloads + Unknown preserved + mixed-event DataFrame + 2 M7 drift-proof bulwarks (`test_deposit_topic0_computed_from_abi_fixture` + `test_withdraw_topic0_computed_from_abi_fixture` recompute keccak256 inline from ICHI_VAULT_ABI and assert module values match). `cd analysis && uv run pytest tests/test_decoders.py -x` exits 0. **Out-of-scope deferred**: `test_revenue_leg.py::test_zero_for_one_swap_fee_on_token0` fails with polars `floor_div operation not supported for dtype decimal[38,0]` — Plan 02-05 territory (decoders.py does NOT import revenue_leg); logged to `.planning/phases/02-…/deferred-items.md` with hint for Plan 02-05 GREEN (use `.cast(pl.Int128)` before floor-div or Decimal-typed lit + `/ → .floor()`). **Wave 1b downstream unblocked**: Plan 02-05 (revenue_leg) imports `{SWAP_TOPIC0}` + consumes Swap payload columns `amount0`/`amount1`/`sqrtPriceX96`/`liquidity`/`tick`; Plan 02-04 (vault_state) joins on `Deposit`/`Withdraw` block boundaries; Plan 02-08 (panel orchestrator) calls `decode_all` as first transform after finality cutoff. Closes PANEL-01 decoders deliverable.
- **Plan 02-00 PANEL-BUILD SCAFFOLD + LOAD-BEARING PROBES LOCKED** [LOCKED 2026-05-26, commits `07edf46` (feat pytest infra + 9 module skeletons + synthetic fixture) + `d418a5a` (feat [panel] finality_lag_blocks=120 + 3 fixture captures) + `36f298b` (feat TS sidecar scaffolds + vitest stubs)]: pytest infrastructure wired in `analysis/` — `[tool.pytest.ini_options] testpaths=["tests"] pythonpath=["src"] addopts="-ra --strict-markers"`, dev-deps `pytest>=9.0.3`, runtime deps `pydantic>=2.0` (2.13.4 installed). 9 Python module skeletons land under `analysis/src/abrigo_x402/`: `ingest.py` (load_jsonl + apply_finality_cutoff), `decoders.py` (Uniswap V3 + ICHI topic0 constants + decode_all), `phantom_filter.py` (USDC/USDT ADAPTERS frozenset + exclude_adapters), `vault_state.py` (load_vault_state + attach_in_range), `revenue_leg.py` (compute_swap_fee with Q96 docstring), `fx_snap.py` (FX_METHODS = ("exact","forward_fill","unavailable") + load_fx_sidecar + attach_rates), `provenance.py` (REQUIRED_KEYS = (chainId, contractAddress, blockRange, fetchTimestamp, dataHash, gitCommit) + with_header + assert_has_header), `panel.py` (build_panel orchestrator signature), `protocol_spec.py` (pydantic Protocol/AnchorPool/Vault/PanelConfig/ProtocolSpec models). Each module: docstring + type-hinted signatures + `NotImplementedError("Plan 02-NN")` body. `protocols/ichi.toml` gains `[panel] finality_lag_blocks = 120` after `make schema-probe` PROBE_PASS (per-protocol field precedent same as 01-01's `cold_backfill_from_block`; baseline e9b214d unchanged). **4 load-bearing fixtures captured under `analysis/tests/fixtures/`**: (1) `ichi_vault_abi.json` — 13 entries from Blockscout-v2 verified ICHIVault source (`name=ICHIVault`, `is_verified=true`, compiler 0.7.6); 9 view fns (`getTotalAmounts`, `totalSupply`, `currentTick`, `baseLower`, `baseUpper`, `getBasePosition`, `pool`, `token0`, `token1`) + 4 events (`Deposit(address indexed sender, address indexed to, uint256 shares, uint256 amount0, uint256 amount1)`, `Withdraw`, `Rebalance`, `CollectFees`); CANONICAL NAMING NOTE — vault uses `getTotalAmounts` NOT `totalAmounts`, `baseLower`/`baseUpper` NOT `lowerTick`/`upperTick`. (2) `mento_cKES_USDm_exchange_id.json` — exchangeId `0x89de88b8eb790de26f4649f543cb6893d93635c728ac857f0926e842fb0d298b` live-resolved via celoClient.readContract(BiPoolManager, getExchangeIds) → iterate 16 exchanges → match (asset0=cKES, asset1=USDm) via getPoolExchange; status=resolved. (3) `phantom_transfer_synthetic.json` — 3-log deterministic fixture (Swap on cKES/USDT pool + user→USDT-adapter Transfer + user→counterparty Transfer in same tx); load-bearing for Plan 02-03 unit tests. (4) `phantom_transfer_usdt_real.json` — status=no-adapter-traffic-found fallback; Blockscout v1 getLogs against USDT-adapter `0x0e2a3e...c6f72` returned zero Transfer logs across windows 10k/50k/200k/1M blocks back from Forno head 67896653 (~11.6 days at Celo 1s/block); plan-body explicitly authorizes this fallback (synthetic remains load-bearing; real fixture activates if adapter traffic surfaces, e.g., in Phase 6 Steer cCOP/USDT iteration). **2 TS sidecar scaffolds**: `fetch/src/mento/historical-rate.ts` (Plan 02-06 target) exports `MENTO_ADDRESSES = {Broker: 0x777A8255..., BiPoolManager: 0x22d9db95..., cKES: 0x456a3D..., USDm: 0x765DE8...}`, `BROKER_ABI` (getAmountOut), `BIPOOL_ABI` (getExchangeIds + getPoolExchange tuple-return), `FxSnapRow` + `FxSnapOptions` types, `snapFxBlocks()` throwing stub. `fetch/src/vault/state-snap.ts` (Plan 02-04 target) exports `VAULT_ADDRESSES = {cKES_USDT_anchor: 0xe304b9..., cKES_USDT_pool: 0x61Ef87...}`, `MULTICALL3_CELO = 0xcA11bde0...`, `VAULT_ABI` pinning canonical ICHIVault names (getTotalAmounts/totalSupply/currentTick/baseLower/baseUpper) verified against fixture, `VaultStateRow` + `VaultStateOptions` types, `snapVaultState()` throwing stub. **4 new vitest test files** (2 source + 2 test) — 6 passing + 10 it.todo placeholders for Plan 02-04/02-06. Full Phase 1+2 suite at end of 02-00: **13 files / 86 passed / 10 todo** (Phase-1 80 baseline preserved + 6 new passing); `pnpm -C fetch exec tsc --noEmit` exits 0; `cd analysis && uv run pytest --collect-only` exits 0 with `collected 0 items`. **3 Rule-1/3 deviations auto-fixed**: (1) Rule-1 plan-body acceptance assertion checked for fn `totalAmounts` but ICHIVault on Celo exposes `getTotalAmounts` (verified live via Blockscout-v2) — fixture reflects on-chain reality + naming-note in `_meta`; (2) Rule-3 Phase-1 `fetch/src/blockscout/v1-getlogs.ts` throws on "No logs found" upstream message (regex /No records/i misses live message) — wrapped capture script in try/catch matching /No (logs|records) found/i; out-of-scope to fix v1-getlogs.ts per scope boundary; (3) Rule-3 sidecar script cwd is `fetch/` not repo-root — both `fetch/scripts/resolve-mento-exchange-id.ts` and `capture-phantom-transfer.ts` use `fileURLToPath(import.meta.url) → dirname → join('..','..')` to compute REPO_ROOT for fixture writes. **2 one-shot sidecar scripts** committed under `fetch/scripts/` (resolve-mento-exchange-id.ts + capture-phantom-transfer.ts) — load-bearing for fixture recapture if needed; not run in CI. **Wave 1b UNBLOCKED**: Plans 02-01..02-07 each can `from abrigo_x402.<module> import <fn>` without ImportError, read fixtures offline from `analysis/tests/fixtures/`, drop bodies into existing TS signatures (snapVaultState/snapFxBlocks), and compute ICHI Deposit/Withdraw topic0 = keccak256('Deposit(address,address,uint256,uint256,uint256)') from captured ABI event signatures. AF-10 fixture remains parked at `tests/fixtures/af_10_dune_plus/env_violating_parked.txt` per Phase 2 execution protocol. Closes scaffold prerequisites for PANEL-01..PANEL-04 (DEMAND-01 already complete in Phase 1).

- **Plan 02-06 PANEL-03 MENTO HISTORICAL-BLOCK FX SNAP LOCKED** [LOCKED 2026-05-26, commits `8afcbd9` (test RED TS) + `561707a` (feat GREEN TS) + `0d5a2be` (test RED Py) + `5e19d07` (feat GREEN Py + notes)]: `fetch/src/mento/historical-rate.ts` `snapFxBlocks()` calls Broker.getAmountOut(BiPoolManager, exchangeId=`0x89de88b8...0d298b`, cKES, USDm, 1e18) via raw `viem.readContract({ blockNumber: BigInt(block) })` per call (RESEARCH §B + Pitfall 1 — Mento SDK 3.2.8 QuoteService silently queries head; SDK BROKER_ABI also incomplete tradingLimits-only so hand-supplied `parseAbi(['function getAmountOut(...)'])` is canonical). Per-block dedup + sorted output; revert handling: `method='unavailable'` rate='0'; JSONL sidecar one row per block at `outputPath` with Blockscout block-URL provenance. Cost-ledger row per readContract call: endpoint='forno', chain='celo', cost_usdc='0', paid_real=false (DEMAND-01 uncapped). `analysis/src/abrigo_x402/fx_snap.py` `load_fx_sidecar(path)` + `attach_rates(panel_df, fx_df)` use `polars.DataFrame.join_asof(strategy='backward')` with PRE-FILTER to `method=='exact'` rows BEFORE the join — load-bearing: if 'unavailable' rows remained, polars would pick up `rate_x1e18='0'` as the most recent quote at a reverted-Broker block; filtering before the join makes forward-fill skip 'unavailable' and land on the most recent 'exact' row preserving provenance signal. `fx_method` column = exact|forward_fill|unavailable; `cKES_per_USDm_rate` String (decimal-string from uint256 — polars Decimal not yet round-trip stable for >38-digit values); `fx_provenance_url` String. **USDT/USD AS SEPARATE COLUMN** (CLAUDE.md non-negotiable): every panel row gets `usdt_usd_rate='1.0'` + `usdt_usd_method='stipulated'` as Phase 2 default; Phase 4 USDT-depeg sensitivity may overwrite specific rows with `usdt_usd_method='depeg-event'` without touching Phase 2 panel construction. Column existence is the structural commitment; default value is not. `notes/fx_snap_decision.md` documents 4 rejected alternatives: (1) USDT/USD=1.0 collapse (would destroy Phase 4 signal); (2) Chainlink CELO/USD triangulation (extra hop + non-Mento noise); (3) Pyth on-Celo (nascent, cKES feed unverified); (4) Mento SDK 3.2.8 QuoteService (no blockNumber param — head-only). 8/8 vitest tests + 7/7 pytest tests passing; `pnpm -C fetch exec tsc --noEmit` exit 0; full Phase 1+2 fetch suite 96/96; analysis suite 61/61 + 1 skipped (pre-existing phantom_filter real-fixture skip per 02-00 fallback). **3 Rule-1/2/3 deviations auto-fixed**: (1) Rule-1 plan-body cost-ledger row shape (`{ts, endpoint, chain, usdc_cost, paid_real, blocks_queried, request_id}`) didn't match real `CostLedgerRowSchema` from Plan 01-02 (`{timestamp, endpoint, query_id, cost_usdc, paid_real, tx_hash, chain, response_bytes, response_sha256, fetch_id}`) — would crash zod validation at runtime; mapped to real schema preserving endpoint='forno' invariant; (2) Rule-3 plan-body single file-scope `TMP` constant for tests → per-test `mkdtempSync(tmpdir + 'fx_snap_test_')` for isolation; (3) Rule-2 added 8th vitest test asserting exact Broker call argument tuple (BiPoolManager, exchangeId, cKES, USDm, 1e18) — load-bearing tuple contract not covered by plan's 5 tests. **Wave 1b complete for 02-06**: Plan 02-08 panel assembly imports `{ load_fx_sidecar, attach_rates, FX_METHODS }`; TS sidecar ready for Phase 2 live-Forno retake (operator runs against deduped block list once 02-08 emits panel events); forward-fill enum committed for PRE_REGISTRATION before any Phase 3 fit; Phase 4 USDT-depeg sensitivity has load-bearing structural commitment in place. Closes PANEL-03 at L3 panel construction layer.

- **Plan 03-00 PHASE-3 WAVE-0 SCAFFOLD LOCKED** [LOCKED 2026-05-27, commits `b091e3e` (feat dgp/ module stubs + conftest extension) + `874b140` (test 9 skip-marked test stubs) + `6abe1e8` (feat synthetic fixtures + lint_artifacts.py fit_report.json walker)]: 9 `analysis/src/abrigo_x402/dgp/*` module stubs landed with CANONICAL Wave-1 symbol names locked verbatim (`wall_clock_split` NOT `wallclock_split`; `compute_held_out_loglik_hawkes`/`_nhpp` NOT `*_loglik_on_test`; `compute_compensator_exp_kernel` NOT `compute_compensator_exp_hawkes`; `Q9_CI_WIDTH_THRESHOLD` NOT `Q9_CI_WIDTH_NULL_FIRE_THRESHOLD`; `STATIONARITY_RATIO_THRESHOLD` NOT `STATIONARITY_RATIO_TOLERANCE`). `WallClockSplit` is a `@dataclass(frozen=True)` with `t_split` / `train_leg_0` / `train_leg_1` / `held_out_leg_0` / `held_out_leg_1` / `window_start` / `window_end` / `held_out_fraction` + `to_metadata()` method — orchestrator 03-07 consumes this surface (`split.t_split` / `split.to_metadata()`), NOT a dict. `InsufficientEvaluationError(RuntimeError)` declared for SC-4 in-sample-only-fit guard. 9 `analysis/tests/test_*.py` skip-marked stubs land 21 functions; `pytest --collect-only` = 102 tests total (Phase 2 + Phase 3 stubs). 2 deterministic synthetic Parquet fixtures captured via `tick.SimuHawkesExpKernels` with `HAWKES_SEED=20260526` + `NHPP_SEED=20260527`, baseline=0.00013 events/sec/leg, decays=0.1, end_time=2,592,000 s (30 days), expected_branching_ratio=0.5 (Hawkes) / 0.0 (NHPP); both fixtures within [100, 800] event-count sanity bounds (hawkes 354+332, nhpp 337+316); sha256 + tick 0.8.0.2 + polars 1.41.0 recorded in `synthetic_fixtures_manifest.json`. `scripts/capture_synthetic_fixtures.py` is the reproducible capture script (Pitfall 9 honoured: `force_simulation=True` NOT passed — would silently allow non-stationary draws). `scripts/lint_artifacts.py` extended with `FIT_REPORT_REQUIRED_KEYS = frozenset({chainId, contractAddress, blockRange, fetchTimestamp, dataHash, gitCommit})` + `lint_fit_report_json(path)` + dormant `repo_root.glob('data/fits/**/fit_report.json')` walker (zero JSON files today → loop short-circuits; activates automatically once Wave 2 plan 03-07 writes the artifact, no further `lint_artifacts.py` edits needed). `make lint-artifacts` PASS (1 parquet PASS PANEL-02; 0 fit_report.json files). 1 Rule-1 deviation auto-fixed: `lr_test.py` docstring originally listed prohibited names `statsmodels.stats.diagnostic.likelihood_ratio_test` + `scipy.stats.chi2(1).sf` verbatim, which tripped the plan's own SC-3 grep gate acceptance criterion (`! grep -rE 'likelihood_ratio_test|chi2\(1\)\.sf' lr_test.py` exits 0); reworded to describe each helper in prose while preserving warning content. **Wave 1 UNBLOCKED**: 03-01..03-06 parallel plans drop bodies into existing canonical-name stubs without touching `dgp/__init__.py` re-exports; 03-07 orchestrator (Wave 2) writes the first `data/fits/<run_id>/fit_report.json` and activates the dormant JSON walker.

### Decisions Pending (Phase 0)

- Q-9 [LOCKED 2026-05-25 via Plan 00-03, commit `5782527`]: cCOP panel — V3-anchor-only primary + V3+V4+Broker unified fallback pre-registered (sample<300 OR CI>0.4, AND permutation p>0.05, 1000 reps, K-S D-max). REPRO-02 dead-code obligation on `analysis/src/abrigo_x402/panel/{unified,cross_class_permutation}.py`. REPRO-04 complete. See `notes/Q9_DECISION.md`.
- Q-4: Per-protocol vs per-vault granularity for ICHI Iteration 1 (recommendation per SUMMARY.md: per-protocol-aggregate with single-vault microcosm sensitivity; final lock in Phase 0, retrospective in Phase 7)
- Q-7 [LOCKED 2026-05-25 via Plan 00-01, commit `6cd61ed`]: TVL floor = `TVL < $10k OR events < 30/30d`. cXOF/USDm `0xAA97…381a` (~$11k TVL) flagged marginal; BRLm/EURm `0xb6c8…dab5` (<$10k) deferred. Reconsideration triggers: cXOF/USDm if TVL≥$20k; BRLm/EURm if events/30d≥60. See `notes/PRE_REGISTRATION.md` §Q-7 Floor + §Deferred Substrate.
- USDT depeg jump-leg calibration source (Phase 4 lock)

### Active Todos

(none — phase planning pending)

### Active Blockers

(none — Phase 0 ready to plan)

### Substrate Findings (from research, 2026-05-25)

- cKES/USDT ~4,440 swaps/30d (14.8× above 300-event Hawkes floor) — SAMPLE-SIZE PASS for Iteration 1
- cCOP/USDT ~580–625 swaps/30d on V3 anchor; +185 Broker swap-equivalents/30d (cCOP only); +90 V4 swaps/30d (cCOP only) — SAMPLE-SIZE PASS for Iteration 2 (boundary on V3-only; comfortable if unified)
- PITFALLS §1 sample-thinness application to abrigo-x402 RETRACTED (was counting-artifact false positive due to 5s/block assumption; actual Celo block time is 1s/block post-2024 hardfork)
- Steer-on-Celo TVL $855 — cost-leg empirical lower-bound check is the binding constraint for Iteration 2 (Phase 6 first step per REPRO-03)
- x402-on-Celo settlement: no Celo facilitator in x402-foundation monorepo as of 2026-05-25; cost leg is MODELED, not paid, in Iterations 1+2

## Session Continuity

**Last session:** 2026-05-27T00:37:18.196Z
**Last action:** Completed Plan 03-00 (Wave 0 scaffold — Phase 3 DGP estimation). Three atomic commits: `b091e3e` (feat — 9 `analysis/src/abrigo_x402/dgp/*` module stubs with canonical Wave-1 symbol names locked + `WallClockSplit` frozen dataclass + `InsufficientEvaluationError` exception + `conftest.py` extended with synthetic-fixture loaders + Pitfall 9 honoured) / `874b140` (test — 9 skip-marked `analysis/tests/test_*.py` stubs, 21 functions, pytest --collect-only = 102 tests total) / `6abe1e8` (feat — 2 deterministic synthetic Parquet fixtures via `tick.SimuHawkesExpKernels` seeds 20260526/20260527, 354+332 / 337+316 events respectively, sha256 + tick 0.8.0.2 + polars 1.41.0 recorded in `synthetic_fixtures_manifest.json` + `scripts/lint_artifacts.py` extended with `FIT_REPORT_REQUIRED_KEYS` + dormant `data/fits/**/fit_report.json` walker). 1 Rule-1 deviation auto-fixed (reworded `lr_test.py` docstring to honour the plan's own SC-3 grep gate — plan-supplied docstring literally contained the prohibited names `likelihood_ratio_test` / `chi2(1).sf`). Pre-commit hooks AF-01..AF-12 PASS on all 3 commits. **Wave 1 UNBLOCKED**: 03-01..03-06 parallel plans can drop bodies into existing canonical-name stubs without touching `dgp/__init__.py` re-exports; orchestrator 03-07 consumes the `WallClockSplit` dataclass surface from Day 1. **NOT exercised earlier**: Plan 01-07 (Wave 2 parallel — x402 mock server + client bridge + integration test: `fetch/src/x402-mock/server.ts` + `fetch/src/x402-mock/client-bridge.ts` + `fetch/tests/x402_mock.test.ts`). Three atomic 01-07 commits: `4718946` (feat — node:http 402 mock with x402 v2 PaymentRequirements + structural X-PAYMENT validation + X-PAYMENT-RESPONSE settlement echo) / `27176ce` (fix — v1-protocol named-network drift adaptation; STACK drift discovered via live probe: @x402/evm v2.13 ExactEvmSchemeV1 registers on NAMED networks ('base-sepolia'), NOT CAIP-2 ('eip155:84532') — plan-body proposal overridden; added `extra: {name: 'USDC', version: '2'}` to PaymentRequirements for EIP-712 domain, load-bearing for ExactEvmScheme.createPaymentPayload) / `e62b694` (feat — client bridge wires @x402/fetch x402Client + @x402/evm/exact/client registerExactEvmScheme with viem privateKeyToAccount; 3-test integration suite: bare 402 → PaymentRequirements / wrapFetchWithPayment round-trip → 200 + 32-byte tx hash / malformed-network rejection with errorReason; cost-ledger row written for x402-mock-sepolia endpoint with paid_real=false, chain='base-sepolia', cost_usdc='0.001'). 3 new vitest tests green; full Phase-1 suite at end of 01-07: **11 passed / 0 skipped / 80 tests passing / 0 fail**; `tsc --noEmit` clean. 2 Rule-3 deviations auto-fixed (both library wire-format drifts the plan body explicitly authorized adapting). Header-only mode is the Phase-1 default; X402_MOCK_REAL_SETTLE=1 env-toggle reserved for manual developer-machine validation against a faucet-funded Base Sepolia wallet (not exercised in CI). x402 plumbing now exercised end-to-end against a representative payload through real @x402/fetch + @x402/evm code, partially relaxing PROJECT.md's cost-leg-modeled-not-paid stance per CONTEXT.md.
**Next action:** Plan 03-01 (Wave 1 — DGP-01 Kirchner INAR(p) NHPP fit). Drops `fit_nhpp_inar` body into the existing stub at `analysis/src/abrigo_x402/dgp/nhpp_inar.py` using `statsmodels.tsa.api.VAR` with non-negativity projection; AIC-min over `BIN_WIDTH_GRID_SECONDS = (60.0, 300.0, 900.0, 3600.0)`. Tests are pre-wired in `analysis/tests/test_nhpp_inar.py` (3 skip-marked stubs) consuming `synthetic_nhpp_baseline_only_legs` fixture (337+316 events). Parallel waves 03-02..03-06 may land concurrently — each drops its body into the canonical-name stubs without surface collision; `dgp/__init__.py` re-exports remain untouched. **Phase 1 history retained below**: Plan 01-08 (Wave 3 — full end-to-end CLI integration acceptance test). Extends `fetch/src/cli.ts` non-dry-run path to actually call `getLogsV1` from 01-05 + `writeCachePayload` from 01-04 + `appendLedger` from 01-02 + `appendManifestIfNew` from 01-04 for the cKES/USDT cold-backfill window. The 4-tier head resolver, parseArgs, and exit-code conventions established in 01-06 remain intact. `make verify-cache-idempotency` uses `dataHash` field from 01-04 writeCachePayload as byte-identity oracle. 01-08 may optionally invoke `startMockServer` from 01-07 for an end-to-end CLI product test; mock binds ephemeral port (no collision). **pnpm invocation note for 01-08**: `pnpm -s -C fetch run fetch ichi ...` (NOT `pnpm -C fetch fetch ichi ...` — collides with pnpm's built-in `fetch`); OR spawn `pnpm -s exec tsx src/cli.ts ...` directly as the integration tests do.
**Resume hint:** `cd analysis && uv run pytest --collect-only -q` exits 0 with 102 tests collected (Phase 2 + Phase 3 stubs); `cd analysis && uv run python -c "from abrigo_x402.dgp import fit_nhpp_inar, fit_hawkes_expkern, parametric_bootstrap_lr, time_rescaling_ks_test_leg, compute_compensator_exp_kernel, profile_likelihood_eta_ci, wall_clock_split, compute_held_out_loglik_hawkes, compute_held_out_loglik_nhpp, baseline_stationarity_check, run_fit, InsufficientEvaluationError, WallClockSplit"` exits 0; `make lint-artifacts` exits 0 ("1 parquet PASS PANEL-02"); SC-3 grep gate at `analysis/src/abrigo_x402/dgp/lr_test.py` PASS. 03-01 callers consume: `from abrigo_x402.dgp.nhpp_inar import fit_nhpp_inar, BIN_WIDTH_GRID_SECONDS, MAX_P`; test fixtures consume: `synthetic_nhpp_baseline_only_legs` (session-scoped, returns `(np.ndarray, np.ndarray)`). Pre-commit hooks AF-01..AF-12 PASS on all 03-00 commits. **Earlier Phase 1 resume hint preserved**: `pnpm -C fetch test --run` exits 0 with 11 passed / 0 skipped / 80 tests passing; `pnpm -C fetch test tests/x402_mock.test.ts --run` exits 0 with 3 tests; `pnpm -C fetch exec tsc --noEmit` exits 0. 01-08 callers consume: `import { startMockServer, validateXPaymentHeader, PAYMENT_REQUIREMENTS } from './x402-mock/server.js'`; `import { makeFetchWithPayment, fetchWithPayment } from './x402-mock/client-bridge.js'`. STACK drift inventory for downstream plans: @x402/evm v2.13 v1-protocol uses named networks + X-PAYMENT header; v2-protocol uses CAIP-2 networks + PAYMENT-SIGNATURE header + `accepted` envelope + `amount` (not `maxAmountRequired`) — pick v1 for any future @x402/fetch integration that expects the X-PAYMENT envelope per RESEARCH §E. AF-10 fixture remains parked for Phase 1 duration.

---

*Created: 2026-05-25 by `/gsd:new-project` roadmapper agent*
