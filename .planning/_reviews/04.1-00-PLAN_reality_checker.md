## VERDICT

PASS

## Scope

Plan 04.1-00: extend `ingest.py` PROVENANCE_COLS with `block_timestamp`, parse `timeStamp` hex via existing `_hex_to_int`, extend zero-null invariant, extend two JSONL fixtures (10-row + 100-row), add two sanity tests, add `ICHI_PANEL_REQUIRED_COLUMNS` frozenset + `lint_ichi_panel_columns` to `scripts/lint_artifacts.py`, single atomic `fix(04.1):` commit at ≥193 passed / 3 skipped.

## Findings

- **`_hex_to_int(None)` already raises the PANEL-01 zero-null message** (ingest.py line 26-28). Plan's added `df["block_timestamp"].null_count() > 0` check is technically redundant for the missing-key path (the helper would raise first), BUT defensive against `"0x0"` literal — note `_hex_to_int("0x0")` returns int `0`, not null, so a literal `0x0` timeStamp would silently pass both the helper AND the null-count check. Verified live: all 832 rows of `pool_events.jsonl` have non-null, non-`0x0` `timeStamp` (range `0x6a0da972`..`0x6a158d28`), so this risk does not materialize on the real panel — but the executor should know the check does not catch literal-zero (low-priority documentation gap, not a blocker).
- **Real-panel timeStamp coverage verified**: 0 rows missing `timeStamp`, 0 rows with null/`"0x0"`. The "pre-Phase-2 cache rows lack it" concern does not apply — the JSONL was fetched in Phase 1 with Blockscout v1 etherscan-compat which ALWAYS carries `timeStamp`. No older cache rows exist.
- **Fixture extension is safe** — `_gen_n100.py` is a generator script but no test pins a sha256 of the fixture bytes (`grep -rn "hash\|sha\|seed"` returned 0 in `_gen_n100.py`); `test_panel_e2e.py` and `test_ingest.py` reference the fixtures by path only and assert row count / column presence, not byte-identity. Adding `timeStamp` to each line will not break downstream tests.
- **`ICHI_PANEL_REQUIRED_COLUMNS` scope guard is safe for synthetic-stacked parquet from 04-09**: BOTH synthetic parquets under `data/raw/ichi/0x61Ef.../` (`synthetic_p4_09_67000000_67000686.parquet`, `synthetic_p4_09_stacked_67000000_67002058.parquet`) already carry `block_timestamp` (verified via `pl.read_parquet_schema`). The `if "data/raw/ichi" in str(p)` lint scope guard will fire on them, but they will PASS — no false-positive failure.
- **Acceptance criteria reference `pl.read_parquet_schema(path).keys()`** in `lint_ichi_panel_columns` — this is a real Polars API (returns dict-like), correct. No silent typo.

## Reality check

Most realistic execution-time failure: if any third test fixture (not the two listed in `files_modified`) is consumed by tests downstream and constructs JSONL rows inline without `timeStamp`, the new zero-null guard will fire and break those tests. Plan's Step A "If failure: search via `grep -rn 'transactionHash\|load_jsonl' analysis/tests/`" is the right escape hatch but not enforced as a pre-edit grep. The 191 → ≥193 expected count is tight; if the executor finds an extra fixture, they extend it and the count drifts higher — plan handles this via `>= 193` formulation. No fantasy-deterministic patterns detected.

## Recommendation

Accept. The plan is precise, the OUT-OF-SCOPE list is verbatim, and the load-bearing assumptions check out against the live source and live JSONL data. Optional documentation nit (literal `0x0` doesn't trip null-count) is not a blocker.
