# Reality Checker — Plan 04.1.1-02b (orchestrator run_fit free-β rewire)

## Scope reviewed

Plan 04.1.1-02b closes the production-path wiring gap surfaced by the Plan 04.1.1-03
BLOCKER (`_artifacts/BLOCKER_03_production_path_not_wired.txt`): `orchestrator.run_fit`
defaults `decays=0.1`, pinning the production HEADLINE/CI/KS/held-out legs at the
kernel-blind β=0.1 instead of the free-β AIC grid the Wave-1/2 estimators support.

## Reality checks

1. **Root cause is real and localized.** Verified `orchestrator.py` L226 `decays: float = 0.1`
   passed to `fit_hawkes_expkern(..., decays=decays)` at L281-283. `hawkes_fit.py` L205/221
   confirms `decays is None` triggers the 6-point AIC grid (`DECAY_GRID` L32, 6 entries);
   a non-None float takes the single-β branch (1-entry `decay_aic_table`). The BLOCKER's
   `decay_aic_table = {"0.1": 9937.65}` (1-point) is exactly this branch.

2. **Propagation already exists.** L315 `hawkes_decays_t = float(hawkes_train["decays"])`
   reads the SELECTED β; L319/341/370/380/385 consume `hawkes_decays_t` for the held-out
   LL, KS, eta-CI, and canonical-LL legs. So once L226 default is None, the AIC-selected β
   flows downstream automatically — the fix is genuinely one line + docstring. No
   duplicate wiring required (verified, not assumed).

3. **AF-03 ordering is satisfiable.** `git log` shows orchestrator.py last touched at
   `7a20c6b` (2026-05-26), predating pre-reg `fda5905` (2026-05-28). The Task-1 pre-reg
   amendment commit lands before the Task-2 orchestrator commit within this plan.

4. **No estimator-math drift.** The plan edits only orchestrator.py + its test + the
   pre-reg note. `hawkes_fit.py` / `lr_test.py` / `profile_likelihood.py` are consumed
   as-is. The pre-reg v2 canonical-estimator definition is ENFORCED, not changed — the
   four-criterion verdict gate (LR α=0.01, KS α=0.05, η-floor 0.2, branching-CI-excludes-zero)
   is untouched and the verdict remains genuinely open.

5. **No production-rep here.** A small synthetic-panel unit test proves the 6-point grid
   runs; the full real-panel fit + run_id emission is deferred to Plan 04.1.1-03 (correct
   scope boundary; avoids silently consuming a non-conforming fit as canonical).

## VERDICT: PASS

The fix is the minimal, correct closure of the BLOCKER. The pre-reg amendment is
append-only (v1 + v2 sections preserved), the ordering invariant holds, and the verdict
gate is unchanged. No silent rescope.
