## VERDICT

PASS

## Scope

Code-reviewer pass on Plan 04.1-02 (Wave 3 Phase-3 fit rerun: invoke `cli.py fit` on the augmented real ICHI panel from Plan 04.1-01; capture deterministically derived NEW_RUN_ID; verify `fit_report.json` + `residuals.parquet` emit with full SC-1 key set; persist NEW_RUN_ID to marker file `_artifacts/new_run_id.txt` for Plan 04.1-03 consumption; regression-test full Phase 2+3+4 suite).

## Findings

- Frontmatter complete: `wave: 3`, `depends_on: ["04.1-01"]` (correct), `files_modified` correctly uses `<NEW_RUN_ID>` placeholder since the directory name is content-addressed and unknowable at planning time.
- CLI subcommand `fit` verified to exist in `cli.py` lines 203-231 with the documented argparse signature (`--pool`, `--panel-path`, `--out-dir`, `--bootstrap-reps`). The plan's invocation matches.
- Handler `_cmd_fit` lines 124-149 confirmed to invoke `run_fit(panel_path, out_dir, bootstrap_reps)` and print JSON summary with `run_id` field — matches the plan's stdout-parsing logic.
- `--bootstrap-reps` correctly NOT overridden (plan honors the PRE_REGISTRATION AF-04 lock at default 1000); explicit OUT-OF-SCOPE bullet states this.
- Marker-file pattern `.planning/phases/04.1-…/_artifacts/new_run_id.txt` is concrete, greppable, and consumed by Plan 04.1-03 — clean inter-plan handoff via the filesystem rather than shell-state.
- Thread-pinning env vars present on the fit invocation (mandatory per Phase 3 SC-5 byte-identity contract; statsmodels VAR.select_order AIC drifts under multi-thread BLAS — correctly cited).
- 18-key SC-1 assertion uses `FIT_REPORT_SC1_KEYS` (verified at `scripts/lint_artifacts.py` lines 45-67, exactly 18 keys: 6 PANEL-02 + run_id + tick_lib_version + 8 DGP result blocks + gate_passes + gate_criteria). Plan's count matches the actual frozenset.
- DGP-01..06 result-block grep in acceptance criteria: `nhpp_inar_params`, `hawkes_mv_params`, `lr_test`, `ks_rescaled_time`, `held_out_loglik`, `branching_ratio_ci` — all 6 verified to be in `FIT_REPORT_SC1_KEYS`.
- `residuals.parquet` presence + `rescaled_dt` column check is sound — Plan 04.1-03 hedge dependence stage consumes this column.
- run_id length assertion (12 hex chars) matches `_derive_run_id(...)[:12]` formula per CONTEXT.md.
- Acceptance criterion that synthetic `0afc6af38e24/` directory remains unchanged is sound — preserves the methodology-validation history per CONTEXT.md `<decisions>`.
- No source-code edits asserted via `git diff HEAD -- analysis/ scripts/` empty — correctly enforces the pure-rerun contract.
- **Minor (heredoc quoting):** Step C uses `$(... [a-f0-9]{12} ...)` inside backticked shell; the `[a-f0-9]{12}` brace-quantifier is regex (Python regex inside the inline `python3 -c`), and the shell `${#NEW_RUN_ID}` length check is the actual gate — sound.
- **Minor (acceptance criterion typo):** one of the acceptance criteria has `"... + open(\'.planning/...new_run_id.txt).read()..."` — missing the trailing single-quote before `).read()`. Bash will likely parse this differently than intended, but it's wrapped in a `python3 -c` and the shell-quoting will resolve since the outer is double-quoted. This is fragile but executable. Cosmetic.
- Out-of-scope list (no code edits, no `--bootstrap-reps` override, no `--require-block-timestamp` flag, no new deps, no post-hoc edits to `fit_report.json`) is appropriate.
- Read-first list correctly identifies the do-not-edit upstream sources and the predecessor SUMMARY pointer.

## Recommendation

Accept. (Optional: fix the missing single-quote in the run_id-equality acceptance criterion to remove the cosmetic shell-parsing fragility.)
