## VERDICT

PASS

## Scope

Plan 04.1-01: re-run `cli.py materialize` against cached JSONL sidecars to regenerate the real ICHI panel parquet at `data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` with the new `block_timestamp` column. Pure rerun — no code edits. Verify sha256 changes, schema carries Int64 `block_timestamp` with zero nulls, 832 rows preserved, lint passes.

## Findings

- **`materialize` subcommand confirmed live** in `analysis/src/abrigo_x402/cli.py` lines 188-197 (`m = sub.add_parser("materialize", ...)`), with exact argparse signature the plan invokes: `--pool`, `--from-block`, `--to-block`, `--protocol-toml`, `--forno-head`. Plan's "Phase 2's CLI subcommand may be named `fetch`?` concern does not apply — `materialize` is the canonical verb introduced in Plan 02-10.
- **Argparse signature exact**: required args `--pool`, `--from-block`, `--to-block`; optional `--protocol-toml` (default `protocols/ichi.toml`), `--forno-head` (default None). Plan's invocation passes the four required/canonical args verbatim.
- **Plan's forno_head derivation comment is muddled** (`"derives as to_block + 120 = 67896773"` — actually `--forno-head` defaults to `None` and the handler computes it from `to_block + finality_lag_blocks`). Not load-bearing; the materialize handler handles `None` internally. Cosmetic only.
- **The `tx_logs.jsonl` "optional" claim is accurate** — but verify the sidecar file does NOT exist in the directory listing: `ls data/raw/ichi/0x61Ef.../` shows `pool_events.jsonl`, `fx_snap.jsonl`, `vault_state.jsonl`, two synthetic parquets, and the real parquet — NO `tx_logs.jsonl`. The plan accounts for this ("if missing, materialize handles `None`"); the materialize handler must gracefully accept its absence. If the handler enforces presence, this plan will fail. (Low probability — Plan 02-10 documents tx_logs as optional.)
- **Pre-04.1 sha256 baseline value `2234b635...26b5b2e` is plan-captured prose, not live-verified.** Plan correctly does not assert exact value, only `PRE_SHA != POST_SHA`. Robust.
- **Column count "32 → 33" claim**: live verification not performed (parquet has the OLD schema pre-rerun; cannot count without re-materializing). The acceptance criterion `assert df.height == 832` is the load-bearing assertion; the column-count claim is documentary.

## Reality check

Most realistic execution-time failure: the `materialize` handler may require ALL four sidecars present (Plan 02-10 may have made `tx_logs.jsonl` mandatory in a later iter despite docstring). If so, the executor will get a `materialize: required sidecar missing: tx_logs.jsonl` error message (visible in cli.py line 76). The fix is either to create an empty `tx_logs.jsonl` or adjust the invocation — but the plan does not pre-flight check this. Recommend adding a Step B.5: `test -f data/raw/ichi/0x61Ef.../tx_logs.jsonl || echo "WARN: tx_logs absent; if materialize requires it, abort and consult"`.

## Recommendation

Accept. The CLI verb is correct, the cached data has `timeStamp` on all 832 rows (Plan 04.1-00 prerequisite holds), and the SHA-change assertion is the right invariant. The `tx_logs.jsonl` optionality is a minor unverified assumption; if it bites, the executor's failure mode is clear and recoverable.
