## VERDICT

PASS

## Scope

Reality-checker pass on `.planning/phases/04.1.1-.../04.1.1-00-PLAN.md` — the AF-03 pre-registration amendment plan for Phase 04.1.1. Single-task docs-only plan: append `## Phase 04.1.1 — LL-fit acceptance & fallback chain` section to `notes/PRE_REGISTRATION.md`, single atomic commit `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback`.

## Findings

- AF-03 temporal ordering invariant is honored by construction: this plan must commit BEFORE Plan 04.1.1-01 authors any `analysis/src/abrigo_x402/dgp/hawkes_fit.py` edit. The plan body is explicit on this and includes the audit `git log` invocation as a verification hook.
- The η-coherence acceptance band [0.283, 0.371] is read DIRECTLY from `data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci.{lower, upper}` (verified at executor read-time: `lower=0.2834212873462106`, `upper=0.37093135885619094`, `method="profile_likelihood"`, `alpha=0.05`). The rounding to 3 sig figs (`[0.283, 0.371]`) is conservative and reproducible.
- The synthetic-regression band [0.45, 0.55] anchors on `analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet` from Plan 03-00 Wave 0 (ground-truth η=0.5; preserved by single-realization simulation). The plan body correctly EXCLUDES `data/fits/ichi/0afc6af38e24/` (3-time-shifted stack; profile-likelihood returns η=0.052, not 0.5) — this is the post-research correction from `04.1.1-CONTEXT.md :: <decisions>`.
- DECAY_GRID extension `(0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)` is mathematically motivated: real-panel mean inter-arrival ~666s; β=0.1/s decays in 10s; extending down to β=1e-4 lets the kernel span 10⁴s timescales matching the panel. AIC-min selection logic preserved.
- Fallback A specification (scipy.optimize.minimize, method=L-BFGS-B, positivity bounds (1e-12, None), 3-start multi-start) is library-bug-isolated from the tick HawkesExpKern MLE path. It wraps `_hawkes_loglik_vectorized` from `analysis/src/abrigo_x402/dgp/lr_test.py` — the canonical LL function already trusted by the LR statistic. This is the Pattern F canonical-LL contract carried into the optimizer layer.
- BLOCKER policy (no Fallback B; no silent profile-likelihood promotion; no silent LS reintroduction) is the AF-03 anti-fishing discipline at the estimator-method level. Correctly pre-registered as a halt-the-pipeline rule rather than a method-shopping fallback chain.
- AF-12 OUT-OF-SCOPE list enumerated verbatim in the plan body (lines 49-60) — silent-rescope defense holds. Cross-checked against `04.1.1-CONTEXT.md :: <domain>` lines 17-27: identical.
- 2-way review-trail enforcement: this file + the paired `_code_reviewer.md` carry the `## VERDICT` first H2 with PASS — pre-commit hook will accept.
- Plan is single-file docs commit; no source-code drift expected; no spillover into `analysis/`, `scripts/`, or `data/`.

## Reality check

The plan's risk surface is exactly one verbatim-quote integrity check: the η-coherence band [0.283, 0.371] must match the on-disk `fit_report.json` to the 3-sig-fig precision claimed. Executor verified this at read time. The DECAY_GRID extension is the only quantitative addition that lacks a prior on-disk source — but its motivation (panel-mean-inter-arrival ~666s vs β=0.1 decay 10s) is mechanically derivable from the existing run_log.txt window metadata; the extension itself is conservative (adds two grid points downward; does not remove any).

The plan body's commit-message template includes the verbatim AF-12 OUT-OF-SCOPE bullet list in the body — this is a forensic AF-03 + AF-12 hybrid signal (auditors grepping the commit history for "no overwrite of ae9e3ba17900" will find it).

The one risk I flag is editorial: the commit subject contains `η-coherence` (a U+03B7 lower-case eta character). The pre-commit hook does not validate Unicode in subjects, but downstream `git log | grep` filters on terminals without UTF-8 will fail to match. Executor should NOT rewrite the subject to ASCII (it's load-bearing AF-03 evidence); reviewers consuming the audit trail should use `git log --encoding=UTF-8`.

## Recommendation

Accept. Plan ready for /gsd:execute-phase 04.1.1 wave 0.
