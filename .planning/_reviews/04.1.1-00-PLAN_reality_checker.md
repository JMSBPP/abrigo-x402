## VERDICT

PASS

## Scope

Reality-checker pass on `.planning/phases/04.1.1-.../04.1.1-00-PLAN.md` (v2) — the AF-03 pre-registration **supersession** plan for Phase 04.1.1. Single-task docs-only plan: REVIEW the already-drafted `## Phase 04.1.1 (v2) — Supersession of the LL-fit acceptance bands after independent diagnostic` section in `notes/PRE_REGISTRATION.md` against the v2 decisions, create the 2-way review trail, and COMMIT the drafted amendment AS-IS (subject `docs(pre-reg:04.1.1): supersede LL-fit bands per independent diagnostic (v2)`). The executor does NOT re-author the amendment — it reviews + commits.

(This file supersedes the v1-cycle reality-check on the same PLAN.md path. The v1 review accepted the [0.283, 0.371] and [0.45, 0.55] bands as valid; those bands are now RETRACTED per the independent diagnostic — see Findings. The v1 review content is preserved in git history at the v1 review-file blob.)

## Findings — every retraction and re-registration rests on the independent diagnostic, not on a result

- **Independent evidence base verified.** `04.1.1-DIAGNOSTIC.md` (2026-05-27, independent Model-QA audit, thread-pinned, no committed code) is the evidentiary foundation for the supersession. Its §1 headline, §2 Q1–Q4 tables, §3 projection-trick proof, and §4 Path-A recommendation are each cited by the corresponding clause of the drafted v2 amendment. The diagnostic committed NO code and left the `hawkes_fit.py` working-tree fix in place — so the supersession is genuinely pre-fit-result.

- **Retraction (a) — η-band [0.283, 0.371] is a constrained-projection artifact.** DIAGNOSTIC §3 traces `profile_likelihood.py` L72–118: per-grid `fit_hawkes_with_fixed_branching_ratio` rescales adjacency by `eta_target/eta_current` (projection trick, L298–300) rather than re-optimising the LL subject to ρ(α/β)=η; `LL_max := max_k profile_LL(grid)` references the projected family, and `eta_hat_unconstrained = 0.000307` is the degenerate LS-fallback at kernel-blind β=0.1. The drafted amendment §"Why the v1 bands are retracted" item 1 reproduces this verbatim. This is result-independent: the artifact is a property of the estimator code, not of any η outcome.

- **Retraction (b) — synthetic [0.45, 0.55] band tested a mislabeled fixture.** DIAGNOSTIC §1/§2-Q1 establishes `tick.SimuHawkesExpKernels` uses the normalized kernel φ(t)=α·β·e^(−βt) → tick branching ratio is ρ(α), not ρ(α/β); the committed `A=0.025, β=0.1` fixture labeled `expected_branching_ratio=0.5` reports `tick.spectral_radius()=0.05`. The drafted amendment item 2 reproduces this. Verifiable independent of any fit.

- **Re-registration — canonical estimator = free-β AIC-selected scipy joint-MLE** wrapping `_hawkes_loglik_vectorized` (Pattern F preserved); `fit_method_used="scipy_canonical_ll"`; AIC-min over `DECAY_GRID=(0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)`. Matches DIAGNOSTIC Q2 (AIC-min β=0.001, ΔAIC=21.9 interior trough) and the CONTEXT v2-SUPERSESSION decision 1. tick likelihood mode removed from live path (RuntimeError at every grid value — DIAGNOSTIC-confirmed dead, not fixable).

- **Estimator soundness pre-established.** DIAGNOSTIC Q1: free-β, true η=0.5, n≈700, 5 seeds → mean η̂=0.540, sd 0.076; Q3: fixed-β-at-truth η̂=0.433 (sd 0.052) at n≈700 → ~13% downward bias. The drafted amendment states the reported real-panel η is a LOWER BOUND. The new synthetic band [0.40, 0.60] at n≈700 accommodates exactly this measured bias — it is calibrated to the diagnostic, not reverse-engineered from a real-panel outcome.

- **No fixed real-panel η band (the AF-03 safeguard).** The amendment explicitly declines to pre-register a fixed real-panel η acceptance band (that would be post-hoc fishing given the η(β) curve is already known) and instead re-derives the CI as a genuine constrained MLE (re-optimize LL subject to ρ(α/β)=η). This is the correct anti-fishing posture.

- **Verdict gate UNCHANGED.** The Phase-0 four-criterion gate (LR-bootstrap α=0.01, time-rescaling KS held-out, branching-CI-excludes-zero, η-floor ≥0.2) is untouched. The amendment is explicit that LR and KS are NOT yet re-established and the result may still be null. No verdict-deciding threshold is moved — this is the single most important AF-03 property and it holds.

- **Option A (AF-12 expansion) authorized correctly.** tick-LS for the 1000 NHPP null replicates (null is η=0 by construction → LS bias irrelevant to the null LR distribution) + scipy canonical for the observed fit + observed-LL-scale fix for the 6.05M pathology. DIAGNOSTIC Q4 measures 178× speedup (143 min → 0.8 min) with no null-side statistical cost. The amendment scopes the `lr_test.py` + `profile_likelihood.py` edits narrowly and notes they were OUT in v1.

- **Append-only integrity confirmed.** The v1 `## Phase 04.1.1 — LL-fit acceptance & fallback chain` section is preserved verbatim (working-tree diff shows ZERO deleted lines; the change is pure addition of lines ~197–241). v1 history is retained as audit trail.

## Reality check

The supersession's entire legitimacy turns on one question: does fixing these bands move a verdict-deciding threshold? It does not. The four-criterion gate is unchanged; both retracted bands rest on verifiable code/fixture errors (`tick.spectral_radius()=0.05`; the projection-trick source); and the LR + KS outcomes are genuinely undetermined as of this commit. Fixing a demonstrably-wrong same-cycle acceptance band that was itself derived from a degenerate estimator is the opposite of spec-swapping to escape a result — it is restoring the canonical estimator the pre-registration always intended.

AF-03 temporal ordering is satisfiable: the most recent `analysis/src/abrigo_x402/dgp/` commit is `7a20c6b` (2026-05-26, Phase 3 `feat(03-07)`); no v2 dgp commit exists yet, so this v2 pre-reg commit will predate every future v2 dgp source commit by construction.

Editorial note (carried from v1 review): the v2 section header contains U+2014 em-dash and Greek η/β; the executor must preserve these — the drafted text already does, and the executor's mandate is review-and-commit-as-is, not re-author.

## Recommendation

Accept. The drafted v2 amendment is faithful to the independent diagnostic and the CONTEXT v2-SUPERSESSION decisions, retracts both v1 bands on result-independent grounds, leaves the verdict gate untouched, and is append-only over v1 history. Ready to commit as-is for /gsd:execute-phase 04.1.1 Wave 0 (v2). No unresolved BLOCKER.
