# Reality Checker review — 04.1.1-05-PLAN execution

Reviewer: Reality Checker (evidence-based, NEEDS-WORK-by-default scrutiny)
Artifact under review: the executed close-the-loop changes for Plan 04.1.1-05
(VERIFICATION append + ae9e3ba17900 README + STATE/ROADMAP close).

## Evidence checked

- `data/fits/ichi/bdaf5c7ba5a2/fit_report.json` — confirms the transcribed numbers:
  `branching_ratio=0.5999724…` (recorded as η=0.600 lower bound), `decays=0.001`
  AIC-min, `lr_test.observed_stat=561.2948…`, `lr_test.p_value=0.0`,
  `lr_test.rejects_at_alpha=true`, `ks_rescaled_time.per_leg[0].p_value=0.04735…`
  (recorded 0.0474), `per_leg[1].p_value=0.05644…` (recorded 0.0564),
  `held_out_loglik.hawkes=−1206.23`, `nhpp=−1320.63`, `gate_passes=false`,
  gate_criteria four booleans = true/true/false/true(+stationary). All match
  the appended table. NO fabrication.
- `data/fits/ichi/bdaf5c7ba5a2/firing_condition.json` — `firing_condition=null_strip_unavailable`,
  `decided_by=…decide_firing_condition`. Matches the recorded DERIVED firing_condition.
- `data/fits/ichi/bdaf5c7ba5a2/gate_report.json` — `any_condition_passed=true`,
  all four convex-dominance conditions pass. Matches the "convex dominates linear" framing.
- `_artifacts/new_run_id.txt` = `bdaf5c7ba5a2`. Matches `ll_fit_rerun_run_id`.
- `DISPOSITION_MEMO_04_1_1_ks_halt.md` (committed 03132cd) — gate_passes=FALSE 3/4,
  KS knife-edge, min-leg aggregator locked, explicit "NOT relabeled positive" list.
  The appended section honors this verbatim: no flip, no narrowing, memo referenced.
- `git diff --numstat` on 04-VERIFICATION-pre.md = `60 0` (append-only confirmed;
  18-row grid + 19 prior fields + 04.1 section preserved verbatim).

## Findings

- BLOCKER: none.
- MAJOR: none.
- MINOR: none. The η is correctly framed as a LOWER BOUND; the verdict is
  recorded AS-OBSERVED (gate_passes=FALSE 3/4); the LS `null_lr` is correctly
  labeled an artifact; the firing_condition was derived (not pre-committed).

## VERDICT: PASS

The recorded verdict is honest and fully traceable to the on-disk artifacts.
No fantasy claims, no verdict inflation, no threshold edits. The knife-edge KS
miss is reported plainly with the locked min-leg aggregator. Supersession of
`ae9e3ba17900` is documented without overwriting its fit_report/residuals.
