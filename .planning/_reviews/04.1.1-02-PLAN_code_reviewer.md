# Code Reviewer Review — Plan 04.1.1-02 (v2)

**Plan:** 04.1.1-02 — lr_test.py Option A + profile_likelihood.py constrained-MLE CI
**Reviewer role:** Code Reviewer (correctness, contracts, scope discipline, anti-pattern gates)
**Date:** 2026-05-28

## Files reviewed

- `analysis/src/abrigo_x402/dgp/lr_test.py`
- `analysis/src/abrigo_x402/dgp/profile_likelihood.py`
- `analysis/tests/test_lr_test.py`
- `analysis/tests/test_profile_likelihood.py`

## Correctness

1. **`_fit_hawkes_ls_for_null`** rescales to t=0 origin (Hawkes LL is shift-invariant) before tick-LS, returns the dict shape the bootstrap loop consumes. The downstream `_hawkes_loglik_vectorized` recomputes the canonical LL at the LS-fitted params, so the null LR_b is on the same scale as the scipy-observed LR (Pattern F canonical-LL contract preserved). Correct.

2. **Observed-LL-scale fix** is automatic and correct: `ll_hawkes_obs` is computed from `hawkes_obs["baseline"]/["adjacency"]` which are now the scipy_canonical_ll params (fit_hawkes_expkern is scipy-only as of Plan 01). No LS params leak into the canonical LL. The documenting comment correctly attributes the prior 6.05M to the LS-into-canonical-LL artifact.

3. **`_constrained_profile_loglik`** enforces ρ(α/β)=η EXACTLY inside the objective: `anorm = araw * (eta_value / rho_raw)` rescales the raw adjacency so its spectral radius equals `eta_value * beta` (since `compute_branching_ratio = ρ(α/β)`, scaling α by `eta/rho_raw` yields ρ=eta_value). This is a genuine per-η constrained re-optimization over (λ₀, α) — NOT a one-shot projection of a single unconstrained fit. Multi-start L-BFGS-B with positivity bounds. Correct.

4. **`LL_max` is the unconstrained joint-MLE LL**, recomputed via `_hawkes_loglik_vectorized` at the scipy canonical params — NOT the max over a projected family. `eta_hat = hawkes_fit["branching_ratio"]` (joint-MLE η), NOT the grid argmax. The χ²(1) deficit is therefore inverted around the true MLE. Correct.

## Contracts & scope discipline

- **SC-3 grep gate** (lr_test.py): `grep -qE "likelihood_ratio_test|chi2\(1\)\.sf"` returns exit 1 (no match). PASS. No statsmodels LR helper, no scipy chi2 survival call introduced.
- **chi2(1) in profile_likelihood.py** is permitted per that file's module docstring (interior-parameter CI inversion); SC-3 is scoped to lr_test.py only. Honored.
- **Pattern I thread-pinning** prepended to both test files (4 `os.environ.setdefault` calls before numpy import).
- **n_reps=1000 production lock (AF-04)** unchanged — `PRODUCTION_N_REPS=1000` untouched; Option A only changes the per-replicate estimator.
- **Return-dict keys** preserved: lr_test returns the same 9 keys; profile_likelihood keeps `method, eta_hat, lower, upper, ci_width, alpha, q9_nullfire_triggered, q9_threshold`. Dropped `eta_hat_unconstrained` (now equals eta_hat) per plan action (3). Downstream `branching_ci_excludes_zero = lower > 0` resolvable.
- **AF-12 OUT-OF-SCOPE held:** no new kernel forms, no new firing conditions, no new gate criteria, no threshold changes (LR α 0.01 / KS α 0.05 / η-floor 0.2 / Q-9 0.4 floor all untouched), no hawkes_fit.py edits (consume-only — `fit_hawkes_with_fixed_branching_ratio` left intact), no synthetic deletion, no run_id overwrite.
- **`_hawkes_loglik_vectorized`, `_nhpp_pointprocess_loglik`, `_simulate_nhpp_under_null`, seed derivation, diagnostic-plot block** all untouched in lr_test.py.

## Test quality

- `test_option_a_null_replicates_use_ls`: asserts the LS tag + dict shape. Good.
- `test_bootstrap_observed_stat_finite_scale`: asserts `< 1e5` (pathology) + finite + n_reps==30. Good.
- `test_constrained_mle_ci_not_projection`: asserts method literal, eta_hat == joint-MLE η, sane [0.30,0.70] range, lower > 0. Strong genuine-CI assertion.
- Removed orphaned `test_fit_hawkes_with_fixed_branching_ratio_projection` (tested the retracted projection helper from profile_likelihood's perspective). Appropriate — the helper is no longer on profile_likelihood's path.

## VERDICT

PASS — both edits are correct, scope-disciplined, and honor the canonical-LL contract (Pattern F), the SC-3 grep gate, Pattern I thread-pinning, and the AF-12 OUT-OF-SCOPE list. The constrained MLE genuinely re-optimizes subject to ρ(α/β)=η; the CI references the true joint-MLE LL. No anti-pattern gate violations.
