## VERDICT

PASS

## Scope

Plan 04.1-03: run `cli.py hedge --run-id <NEW_RUN_ID> --stage all` against real-data run_id; write archival `0afc6af38e24/README.md`; APPEND "04.1 Real-Data Rerun" section + new frontmatter fields to `04-VERIFICATION-pre.md`; single atomic `docs(04.1):` commit; full regression. Revised after iter-1 NEEDS WORK on 4 substantive items.

## Findings

- **Iter-1 fix #1 (GATE_REPORT_REQUIRED_KEYS pre-flight, closes the fantasy-value risk):** the fragile `.get('vol_of_vol_gt_zero', {}).get('passed', False)` chain replaced with a strict schema pre-flight that imports `GATE_REPORT_REQUIRED_KEYS` from `scripts/lint_artifacts.py`, asserts every required key is present, then uses direct subscript access `g['vol_of_vol_gt_zero']['passed']`. KeyError on schema drift is now the DESIRED behavior. Two new acceptance criteria: zero `.get(key, {}).get(...)` chains in plan body, and grep-for-import. This was the highest-risk item; closure is mechanical.
- **Iter-1 fix #2 (split conflated boolean):** `real_data_rerun_passed` replaced with TWO orthogonal fields — `real_data_rerun_artifacts_complete: <bool>` (artifact-emission completeness) + `real_data_rerun_firing_condition: <null | "null_cost" | "null_lr" | "null_convex" | "null_strip_unavailable">` (scientific outcome enum matching Plan 04-08's `decide_firing_condition`). Phase 5 now has unambiguous signals. Total appended frontmatter is 4 fields (not 3): `real_data_rerun_run_id`, `real_data_rerun_artifacts_complete`, `real_data_rerun_firing_condition`, `substrate_substitution_resolved`. Post-amendment total frontmatter = 19 fields.
- **Iter-1 fix #3 (HEDGE-05 expected-fire framing):** new paragraph in `<objective>` + standalone paragraph in appended VERIFICATION section: "Real-data HEDGE-05 firing on condition (b) is EXPECTED, not anomalous … n=832 is below the 300-event identifiability floor from PRE_REGISTRATION … `null_lr` or `null_convex` IS the v1.0 scientific result." The executor and Phase 5 reader cannot mistake a legitimate HEDGE-05 fire for a methodology failure.
- **Iter-1 fix #4 (BIC noise-floor caveat):** comparison-table BIC winner row now labeled `winner ± Δ` with `Δ_BIC < 5 = noise-floor regime per VINE_FALLBACK_DELTA_BIC_THRESHOLD` caveat; real-column placeholder annotated with conditional "if |Δ| < 5, append 'noise-floor regime — no strong winner' caveat"; standalone footnote paragraph after the table. Phase 5 narrative must treat both columns as "no strong copula winner" when |Δ_BIC| < 5.
- **Bonus iter-2 fixes (Code Reviewer-flagged):** factual 14→15 frontmatter field count corrected at 6 sites with explicit field list; Edit-tool `old_string`/`new_string` anchors specified verbatim so executor cannot guess.

## Reality check

Iter-2 closed the fantasy-positive failure mode at its source. The brittle `.get().get()` chain → KeyError-on-schema-drift; the conflated bool → two orthogonal fields; the HEDGE-05 expected-fire path → framed as the v1.0 scientific result; the BIC noise-floor → explicit annotation. Phase 5's downstream PDF render cannot accidentally treat a noise-floor BIC flip or a legitimate HEDGE-05 fire as a defect.

## Recommendation

Accept. Iter-1 NEEDS WORK items closed. Plans ready for `/gsd:execute-phase 04.1`.
