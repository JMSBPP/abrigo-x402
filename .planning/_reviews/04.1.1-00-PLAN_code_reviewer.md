## VERDICT

PASS

## Scope

Code-reviewer pass on `.planning/phases/04.1.1-.../04.1.1-00-PLAN.md` (v2) — the AF-03 pre-registration supersession plan. Single-task docs-only plan: review-then-commit the already-drafted v2 supersession section in `notes/PRE_REGISTRATION.md`; single atomic commit; create the 2-way review trail. No source-code edits in this plan.

(This file supersedes the v1-cycle code-review on the same PLAN.md path; v1 content is preserved in git history.)

## Findings

- **Plan frontmatter consistent with the AF-03 amendment precedent** (`docs(pre-reg:)` commit pattern, cf. `2dc3877` Carr-Madan amendment and `f7ae84d` v1 LL-fit amendment). `wave: 0`, `depends_on: []`, `files_modified: [notes/PRE_REGISTRATION.md]`, `autonomous: true`, `gap_closure: true`, `requirements: [DGP-02, DGP-03, DGP-06, DEPEND-01, DEPEND-02, HEDGE-01..05]` — cross-references only, no new IDs (AF-12 silent-rescope defense holds).

- **`must_haves` block is greppable + audit-friendly:** 7 truths encoding the retraction of both bands, the free-β scipy canonical estimator, the synthetic [0.40, 0.60] band, the no-fixed-real-band rule, the unchanged four-criterion gate, the Option A authorization, and the ordering invariant; 1 artifact entry; 1 key_link encoding the AF-03 `git log --pretty=format:'%H %s %ai'` ordering pattern over `notes/PRE_REGISTRATION.md` + `analysis/src/abrigo_x402/dgp/`.

- **Review-then-commit contract is mechanical.** The drafted v2 section (lines ~197–241) is uncommitted (` M` in `git status --short`). The plan's `<action>` STEP 1 enumerates 10 verbatim-presence checks (a)–(j); all 10 confirmed present in the working tree. The executor's mandate is explicitly review-and-commit-as-is, NOT re-author — no paraphrase license. This eliminates the principal authoring-drift risk.

- **Pre-commit hook contract satisfied.** `review_trail.sh` requires `.planning/_reviews/04.1.1-00-PLAN_{reality_checker,code_reviewer}.md` with `## VERDICT` first H2 + PASS + no unresolved BLOCKER. This file + the paired `_reality_checker.md` (refreshed to v2 content) satisfy the contract.

- **`af_lint.sh` AF-12 OUT-OF-SCOPE:** the v2 section introduces NO forbidden tokens (`transaction_index|gas_used|panel_schema_version|panel_augment`); the four Phase-4 source-grep gates target `analysis/src/abrigo_x402/hedge/*.py` only and do not trigger on `notes/PRE_REGISTRATION.md`. The `docs(pre-reg:04.1.1):` subject prefix is the af_lint.sh-recognized form for PRE_REGISTRATION.md edits.

- **Automated `<verify>` block passes against the working tree** (run pre-commit): v2 header = 1; `free-β AIC-selected scipy joint-MLE` = 1; `scipy_canonical_ll` = 4 (≥2: v1 + v2); `[0.40, 0.60]` = 1; `genuine constrained MLE` = 1; `adjust_spectral_radius(0.5)` = 2; `Option A` = 3; `four-criterion gate` = 8; v1 header `## Phase 04.1.1 — LL-fit acceptance & fallback chain` still = 1.

- **Append-only / no-spillover.** Working-tree `git diff notes/PRE_REGISTRATION.md | grep '^-' | grep -v '^---' | wc -l` = 0 (zero deletions — v1 history preserved verbatim). The plan instructs staging ONLY `notes/PRE_REGISTRATION.md` + the two review files via explicit per-file `git add` — the uncommitted `hawkes_fit.py` working-tree fix and the six v2 PLAN.md files are NOT swept in (they belong to later waves).

- **Commit subject matches the plan body exactly** (`docs(pre-reg:04.1.1): supersede LL-fit bands per independent diagnostic (v2)`), supplied via HEREDOC with the AF-12 OUT-OF-SCOPE bullets in the body for forensic grep-ability.

## Reality check

The unicode surface (η = U+03B7, β = U+03B2, — = U+2014, ∈ = U+2208) is already correct in the drafted text; the executor preserves it by committing as-is rather than retyping. No CLI flag additions, no new module imports, no test-surface changes, no schema bumps, no PANEL-02 header changes in this plan. The pre-commit battery (AF-01..AF-12 + the 4 Phase-4 grep gates) will all PASS — none target `notes/PRE_REGISTRATION.md`, and the review-trail enforcement is satisfied by this 2-way trail.

One scoping flag for the executor (non-blocking): the prompt's alternate subject `... after diagnostic` differs from the plan body's `... per independent diagnostic (v2)`. The plan body is authoritative and its acceptance criteria grep for the latter — use the plan-body subject exactly.

## Recommendation

Accept. The plan is a faithful review-then-commit of a pre-drafted, diagnostic-grounded supersession; append-only over v1 history; staging scoped to the amendment + review files only; verdict gate untouched. Ready for execution. No unresolved BLOCKER.
