## VERDICT

PASS

## Scope

Plan 04.1-02: run `cli.py fit` against the augmented real ICHI panel from Plan 04.1-01 under thread-pinned BLAS; capture new 12-hex run_id; verify `fit_report.json` + `residuals.parquet` emit; persist `new_run_id.txt` for Plan 04.1-03; full regression suite. Revised after iter-1 NEEDS WORK on shell typo + Pattern F double-fallback + byte-identity scope.

## Findings

- **Iter-1 fix #1 (shell quote typo):** unbalanced `_artifacts/new_run_id.txt).read()` → `_artifacts/new_run_id.txt').read()`. Python syntax error eliminated; literal shell invocation now executes cleanly.
- **Iter-1 fix #2 (Pattern F double-fallback diagnostic):** new Step C-bis prepended before Step D. If both `gofit="likelihood"` AND `gofit="least-squares"` fail on the real panel, the executor writes `data/fits/ichi/<errored-run-id>/fit_error.json` with the prescribed schema (`{stage, exception_type, exception_msg, panel_dataHash, gitCommit, fetchTimestamp}`) and STOPS the pipeline cold. Two BLOCKER paths documented (Path A: fix fit code in new 04.1-NN; Path B: document failure as v1.0 finding + trigger HEDGE-05 condition b). Acceptance criterion: `! test -f fit_error.json` (success) OR `test -f fit_error.json && exit 1` (BLOCKER).
- **Iter-1 fix #3 (byte-identity scope clarification):** `must_haves.truths` now states the correct invariant — `_derive_run_id(panel_dataHash, gitCommit, tick_version)[:12]` run_id determinism under unchanged inputs; raw parquet byte-identity and fit_report.json byte-identity are NOT Phase 04.1 requirements. Phase 3 SC-5 byte-identity contract applied to synthetic fixtures only.
- `fit` subcommand argparse signature still matches `cli.py:203-231`. Thread-pinning preserved.

## Reality check

Iter-2 closed the diagnostic-thinness gap I flagged. The real-panel risk hierarchy is now: (1) double-fallback failure → captured as `fit_error.json` and surfaced as BLOCKER (not silent fantasy parameters); (2) η̂ near stationarity boundary → handled by existing `baseline_stationarity_check` and naturally fires HEDGE-05 condition (b) — that's the intended path per Plan 04.1-03's HEDGE-05 expected-fire framing.

## Recommendation

Accept. Iter-1 NEEDS WORK items closed. Plans ready for `/gsd:execute-phase 04.1`.
