# Reality Checker — Plan 04.1.1-04

Plan: `04.1.1-04-PLAN.md` — hedge production rerun on the corrected canonical run_id `bdaf5c7ba5a2`; derive firing_condition AS-OBSERVED via `decide_firing_condition` + emit the standard 6-artifact hedge set. NO source edits, NO verdict tuning.

## VERDICT

PASS

The hedge orchestrator ran end-to-end on the corrected `scipy_canonical_ll` run_id under thread-pinned BLAS with zero source edits. Every artifact and the manifest are reproducible against the live `data/fits/ichi/bdaf5c7ba5a2/` directory. The firing_condition was DERIVED by the locked decision tree (not pre-committed, not tuned) and the verdict gate_passes=FALSE (3/4) was recorded AS-OBSERVED, not flipped. Detail below.

## Claims checked against the live artifacts

1. **Invocation thread-pinned, ran to completion.** `run_log.txt` carries the `hedge.orchestrator.run_hedge completed` sentinel (1 hit). The printed JSON summary lists all artifact paths + `firing_condition: null_strip_unavailable` + `quarto_skipped: true`. CONFIRMED.

2. **6-artifact set present.** `joint_dist.json` (degenerate family) + `gate_report.json` + `strip_degenerate.json` + `stress_report.json` + `firing_condition.json` + `run_log.txt` all exist under `data/fits/ichi/bdaf5c7ba5a2/`. The plan's automated verify-block returns VERIFY PASS. CONFIRMED.

3. **firing_condition DERIVED, not pre-committed.** `firing_condition.json :: firing_condition = null_strip_unavailable`, `decided_by = abrigo_x402.hedge.null_result.decide_firing_condition`. Tree trace: (a) cost not fired; (b) lr NOT fired (lr_test.p_value=0.0 < 0.05 → LR rejects); (c) convex NOT fired (any_condition_passed=True); (d) strip FIRED (strip_degenerate.json exists). The value follows mechanically from the live fit_report + gate_report; no flag was passed to force it. CONFIRMED.

4. **Divergence-vs-LS recorded honestly.** OLD ae9e3ba17900 = `null_lr` (LR p=0.58 did not reject); NEW bdaf = `null_strip_unavailable` (LR p=0.0 rejects). The manifest states the LS `null_lr` was an artifact and is no longer carried forward. CONFIRMED, framed honestly (not as a verdict flip).

5. **Convexity-justified / calibration-caveated framing accurate.** gate_report `any_condition_passed=True` with all 4 convex conditions passing (vol_of_vol=1.180, skew=1.849/excess_kurt=3.602, branching_ratio=0.600≥0.2, depeg jump stipulated). The strip is degenerate (`build_failed_upstream`, n_min=79<101). Convex SHAPE robust, strip CALIBRATION unavailable — matches CONTEXT v2 invariant 4 exactly. CONFIRMED.

6. **Verdict NOT flipped.** gate_passes=FALSE (3/4) stands. No KS α / aggregator / held-out split / threshold change applied (DISPOSITION_MEMO + CORRECTIONS.md hold). The hedge run does not touch the verdict gate — it only routes the firing tree. CONFIRMED.

7. **NO source edits.** `git diff HEAD -- analysis/ scripts/` == 0 lines. Rule-3 patches greppable (12 hits) and fired this run (n_min guard → degenerate joint_dist; quarto_skipped sentinel). `ae9e3ba17900` firing_condition still `null_lr` (untouched). CONFIRMED.

## Fantasy-claim scan

No fantasy positives. The result is a null/near-miss (firing_condition=null_strip_unavailable; gate_passes=FALSE) recorded faithfully. The descriptive evidence (η≈0.6, LR rejects, all-4 convex gate) is reported AS DESCRIPTIVE, with the strip-degenerate calibration caveat stated plainly. This is the pre-registered handling for gate_passes=FALSE — not a relabel.

## Residual notes (non-blocking)

- The held-out residual n_min=79 (< the n=83+79 implied by the fit) reflects the Rule-3 DEPEND-01 lag-radius floor on the dependence-stage residuals; correctly emitted as a degenerate joint_dist rather than crashing. This is the designed n<101 real-panel path.
- `run_log.txt` is produced by tee-capturing the deterministic single-threaded stdout (operator step, as in Plan 04.1-03); not written by the orchestrator. Deterministic under thread-pinning.
