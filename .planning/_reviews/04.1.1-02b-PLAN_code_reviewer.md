# Code Reviewer — Plan 04.1.1-02b (orchestrator run_fit free-β rewire)

## Files in scope

- `notes/PRE_REGISTRATION.md` — append-only AF-12 expansion authorizing the orchestrator edit (Task 1)
- `analysis/src/abrigo_x402/dgp/orchestrator.py` — `run_fit` signature `decays: float = 0.1` → `decays: float | None = None` (Task 2 GREEN)
- `analysis/tests/test_dgp_orchestrator.py` — new `test_run_fit_uses_free_beta_aic_grid` (Task 2 RED→GREEN)

## Review findings

1. **Signature change is type-safe.** `decays: float | None = None` is consumed by
   `fit_hawkes_expkern(leg_0, leg_1, decays=...)` whose own signature is
   `decays: float | None = None` (hawkes_fit.py L200). No downstream call casts `decays`
   directly before the fit — the only `float(...)` cast is on `hawkes_train["decays"]`
   (L315), which is always a concrete float in the returned fit dict. Passing None is safe.

2. **Docstring updated to match behavior.** L254 changes from "Default 0.1 per DECAY_GRID"
   to documenting the free-β AIC default + the diagnostics-only float-pin path. Prevents
   doc drift from the new default.

3. **Test asserts the right invariants.** `test_run_fit_uses_free_beta_aic_grid` checks
   `fit_report["hawkes_mv_params"]["decay_aic_table"]` has all 6 DECAY_GRID entries and
   `fit_method_used == "scipy_canonical_ll"`. Against the old `decays=0.1` default the
   table is 1-entry → test fails (genuine RED). The test does NOT pass `decays`, exercising
   the production default path. Thread-pinning (Pattern I) is the first executable block.

4. **No weakening of existing tests.** `test_byte_identical.py` calls `run_fit` without
   passing `decays`, so it now exercises free-β AIC on both runs — byte-identity is
   preserved because the AIC selection is deterministic under thread-pinned BLAS (the same
   reason SC-5 pins threads). No existing test hard-expects `decays=0.1`.

5. **Atomic-commit discipline.** Task 1 (pre-reg + reviews) commits before Task 2
   (orchestrator + test), per AF-03. Explicit per-file `git add`; no `git add -A`. The
   uncommitted run dir `000c1cdce376/`, the untracked v2 PLAN.md files, and the blocker
   note are NOT swept in.

6. **No estimator-math edits.** `git show --stat` on the GREEN commit will show only
   orchestrator.py + test_dgp_orchestrator.py — hawkes_fit.py / lr_test.py /
   profile_likelihood.py untouched.

## VERDICT: PASS

The change is minimal, type-correct, well-tested, and respects the commit-ordering and
scope invariants. No issues blocking implementation.
