# Reality Check — Plan 04.1.1-01 (v2)

Reviewer pass date: 2026-05-28
Scope: pre-commit reality check of the executed Plan 04.1.1-01-v2 (canonical scipy_canonical_ll
estimator + regenerated η=0.5 fixture + revised v2-band tests) against the on-disk facts.

## Verifiable claims checked

1. **AF-03 ordering gate** — `git log --pretty=format:'%H %s %ai' -- notes/PRE_REGISTRATION.md
   analysis/src/abrigo_x402/dgp/` shows pre-reg v2 commit `fda5905` (2026-05-28 07:12:14 -0400)
   is the most recent PRE_REGISTRATION touch; latest `dgp/` commit `7a20c6b`/`74701d0`
   (2026-05-26) PREDATES it. No v2 `dgp/` commit exists yet. → GATE SATISFIED before edits.

2. **Mislabeled v1 fixture confirmed** — the committed `synthetic_hawkes_eta_05.parquet`
   recovers η̂≈0.0475 under the canonical scipy fit (observed in the RED run), corroborating
   the diagnostic's claim that tick's normalized kernel makes true η = ρ(α) = 0.05, NOT the
   labeled 0.5. The v1 synthetic band [0.45, 0.55] was therefore testing a panel that never
   had η=0.5. → RETRACTION justified on result-independent grounds.

3. **Estimator soundness** — the same scipy_canonical_ll path, on a genuinely-η=0.5 simulation
   via `tick.adjust_spectral_radius(0.5)` at matched β (DIAGNOSTIC §Q1: mean η̂=0.540, sd 0.076,
   5 seeds; ~13% downward bias at n≈700), recovers 0.5 within tolerance. The [0.40, 0.60] band
   accommodates the measured bias. → band defensible, not fishing.

4. **Real-panel band retraction** — no fixed real-panel η band is asserted anywhere in the
   revised test file; only the η-floor 0.2 LOWER BOUND. The retracted [0.283, 0.371] /
   [0.45, 0.55] literals are absent (grep-enforced). → AF-03 fishing avoided.

5. **Pattern F preserved** — scipy wraps `_hawkes_loglik_vectorized` from `lr_test.py`
   (`from .lr_test import _hawkes_loglik_vectorized` present). `lr_test.py` and
   `profile_likelihood.py` are NOT edited in this plan. → in-scope boundary respected.

6. **BLOCKER discipline** — `_fit_with_scipy_canonical_ll` raises RuntimeError on all-multistart
   failure; `fit_hawkes_expkern` only raises "All decay grid candidates failed" when EVERY
   decay fails. No code path returns `fit_method_used == "least-squares"`. → no silent LS.

## Residual uncertainty
- Finite-sample downward bias (~13%) at n≈700 is a documented residual, surfaced in
  PRE_REGISTRATION as "reported real-panel η is a LOWER BOUND". Not a defect.
- Full LR/KS re-derivation is Plan 02-v2's job; this plan does NOT claim any LR/KS outcome.

## VERDICT
PASS — the executed plan matches the pre-registered v2 corrections; the retractions rest on
verifiable errors (tick.spectral_radius()=0.05; the projection-trick source); AF-03 ordering
holds; no verdict-deciding threshold is altered.
