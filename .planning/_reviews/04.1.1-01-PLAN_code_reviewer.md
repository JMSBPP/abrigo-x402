# Code Review — Plan 04.1.1-01 (v2)

Reviewer pass date: 2026-05-28
Scope: code-level review of the three changed surfaces — the regenerated fixture + generator,
`hawkes_fit.py` scipy-primary finalization, and the revised `test_hawkes_fit.py`.

## hawkes_fit.py
- `_fit_at_decay` reduced to: rescale to t=0-origin → `_fit_with_scipy_canonical_ll`. The tick
  `gofit="likelihood"` primary attempt and the scipy-as-fallback wrapper are REMOVED. No
  `why_not_likelihood` field is set on the live path. ✓
- `_fit_with_scipy_canonical_ll` unchanged: multi-start (3 points), positivity bounds
  `(1e-12, None)`, raises RuntimeError on all-starts-fail, returns
  `fit_method_used="scipy_canonical_ll"`. ✓ (Pattern F: wraps `_hawkes_loglik_vectorized`).
- `DECAY_GRID = (0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)` verbatim. `_K_PARAMS = 7` unchanged. ✓
- `_fit_with_gofit` retained with the added grep-discoverability docstring; `_GOFIT_PRIMARY`/
  `_GOFIT_FALLBACK`/`_PENALTY` constants retained. Dead on the live path but importable. ✓
- `fit_hawkes_with_fixed_branching_ratio` (projection trick) left UNTOUCHED so the
  profile_likelihood import still resolves — its replacement is Plan 02-v2. ✓
- Module docstring "Runtime deviation" paragraph updated to state scipy_canonical_ll is the
  live estimator and tick.likelihood is removed; cites PRE_REGISTRATION §Phase 04.1.1 (v2). ✓
- `fit_hawkes_expkern` AIC-min loop unchanged: marks failing decays AIC=inf, only raises when
  every candidate fails. No LS returnable. ✓

## fixture + generator
- `_gen_synthetic_hawkes_eta_05_v2.py`: thread-pinning prelude FIRST (OMP/MKL/OpenBLAS/NumExpr)
  before any numpy/tick import; `adjust_spectral_radius(0.5)` with a 1e-9 assert; n-band assert
  [600,900]; manifest update preserves `synthetic_nhpp_baseline_only`. ✓ (gitignored like
  `_gen_n100.py`; committed output parquet + manifest).
- Regenerated parquet: columns `{leg, event_time}`, true tick.spectral_radius()==0.5, n≈700. ✓

## test_hawkes_fit.py
- Band constants replaced: `_V2_ETA_BAND_SYNTH = (0.40, 0.60)`, `_V2_ETA_FLOOR_REAL = 0.2`.
  Retracted `_AF_03_ETA_BAND_SYNTH`/`_AF_03_ETA_BAND_REAL` and the `0.283`/`0.371`/`0.45`/`0.55`
  literals are GONE. ✓
- `test_hawkes_likelihood_mode_succeeds_on_synthetic` asserts EXACTLY scipy_canonical_ll. ✓
- `test_likelihood_mode_eta_recovers_synthetic_ground_truth` uses [0.40, 0.60]. ✓
- `test_likelihood_mode_eta_within_v2_acceptance` (renamed from `_within_profile_ci`): synthetic
  band [0.40,0.60]; real_panel scipy_canonical_ll + η≥0.2 lower bound, NO fixed band; skip guard
  when real parquet absent. ✓
- `test_scipy_canonical_ll_is_primary_path` (renamed from `_scipy_fallback_path_isolated`):
  direct `_fit_at_decay` call, no mock, asserts scipy_canonical_ll + in-bounds η; the unused
  `from unittest.mock import patch` import removed. ✓
- AF-12 OUT-OF-SCOPE comment block notes the v2 expansion (lr_test.py + profile_likelihood.py
  now IN scope for Plans 02-v2). ✓

## Risks / notes
- The RED run confirmed the revised band tests fail on the OLD fixture (η=0.047) while the
  scipy_canonical_ll method assertion passes — clean RED isolating the fixture from the estimator.
- Full quick suite (test_lr_test + test_profile_likelihood) run as a non-blocking diagnostic;
  any churn from the fixture change is flagged to Plan 02-v2 which owns those files.

## VERDICT
PASS — changes are minimal, scoped to the three plan surfaces, preserve Pattern F, honor the
BLOCKER-no-silent-LS discipline, and remove the retracted constants. Ready to commit.
