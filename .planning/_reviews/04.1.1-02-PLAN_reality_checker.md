# Reality Checker Review — Plan 04.1.1-02 (v2)

**Plan:** 04.1.1-02 — lr_test.py Option A + profile_likelihood.py constrained-MLE CI
**Reviewer role:** Reality Checker (does the implementation match what the plan claims, on real artifacts?)
**Date:** 2026-05-28

## Scope reviewed

- `analysis/src/abrigo_x402/dgp/lr_test.py` — Option A null-replicate split + observed-LL-scale fix
- `analysis/src/abrigo_x402/dgp/profile_likelihood.py` — genuine constrained-MLE η-CI
- `analysis/tests/test_lr_test.py` + `test_profile_likelihood.py`

## Evidence checked (measured, not asserted)

1. **AF-03 ordering (real git log):** `fda5905` (pre-reg v2, 2026-05-28 07:12:14) predates the lr_test.py commit `5ad2116` (08:10:41) and the profile_likelihood.py commit. Verified via `git log --pretty=format:'%H %ai %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/`. PASS.

2. **Option A null replicate is genuinely the cheap path:** `_fit_hawkes_ls_for_null` calls `_fit_with_gofit(..., gofit="least-squares")` (tick-LS, ~0.024s/rep per DIAGNOSTIC §Q4), tagged `fit_method_used == "least-squares-null-replicate"`. The bootstrap loop no longer calls `fit_hawkes_expkern` for null replicates (grep confirms `fit_hawkes_expkern` appears only at the observed-fit site L292 + import L50). The observed fit stays scipy_canonical_ll. PASS.

3. **Observed-LL-scale pathology resolved on real measurement:** `test_bootstrap_observed_stat_finite_scale` asserts `abs(observed_stat) < 1e5` on the synthetic-NHPP fixture and passed. The prior 6.05M statistic was LS-fallback params fed into the canonical LL; with the scipy observed fit the observed_stat is finite. PASS.

4. **Constrained-MLE CI is genuine (measured on the regenerated fixture sha256 c73a9ca3, true η=0.5, n=721):** direct probe returned `method=constrained_mle_profile, eta_hat=0.4887 (= joint-MLE η, NOT grid argmax), lower=0.4366 (>0), upper=0.5487, width=0.1121`. The CI brackets the truth (0.5) and the joint-MLE η, excludes zero, and is a tight interior interval — NOT the retracted [0.283, 0.371] projection band. The per-η inner solve re-optimizes the canonical LL subject to ρ(α/β)=η exactly (alpha renormalized inside the objective). PASS.

5. **Tractability:** full constrained-MLE CI = ~146s (one call), one constrained LL eval ~2.8s. The 30-grid is tractable. The lr_test.py n_reps=30 smoke completes in seconds; the full test_lr_test.py suite (incl. n_reps=200 power/size tests) ran 8 passed in 253s. Option A makes the 2×1000 production bootstrap minutes, not 143 min. PASS.

## Concerns / residual risk

- The constrained-MLE CI grid (30 points × 3-start L-BFGS-B) makes the full test_profile_likelihood.py suite slow (~12 min). This is acceptable for a TDD gate but the production fit_report path (Plan 04.1.1-03) should be timed. Flagged, not blocking.
- `fit_hawkes_with_fixed_branching_ratio` remains in hawkes_fit.py (consume-only invariant honored) but is now dead from profile_likelihood's perspective; still re-exported in `dgp/__init__.py`. No orphaned importers in tests/src. Acceptable.

## VERDICT

PASS — the implementation matches the plan's claims on real artifacts. Option A null/observed split is genuine and cheap; the observed-LL-scale pathology is resolved (finite observed_stat); the η-CI is a genuine constrained MLE centered on the joint-MLE η that brackets the true η=0.5 and excludes zero. AF-03 ordering holds for both source files.
