## VERDICT

PASS

## Scope

Code-reviewer pass on Plan 04.1-01 (Wave 2 panel rerun: invoke `cli.py materialize` against cached JSONL sidecars to regenerate `data/raw/ichi/0x61Ef.../67378253_67896653.parquet` with the new `block_timestamp` column from Plan 04.1-00's ingest edit; verify schema; confirm sha256 changed; lint PASS; pure rerun, no code edits).

## Findings

- Frontmatter complete: `wave: 2`, `depends_on: ["04.1-00"]` (correct upstream), `files_modified` declares the single parquet path, `requirements: [PANEL-01, PANEL-02, DEPEND-01]` appropriate for a panel re-materialization step.
- CLI subcommand `materialize` verified to exist in `analysis/src/abrigo_x402/cli.py` lines 188-197 with exact argparse signature (`--pool`, `--from-block`, `--to-block`, `--protocol-toml`, `--forno-head`). The plan's invocation matches verbatim.
- `materialize` handler at lines 58-121 reads `pool_events.jsonl` + `vault_state.jsonl` + `fx_snap.jsonl` + optional `tx_logs.jsonl` from `data/raw/ichi/<pool>/` (verified), calls `build_panel(...)`, writes parquet with full PANEL-02 header — plan's flow description matches.
- Pre/post sha256 comparison + the documented baseline `2234b635729e2fbc307989f75bfdc8b21e29cd06b720b45682d2d385a26b5b2e` is a clean, falsifiable verification step. Plan correctly notes "if different, do NOT abort — record the actual value" (defensive).
- Schema-verification Polars one-liner is concrete: checks presence + dtype + null_count + height=832. All grep/test-verifiable.
- Thread-pinning env-vars (`OMP_NUM_THREADS=1` etc.) preserved in invocation — consistent with Phase 3 SC-5 byte-identity contract, even though materialize itself is not byte-identity-sensitive. Defensible.
- `<acceptance_criteria>` enumerate 9 distinct checks: file existence, column presence, dtype, zero-null, height=832, sha256 change, lint PASS, `git diff HEAD -- analysis/` empty (no source drift), cost-ledger unchanged. All falsifiable.
- Read-first list correctly identifies `cli.py` + `panel.py` as do-not-edit upstream sources and the 04.1-00-SUMMARY.md as the load-bearing predecessor pointer. The `protocols/ichi.toml` reference is appropriate for confirming `finality_lag_blocks`.
- Out-of-scope list correctly excludes re-fetching, code edits, schema bumps, and CLI flag additions. AF-12-defended.
- Plan correctly anticipates the `.gitignore` ambiguity for `data/raw/` — provides a conditional `git add` + commit fallback path. Pragmatic.
- **Minor:** column-count line in Step C narrative says "32 → 33" — the actual current count was not verified by this review; the plan's automated check uses `len(df.columns)` not a hard-coded 33, so any drift is caught.
- **Minor:** plan correctly does NOT touch any Phase 2/3 source — verified by `git diff HEAD -- analysis/` empty assertion.

## Recommendation

Accept.
