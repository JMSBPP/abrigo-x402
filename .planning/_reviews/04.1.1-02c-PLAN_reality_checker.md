# Reality Checker — Plan 04.1.1-02c

Plan: `04.1.1-02c-PLAN.md` — verdict-gate correctness fixes (LR common-t0 time origin + β coherence + scipy stationarity rejection + profile-likelihood CI grid-floor).

## VERDICT

PASS

The three findings are reproducible in the live source; the fixes are implementation-correctness enforcement of already-locked conditions, not spec changes. AF-03 ordering (pre-reg note first) is honored. Detail below.

## Claims checked against the live source

1. **THE BLOCKER is real.** `lr_test.py` `_hawkes_loglik_vectorized` (L78) sets `t_end = max(leg_0.max(), leg_1.max())` and the integral term is `baseline[i]·t_end` (L100). The observed leg is scored on absolute epoch arrays `leg_0_times`/`leg_1_times` (L318-320) and each null replicate on `sim_*_abs = sim + window_start` (L361-362, L384-390). With `window_start` the real-panel epoch (~1.7e9) the integral baseline·t_end is epoch-inflated. The LS null fitter `_fit_hawkes_ls_for_null` (L199) DOES rescale to `t0=0` internally (`s0 = sim_0 - t0`) but the scoring at L384-390 uses the un-shifted `sim_*_abs` → fit/scoring origin mismatch. CONFIRMED — the time-origin asymmetry exists exactly as the plan states.

2. **β divergence is real.** `parametric_bootstrap_lr` calls `fit_hawkes_expkern(leg_0_times, leg_1_times)` (L292) with no `decays` → free-β AIC grid, independent of the orchestrator's `hawkes_decays_t` (orchestrator L318). The orchestrator's LR call (L292) passes no `hawkes_decays`. CONFIRMED — the two β can diverge.

3. **No stationarity guard in scipy fit.** `_fit_with_scipy_canonical_ll` `neg_ll` (L128-134) computes LL with bounds `(1e-12, None)` and no ρ(α/β)<1 rejection. CONFIRMED — an explosive optimum is reachable.

4. **CI grid floor.** `ETA_GRID_DEFAULT = tuple(np.linspace(0.02, 0.95, 30))` (profile_likelihood.py L45). The brentq lower-bracket clamps at `max(1e-4, ...)` (L230) but the grid's lowest sampled point is 0.02, so `branching_ci_excludes_zero` can be a grid-floor artifact. CONFIRMED.

## Scope discipline

The plan's IN-scope set (lr_test.py, hawkes_fit.py, profile_likelihood.py, orchestrator.py + tests) matches the §Phase 04.1.1 (v2) authorized expansion and the new 02c pre-reg note. No threshold constants are touched. The χ²(1) basis and the four-criterion gate are preserved. The estimator split (Option A) is kept — only the time origin is corrected.

