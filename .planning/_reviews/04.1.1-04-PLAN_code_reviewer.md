# Code Reviewer — Plan 04.1.1-04

Plan: `04.1.1-04-PLAN.md` — hedge production rerun on `bdaf5c7ba5a2`; consume the corrected estimator + hedge code as-is, emit the 6-artifact set, derive firing_condition.

Reviewer-2 selection rationale: this is a pure-invocation plan that consumes already-shipped DGP + hedge source; the closest-fit specialist is the Code Reviewer (source-drift integrity, decision-tree wiring correctness, Rule-3 patch preservation, artifact-contract conformance) rather than a domain authoring specialist.

## VERDICT

PASS

No source edited; the preserved Rule-3 orchestrator patches behaved exactly as designed on the n<101 real-panel substrate; the firing decision routed correctly through the locked 4-condition tree; all emitted artifacts conform to their REQUIRED_*_KEYS contracts.

## Source-drift check

- `git diff HEAD -- analysis/ scripts/` == empty (0 lines). No edits to `hedge/orchestrator.py`, `hedge/null_result.py`, `cli.py`, or any `dgp/` module.
- Rule-3 Plan 04.1-03 (36881ea) patches PRESERVED and greppable (`n_min|quarto_skipped|build_failed_upstream` → 12 hits):
  - dependence-stage `_n_min_check <= 2*50+1` guard (L359-360) → fired (n_min=79) → emitted degenerate `joint_dist.json` with REQUIRED_JOINT_DIST_KEYS + `degenerate_reason`.
  - null-stage `quarto_skipped` sentinel → fired (quarto absent) → recorded `quarto_skipped: true` + wrote `firing_condition.json`. PASS.

## Decision-tree wiring correctness

`decide_firing_condition` (null_result.py L65-131) evaluated in the load-bearing sequential order:
- (a) cost: cost_leg_bound_path None → skipped. Correct.
- (b) lr: `fit_report.lr_test.p_value = 0.0`; `0.0 >= 0.05` is False → not fired. Correct (LR rejects ⇒ NOT null_lr — the divergence from the LS run is sound, not a wiring regression).
- (c) convex: `gate_report.any_condition_passed = True` → `not True` = False → not fired. Correct.
- (d) strip: orchestrator passes `run_dir=run_dir` (L562) and `strip_degenerate.json` exists → returns `null_strip_unavailable`. Correct — the iter-3 fourth branch is reached exactly because run_dir is threaded.

## Artifact-contract conformance

- `joint_dist.json`: degenerate path carries `empirical_copula.family=degenerate`, `degenerate_reason`, `cross_correlogram`, `permutation_null`, `vine_fallback_used` — REQUIRED_JOINT_DIST_KEYS satisfied.
- `gate_report.json`: `any_condition_passed` + 4 per-condition evidence blocks — REQUIRED_GATE_REPORT_KEYS satisfied.
- `strip_degenerate.json`: `reason=build_failed_upstream`, `char_func_source=build_failed`, `recommended_method`, `max_negative_value`, `total_negative_mass`, `strip_degenerate=True` — STRIP_DEGENERATE_REQUIRED_KEYS satisfied.
- `stress_report.json`: `divergence_pct` (NaN), `divergence_flag` (False), prices, `comonotone_method` — REQUIRED_STRESS_REPORT_KEYS satisfied.
- `firing_condition.json`: `firing_condition` + `decided_by` + run provenance fields.

## Determinism / thread-pinning

Invocation prepended OMP/MKL/OpenBLAS/NumExpr=1 (Pattern I). The hedge stages are deterministic under single-threaded BLAS; re-running for the run_log capture produced identical artifacts (idempotent).

## Findings

No BLOCKER / MAJOR / MINOR. The plan is a faithful consume-only rerun; the architecture's four-condition firing tree routed the corrected-fit outcome correctly, exactly as it did for the LS run (different condition, same machinery).
