# Reality Checker — Plan 04.1.1-03

Plan: `04.1.1-03-PLAN.md` — production-rep `cli.py fit` on the real ICHI cKES/USDT panel via the corrected scipy_canonical_ll estimator; record the four-criterion gate verdict AS-OBSERVED.

## VERDICT

PASS

The production-rep ran on the real panel against the corrected estimator with no source edits. Every claim recorded in `_artifacts/production_rep_fit_summary.txt` and `04.1.1-03-SUMMARY.md` is reproducible against the live `data/fits/ichi/bdaf5c7ba5a2/fit_report.json`. The verdict is recorded as-observed; no firing_condition pre-committed. Detail below.

## Claims checked against the live artifact

1. **New run_id distinct from the LS-degenerate and the stale wiring-artifact.** `_artifacts/new_run_id.txt` = `bdaf5c7ba5a2`; ≠ `ae9e3ba17900` (LS-degenerate) and ≠ `000c1cdce376` (pre-02b β=0.1 artifact). Derived from sha256(dataHash a72a4ee… + gitCommit 880ef38… + tick 0.8.0.2)[:12] — new gitCommit from the v2 edits → new content-addressed run_id, exactly Pattern H. CONFIRMED.

2. **fit_method_used = scipy_canonical_ll on BOTH surfaces.** `hawkes_mv_params.fit_method_used == "scipy_canonical_ll"` and `input_diagnostics.hawkes_fit_method_used == "scipy_canonical_ll"`. NOT "least-squares". CONFIRMED.

3. **6-entry decay_aic_table, AIC-min at β=0.001.** Keys {0.0001(Inf), 0.001(9800.78), 0.01(9833.86), 0.1(9937.65), 1.0(10077.41), 10.0(10117.49)}; argmin finite AIC = β=0.001. Matches DIAGNOSTIC Q2's predicted interior trough. CONFIRMED.

4. **η=0.600 recorded as a LOWER BOUND.** branching_ratio=0.5999724…; the summary labels it a lower bound per the ~13% downward n≈700 bias (DIAGNOSTIC Q1/Q3). Diagnostic predicted ≈0.628. CONFIRMED, framed honestly.

5. **branching_ratio_ci.method == constrained_mle_profile.** lower=0.001 (>0), upper=0.95, width=0.949 — the summary records VERBATIM that the χ²(1) inversion clamps to the [1e-3, 0.95] grid bounds and is wide; it is NOT narrowed or editorialized. CONFIRMED.

6. **LR observed_stat finite-scale.** observed_stat=561.29 (O(10²)) — the 6.05M LS-into-canonical-LL pathology is resolved (02c common-t0 fix). p_value=0.0, rejects_at_alpha=True, n_reps=1000, alpha=0.01, n_failed=0. CONFIRMED — no epoch-scaling regression.

7. **Four-criterion gate as-observed.** lr_rejects=true, eta_floor_met=true, branching_ci_excludes_zero=true, ks_held_out_passes=false (combined KS p=0.0474 ≤ 0.05; leg-0 marginally below threshold), gate_passes=false. The summary's VERDICT line is purely descriptive of the four booleans. CONFIRMED.

8. **ae9e3ba17900 preserved.** Its fit_report still reads fit_method_used=least-squares; not overwritten. CONFIRMED.

9. **Bootstrap tractable.** ~213s wall-clock (Option A); not the ~143-min all-scipy path. CONFIRMED.

## Scope discipline

`git diff HEAD -- analysis/ scripts/` is empty (pure rerun — no source edits). No firing_condition pre-committed (decide_firing_condition is left for Plan 04). No new gate criteria / firing conditions / kernel forms / thresholds. AF-12 OUT-OF-SCOPE honored. AF-03 ordering: this production-rep commit lands after all v2 pre-reg + dgp commits.

## Risk notes

- The held-out KS leg-0 p=0.0474 is a hair below 0.05 — gate_passes=false rests on a marginal KS rejection. This is recorded faithfully as-observed; no narrowing.
- The CI clamps to grid bounds (wide). Recorded verbatim; the branching_ci_excludes_zero criterion is still satisfied (lower 0.001 > 0).
