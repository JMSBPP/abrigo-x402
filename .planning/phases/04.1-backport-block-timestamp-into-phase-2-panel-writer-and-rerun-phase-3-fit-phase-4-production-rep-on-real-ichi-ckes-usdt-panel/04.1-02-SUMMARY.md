---
phase: 04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel
plan: "02"
subsystem: dgp-fit
tags: [phase-3-rerun, real-data, ichi, cKES-USDT, NHPP, Hawkes, run_id, fit_report]
dependency_graph:
  requires:
    - "data/raw/ichi/0x61Ef.../67378253_67896653.parquet (augmented panel from 04.1-01, sha256 a72a4ee...)"
    - "block_timestamp column on the real ICHI panel (Plan 04.1-00 ingest extension)"
    - "cli.py fit subcommand (unchanged since Plan 03-07)"
  provides:
    - "data/fits/ichi/ae9e3ba17900/fit_report.json (first real-data Phase 3 fit; 18 SC-1 keys + DGP-01..06 result blocks)"
    - "data/fits/ichi/ae9e3ba17900/residuals.parquet (rescaled_dt + Lambda_at_event, 162 rows)"
    - "ae9e3ba17900 (12-hex run_id; persisted to _artifacts/new_run_id.txt for Plan 04.1-03 consumption)"
  affects:
    - "Plan 04.1-03 hedge orchestrator (will consume run_id ae9e3ba17900 via cli.py hedge --run-id)"
    - "ROADMAP progress (Plan 04.1-02 complete; 50/51 plans)"
tech_stack:
  added: []
  patterns:
    - "Pattern F LL-fallback canonical-loglik contract (hawkes_fit fell back to gofit=least-squares; lr_test uses canonical _hawkes_loglik_vectorized; no crash)"
    - "Thread-pinned BLAS for Phase 3 SC-5 byte-identity contract (OMP/MKL/OpenBLAS/NumExpr=1)"
key_files:
  created:
    - ".planning/phases/04.1-.../_artifacts/new_run_id.txt"
    - "data/fits/ichi/ae9e3ba17900/fit_report.json (.gitignore-d under data/fits/)"
    - "data/fits/ichi/ae9e3ba17900/residuals.parquet (.gitignore-d under data/fits/)"
  modified: []
decisions:
  - "Plan 04.1-02 executed Phase 3 fit on real ICHI cKES/USDT panel for the first time (Plan 04-09's run was on a synthetic-stacked substrate). Run_id ae9e3ba17900 derived deterministically from _derive_run_id(panel_dataHash=a72a4ee..., gitCommit=84d8533..., tick_version)[:12]."
  - "Hawkes fit cascaded through gofit=likelihood and fell back to gofit=least-squares (input_diagnostics.hawkes_fit_method_used=least-squares). Did NOT raise. Pattern F double-fallback diagnostic sentinel (fit_error.json) NOT triggered; pipeline proceeded successfully."
  - "gate_passes=False on real data: lr_rejects=False (p=0.58, NHPP indistinguishable from Hawkes on this rep) + ks_held_out_passes=False. eta_floor met (eta_hat=0.341 ≥ 0.2). This is EXPECTED per 04.1-CONTEXT iter-2 invariant 5 — real-data n=778 events (382+396 per leg) may sit below the n=300 Q-9 identifiability floor combined with least-squares-fallback bias. The null outcome is itself the v1.0 finding and triggers Plan 04.1-03's HEDGE-05 firing-condition path (b) (null_lr / fit-degenerate)."
  - "Synthetic substrate run_id 0afc6af38e24 (Plan 04-09) and prior run de46bb72de52 left undisturbed per archival policy (Plan 04.1 OUT-OF-SCOPE)."
metrics:
  duration_sec: 152
  tasks_completed: 1
  files_created: 3
  completed_date: "2026-05-27"
---

# Phase 04.1 Plan 02: Real-Data Phase 3 Fit on Augmented ICHI cKES/USDT Panel Summary

First real-data Phase 3 DGP fit succeeded on Plan 04.1-01's augmented panel. New run_id `ae9e3ba17900` produced under `data/fits/ichi/`; `fit_report.json` carries all 18 FIT_REPORT_SC1_KEYS; `residuals.parquet` carries `rescaled_dt`; gate_passes=False (lr_rejects=False, ks_held_out=False); expected null outcome path activated for Plan 04.1-03.

## Captured run_id

**`ae9e3ba17900`** (12 hex chars; persisted to `.planning/phases/04.1-.../_artifacts/new_run_id.txt` for Plan 04.1-03 substitution into `cli.py hedge --run-id ae9e3ba17900 --stage all`).

## Fit Provenance

| Field | Value |
|---|---|
| Panel sha256 | `a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a` |
| gitCommit (HEAD) | `84d85338044c88776ec2b02f015a99a3cd374691` |
| Wall-clock | 152 s (under thread-pinned BLAS, 1000-rep bootstrap) |
| Total events | 778 (382 leg_0 + 396 leg_1) |
| Train window | 413636.8 s (~4.79 days) |
| Held-out window | 103409.2 s (~1.20 days, 20%) |
| Held-out events per leg | [83, 79] |
| Tick library version | (from fit_report.json `tick_lib_version`) |

## Acceptance Grid (Phase 04.1-02)

| # | Criterion | Verdict |
|---|---|---|
| 1 | Thread-pinning env vars set before fit | PASS |
| 2 | cli.py fit exited 0 (no fit_error.json) | PASS |
| 3 | `data/fits/ichi/ae9e3ba17900/fit_report.json` exists | PASS |
| 4 | All 18 FIT_REPORT_SC1_KEYS present | PASS |
| 5 | `data/fits/ichi/ae9e3ba17900/residuals.parquet` exists with `rescaled_dt` | PASS (162 rows, cols=[leg, event_time, Lambda_at_event, rescaled_dt]) |
| 6 | `_artifacts/new_run_id.txt` persisted with 12-hex run_id | PASS |
| 7 | `scripts/lint_artifacts.py` exits 0 | PASS (3 fit_report.json PASS SC-1 + 4 phase-4 artifacts PASS) |
| 8 | Phase 2+3+4 test suite stays green | PASS (194 passed / 3 skipped / 129.85 s) |
| 9 | `git diff HEAD -- analysis/ scripts/` empty (no source drift) | PASS |
| 10 | Synthetic `data/fits/ichi/0afc6af38e24/` untouched | PASS |
| 11 | Pattern F double-fallback diagnostic clear (no fit_error.json) | PASS |

## Key fit_report.json Result Blocks (Pre-cursors for Plan 04.1-03 Gate)

### LR Test (DGP-04)
- `observed_stat`: 6048633.15
- `p_value`: **0.58** (> 0.01 → NHPP NOT rejected at α=0.01)
- `rejects_at_alpha`: **False**
- `n_reps`: 1000 (PRE_REGISTRATION AF-04 lock honoured)
- `n_failed`: 0 (all bootstrap reps completed)
- **Implication for Plan 04.1-03:** `lr_rejects=False` → `HEDGE-05` firing-condition path (b) `null_lr` candidate.

### Branching Ratio CI (DGP-06)
- `eta_hat`: **0.341** (profile-likelihood point estimate)
- `lower`/`upper`: [0.283, 0.371]
- `ci_width`: 0.0875 (< 0.4 threshold → `q9_nullfire_triggered`: **False**)
- `method`: `profile_likelihood` (Filimonov-Sornette 2014)
- `eta_hat_unconstrained`: 0.000307 (LS-fallback artefact; projection-trick grid argmax used for self-consistent LL_max per 03-06 deviation)

### Held-Out Log-Likelihood (DGP-04 component)
- `nhpp`: **-1320.63**
- `hawkes`: **-2326.10** (Hawkes WORSE out-of-sample by ~1005 nats — consistent with `lr_rejects=False`; the data does not exhibit Hawkes self-excitation on the held-out 20% window)

### Stationarity Diagnostic (DGP-03)
- `decision`: **stationary** (both legs)
- `train_rate` ≈ `held_out_rate` (per-leg ratios 0.110 and 0.003 < 0.25 threshold)
- **Implication:** Stationary baseline supported — Hawkes' poor fit is genuine, not a non-stationarity artefact.

### Input Diagnostics (PANEL-01 sanity)
- `hawkes_fit_method_used`: **`least-squares`** (Pattern F fallback fired; likelihood-mode failed first)
- `hawkes_loglik_source`: `abrigo_x402.dgp.lr_test._hawkes_loglik_vectorized` (canonical-LL contract Pattern F honoured)
- `nhpp_loglik_source`: `abrigo_x402.dgp.lr_test._nhpp_pointprocess_loglik`
- `simultaneous_event_pairs`: 1
- `tie_counts`: `{leg_0: 6, leg_1: 6}` (block-level ties at 1s granularity)

### Gate Criteria (Phase 3 SC-1..6 composite)
| Criterion | Value | Verdict |
|---|---|---|
| `branching_ci_excludes_zero` | true | PASS |
| `eta_floor_met` (η ≥ 0.2) | 0.341 ≥ 0.2 → true | PASS |
| `lr_rejects` (p < 0.01) | 0.58 < 0.01 → false | **FAIL** |
| `ks_held_out_passes` (α=0.05) | false | **FAIL** |
| `stationary` | true | PASS |
| **`gate_passes` overall** | **False** | **NULL-RESULT PATH** |

## Pattern F LL-Fallback Regime — Did NOT Crash

Per `analysis/src/abrigo_x402/dgp/hawkes_fit.py:34`, the Hawkes fit cascades `gofit="likelihood"` → `gofit="least-squares"`. On the real cKES/USDT panel (n=778 events, bursty MEV-driven clustering), `gofit=likelihood` failed (likely numerical), but `gofit=least-squares` succeeded. The 04.1-02 iter-2 diagnostic sentinel `fit_error.json` was NOT triggered (no double-fallback crash); pipeline proceeded normally. `input_diagnostics.hawkes_fit_method_used="least-squares"` records the fallback for downstream provenance audit. The canonical-LL contract (Pattern F) absorbs the LS-fallback objective-scale mismatch: `lr_test` and `branching_ratio_ci` consistently use `_hawkes_loglik_vectorized` for their loglik computations regardless of which fit method succeeded.

## Implications for Plan 04.1-03 (Hedge Rerun)

This is the FIRST real-data fit, and the **expected null outcome** materialized:
- **`lr_rejects=False` + `ks_held_out_passes=False`** → Plan 04.1-03's `decide_firing_condition` will likely return `firing_condition="null_lr"` (DGP indistinguishable from NHPP; Hawkes self-excitation not statistically detectable on n=382+396 events).
- **Held-out NHPP > Hawkes by ~1005 nats** further reinforces the null result.
- This is a **legitimate scientific finding** per 04.1-CONTEXT iter-2 invariant 5: real-data parameters are what they are; the synthetic substrate's known η=0.5 properties do NOT apply here.
- Plan 04.1-03 will invoke `cli.py hedge --run-id ae9e3ba17900 --stage all`; the HEDGE-05 null-result PDF path (via `render_null_result_pdf` + `HEDGE05_SIGNATURE`) is architecturally ready to handle this (Plan 04-08 Path A design).

## Deviations from Plan

**None.** Plan 04.1-02 executed exactly as written:
- Step A (pre-flight): augmented panel validated (sha256 matches Plan 04.1-01's recorded value).
- Step B (fit run): single `cli.py fit` invocation under thread-pinned BLAS; default `--bootstrap-reps` (1000) honoured.
- Step C (capture): NEW_RUN_ID extracted from stdout JSON; verified 12-hex and not in pre-existing list; persisted to `_artifacts/new_run_id.txt`.
- Step C-bis (Pattern F diagnostic): not triggered (no fit failure).
- Step D (verify artifacts + SC-1 lint): all PASS.
- Step E (rescaled_dt sanity): PASS.
- Step F (regression suite): 194 passed / 3 skipped / 0 failures.

Zero source-code drift in `analysis/` and `scripts/` (`git diff HEAD --` empty).

## Pointer Forward

Plan 04.1-03 reads `.planning/phases/04.1-.../_artifacts/new_run_id.txt` to substitute `ae9e3ba17900` into:
```bash
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run python -m abrigo_x402.cli hedge --run-id ae9e3ba17900 --stage all
```
And appends a "04.1 Real-Data Rerun" section to `.planning/phases/04-.../04-VERIFICATION-pre.md` documenting the firing-condition outcome (anticipated: `null_lr` per the above gate_criteria).

## Self-Check: PASSED

- `data/fits/ichi/ae9e3ba17900/fit_report.json` exists — FOUND
- `data/fits/ichi/ae9e3ba17900/residuals.parquet` exists — FOUND
- `.planning/phases/04.1-.../_artifacts/new_run_id.txt` contains `ae9e3ba17900` — FOUND
- `! test -f data/fits/ichi/ae9e3ba17900/fit_error.json` — PASS (no fit failure)
- `scripts/lint_artifacts.py` exits 0 — PASS (3 fit_report.json PASS SC-1)
- Phase 2+3+4 test suite 194 passed / 3 skipped — PASS
- `git diff HEAD -- analysis/ scripts/` empty — PASS (no source drift)
