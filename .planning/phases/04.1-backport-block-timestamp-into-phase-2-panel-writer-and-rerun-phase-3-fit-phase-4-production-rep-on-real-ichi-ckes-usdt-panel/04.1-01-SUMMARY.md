---
phase: 04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel
plan: "01"
subsystem: data-pipeline
tags: [polars, parquet, ichi, ckes-usdt, blockscout, materialize, panel-02]

requires:
  - phase: 04.1-00
    provides: ingest.py block_timestamp PROVENANCE_COLS extension + lint_artifacts ICHI_PANEL_REQUIRED_COLUMNS frozenset
provides:
  - augmented real ICHI cKES/USDT panel parquet (832 rows × 33 cols) with block_timestamp Int64 column populated from cached pool_events.jsonl timeStamp hex
  - new panel sha256 (a72a4eea...) that propagates into a fresh Phase 3 run_id in Plan 04.1-02
  - persisted _artifacts/new_dataHash.txt pointer for downstream provenance audit
affects: [04.1-02, 04.1-03, 05]

tech-stack:
  added: []
  patterns:
    - "Pure rerun pattern: data-only regeneration via existing CLI subcommand; zero source-code edits"
    - "Cached-sidecar materialization (no cost-ledger budget burn)"

key-files:
  created:
    - .planning/phases/04.1-.../_artifacts/new_dataHash.txt
    - .planning/phases/04.1-.../04.1-01-SUMMARY.md
  modified:
    - data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet (gitignored; in-place overwrite)

key-decisions:
  - "In-place parquet overwrite (no _v2 suffix) — old bytes were broken; no audit reason to preserve"
  - "Pure rerun: zero source-code edits (all code changes already landed in 04.1-00)"

patterns-established:
  - "AF-12 silent-rescope defense holds: no new columns beyond block_timestamp, no refactors, no re-fetches"

requirements-completed: [PANEL-01, PANEL-02, DEPEND-01]

duration: ~7min
completed: 2026-05-27
---

# Phase 04.1 Plan 01: Re-materialize real ICHI panel with block_timestamp Summary

**Real ICHI cKES/USDT panel parquet regenerated via `cli.py materialize` against cached JSONL sidecars; block_timestamp Int64 column now populated (832 rows, zero nulls); new sha256 propagates fresh Phase 3 run_id in Plan 04.1-02.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-05-27T22:43Z
- **Completed:** 2026-05-27T22:47Z
- **Tasks:** 1
- **Files modified:** 1 parquet (in-place); 1 artifact pointer created

## Accomplishments

- Re-ran `python -m abrigo_x402.cli materialize` end-to-end on cached `pool_events.jsonl` + `vault_state.jsonl` + `fx_snap.jsonl` (no `tx_logs.jsonl` — optional, handled as `None`)
- Augmented parquet at `data/raw/ichi/0x61Ef.../67378253_67896653.parquet` carries `block_timestamp` Polars Int64 column with zero nulls across 832 rows
- Confirmed sha256 mutation: `2234b635...` → `a72a4eea...` (one-column-additive change → new bytes → new `dataHash` in PANEL-02 header)
- `scripts/lint_artifacts.py` exits 0: 1 parquet PASS PANEL-02 + ICHI_PANEL_REQUIRED_COLUMNS frozenset check (extended in 04.1-00) now finds `block_timestamp`
- Zero source-code drift: `git diff HEAD -- analysis/src/` is empty (pure rerun)
- Zero cost-ledger budget burn: no Forno or Blockscout HTTP calls (cached sidecars only)

## Pre vs Post sha256

| Field | Value |
|---|---|
| Pre-04.1 sha256 (baseline) | `2234b635729e2fbc307989f75bfdc8b21e29cd06b720b45682d2d385a26b5b2e` |
| Post-04.1 sha256 (new dataHash) | `a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a` |
| Row count | 832 (preserved) |
| Column count | 32 → 33 (one new: `block_timestamp`) |
| Parquet size | 126863 → 128791 bytes |

## block_timestamp range

| Bound | unix-seconds | ISO-8601 |
|---|---|---|
| min | 1779280242 | 2026-05-20T12:30:42+00:00 |
| max | 1779797288 | 2026-05-26T12:08:08+00:00 |

Span ≈ 6 days; consistent with the [67378253, 67896653] Celo block range (~5.96 days at ~1 block/s).

## Task Commits

No per-task code commit — this is a pure data rerun and the parquet is `.gitignore`-d (`.gitignore:43:data/raw/*/`). The "commit" is the file on disk; downstream Plan 04.1-02 keys on the new sha256 (`a72a4eea...`) persisted to `_artifacts/new_dataHash.txt`, not on a git hash.

**Plan metadata commit (final):** see git log for `docs(04.1-01): complete plan` — captures SUMMARY.md + new_dataHash.txt + STATE.md + ROADMAP.md.

## Files Created/Modified

- `data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` — augmented panel (in-place overwrite; gitignored)
- `.planning/phases/04.1-.../_artifacts/new_dataHash.txt` — `a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a` (downstream provenance pointer)

## Decisions Made

None — plan executed exactly as written. AF-12 OUT-OF-SCOPE list held perfectly: no source edits, no re-fetches, no PANEL-02 schema bump, no new CLI flags.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Self-Check: PASSED

- Parquet exists at `data/raw/ichi/0x61Ef.../67378253_67896653.parquet`: FOUND
- `block_timestamp` Int64 column present, zero nulls, 832 rows: PASS (verified live)
- sha256 changed (`2234b635...` → `a72a4eea...`): PASS
- `lint_artifacts.py` exit 0: PASS (1 parquet PASS PANEL-02; 2 fit_report.json PASS SC-1; 4 phase-4 artifacts PASS)
- `git diff HEAD -- analysis/src/` empty: PASS
- `_artifacts/new_dataHash.txt` written: FOUND

## Next Phase Readiness

Plan 04.1-02 can now invoke `cli.py fit --panel data/raw/ichi/0x61Ef.../67378253_67896653.parquet --out-dir data/fits/ichi/` against the augmented panel. Phase 3's `_derive_run_id(panel_dataHash, gitCommit, tick_version)[:12]` will produce a fresh run_id (distinct from the synthetic-substrate `0afc6af38e24`) because `panel_dataHash` is now `a72a4eea...` not `2234b635...`. The `block_timestamp` Polars Int64 column unblocks `_extract_legs_from_panel`'s `swaps.get_column("block_timestamp").cast(pl.Float64).to_numpy()` consumption.

---
*Phase: 04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel*
*Completed: 2026-05-27*
