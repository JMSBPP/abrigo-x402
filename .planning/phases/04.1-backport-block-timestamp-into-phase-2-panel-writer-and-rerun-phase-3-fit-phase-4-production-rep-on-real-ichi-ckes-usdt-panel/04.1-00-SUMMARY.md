---
phase: 04.1
plan: 00
status: complete
date: 2026-05-27
---

# Plan 04.1-00 — Backport block_timestamp into Phase 2 panel writer

## Self-Check: PASSED

## One-liner

Edit Phase 2's `analysis/src/abrigo_x402/ingest.py` PROVENANCE_COLS to add `block_timestamp` (parsed from cached `timeStamp` hex via `_hex_to_int`); extend zero-null invariant; add 3 defense-in-depth tests; extend `scripts/lint_artifacts.py` ICHI_PANEL_REQUIRED_COLUMNS frozenset. Single atomic `fix(04.1):` commit. Closes the Phase 2→Phase 3 column-wire gap that blocked the real-data DGP fit in Plan 04-09.

## Acceptance criteria — all green

| Criterion | Evidence |
|---|---|
| PROVENANCE_COLS exact form | `analysis/src/abrigo_x402/ingest.py:23` = `("blockNumber", "blockHash", "logIndex", "txHash", "contractAddress", "block_timestamp")` (verbatim) |
| Hex→Int64 parse pattern | `ingest.py:68` = `"block_timestamp": _hex_to_int(row.get("timeStamp"))` |
| Polars schema entry | `ingest.py:81` = `"block_timestamp": pl.Int64` |
| Zero-null invariant verbatim | `ingest.py:87-88` raises `ValueError("null block_timestamp in JSONL row (PANEL-01 zero-null invariant)")` |
| 3 defense-in-depth tests | `test_block_timestamp_present_and_int64`, `test_block_timestamp_hex_parse_raises_on_null`, `test_block_timestamp_post_df_null_count_raises` |
| Fixture extension | Both `ichi_anchor_block_67000000_67001000.jsonl` (10 rows) + `synthetic_swaps_n100.jsonl` (100 rows) now carry `timeStamp` hex field per row |
| Lint extension | `scripts/lint_artifacts.py` extended with `ICHI_PANEL_REQUIRED_COLUMNS` frozenset containing `block_timestamp` |
| AF-12 forbidden tokens | `grep -c "transaction_index\|gas_used\|panel_schema_version\|panel_augment" analysis/src/abrigo_x402/ingest.py` = **0** |
| Phase 2+3+4 regression | **194 passed, 3 skipped, 0 failures** under thread-pinned BLAS (was 191 before; +3 new tests) |
| Atomic commit | Single `fix(04.1):` commit (this plan) |

## Key files modified

- `analysis/src/abrigo_x402/ingest.py` — PROVENANCE_COLS + row-dict parse + Polars schema + zero-null invariant extension
- `analysis/tests/test_ingest.py` — 3 new defense-in-depth tests (12 total, all passing)
- `analysis/tests/fixtures/ichi_anchor_block_67000000_67001000.jsonl` — timeStamp hex per row
- `analysis/tests/fixtures/synthetic_swaps_n100.jsonl` — timeStamp hex per row
- `scripts/lint_artifacts.py` — ICHI_PANEL_REQUIRED_COLUMNS frozenset extension

## Deviations

None. AF-12 scope fence held perfectly — no new columns beyond `block_timestamp`, no refactors, no new dependencies, no re-fetching, no PANEL-02 schema bump, no new pre-commit gates.

## What this enables

Plan 04.1-01 can now re-materialize the real ICHI panel parquet (`data/raw/ichi/0x61Ef.../67378253_67896653.parquet`) with the `block_timestamp` column populated from the already-cached `pool_events.jsonl` timeStamp field. No Forno/Blockscout re-fetch needed — the data was on disk all along; we just weren't propagating it to the panel.

Once 04.1-01 lands the augmented panel, Plan 04.1-02 reruns Phase 3's `cli.py fit` against it (producing a new content-addressed `<run_id>` from the new `dataHash`), and Plan 04.1-03 reruns Phase 4's `cli.py hedge` against that new run_id — finally producing real-data v1.0 evidence for Phase 5's PDF deliverable.
