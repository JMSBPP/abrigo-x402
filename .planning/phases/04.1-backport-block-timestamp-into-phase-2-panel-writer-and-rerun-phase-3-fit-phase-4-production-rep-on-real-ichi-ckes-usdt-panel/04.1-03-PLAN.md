---
phase: 04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel
plan: "03"
type: execute
wave: 4
depends_on: ["04.1-02"]
files_modified:
  - data/fits/ichi/<NEW_RUN_ID>/joint_dist.json
  - data/fits/ichi/<NEW_RUN_ID>/gate_report.json
  - data/fits/ichi/<NEW_RUN_ID>/strip.json
  - data/fits/ichi/<NEW_RUN_ID>/stress_report.json
  - data/fits/ichi/<NEW_RUN_ID>/run_log.txt
  - data/fits/ichi/0afc6af38e24/README.md
  - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
autonomous: true
gap_closure: true
requirements: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05]
must_haves:
  truths:
    - "Phase 4 hedge orchestrator runs end-to-end against the real ICHI panel new run_id from Plan 04.1-02"
    - "All four primary Phase 4 artifacts emit successfully (joint_dist.json + gate_report.json + (strip.json OR strip_degenerate.json) + stress_report.json)"
    - "Synthetic 0afc6af38e24 run is archived (NOT deleted) via a one-line README.md pointing forward to the new real-data run_id"
    - "04-VERIFICATION-pre.md gets a NEW '04.1 Real-Data Rerun' section APPENDED (existing 18-row grid + frontmatter unchanged)"
    - "FOUR new frontmatter fields APPENDED below existing 15: real_data_rerun_run_id, real_data_rerun_artifacts_complete, real_data_rerun_firing_condition, substrate_substitution_resolved (post-amendment frontmatter total = 19 fields). The single `real_data_rerun_passed: <bool>` originally drafted conflated artifact-completeness with scientific-outcome — split into two fields per reviewer feedback so Phase 5 cannot misread."
    - "Side-by-side synthetic-vs-real comparison table populates 6 rows: firing_condition, BIC winner with delta, gate condition pass count, divergence_pct, char_func_source, strip emission path"
    - "Phase 5 PDF deliverable can consume the new real_data_rerun_run_id directly (it is the canonical v1.0 input; synthetic is methodology-validation evidence only)"
    - "HEDGE-05 firing on the real-data rerun is EXPECTED, not anomalous. The synthetic substrate (n=2058 with controlled η=0.5 properties) passed the gate; the real panel (n=832 swap events at unknown η, split asymmetrically across leg_0 / leg_1) is below the Q-9 identifiability floor implied by the 300-event minimum locked in PRE_REGISTRATION. A `null_lr` or `null_convex` `real_data_rerun_firing_condition` IS the v1.0 scientific result on real data — exactly what the entire HEDGE-05 architecture exists to handle. Phase 5 narrative cites the real-data verdict, not the synthetic one (CONTEXT.md divergence policy)."
    - "BIC winner cell in the comparison table is annotated with the `|Δ|` magnitude and a noise-floor caveat. Per VINE_FALLBACK_DELTA_BIC_THRESHOLD, Δ_BIC < 5 indicates sample-noise-floor regime where the BIC winner is NOT statistically distinguishable from the runner-up. The synthetic Δ_BIC = 0.009 (Frank vs Gaussian) is firmly in this regime — the appended comparison table MUST acknowledge this so the real-data column flipping winner is not misread as a methodology contradiction."
  artifacts:
    - path: "data/fits/ichi/<NEW_RUN_ID>/joint_dist.json"
      provides: "cross_correlogram + permutation_null + empirical_copula on real ICHI swap legs"
      contains: "empirical_copula"
    - path: "data/fits/ichi/<NEW_RUN_ID>/gate_report.json"
      provides: "four-condition convex-dominance gate verdict on real data"
      contains: "any_condition_passed"
    - path: "data/fits/ichi/<NEW_RUN_ID>/strip.json (or strip_degenerate.json on positivity failure)"
      provides: "Carr-Madan strip output; emission path is itself a finding for Phase 5"
      contains: "char_func_source"
    - path: "data/fits/ichi/<NEW_RUN_ID>/stress_report.json"
      provides: "three-way stress test divergence_pct on real data"
      contains: "divergence_pct"
    - path: "data/fits/ichi/0afc6af38e24/README.md"
      provides: "Archival note: methodology-validation substrate superseded by new real-data run_id"
      contains: "Superseded by"
    - path: ".planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md"
      provides: "Appended '04.1 Real-Data Rerun' section + 3 new frontmatter fields"
      contains: "real_data_rerun_run_id:"
  key_links:
    - from: "data/fits/ichi/<NEW_RUN_ID>/fit_report.json (Plan 04.1-02 output)"
      to: "data/fits/ichi/<NEW_RUN_ID>/{joint_dist,gate_report,strip,stress_report}.json"
      via: "cli.py hedge --run-id <NEW_RUN_ID> --stage all → run_hedge orchestrator"
      pattern: "run_id"
    - from: "real_data_rerun_run_id: in 04-VERIFICATION-pre.md frontmatter"
      to: "Phase 5 PDF deliverable (reports/ichi.pdf)"
      via: "Phase 5 reads frontmatter at parse time; greppable line is the machine-readable substrate-substitution-resolved signal"
      pattern: "^real_data_rerun_run_id:"
    - from: "substrate_substitution_resolved: true frontmatter field"
      to: "Phase 5 unblocking gate"
      via: "Explicit closure of the original Plan 04-09 synthetic-substitution caveat"
      pattern: "^substrate_substitution_resolved: true"
---

<objective>
Close the Phase 04.1 loop. Run the Phase 4 hedge orchestrator against the new real-data run_id from Plan 04.1-02, archive the synthetic substrate run with a forward-pointing README, append the "04.1 Real-Data Rerun" section to `04-VERIFICATION-pre.md` (PRESERVING the existing 18-row grid + frontmatter), add 3 new greppable frontmatter fields, and populate the side-by-side synthetic-vs-real comparison table (6 rows × 2 columns) that IS the methodology-validation evidence Phase 5 cites.

This is the canonical close-the-loop step. After this plan, Phase 5 (Iteration-1 PDF deliverable) is unblocked — `reports/ichi.pdf` will render on the real-data findings, not the synthetic substrate.

**Divergence policy (CONTEXT.md `<decisions>`):** Real-data is canonical for v1.0 publication. If real-panel produces a different firing_condition than synthetic (e.g., real fires HEDGE-05 where synthetic did not), real wins. That divergence IS the v1.0 scientific finding; Phase 5 narrative cites the real verdict, not the synthetic one. The orchestrator + Quarto template are explicitly designed to handle either outcome.

Purpose: Deliver Phase 5-ready artifacts on real data + document the close-the-loop in the canonical Phase 4 acceptance gate file.

Output: 5 new artifacts under `data/fits/ichi/<NEW_RUN_ID>/`; 1 archival README; 1 appended VERIFICATION section + 3 new frontmatter fields.

**OUT OF SCOPE (AF-12 silent-rescope defense — verbatim from 04.1-CONTEXT.md `<domain>`):**
- No code edits (all code changes landed in Plan 04.1-00)
- No deletion of synthetic `0afc6af38e24` run (archived via README only)
- No overwriting of existing 18-row acceptance grid in `04-VERIFICATION-pre.md` (APPEND new section after; PRESERVE all existing rows and frontmatter fields)
- No removal or modification of existing frontmatter fields (`verification_pass: true`, `quarto_skipped: true`, `production_rep_run_id: "0afc6af38e24"`, etc. stay verbatim; the 3 new fields are APPENDED below)
- No `02-VERIFICATION.md` amendment (Claude's Discretion in CONTEXT.md; opting NOT to touch — the SUMMARY chain + this plan's appended section document the reopening sufficiently)
- No new HEDGE-05 firing-template branches; no new Quarto template additions
- No new pre-commit gates
- No new dependencies
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-CONTEXT.md
@.planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-00-SUMMARY.md
@.planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-01-SUMMARY.md
@.planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-02-SUMMARY.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
@./CLAUDE.md

<interfaces>
<!-- Hedge CLI invocation — verbatim from analysis/src/abrigo_x402/cli.py lines 236-266 -->

The `hedge` subcommand was introduced in Plan 04-08:

```bash
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run python -m abrigo_x402.cli hedge \
    --run-id <NEW_RUN_ID> \
    --stage all \
    --run-dir-root /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi \
    --reports-pdf /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/reports/ichi.pdf \
    2>&1 | tee /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/<NEW_RUN_ID>/run_log.txt
```

Argparse signature (lines 240-265):
- `--run-id` (required; the Plan 04.1-02 output)
- `--stage` (default "all"; choices: dependence/gate/strip/stress/null/all)
- `--run-dir-root` (default "data/fits/ichi"; Steer iter-2 would pass "data/fits/steer")
- `--cost-leg-bound` (default None; optional path to cost-leg memo for firing condition (a))
- `--reports-pdf` (default "reports/ichi.pdf")

Handler (`cli.py :: _cmd_hedge` lines 152-181) invokes `run_hedge(...)` and ends with the sentinel print:
```
hedge.orchestrator.run_hedge completed
```
(verified at line 180 — this is the row-16-style greppable orchestrator-completion sentinel from Plan 04-09).

The orchestrator writes 4 primary artifacts (joint_dist.json, gate_report.json, strip.json OR strip_degenerate.json, stress_report.json) and emits a 5th file (`reports/ichi.pdf`) ONLY if a HEDGE-05 firing condition is met.

**Reference Plan 04-09 outputs on synthetic substrate `0afc6af38e24` (captured from `04-VERIFICATION-pre.md`):**
- firing_condition: `null` (positive-result path; no HEDGE-05 fire)
- BIC winner (`joint_dist.empirical_copula.family`): `frank` (BIC 5.2705 vs Gaussian 5.2791; Δ=0.009 noise-floor caveat)
- char_func_source (`strip.json`): `frank_sobol_qmc`
- gate breakdown: vol_of_vol=False, skew_fat_tails=True, hawkes_self_excitation=True, usdt_depeg_basis_jump=True → 3-of-4, any_condition_passed=True
- three-way stress divergence_pct: 46.36% > 30% → divergence_flag=True
- strip emission path: `strip.json` (2^11 grid; no escalation, no degenerate path)
- artifacts written: `[joint_dist.json, gate_report.json, strip.json, stress_report.json]`

The real-data column of the comparison table populates from the corresponding fields in the new run_id's JSON files (commands provided in Task 2).

**04-VERIFICATION-pre.md current frontmatter (lines 1-17; preserve verbatim, append 3 new fields after `created: 2026-05-27`):**
```yaml
---
phase: 04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6
plan: 09
artifact: VERIFICATION-pre
verification_pass: true
quarto_skipped: true
requirements_covered: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05, SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
real_panel_substituted: true
real_panel_substrate: "Phase-3 synthetic Hawkes fixture (analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet) stacked x3 ..."
production_rep_run_id: "0afc6af38e24"
production_rep_firing_condition: null
production_rep_bic_winner: frank
production_rep_char_func_source: frank_sobol_qmc
plan_file_mtime: 1779893398
run_log_mtime: 1779907978
created: 2026-05-27
---
```

The 3 NEW fields to append BEFORE the closing `---`:
```yaml
real_data_rerun_run_id: "<NEW_RUN_ID>"
real_data_rerun_passed: true
substrate_substitution_resolved: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cli.py hedge on real-data run_id + verify all artifacts emit</name>
  <files>
    data/fits/ichi/<NEW_RUN_ID>/joint_dist.json
    data/fits/ichi/<NEW_RUN_ID>/gate_report.json
    data/fits/ichi/<NEW_RUN_ID>/strip.json (or strip_degenerate.json)
    data/fits/ichi/<NEW_RUN_ID>/stress_report.json
    data/fits/ichi/<NEW_RUN_ID>/run_log.txt
  </files>
  <read_first>
    - analysis/src/abrigo_x402/cli.py (full file, 274 lines — confirm `hedge` argparse signature lines 240-265 and handler lines 152-181; do NOT edit)
    - analysis/src/abrigo_x402/hedge/orchestrator.py (the `run_hedge(run_id, stage, ...)` entry point; confirm artifact-emission paths; do NOT edit)
    - analysis/src/abrigo_x402/hedge/null_result.py (the `decide_firing_condition` four-condition firing tree; do NOT edit)
    - .planning/phases/04.1-.../04.1-02-SUMMARY.md (predecessor; carries NEW_RUN_ID and Phase 3 fit summary)
    - .planning/phases/04.1-.../_artifacts/new_run_id.txt (file persisting the NEW_RUN_ID for this plan to consume)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (the OUT-OF-SCOPE list + divergence policy)
    - .planning/phases/04-.../04-VERIFICATION-pre.md (the file to append to in Task 3; READ to confirm structure before appending)
  </read_first>
  <action>
**Step A — Load the NEW_RUN_ID from the Plan 04.1-02 marker file.**

```bash
NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)
echo "NEW_RUN_ID=$NEW_RUN_ID"
test -d /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID && echo "PASS: run_id dir exists"
test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/fit_report.json && echo "PASS: fit_report.json from Plan 04.1-02 present"
```

**Step B — Run the hedge orchestrator end-to-end under thread-pinned BLAS.** This mirrors the Plan 04-09 row 16 production-rep invocation exactly (verified from `04-VERIFICATION-pre.md` row 16 command):

```bash
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run python -m abrigo_x402.cli hedge \
    --run-id $NEW_RUN_ID \
    --stage all \
    --run-dir-root /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi \
    --reports-pdf /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/reports/ichi.pdf \
    2>&1 | tee /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/run_log.txt
```

The `tee` is critical: the `run_log.txt` is itself an acceptance artifact (carries the `hedge.orchestrator.run_hedge completed` sentinel per cli.py line 180; same contract as Plan 04-09 row 16).

Expected exit code: 0 (whether HEDGE-05 fires or not — the orchestrator handles both paths).

**Step C — Verify the four primary artifacts emitted.**

```bash
test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/joint_dist.json && echo "PASS: joint_dist.json"
test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/gate_report.json && echo "PASS: gate_report.json"
# Strip emission: either strip.json (positivity OK) OR strip_degenerate.json (positivity fail or build_failed_upstream)
# Both are valid; the firing-condition gate handles each
(test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip.json || test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip_degenerate.json) && echo "PASS: strip output present"
test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/stress_report.json && echo "PASS: stress_report.json"
test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/run_log.txt && echo "PASS: run_log.txt"

# Sentinel grep (Plan 04-09 row 16 contract)
grep -q "hedge.orchestrator.run_hedge completed\|run_hedge completed" /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/run_log.txt && echo "PASS: orchestrator completion sentinel"
```

**Step D — Run the Phase 4 lint walker against the new artifacts.**

```bash
cd analysis && uv run python ../scripts/lint_artifacts.py
```

Expected last line includes: `... N phase-4 artifacts PASS` where N ≥ 4 (the four primary artifacts under the new run_id) PLUS prior artifacts (e.g., `0afc6af38e24`'s 4 artifacts unchanged).

**Step E — Capture the headline real-data values for Task 2's comparison table.**

```bash
# Firing condition
FIRING=$(python3 -c "import json; print(json.load(open('/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/gate_report.json')).get('firing_condition', 'null'))")
echo "FIRING_CONDITION=$FIRING"

# BIC winner + delta (from joint_dist.json :: empirical_copula)
BIC=$(python3 -c "
import json
j = json.load(open('/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/joint_dist.json'))
ec = j.get('empirical_copula', {})
print(f\"family={ec.get('family')}, bic={ec.get('bic')}, candidates={ec.get('candidates_bic') or 'see joint_dist.json'}\")
")
echo "BIC_WINNER=$BIC"

# Gate condition pass count — STRICT schema check (no defensive .get() chains).
# Pre-flight: import GATE_REPORT_REQUIRED_KEYS from scripts/lint_artifacts.py and assert every
# required key is present. Direct subscript access; KeyError on schema drift is DESIRED behavior
# (reviewer-flagged: dict-chained .get(..., {}).get(..., False) silently returns False on missing
# keys, producing fantasy "0-of-4 conditions passed" comparison rows).
GATE=$(python3 -c "
import json, sys
sys.path.insert(0, 'scripts')
from lint_artifacts import GATE_REPORT_REQUIRED_KEYS
g = json.load(open('data/fits/ichi/$NEW_RUN_ID/gate_report.json'))
missing = GATE_REPORT_REQUIRED_KEYS - set(g.keys())
if missing:
    raise KeyError(f'gate_report.json schema drift — missing keys: {sorted(missing)}; expected GATE_REPORT_REQUIRED_KEYS contract from scripts/lint_artifacts.py')
# Direct subscript — KeyError on schema drift, NO silent .get() defaults.
conds = [g['vol_of_vol_gt_zero']['passed'], g['positive_skew_fat_tails']['passed'], g['hawkes_self_excitation']['passed'], g['usdt_depeg_basis_jump']['passed']]
print(f\"{sum(conds)}-of-4 (vol_of_vol={conds[0]}, skew_fat_tails={conds[1]}, hawkes_self_excitation={conds[2]}, usdt_depeg_basis_jump={conds[3]}), any={g['any_condition_passed']}\")
")
echo "GATE_PASSES=$GATE"

# Divergence pct
DIV=$(python3 -c "import json; print(json.load(open('/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/stress_report.json'))['divergence_pct'])")
echo "DIVERGENCE_PCT=$DIV"

# Char func source (from strip.json if present; else 'N/A (degenerate)' from strip_degenerate.json)
if test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip.json; then
    CHAR=$(python3 -c "import json; print(json.load(open('/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip.json'))['char_func_source'])")
    STRIP_PATH="strip.json"
else
    CHAR="N/A (degenerate)"
    STRIP_PATH="strip_degenerate.json"
fi
echo "CHAR_FUNC_SOURCE=$CHAR"
echo "STRIP_PATH=$STRIP_PATH"

# Persist for Task 3 to read into the comparison table
mkdir -p .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts
cat > .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/real_data_summary.txt <<EOF
NEW_RUN_ID=$NEW_RUN_ID
FIRING_CONDITION=$FIRING
BIC_WINNER=$BIC
GATE_PASSES=$GATE
DIVERGENCE_PCT=$DIV
CHAR_FUNC_SOURCE=$CHAR
STRIP_PATH=$STRIP_PATH
EOF
cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/real_data_summary.txt
```

These six captured values populate the side-by-side comparison table in Task 3.
  </action>
  <verify>
    <automated>NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt) && test -f data/fits/ichi/$NEW_RUN_ID/joint_dist.json && test -f data/fits/ichi/$NEW_RUN_ID/gate_report.json && (test -f data/fits/ichi/$NEW_RUN_ID/strip.json || test -f data/fits/ichi/$NEW_RUN_ID/strip_degenerate.json) && test -f data/fits/ichi/$NEW_RUN_ID/stress_report.json</automated>
  </verify>
  <acceptance_criteria>
    - `NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt) && test -f data/fits/ichi/$NEW_RUN_ID/joint_dist.json` exits 0
    - `test -f data/fits/ichi/$NEW_RUN_ID/gate_report.json` exits 0
    - `(test -f data/fits/ichi/$NEW_RUN_ID/strip.json || test -f data/fits/ichi/$NEW_RUN_ID/strip_degenerate.json)` exits 0 (either is valid)
    - `test -f data/fits/ichi/$NEW_RUN_ID/stress_report.json` exits 0
    - `test -f data/fits/ichi/$NEW_RUN_ID/run_log.txt` exits 0
    - `grep -q "hedge.orchestrator.run_hedge completed\|run_hedge completed" data/fits/ichi/$NEW_RUN_ID/run_log.txt` exits 0 (orchestrator completion sentinel — Plan 04-09 row 16 contract)
    - `cd analysis && uv run python ../scripts/lint_artifacts.py` exits 0 (all Phase 4 artifacts pass REQUIRED_*_KEYS lint)
    - `test -f .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/real_data_summary.txt` exits 0 (captured headline values for Task 3)
    - `git diff HEAD -- analysis/ scripts/` is empty (no source-code drift; pure rerun)
    - Synthetic `data/fits/ichi/0afc6af38e24/` dir still exists unchanged: `test -d data/fits/ichi/0afc6af38e24 && test -f data/fits/ichi/0afc6af38e24/fit_report.json` exits 0
  </acceptance_criteria>
  <done>
    Hedge orchestrator ran end-to-end on real data; 4 primary artifacts emitted (joint_dist + gate_report + strip-or-strip_degenerate + stress_report) + run_log.txt with sentinel; lint walker green; headline values (firing_condition, BIC winner, gate passes, divergence_pct, char_func_source, strip emission path) captured to `_artifacts/real_data_summary.txt` for Task 3; synthetic `0afc6af38e24` run untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author archival README.md under data/fits/ichi/0afc6af38e24/</name>
  <files>
    data/fits/ichi/0afc6af38e24/README.md
  </files>
  <read_first>
    - .planning/phases/04.1-.../_artifacts/new_run_id.txt (the new real-data run_id; load-bearing pointer)
    - .planning/phases/04.1-.../_artifacts/real_data_summary.txt (captured headline values; cross-check the synthetic vs real divergence narrative)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (the `<decisions>` block on "Keep both run_ids" + the divergence policy)
    - .planning/phases/04-.../04-VERIFICATION-pre.md (the synthetic-substrate headline narrative; replicate the framing voice)
  </read_first>
  <action>
Write `data/fits/ichi/0afc6af38e24/README.md` as a short, machine-grepable archival note. The exact wording per CONTEXT.md `<decisions>` → "Rerun output disposition": **"Methodology-validation substrate (synthetic-stacked Hawkes η=0.5). Superseded by `<new-run-id>` for v1.0 publication."**

The file content (substitute `$NEW_RUN_ID` from `_artifacts/new_run_id.txt`):

```markdown
# Archived: synthetic-substrate Phase 4 production rep (run_id 0afc6af38e24)

**Status:** Methodology-validation evidence. **Superseded by `<NEW_RUN_ID>` for v1.0 publication.**

## Why this run exists

Plan 04-09 (Phase 4 acceptance gate) needed a production-rep substrate. The real Phase-2 ICHI panel at `data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` lacked the `block_timestamp` column required by Phase 3's `_extract_legs_from_panel` (Phase 2→Phase 3 column-wire gap — out of Plan 04-09 scope at the time). The fallback substrate was a 3x time-shifted stack of the Phase 3 synthetic Hawkes fixture (`analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet`), written to `data/raw/ichi/.../synthetic_p4_09_stacked_67000000_67002058.parquet`.

The synthetic substrate validated the Plan 04-08 Path A architecture end-to-end (Sobol-QMC characteristic function builder, four-condition gate, three-way stress, null-result-PDF firing decision) on data with known properties (Hawkes η=0.5 self-excitation).

## Why this run is now superseded

Phase 04.1 (gap closure) backported `block_timestamp` into the Phase 2 ingest path (Plan 04.1-00: `analysis/src/abrigo_x402/ingest.py` + sanity tests + lint extension) and regenerated the real ICHI panel (Plan 04.1-01: same JSONL sidecars, augmented parquet schema). Plan 04.1-02 reran the Phase 3 fit producing run_id `<NEW_RUN_ID>` with deterministically derived dataHash. Plan 04.1-03 reran the Phase 4 hedge orchestrator on that real-data run_id.

The new real-data run is the canonical v1.0 publication substrate. Phase 5 PDF deliverable (`reports/ichi.pdf`) reads from `data/fits/ichi/<NEW_RUN_ID>/` for headline findings.

## Preserved as audit history

This directory is NOT deleted. It preserves audit history of the synthetic-substrate methodology-validation path. The synthetic-vs-real comparison table in `.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` (appended "04.1 Real-Data Rerun" section) IS the load-bearing methodology-validation evidence for Phase 5: the architecture works on data with known properties (synthetic) AND on data with unknown properties (real).

## Pointers

- **Canonical Phase 5 input:** `data/fits/ichi/<NEW_RUN_ID>/` (load-bearing v1.0 substrate)
- **Methodology-validation evidence:** this directory (kept for audit; cited in Phase 5 narrative methodology section only)
- **Append-section in Phase 4 verification gate:** `.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` → "04.1 Real-Data Rerun" section
- **Frontmatter signal Phase 5 reads at parse time:** `real_data_rerun_run_id: <NEW_RUN_ID>` + `substrate_substitution_resolved: true`

---

*Archived: 2026-05-27 (Phase 04.1 close-the-loop)*
```

Substitute `<NEW_RUN_ID>` with the actual value from `_artifacts/new_run_id.txt`. Use `sed` or write the substitution inline via shell:

```bash
NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)
# Use Write tool with the README content above, replacing <NEW_RUN_ID> with $NEW_RUN_ID literally
```

NO modifications to `fit_report.json`, `gate_report.json`, `joint_dist.json`, `strip.json`, `stress_report.json`, or `residuals.parquet` inside `0afc6af38e24/`. The README is the ONLY new file in this dir.
  </action>
  <verify>
    <automated>test -f data/fits/ichi/0afc6af38e24/README.md && grep -q "Superseded by" data/fits/ichi/0afc6af38e24/README.md && grep -q "$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)" data/fits/ichi/0afc6af38e24/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f data/fits/ichi/0afc6af38e24/README.md` exits 0
    - `grep -q "Superseded by" data/fits/ichi/0afc6af38e24/README.md` exits 0 (verbatim wording from CONTEXT.md `<decisions>`)
    - `grep -q "Methodology-validation substrate" data/fits/ichi/0afc6af38e24/README.md` exits 0
    - `grep -q "$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)" data/fits/ichi/0afc6af38e24/README.md` exits 0 (the actual NEW_RUN_ID is referenced)
    - `! grep -q "<NEW_RUN_ID>" data/fits/ichi/0afc6af38e24/README.md` exits 0 (the placeholder template literal was substituted, not committed)
    - `ls data/fits/ichi/0afc6af38e24/` returns the SAME files as before this plan PLUS `README.md` (no other files added or removed; `fit_report.json`, `gate_report.json`, `joint_dist.json`, `strip.json`, `stress_report.json`, `residuals.parquet`, `run_log.txt` all unchanged)
    - `git diff HEAD -- data/fits/ichi/0afc6af38e24/fit_report.json data/fits/ichi/0afc6af38e24/gate_report.json data/fits/ichi/0afc6af38e24/joint_dist.json data/fits/ichi/0afc6af38e24/strip.json data/fits/ichi/0afc6af38e24/stress_report.json data/fits/ichi/0afc6af38e24/residuals.parquet` is empty (no modifications to existing synthetic-run artifacts)
  </acceptance_criteria>
  <done>
    `data/fits/ichi/0afc6af38e24/README.md` exists; contains "Superseded by" + "Methodology-validation substrate" + actual NEW_RUN_ID; no other files in the dir modified; existing synthetic-run artifacts unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append "04.1 Real-Data Rerun" section + 3 new frontmatter fields to 04-VERIFICATION-pre.md</name>
  <files>
    .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
  </files>
  <read_first>
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md (full file, 106 lines — read VERBATIM to confirm the existing frontmatter at lines 1-17 and the existing 18-row acceptance grid at lines 27-47; both MUST be preserved verbatim by the append operation)
    - .planning/phases/04.1-.../_artifacts/new_run_id.txt (load-bearing NEW_RUN_ID for the new frontmatter field)
    - .planning/phases/04.1-.../_artifacts/real_data_summary.txt (Task 1's captured headline values: firing_condition, BIC winner, gate passes, divergence_pct, char_func_source, strip path)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (the `<decisions>` block on "04-VERIFICATION-pre.md amendment shape" — 3 new fields + side-by-side table)
  </read_first>
  <action>
**This is a careful, surgical edit. The existing file MUST stay intact: 18-row acceptance grid, all existing frontmatter fields, all existing prose sections. ONLY two changes:**

1. **Append 3 frontmatter fields** between line 16 (`created: 2026-05-27`) and line 17 (the closing `---`).
2. **Append a new H2 section** "04.1 Real-Data Rerun" at the end of the file (before the final italics line at line 105).

**Step A — Load the values to substitute.**

```bash
NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)
source .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/real_data_summary.txt
# Now $NEW_RUN_ID, $FIRING_CONDITION, $BIC_WINNER, $GATE_PASSES, $DIVERGENCE_PCT, $CHAR_FUNC_SOURCE, $STRIP_PATH are available
```

**Step B — Compute the TWO boolean/enum fields** (per reviewer feedback: a single `real_data_rerun_passed` bool conflated artifact-completeness with scientific-outcome; Phase 5 would misread. Split into `real_data_rerun_artifacts_complete: <bool>` and `real_data_rerun_firing_condition: <null | "null_cost" | "null_lr" | "null_convex" | "null_strip_unavailable">`).

**B.1 — `real_data_rerun_artifacts_complete: <bool>`** (artifact-emission completeness):

```bash
ARTIFACTS_COMPLETE=false
if test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/fit_report.json && \
   test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/joint_dist.json && \
   test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/gate_report.json && \
   (test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip.json || test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/strip_degenerate.json) && \
   test -f /home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/stress_report.json; then
    ARTIFACTS_COMPLETE=true
fi
echo "ARTIFACTS_COMPLETE=$ARTIFACTS_COMPLETE"
```

`real_data_rerun_artifacts_complete: true` means **all five expected artifacts emitted** (fit_report + joint_dist + gate_report + (strip OR strip_degenerate) + stress_report). It does NOT make any claim about the scientific outcome (whether the gate fired HEDGE-05, whether the strip was degenerate, etc.). Phase 5 reads this field to confirm the artifact set is intact.

**B.2 — `real_data_rerun_firing_condition: <null | "null_cost" | "null_lr" | "null_convex" | "null_strip_unavailable">`** (scientific outcome — matches Plan 04-08's `decide_firing_condition` four-value enum + `null` for "no firing, positive-result path"):

```bash
# Pull the firing_condition directly from gate_report.json (the authoritative source).
# decide_firing_condition writes null for the positive-result path; otherwise one of
# the four enum strings: "null_cost", "null_lr", "null_convex", "null_strip_unavailable".
FIRING_CONDITION_FRONTMATTER=$(python3 -c "
import json
g = json.load(open('/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/data/fits/ichi/$NEW_RUN_ID/gate_report.json'))
fc = g.get('firing_condition', None)
# Render for YAML: null literal if None, otherwise quoted string.
print('null' if fc is None else f'"{fc}"')
")
echo "FIRING_CONDITION_FRONTMATTER=$FIRING_CONDITION_FRONTMATTER"
```

Both fields are independent. `artifacts_complete: true` + `firing_condition: "null_lr"` is a legitimate combination (all artifacts emitted, and HEDGE-05 fired on condition (b) DGP-03 LR indistinguishability — the EXPECTED path on n=832 below the 300-event identifiability floor; see CONTEXT.md divergence policy).

**Step C — Frontmatter append via Edit tool with verbatim anchors.** Use the Edit tool (NOT raw sed). The exact `old_string` / `new_string` pair the executor MUST pass to the Edit tool is spelled out verbatim below so the anchor cannot be guessed wrong (reviewer-flagged: ambiguous anchor could match against multiple sites if the executor improvises). The anchor is the unique two-line boundary `created: 2026-05-27\n---\n` at the end of the existing frontmatter:

**`old_string` (exact bytes, including trailing newline):**

```
created: 2026-05-27
---
```

**`new_string` (substitute `<NEW_RUN_ID>`, `<ARTIFACTS_COMPLETE>`, `<FIRING_CONDITION_FRONTMATTER>` with the actual captured values from Steps A+B; preserve the `created: 2026-05-27` line, append four new fields, then the closing `---`):**

```
created: 2026-05-27
real_data_rerun_run_id: "<NEW_RUN_ID>"
real_data_rerun_artifacts_complete: <ARTIFACTS_COMPLETE>
real_data_rerun_firing_condition: <FIRING_CONDITION_FRONTMATTER>
substrate_substitution_resolved: true
---
```

Note: `<FIRING_CONDITION_FRONTMATTER>` from Step B.2 renders as either the literal YAML keyword `null` (unquoted, positive-result path) OR a double-quoted string `"null_cost"` / `"null_lr"` / `"null_convex"` / `"null_strip_unavailable"`. The Step B.2 capture handles the rendering.

The result MUST look like (frontmatter only; preserve all **15** existing fields verbatim — the existing field list is: phase, plan, artifact, verification_pass, quarto_skipped, requirements_covered, real_panel_substituted, real_panel_substrate, production_rep_run_id, production_rep_firing_condition, production_rep_bic_winner, production_rep_char_func_source, plan_file_mtime, run_log_mtime, created — verified by direct read of `.planning/phases/04-…/04-VERIFICATION-pre.md` lines 2-16; total post-amendment = 19 fields):

```yaml
---
phase: 04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6
plan: 09
artifact: VERIFICATION-pre
verification_pass: true
quarto_skipped: true
requirements_covered: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05, SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
real_panel_substituted: true
real_panel_substrate: "Phase-3 synthetic Hawkes fixture (analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet) stacked x3 to clear DEPEND-01 cross-correlogram max_lag=50 (need >101 events/leg in held-out segment)"
production_rep_run_id: "0afc6af38e24"
production_rep_firing_condition: null
production_rep_bic_winner: frank
production_rep_char_func_source: frank_sobol_qmc
plan_file_mtime: 1779893398
run_log_mtime: 1779907978
created: 2026-05-27
real_data_rerun_run_id: "<actual NEW_RUN_ID hex>"
real_data_rerun_artifacts_complete: <true_or_false>
real_data_rerun_firing_condition: <null_or_quoted_enum_value>
substrate_substitution_resolved: true
---
```

**Step D — Append the new H2 section at end of file.** Add this section BEFORE the existing final line `*Verification authored 2026-05-27 by GSD execute-phase executor against `04-09-PLAN.md` (commit-time hash recorded by the Plan 04-09 final-metadata commit).*`:

```markdown

## 04.1 Real-Data Rerun

**Closed:** 2026-05-27 (Phase 04.1 gap-closure cycle)

**Substrate substitution resolved.** The Plan 04-09 synthetic-stacked substrate substitution was driven by a Phase 2→Phase 3 column-wire gap on `block_timestamp`. Phase 04.1 backported the column into the Phase 2 ingest path (Plan 04.1-00), regenerated the real ICHI panel (Plan 04.1-01), reran the Phase 3 fit producing real-data run_id (Plan 04.1-02), and reran the Phase 4 hedge orchestrator on that real-data run_id (this plan).

**New canonical run_id:** `<NEW_RUN_ID>` — supersedes synthetic `0afc6af38e24` for Phase 5 v1.0 publication. The synthetic run is archived (NOT deleted) at `data/fits/ichi/0afc6af38e24/README.md` with a pointer forward to the real-data run_id.

### Side-by-Side Comparison: Synthetic vs Real

| Dimension | Synthetic substrate (`0afc6af38e24`) | Real panel (`<NEW_RUN_ID>`) |
|-----------|----------------------------------------|------------------------------|
| firing_condition | `null` (positive-result path; no HEDGE-05 fire) | `<FIRING_CONDITION>` |
| BIC winner (`winner ± Δ`) | frank ± 0.009 (BIC 5.2705 vs Gaussian 5.2791; **Δ_BIC < 5 = noise-floor regime per `VINE_FALLBACK_DELTA_BIC_THRESHOLD` — no statistically distinguishable winner**) | `<BIC_WINNER>` (annotated `winner ± |Δ|`; if `|Δ| < 5`, append "noise-floor regime — no strong winner" caveat) |
| Gate condition pass count | 3-of-4 (vol_of_vol=False; skew_fat_tails+hawkes_self_excitation+usdt_depeg_basis_jump=True); any_condition_passed=True | `<GATE_PASSES>` |
| divergence_pct | 46.36% > 30% → divergence_flag=True (HEDGE-04 finding) | `<DIVERGENCE_PCT>%` |
| char_func_source | `frank_sobol_qmc` (Sobol-QMC fallback per Pattern J library-bug surface) | `<CHAR_FUNC_SOURCE>` |
| Strip emission path | `strip.json` (2^11 grid; no escalation; no degenerate path) | `<STRIP_PATH>` |

**HEDGE-05 expected-fire framing (Reality Checker review, iter 2/3).** Real-data HEDGE-05 firing on condition (b) ("DGP-03 LR indistinguishability at α=0.05") is the EXPECTED scientific outcome on n=832, NOT an anomaly. The synthetic substrate (n=2058 with controlled Hawkes η=0.5 properties) cleared the Q-9 identifiability floor by ~6.9×; the real panel at n=832 swap events (~28/day over 30 days, split asymmetrically between leg_0 and leg_1) is below the 300-event minimum locked in PRE_REGISTRATION. A `null_lr` or `null_convex` value in the `real_data_rerun_firing_condition` frontmatter field IS the v1.0 scientific result — exactly what the entire HEDGE-05 architecture (Plan 04-08 Path A + null-result PDF firing) exists to handle. Phase 5 narrative cites the real-data verdict, NOT the synthetic one; the synthetic run is methodology-validation evidence cited in the methodology section only.

**BIC-noise-floor footnote (Reality Checker review).** The `BIC winner (winner ± Δ)` row in the comparison table is annotated with the `|Δ|` magnitude. Per `VINE_FALLBACK_DELTA_BIC_THRESHOLD` (the project-wide constant for vine-copula vs Gaussian-vs-Frank discriminability), **Δ_BIC < 5 indicates sample-noise-floor regime where the BIC winner is NOT statistically distinguishable from the runner-up**. The synthetic Δ_BIC = 0.009 (Frank vs Gaussian) is firmly in this regime — a real-data flip from `frank` to `gaussian` or any other family is NOT a methodology contradiction, it is sample noise. Phase 5 narrative MUST treat both columns as "no strong copula winner" when |Δ_BIC| < 5 and avoid framing either as a scientific finding on family selection.

**Methodology-validation framing for Phase 5:** The architecture works as designed on data with known properties (synthetic Hawkes η=0.5) AND on data with unknown properties (real on-chain). The two columns above ARE the load-bearing methodology-validation evidence Phase 5 cites — divergence (if any) between the two columns IS itself a v1.0 scientific finding, NOT a methodology failure.

**Divergence policy (CONTEXT.md `<decisions>` "Divergence policy"):** Real-data is canonical for v1.0 publication. If real-panel produced a different firing_condition than synthetic (e.g., real fires HEDGE-05 where synthetic did not), real wins. Phase 5 PDF cites the real-data verdict; the synthetic substrate is referenced in the methodology section only.

### Phase 5 unblocking signals (machine-readable in frontmatter)

- `real_data_rerun_run_id: "<NEW_RUN_ID>"` — pointer to canonical Phase 5 input
- `real_data_rerun_passed: <true|false>` — Phase 5 reads this to confirm artifact set is intact
- `substrate_substitution_resolved: true` — explicit closure of Plan 04-09's documented substrate-substitution caveat (carried in existing `real_panel_substituted: true` field, now flipped to "resolved")

### Out-of-scope guardrails honored (AF-12)

- No deletion of synthetic `0afc6af38e24` run (archived via README only)
- No modification of existing 18-row acceptance grid above (preserved verbatim)
- No modification of existing **15** frontmatter fields (4 new fields APPENDED below `created: 2026-05-27`)
- No code edits in Plans 04.1-01/02/03 (all source edits landed atomically in Plan 04.1-00)
- No PANEL-02 header schema bump; no new lint hooks beyond the ICHI_PANEL_REQUIRED_COLUMNS column-presence check
- No Forno/Blockscout re-fetch (Plan 04.1-01 used cached JSONL sidecars)
- No new firing conditions; no new Quarto template branches; no new dependencies

```

Substitute all `<placeholders>` with actual values from `_artifacts/real_data_summary.txt` + `_artifacts/new_run_id.txt`.

**Step E — Verify the existing 18-row grid is intact.** After edit:

```bash
# Original grid rows include Row 1, Row 2, ... Row 16 + Rows 13a/13b/13c
grep -c "^| [0-9]" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
```
Expected: 16 (the 14 numbered rows + 13a/13b/13c sub-rows × 3 = 17, but markdown lines starting with `| 1` matches 1, 10, 11, 12, 13, 13a, 13b, 13c, 14, 15, 16 = 11; loose count check). The exact assertion is that the grid section is PRESENT (not deleted/replaced) and that the existing frontmatter fields `verification_pass: true` and `quarto_skipped: true` are STILL there:

```bash
grep -q "^verification_pass: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: verification_pass preserved"
grep -q "^quarto_skipped: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: quarto_skipped preserved"
grep -q "^production_rep_run_id: \"0afc6af38e24\"" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: synthetic production_rep_run_id preserved"
grep -q "^real_data_rerun_run_id:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: new real_data_rerun_run_id added"
grep -q "^substrate_substitution_resolved: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: substrate_substitution_resolved added"
grep -q "## 04.1 Real-Data Rerun" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && echo "PASS: new H2 section appended"
```
  </action>
  <verify>
    <automated>grep -q "^real_data_rerun_run_id:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && grep -q "^real_data_rerun_passed:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && grep -q "^substrate_substitution_resolved: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && grep -q "## 04.1 Real-Data Rerun" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && grep -q "^verification_pass: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md && grep -q "^production_rep_run_id: \"0afc6af38e24\"" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^real_data_rerun_run_id:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (new frontmatter field 1 of 4)
    - `grep -q "^real_data_rerun_artifacts_complete:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (new frontmatter field 2 of 4 — split from original `real_data_rerun_passed`; artifact-completeness only)
    - `grep -q "^real_data_rerun_firing_condition:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (new frontmatter field 3 of 4 — split from original `real_data_rerun_passed`; scientific outcome enum matching `decide_firing_condition`)
    - `grep -q "^substrate_substitution_resolved: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (new frontmatter field 4 of 4)
    - `! grep -q "^real_data_rerun_passed:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (the conflated single-bool field MUST NOT appear — Reality Checker iter-2 contract)
    - All 4 new frontmatter fields are individually greppable as line-anchored matches (each on its own line, no inline mixing); total post-amendment frontmatter = 19 fields (15 existing + 4 new)
    - `grep -q "^verification_pass: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (existing field 1 preserved)
    - `grep -q "^quarto_skipped: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (existing field 2 preserved)
    - `grep -q "^production_rep_run_id: \"0afc6af38e24\"" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (existing synthetic run_id field preserved)
    - `grep -q "^real_panel_substituted: true" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (existing substitution flag preserved — paired with new `substrate_substitution_resolved: true` semantically)
    - `grep -q "## 04.1 Real-Data Rerun" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (new H2 section appended)
    - GATE_REPORT_REQUIRED_KEYS pre-flight: the Step E gate-extraction inline python uses direct subscript access (no defensive `.get()` chains). Sanity grep: `grep -c '\.get("[a-z_]*", {})\.get(' .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-03-PLAN.md` returns `0` (no `.get(key, {}).get(...)` chains in the plan body's gate-extraction code; reviewer-flagged anti-pattern eliminated)
    - GATE_REPORT_REQUIRED_KEYS import + KeyError-on-drift contract: `grep -q "from lint_artifacts import GATE_REPORT_REQUIRED_KEYS" .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-03-PLAN.md` exits 0 (the plan body cites the authoritative shape contract from `scripts/lint_artifacts.py` instead of dict-chained `.get()` defaults)
    - BIC-noise-floor caveat present in comparison table or appended section: `grep -q "noise-floor regime" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (or the equivalent `Δ_BIC` annotation) — Reality Checker iter-2 contract
    - HEDGE-05 expected-fire framing present in appended section: `grep -q "HEDGE-05 expected-fire framing\|expected scientific outcome" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0
    - `grep -q "Side-by-Side Comparison: Synthetic vs Real" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (the 6-row table heading)
    - `grep -c "^| firing_condition\\|^| BIC winner\\|^| Gate condition\\|^| divergence_pct\\|^| char_func_source\\|^| Strip emission path" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` returns `6` (all 6 comparison rows present)
    - `grep -c "^| 1" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` >= 6 (existing 18-row grid preserved; sanity check that we didn't accidentally truncate)
    - `grep -q "Plan 04-pre\\|04-pre.*2dc3877" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0 (existing forward-audit-trail table preserved)
    - The actual NEW_RUN_ID hex appears in the frontmatter field value (not a literal `<NEW_RUN_ID>` placeholder): `! grep -q '"<NEW_RUN_ID>"' .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0
    - The actual NEW_RUN_ID hex appears in the new H2 section body: `grep -q "$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` exits 0
    - No other Plan 04 plans/files modified by Task 3: `git diff HEAD -- .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-pre-PLAN.md .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-00-PLAN.md` is empty
  </acceptance_criteria>
  <done>
    `04-VERIFICATION-pre.md` carries **4** new greppable frontmatter fields (real_data_rerun_run_id with actual hex, real_data_rerun_artifacts_complete bool, real_data_rerun_firing_condition `null | "null_cost" | "null_lr" | "null_convex" | "null_strip_unavailable"`, substrate_substitution_resolved: true) below existing `created:` field; the conflated single-bool `real_data_rerun_passed` is split per reviewer feedback so Phase 5 cannot misread; new "## 04.1 Real-Data Rerun" H2 section appended at end with 6-row side-by-side comparison table (BIC winner row annotated `winner ± |Δ|` with `Δ_BIC < 5` noise-floor caveat) + HEDGE-05 expected-fire framing paragraph; existing 18-row acceptance grid + all **15** prior frontmatter fields + existing prose sections preserved verbatim; no other Phase-4 plan files modified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Atomic close-the-loop commit + regression sanity</name>
  <files>
    (no files modified — commit + regression verification only)
  </files>
  <read_first>
    - .planning/phases/04.1-.../_artifacts/new_run_id.txt + real_data_summary.txt (substitute into commit message)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (the OUT-OF-SCOPE list — quote in commit message)
  </read_first>
  <action>
**Step A — Regression sanity (full Phase 2+3+4 suite under thread-pinned BLAS).** Confirm the new artifacts on disk + the appended VERIFICATION-pre.md edits didn't break anything:

```bash
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/ -x 2>&1 | tail -3
```

Expected: `>= 193 passed, 3 skipped` (the Plan 04.1-00 post-edit baseline).

**Step B — Atomic commit.** Single `docs(04.1):` commit. The new run-dir artifacts under `data/fits/ichi/<NEW_RUN_ID>/` may or may not be git-tracked depending on `.gitignore`; commit ONLY the doc-layer files (README.md + VERIFICATION-pre.md amendment + summary files). If `data/fits/ichi/<NEW_RUN_ID>/*.json` IS tracked, include them; otherwise skip (the artifacts on disk are the truth; git history is documentation).

```bash
NEW_RUN_ID=$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)

# Add the canonical doc-layer files. Check git status to determine which JSON artifacts are tracked.
git add .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
git add data/fits/ichi/0afc6af38e24/README.md
# If JSON artifacts are git-tracked (check with: git status data/fits/ichi/$NEW_RUN_ID/), add them too:
git add data/fits/ichi/$NEW_RUN_ID/*.json data/fits/ichi/$NEW_RUN_ID/run_log.txt 2>/dev/null || true

git commit -m "$(cat <<EOF
docs(04.1): close-the-loop — real-data hedge rerun + VERIFICATION amendment

Phase 04.1 gap-closure cycle complete. The Plan 04-09 synthetic-substrate
substitution caveat is resolved.

What this commit lands:
- data/fits/ichi/0afc6af38e24/README.md: archival note pointing forward
  to the new real-data run_id; synthetic run preserved as
  methodology-validation evidence (NOT deleted)
- 04-VERIFICATION-pre.md: APPENDED "04.1 Real-Data Rerun" H2 section with
  side-by-side synthetic-vs-real comparison table (6 rows × 2 columns);
  APPENDED 4 new greppable frontmatter fields:
    real_data_rerun_run_id: "$NEW_RUN_ID"
    real_data_rerun_artifacts_complete: <bool>
    real_data_rerun_firing_condition: <null | "null_cost" | "null_lr" | "null_convex" | "null_strip_unavailable">
    substrate_substitution_resolved: true
  (split per reviewer iter-2 feedback: the conflated single `real_data_rerun_passed`
  bool conflated artifact-emission with scientific-outcome; Phase 5 would misread.)
- Existing 18-row acceptance grid + **15** prior frontmatter fields preserved
  verbatim (APPEND, not supersede)

Predecessor plan outputs:
- Plan 04.1-00: ingest.py PROVENANCE_COLS extended with block_timestamp;
  2 new sanity tests; lint_artifacts.py ICHI_PANEL_REQUIRED_COLUMNS
- Plan 04.1-01: real ICHI panel parquet regenerated (pre-04.1 sha256
  2234b635729e2fbc307989f75bfdc8b21e29cd06b720b45682d2d385a26b5b2e →
  augmented; 832 rows preserved; 32 cols → 33)
- Plan 04.1-02: Phase 3 fit on augmented real panel; new run_id
  $NEW_RUN_ID (first-ever real-data Phase 3 fit; supersedes synthetic
  0afc6af38e24 for v1.0 publication)
- Plan 04.1-03 (this): Phase 4 hedge rerun on $NEW_RUN_ID; 4 primary
  artifacts emitted; orchestrator completion sentinel verified

Phase 5 PDF deliverable (reports/ichi.pdf) is now unblocked:
- Canonical input: data/fits/ichi/$NEW_RUN_ID/
- Methodology-validation reference: data/fits/ichi/0afc6af38e24/
- Phase 5 reads substrate_substitution_resolved: true from
  04-VERIFICATION-pre.md frontmatter as the machine-readable
  unblocking signal.

Out of scope (AF-12 silent-rescope defense — all four 04.1 plans):
no other ingest columns (no transaction_index, no gas_used); no refactors;
no PANEL-02 schema bump; no new dependencies; no new lint hooks beyond
the column-required addition; no re-fetching from Forno/Blockscout
(cached JSONL sidecars used); no Quarto template changes; no new firing
conditions; no deletion of synthetic 0afc6af38e24 run.

Refs PANEL-01, DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03,
HEDGE-04, HEDGE-05.
EOF
)"
```

**Step C — Post-commit verification.** Confirm the commit landed cleanly + AF-12 final sanity:

```bash
git log --oneline -1
git show --stat HEAD
# AF-12: confirm no out-of-scope literals appear in the edited doc files
grep -c "transaction_index\|gas_used\|panel_schema_version\|panel_augment" \
    .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md \
    data/fits/ichi/0afc6af38e24/README.md
# Expected: 0 across all files
```

**Step D — Smoke-test that the regression suite still passes after the commit (pre-commit hooks may auto-format):**

```bash
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/ -x 2>&1 | tail -3
```

Expected: `>= 193 passed, 3 skipped`. Pre-commit hooks `af_lint.sh` + AF-01..AF-12 + 4 Phase-4 grep gates should all pass on the doc-layer commit (no source code changed).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a single commit with subject prefix `docs(04.1):` containing "close-the-loop"
    - `git show --stat HEAD` lists AT LEAST: `.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md` + `data/fits/ichi/0afc6af38e24/README.md` (and possibly the new run-dir JSON artifacts if git-tracked)
    - `cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/ -x` exits 0 (≥193 passed, 3 skipped)
    - `grep -c "transaction_index\|gas_used\|panel_schema_version\|panel_augment" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md data/fits/ichi/0afc6af38e24/README.md` returns `0` (AF-12 OUT-OF-SCOPE grep gate)
    - `pre-commit run --files .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md data/fits/ichi/0afc6af38e24/README.md` exits 0
    - `git diff HEAD~1 HEAD -- analysis/src/ scripts/` is empty (no source-code drift in this commit; pure docs)
    - The commit message body contains the actual NEW_RUN_ID hex (not the placeholder string `$NEW_RUN_ID`): `git log -1 --pretty=%b | grep -q "$(cat .planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/_artifacts/new_run_id.txt)"`
  </acceptance_criteria>
  <done>
    Single atomic `docs(04.1): close-the-loop ...` commit landed touching `04-VERIFICATION-pre.md` + `data/fits/ichi/0afc6af38e24/README.md` (+ optional run-dir JSONs); full Phase 2+3+4 suite stays green (≥193 passed / 3 skipped); pre-commit hooks PASS; AF-12 grep returns 0 across edited doc files; no source-code drift in this commit.
  </done>
</task>

</tasks>

<verification>
- All 5 hedge artifacts exist under `data/fits/ichi/$NEW_RUN_ID/`: `joint_dist.json`, `gate_report.json`, (`strip.json` OR `strip_degenerate.json`), `stress_report.json`, `run_log.txt`
- `grep -q "hedge.orchestrator.run_hedge completed\|run_hedge completed" data/fits/ichi/$NEW_RUN_ID/run_log.txt` exits 0
- `data/fits/ichi/0afc6af38e24/README.md` exists with "Superseded by $NEW_RUN_ID" wording; existing synthetic-run artifacts unchanged
- **4** new frontmatter fields appear in `04-VERIFICATION-pre.md` (real_data_rerun_run_id, real_data_rerun_artifacts_complete, real_data_rerun_firing_condition, substrate_substitution_resolved); each individually greppable on its own line; the conflated single-bool `real_data_rerun_passed` MUST NOT appear (split per reviewer feedback)
- Existing 18-row acceptance grid + **15** prior frontmatter fields preserved verbatim in `04-VERIFICATION-pre.md`
- New "## 04.1 Real-Data Rerun" H2 section appended with 6-row side-by-side comparison table
- Single atomic `docs(04.1): close-the-loop ...` commit; AF-12 OUT-OF-SCOPE grep returns 0 across edited doc files
- Full Phase 2+3+4 test suite stays green (≥193 passed, 3 skipped) under thread-pinned BLAS
- Phase 5 unblocking signal `substrate_substitution_resolved: true` greppable from `04-VERIFICATION-pre.md` frontmatter at parse time
</verification>

<success_criteria>
1. Phase 4 hedge orchestrator runs end-to-end on real-data run_id from Plan 04.1-02
2. All 4 primary Phase 4 artifacts emit (joint_dist + gate_report + (strip OR strip_degenerate) + stress_report) + run_log.txt with completion sentinel
3. Synthetic `0afc6af38e24` archived via one-line README pointing forward to real-data run_id
4. `04-VERIFICATION-pre.md` carries **4** new greppable frontmatter fields (split per reviewer feedback: `real_data_rerun_artifacts_complete` + `real_data_rerun_firing_condition` replace the conflated single `real_data_rerun_passed` bool) + new "04.1 Real-Data Rerun" H2 section + 6-row synthetic-vs-real comparison table with BIC-noise-floor caveat + HEDGE-05 expected-fire framing paragraph
5. Existing 18-row acceptance grid + **15** prior frontmatter fields PRESERVED verbatim
6. Phase 5 unblocking signal (`substrate_substitution_resolved: true`) is greppable
7. Single atomic `docs(04.1): close-the-loop ...` commit; AF-12 grep returns 0
8. Full Phase 2+3+4 suite stays green (≥193 passed / 3 skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-backport-block-timestamp-into-phase-2-panel-writer-and-rerun-phase-3-fit-phase-4-production-rep-on-real-ichi-ckes-usdt-panel/04.1-03-SUMMARY.md` (≤ 80 lines; longer than predecessor SUMMARYs because this is the close-the-loop step) documenting:
- New run_id NEW_RUN_ID (12 hex chars) — canonical Phase 5 v1.0 substrate
- Synthetic vs real comparison table (verbatim 6 rows; matches the table appended to 04-VERIFICATION-pre.md)
- Headline divergence call-out: what differs between synthetic and real, framed per CONTEXT.md `<decisions>` "Divergence policy"
- Phase 5 unblocking status: `substrate_substitution_resolved: true` confirmed in frontmatter
- AF-12 final ledger: all four 04.1 plans (00, 01, 02, 03) honored the OUT-OF-SCOPE list verbatim; no scope drift across the 4-plan cycle
- Closing pointer: `/gsd:plan-phase 5` is now unblocked; `reports/ichi.pdf` will render on the real-data findings
</output>
