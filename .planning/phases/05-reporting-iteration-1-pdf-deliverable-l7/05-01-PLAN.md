---
phase: 05-reporting-iteration-1-pdf-deliverable-l7
plan: 01
type: tdd
wave: 1
depends_on: [05-00]
files_modified:
  - analysis/src/abrigo_x402/report/__init__.py
  - analysis/src/abrigo_x402/report/sensitivity_sweep.py
  - analysis/tests/test_sensitivity_sweep.py
  - data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json
autonomous: true
requirements: [REPORT-03]
specialist:
  name: Analytics Reporter (analytics/quant specialist)
  why: "REPORT-03 is a numerical per-cell re-evaluation of the 4 qualitative convex-dominance conditions over a locked 3×3 cost-prior grid, reading gate_report.json + firing_condition.json and re-running the existing gate machinery per cell. This is analytical-correctness work (reading the right artifact fields, honest invariance reporting), not prose or build wiring — an analytics/quant specialist per CONTEXT's per-task directive."
must_haves:
  truths:
    - "sensitivity_sweep.json exists at data/fits/ichi/bdaf5c7ba5a2/ with exactly 9 cells over the PRE-REG-LOCKED grid rate_per_event ∈ {1,5,10} × USD_per_query ∈ {2.5e-6,5e-6,7.5e-6}."
    - "Each cell carries the 4 qualitative convex-dominance booleans (vol_of_vol>0, skew/fat-tails, η≥threshold, USDT-depeg jump) + the firing_condition, derived by ACTUALLY INVOKING the existing gate machinery once and broadcasting (honestly labeled `evaluated_once: true, broadcast_to_grid: true, depends_on_cost_priors: false` — NOT a fabricated per-cell `recomputed: true` literal attached to a constant copy)."
    - "If the conditions are invariant to the cost priors (likely — they derive from the DGP density, not the cost leg), that invariance is reported honestly as a top-level finding flag."
    - "NO new cost-leg model, NO dollar-Δ metric, NO κ index is introduced anywhere in analysis/src/ (forbidden-regex byte-identical to the Wave-0 guard)."
  artifacts:
    - path: "analysis/src/abrigo_x402/report/sensitivity_sweep.py"
      provides: "compute_sensitivity_sweep(run_dir) → writes sensitivity_sweep.json; evaluates the 4 qualitative conditions + firing_condition once via the gate machinery, broadcasts honestly per grid cell"
      min_lines: 40
    - path: "data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json"
      provides: "the 9-cell qualitative convex-dominance sweep the PDF presents"
      contains: "all_cells_any_condition_passed"
  key_links:
    - from: "analysis/src/abrigo_x402/report/sensitivity_sweep.py"
      to: "analysis/src/abrigo_x402/hedge/falsification.py + data/fits/ichi/bdaf5c7ba5a2/gate_report.json + firing_condition.json"
      via: "actual invocation of the existing four-condition gate evaluator (or read of its evidence dict), broadcast to the grid"
      pattern: "evaluate_four_conditions|any_condition_passed"
    - from: "analysis/tests/test_sensitivity_sweep.py"
      to: "sensitivity_sweep.json"
      via: "schema + grid + honest-broadcast assertions (asserts the gate was invoked, not a literal flag)"
      pattern: "rate_per_event"
---

<objective>
Produce `data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json` (REPORT-03): the 3×3 cost-prior sweep where each cell carries the 4 QUALITATIVE convex-dominance conditions + the firing_condition — evaluated by actually invoking the existing gate machinery ONCE and broadcasting (honestly labeled), answering "does the convex-demand call survive ±cost-prior perturbation?"

Purpose: REPORT-03 requires "all downstream estimates re-run" across the cost-prior grid. The CONTEXT (decision 4, CORRECTED) supersedes the older RESEARCH "dominance-Δ" framing: research found NO cost model exists and the dollar-Δ is blocked by the degenerate joint_dist (n_min=79). The primary metric is the 4 qualitative conditions from the gate, re-evaluated per cell — booleans, not a dollar margin. CRITICAL HONESTY FIX (reviewer MAJOR): the conditions are PARAMETER-FREE w.r.t. the cost priors, so the honest implementation INVOKES the gate evaluator once and BROADCASTS, labeling each cell `evaluated_once: true, broadcast_to_grid: true, depends_on_cost_priors: false` — it does NOT attach a fabricated `recomputed: true` to a constant copy (that would assert work that did not happen, the exact label-vs-reality gap AF-03 exists to catch). If they are cost-prior-invariant (the expected result), report that invariance honestly: the dominance is convexity-driven (fat tails), not cost-prior-driven.
Output: `sensitivity_sweep.json` (9 cells) + a small `abrigo_x402.report.sensitivity_sweep` module + GREEN tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-RESEARCH.md
@data/fits/ichi/bdaf5c7ba5a2/gate_report.json
@data/fits/ichi/bdaf5c7ba5a2/firing_condition.json
@data/fits/ichi/bdaf5c7ba5a2/fit_report.json
@notes/PRE_REGISTRATION.md

<interfaces>
The 4 qualitative convex-dominance conditions live in gate_report.json (read on disk, verified):
```json
{
  "vol_of_vol_gt_zero": {"passed": true, "evidence": {...}},
  "positive_skew_fat_tails": {"passed": true, "evidence": {...}},
  "hawkes_self_excitation": {"passed": true, "evidence": {"branching_ratio": 0.5999724, "branching_ratio_floor": 0.2, ...}},
  "usdt_depeg_basis_jump": {"passed": true, "evidence": {"source": "literature_range_stipulation", "base_triple": {"lambda_J": 0.05, "mu_J": -0.05, "sigma_J": 0.02}, "sensitivity_summary": {"n_flips": 0}}},
  "any_condition_passed": true
}
```
firing_condition.json: `{"firing_condition": "null_strip_unavailable", ...}`.

CRITICAL — the 4 conditions are PARAMETER-FREE w.r.t. the cost priors: vol_of_vol, skew/kurtosis, branching_ratio η, and the jump-triple all derive from the DGP density + the held-out residuals, NOT from rate_per_event/USD_per_query. The cost priors enter NOWHERE in the existing gate. This is the expected result: the sweep demonstrates the convex call is cost-prior-INVARIANT (convexity-driven). Report that as the honest finding via the `evaluated_once + broadcast_to_grid + depends_on_cost_priors:false` triple — do NOT invent a cost-dependent quantity to manufacture variation, and do NOT label a constant copy `recomputed: true`.

HONEST-INVOCATION REQUIREMENT (reviewer MAJOR-D): the module must ACTUALLY call the gate evaluator (`abrigo_x402.hedge.falsification.evaluate_four_conditions`, or — if that requires re-fitting inputs unavailable read-only — read the gate's OWN evidence dict from gate_report.json) so the four booleans come from a real evaluation, not an inline hardcoded copy. The test asserts the invocation actually happened (see Test 2 below: assert the source is the gate evidence dict / call-count, NOT a bare literal).

PRE-REG-LOCKED GRID (notes/PRE_REGISTRATION.md §Prior Parameters, line 28 + line 162):
  rate_per_event ∈ {1, 5, 10}   (queries/event — central 5; this is the LOCKED grid, NOT {2.5,5,7.5})
  USD_per_query ∈ {2.5e-6, 5e-6, 7.5e-6}   (±50% of $5e-6 headline)
Use {1,5,10} for rate_per_event verbatim. The CONTEXT decision 4 explicitly says: "do NOT use (2.5,5,7.5) for rate_per_event".

CANONICAL FORBIDDEN-REGEX (locked in 05-00; byte-identical here — Wave-0 and Wave-1 use the SAME expression so they cannot diverge):
```
grep -rIE 'kappa|κ|dominance_delta|def +cost_leg|cost_of_convexity' analysis/src/   # must find ZERO hits
```
`rate_per_event`/`USD_per_query` are NOT in the forbidden set — they are legitimate pre-reg DATA LABELS (the GRID_* constants), not a cost model. The guard forbids a cost-MODEL (κ / dominance_delta / cost_leg / cost_of_convexity), not the grid axes. The `caveat` string may contain the phrase "cost leg modeled-not-paid" safely (no `cost_leg` token, no `def`).

HONEST TOP-LEVEL FLAG NAME (reviewer MINOR-3): name the top-level summary `all_cells_any_condition_passed` (mirrors the artifact field `any_condition_passed`), NOT `all_cells_convex_dominant` — the latter risks being quoted out of context in the PDF as if the run "passed" (it is 3/4 FALSE). `any_condition_passed=true` means the convex-demand SHAPE signal holds, NOT the overall gate verdict.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — sensitivity_sweep module invokes the gate evaluator once + broadcasts honestly per locked grid cell</name>
  <specialist>Analytics Reporter (analytics/quant specialist) — per-cell qualitative evaluation reading the correct gate artifact fields; honest broadcast labeling (no fabricated recompute flag); honest invariance reporting.</specialist>
  <read_first>
    - data/fits/ichi/bdaf5c7ba5a2/gate_report.json (the 4 conditions + evidence + any_condition_passed) — read-only source
    - data/fits/ichi/bdaf5c7ba5a2/firing_condition.json (null_strip_unavailable) — read-only source
    - data/fits/ichi/bdaf5c7ba5a2/fit_report.json (branching_ratio, gate_criteria) — read-only source for η
    - notes/PRE_REGISTRATION.md §Prior Parameters (lines 26-29, 162 — the locked {1,5,10}×{2.5e-6,5e-6,7.5e-6} grid)
    - analysis/tests/test_sensitivity_sweep.py (the Wave-0 xfail scaffold — REMOVE the xfail marks as you implement)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decision 4 CORRECTED — qualitative conditions, no cost-model/Δ/κ)
    - analysis/src/abrigo_x402/hedge/falsification.py (the existing four-condition gate — DO NOT modify; understand evaluate_four_conditions shape so you can INVOKE it / read its evidence)
  </read_first>
  <behavior>
    - Test 1 (test_schema_and_grid): sensitivity_sweep.json has keys {run_id, grid, all_cells_any_condition_passed, conditions_cost_prior_invariant, caveat}; grid is a list of EXACTLY 9 cells; the set of rate_per_event values across cells == {1,5,10}; the set of USD_per_query values == {2.5e-6,5e-6,7.5e-6}; run_id == "bdaf5c7ba5a2".
    - Test 2 (test_per_cell_evaluated_and_broadcast): each of the 9 cells has keys {rate_per_event, USD_per_query, conditions: {vol_of_vol_gt_zero, positive_skew_fat_tails, hawkes_self_excitation, usdt_depeg_basis_jump}, any_condition_passed, firing_condition, evaluated_once: true, broadcast_to_grid: true, depends_on_cost_priors: false}; conditions are booleans; firing_condition == "null_strip_unavailable" in every cell. ASSERT THE INVOCATION WAS REAL (not a literal): assert the per-cell condition booleans equal the gate's OWN evidence-dict values loaded from gate_report.json (i.e. the cells were sourced from the gate evaluation, not hardcoded inline) — e.g. assert `cell["conditions"]["hawkes_self_excitation"] == gate_report["hawkes_self_excitation"]["passed"]` for all four, and assert NO cell carries a bare `recomputed: true` literal. (This catches the label-vs-reality gap: a constant copy with a fabricated flag would fail because the test ties the value to the gate source.)
    - Test 3 (test_invariance_reported_honestly): since the 4 conditions are parameter-free w.r.t. cost priors, conditions_cost_prior_invariant == true AND all_cells_any_condition_passed == true (all 9 cells have any_condition_passed == true); caveat string contains "modeled-not-paid" and "convexity-driven".
    - Test 4 (test_no_new_cost_model_introduced): KEEP from Wave 0 with the CANONICAL forbidden-regex (byte-identical to 05-00): `grep -rIE 'kappa|κ|dominance_delta|def +cost_leg|cost_of_convexity' analysis/src/` returns ZERO hits (returncode == 1). `rate_per_event|USD_per_query` are NOT forbidden (legitimate grid labels). Define the regex as a single module-level constant in the test and reuse the IDENTICAL string in the acceptance one-liner so the test and the acceptance cannot diverge.
  </behavior>
  <action>
**RED:** Edit `analysis/tests/test_sensitivity_sweep.py` — remove the `@pytest.mark.xfail` decorators added in Wave 0; flesh out the 4 tests per the `<behavior>` block above. Add a module-scope fixture that runs `compute_sensitivity_sweep(<run_dir>)` once and reads back the JSON, where `<run_dir>` is resolved CWD-INDEPENDENTLY via `Path(__file__).resolve().parents[2] / "data/fits/ichi/bdaf5c7ba5a2"` (MINOR-1 fix — do NOT use `Path("../data/...")`). Run → MUST fail (module not yet implemented). Commit `test(05-01): RED sensitivity_sweep qualitative 3×3 (honest broadcast)`.

**GREEN:** Create `analysis/src/abrigo_x402/report/__init__.py` (empty or re-export) and `analysis/src/abrigo_x402/report/sensitivity_sweep.py` with:
   - `GRID_RATE_PER_EVENT = (1, 5, 10)` and `GRID_USD_PER_QUERY = (2.5e-6, 5e-6, 7.5e-6)` — the LOCKED pre-reg grid (rate is {1,5,10}, NOT {2.5,5,7.5}). Put a comment citing `notes/PRE_REGISTRATION.md §Prior Parameters lines 28-29`.
   - `def compute_sensitivity_sweep(run_dir: Path) -> dict:` that:
     1. Loads `gate_report.json` + `firing_condition.json` from `run_dir` (read-only).
     2. EVALUATES the 4 conditions ONCE by sourcing them from the gate's own evidence: extract `vol_of_vol_gt_zero.passed`, `positive_skew_fat_tails.passed`, `hawkes_self_excitation.passed`, `usdt_depeg_basis_jump.passed`, plus `any_condition_passed`, and `firing_condition`. (If `falsification.evaluate_four_conditions` can be invoked read-only on the on-disk evidence WITHOUT re-fitting, prefer that direct invocation so the booleans provably come from the evaluator; otherwise read the gate's evidence dict — either way the booleans are the gate's, not inline literals.)
     3. Builds the 9 cells via `itertools.product(GRID_RATE_PER_EVENT, GRID_USD_PER_QUERY)`. The 4 conditions are parameter-free w.r.t. the cost priors (they derive from the DGP density / residuals already on disk), so the SAME evaluated booleans are BROADCAST to every cell. Label each cell HONESTLY: `evaluated_once: true, broadcast_to_grid: true, depends_on_cost_priors: false` — do NOT attach `recomputed: true` (that would assert a per-cell re-computation that does not happen; the honest fact is one evaluation broadcast because the conditions do not depend on the cost priors). Do NOT fabricate a cost-dependent perturbation; do NOT call the DGP fit; do NOT build a strip.
     4. Sets `conditions_cost_prior_invariant = (len({tuple(cell['conditions'].values()) for cell in grid}) == 1)` → true here.
     5. Sets `all_cells_any_condition_passed = all(cell['any_condition_passed'] for cell in grid)`.
     6. Writes `run_dir / "sensitivity_sweep.json"` with a PANEL-02 metadata header copied from gate_report.json's header fields (chainId, contractAddress, blockRange, dataHash, gitCommit, run_id) + the sweep payload + `caveat: "cost leg modeled-not-paid (no x402-on-Celo settlement exists per PRE_REGISTRATION); the 4 convex-dominance conditions derive from the DGP density and are cost-prior-invariant → dominance is convexity-driven, not cost-prior-driven."`.
     7. Returns the dict.
   - A `__main__` / CLI-free design is fine; the module function is the entry point the test + the .qmd cell call.
   Run the 4 tests under thread-pinned BLAS → MUST pass. Generate the artifact by running `compute_sensitivity_sweep` against the real run dir, then `git add data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json` (PLAIN add — the Plan-05-00 nested-.gitignore allowlist already re-includes `ichi/bdaf5c7ba5a2/*.json`, so no `-f` is needed; -f would mask a misconfigured ignore — MINOR-2 fix). Commit `feat(05-01): sensitivity_sweep qualitative 3×3 honest broadcast — GREEN`.

DO NOT introduce `rate_per_event`/`USD_per_query` as a cost MODEL — they appear ONLY as grid labels in the sweep module's GRID_* constants. DO NOT add a κ / kappa / dominance_delta / cost_leg / cost_of_convexity function anywhere. DO NOT modify falsification.py or any DGP/hedge source. DO NOT re-run the fit or hedge orchestrators.
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_sensitivity_sweep.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/test_sensitivity_sweep.py -x` exits 0 (all 4 tests GREEN, no xfail remaining)
    - (from repo root) `python -c "import json; d=json.load(open('data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json')); assert len(d['grid'])==9; assert {c['rate_per_event'] for c in d['grid']}=={1,5,10}; assert {c['USD_per_query'] for c in d['grid']}=={2.5e-6,5e-6,7.5e-6}; print('OK')"` prints OK
    - (from repo root) `python -c "import json; d=json.load(open('data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json')); assert all(c['firing_condition']=='null_strip_unavailable' for c in d['grid']); assert all(c['evaluated_once'] and c['broadcast_to_grid'] and c['depends_on_cost_priors'] is False for c in d['grid']); assert d['all_cells_any_condition_passed'] is True; assert d['conditions_cost_prior_invariant'] is True; assert all('recomputed' not in c for c in d['grid']); print('OK')"` prints OK (honest broadcast labels present; no fabricated `recomputed` flag)
    - `! grep -rIE 'kappa|κ|dominance_delta|def +cost_leg|cost_of_convexity' analysis/src/` exits 0 (CANONICAL regex, byte-identical to 05-00 — no cost-model / no κ; AF-12 + CLAUDE.md non-negotiable)
    - `git ls-files data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json | wc -l` == 1 (artifact tracked via plain add — NOT -f-smuggled) AND `git check-ignore -q data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json; test $? -ne 0` (NOT ignored — the 05-00 allowlist works)
    - `cd analysis && uv run pytest -x -q` full suite green
  </acceptance_criteria>
  <done>sensitivity_sweep.json exists with 9 cells over the locked {1,5,10}×{2.5e-6,5e-6,7.5e-6} grid; the 4 qualitative conditions + firing_condition are sourced from a real gate evaluation and broadcast HONESTLY (`evaluated_once/broadcast_to_grid/depends_on_cost_priors:false`, no fabricated `recomputed` flag); cost-prior invariance reported honestly; no cost model / no κ; the artifact is plain-`git add`ed (not -f); tests GREEN; full suite green.</done>
</task>

</tasks>

<verification>
- `cd analysis && uv run pytest tests/test_sensitivity_sweep.py -x` green
- `! grep -rIE 'kappa|κ|dominance_delta|def +cost_leg|cost_of_convexity' analysis/src/` — no new cost model, no κ index (CANONICAL regex; AF-12 + CLAUDE.md)
- sensitivity_sweep.json schema: 9 cells, locked grid, qualitative booleans sourced from the gate, firing_condition per cell, honest broadcast labels (no `recomputed`), honest invariance flag + modeled-not-paid caveat
- Artifact tracked via plain `git add` (not -f) and confirmed NOT ignored
- Full suite green under thread-pinned BLAS
</verification>

<success_criteria>
REPORT-03 satisfied via the CORRECTED qualitative-conditions metric: 9-cell sweep over the pre-reg-locked grid, each cell carrying the 4 convex-dominance conditions sourced from a REAL gate evaluation and broadcast honestly (labeled `evaluated_once/broadcast_to_grid/depends_on_cost_priors:false`, never a fabricated `recomputed: true` on a constant copy), honestly reporting cost-prior invariance as the convexity-driven finding. No new cost-leg model, no dollar-Δ, no κ index.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-01-SUMMARY.md`
</output>
</output>
