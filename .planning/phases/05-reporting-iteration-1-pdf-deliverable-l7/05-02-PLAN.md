---
phase: 05-reporting-iteration-1-pdf-deliverable-l7
plan: 02
type: tdd
wave: 1
depends_on: [05-00, 05-01]
files_modified:
  - analysis/src/abrigo_x402/report/spot_check.py
  - analysis/tests/test_spot_check.py
  - analysis/tests/test_manifest.py
  - reports/MANIFEST.md
  - Makefile
autonomous: true
requirements: [REPORT-02, REPORT-04]
specialist:
  name: DevOps Automator
  why: "Spot-check is a deterministic seeded draw + network-optional curl logging; the manifest is a sha256 recompute-and-match gate filling the Makefile verify-reproducibility STUB. Both are build/repro infrastructure (curl, sha256sum, exit codes) — DevOps Automator per CONTEXT decision 6 + the per-task directive."
must_haves:
  truths:
    - "A seeded uniform-random draw (seed derived from run_id bdaf5c7ba5a2) selects 5 distinct rows from the 832-row panel parquet, deterministically re-derivable, and emits https://celo.blockscout.com/tx/0x... URLs."
    - "reports/MANIFEST.md pins the panel parquet sha256 (a72a4ee…), analysis/uv.lock SHA, pnpm-lock.yaml SHA (NOT package-lock.json / root uv.lock), subgraph block-pins + chainId, the bdaf5c7ba5a2 artifact sha256s (incl. CORRECTIONS.md + sensitivity_sweep.json), the spot-check seed, and a placeholder for reports/ichi.pdf."
    - "make verify-reproducibility recomputes-and-matches MANIFEST pins with the 3-state rule (PENDING only for reports/ichi.pdf; any OTHER missing pinned file = FAIL; present+mismatch = FAIL; present+match = OK; OK_COUNT must equal PIN_COUNT), exits 0 on full match and 1 on any tamper, run via the parameterized MANIFEST var against a tmp COPY in the tamper test (never mutating the real file). Verified against a CLEAN CHECKOUT (git worktree of HEAD), not just the working tree."
    - "Build-time curl logs HTTP status per row and never fails the build on offline (logs 'unverified (no network)')."
  artifacts:
    - path: "analysis/src/abrigo_x402/report/spot_check.py"
      provides: "seeded_spot_check(run_id, panel_path) → 5 (rowindex, txHash, url) tuples; seed recorded; deterministic"
      min_lines: 30
    - path: "reports/MANIFEST.md"
      provides: "the reproducibility manifest (REPORT-04), canonical pin format `<sha256><2 spaces><path>`"
      contains: "a72a4ee"
    - path: "Makefile"
      provides: "finalized verify-reproducibility recompute-and-match (3-state rule, canonical awk parse, parameterized MANIFEST)"
      contains: "verify-reproducibility:"
  key_links:
    - from: "Makefile verify-reproducibility"
      to: "reports/MANIFEST.md"
      via: "grep ^<64hex>  <path> + awk parse + sha256sum recompute + 3-state compare + OK==PIN guard"
      pattern: "MANIFEST"
    - from: "analysis/src/abrigo_x402/report/spot_check.py"
      to: "data/raw/ichi/.../67378253_67896653.parquet (now committed via 05-00 root-.gitignore negation)"
      via: "polars read + numpy default_rng(seed) choice"
      pattern: "default_rng"
---

<objective>
Produce the REPORT-02 deterministic spot-check (5 seeded Blockscout rows + network-optional curl logging) and the REPORT-04 reproducibility manifest (`reports/MANIFEST.md` + a finalized `make verify-reproducibility` recompute-and-match gate with the 3-state missing-file rule).

Purpose: REPORT-02 needs 5 verifiable panel rows with clickable Blockscout URLs, drawn with a recorded seed so a reviewer can re-derive the exact 5. REPORT-04 needs a manifest sufficient for a FRESH CLONE to reproduce the headline numbers — pinning the CORRECT lockfiles (analysis/uv.lock + pnpm-lock.yaml; NOT package-lock.json / root uv.lock which do not exist), the panel sha256, and the committed run-dir artifact sha256s (Option C-hybrid). FRESH-CLONE FIX (reviewer BLOCKER-1): the panel parquet is now COMMITTED via the Plan-05-00 scoped root-.gitignore negation (the JSONL source cache is NOT git-tracked, so `cli.py materialize`-on-absence is impossible offline — committing the 128KB panel is the chosen path; do NOT wire a materialize regeneration step that cannot run). CORRECTIONS.md + sensitivity_sweep.json are likewise now tracked via Plan-05-00's nested-.gitignore allowlist + Plan-05-01's commit, so every pinned path exists on a fresh clone.
Output: `spot_check.py` module + `reports/MANIFEST.md` + finalized `verify-reproducibility` + GREEN tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-RESEARCH.md
@data/fits/ichi/bdaf5c7ba5a2/fit_report.json
@Makefile

<interfaces>
Panel parquet: `data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` — 832 rows × 33 cols, carries `txHash` (full 0x... 64-hex). sha256 = `a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a` (== fit_report.json dataHash). NOW COMMITTED (Plan 05-00 root-.gitignore scoped negation re-includes this one parquet; the JSONL siblings stay ignored). NOTE: 832 raw rows → 778 arrival events after the PANEL-04 phantom-transfer filter; draw from the 832-row parquet (it retains txHash) and STATE the 832→778 relationship in the MANIFEST (Plan 05-03 writes the spot-check prose).

Deterministic seed derivation (RESEARCH Pattern 2):
```python
import hashlib, numpy as np, polars as pl
run_id = "bdaf5c7ba5a2"
seed = int(hashlib.sha256(run_id.encode()).hexdigest()[:8], 16)  # record in MANIFEST
df = pl.read_parquet(panel_path)                                  # 832 rows
idx = np.random.default_rng(seed).choice(df.height, size=5, replace=False)
rows = df[sorted(idx.tolist())]
urls = [f"https://celo.blockscout.com/tx/{h}" for h in rows["txHash"].to_list()]
```
Resolve `panel_path` CWD-INDEPENDENTLY via `Path(__file__).resolve().parents[2] / "data/raw/ichi/0x61Ef.../67378253_67896653.parquet"` (MINOR-1 — do NOT use `Path("../data/...")`). Filter/assert drawn rows carry a well-formed `0x[0-9a-fA-F]{64}` txHash and assert `panel_rows == 832` (MINOR — guards a silent re-draw if the panel ever changes; the panel is sha256-pinned so this is belt-and-suspenders).

Network-optional curl (RESEARCH Pattern 3) — add `--connect-timeout 5` so a misconfigured proxy can't stall up to max-time × 5 (MINOR):
```bash
code=$(curl -s -I -o /dev/null -w "%{http_code}" --connect-timeout 5 --max-time 10 "$url" 2>/dev/null || echo "000")
[ "$code" = "000" ] && echo "$url -> unverified (no network)" || echo "$url -> HTTP $code"
```

CANONICAL MANIFEST PIN FORMAT + PARSE (LOCKED in 05-00; restated IDENTICALLY here — the two plans MUST NOT diverge). Pin lines are STANDARD `sha256sum` format: `<64-hex><TWO spaces><relative-path>`. The verify loop in 05-00's Makefile body parses with `grep -E "^[a-f0-9]{64}  " "$MANIFEST" | awk '{print $1, $2}'` (awk splits on whitespace runs → `$1`=hash, `$2`=path, NO leading space — `read`/`sed` would leave a leading space on a two-space line). 05-02 does NOT re-author the parse — it INHERITS the 05-00 loop body verbatim and only removes the SKIP-on-absent-MANIFEST early-exit framing (the MANIFEST now exists). The loop already implements the 3-state rule + the OK_COUNT==PIN_COUNT guard.

3-STATE MISSING-FILE RULE (reviewer BLOCKER-2 — already wired into the 05-00 loop body; restate the CONTRACT here for the tests):
  - pinned path ABSENT and path == `reports/ichi.pdf` → log `PENDING: <path>`, SKIP (not counted, not a failure) — the ONLY allow-listed PENDING path.
  - pinned path ABSENT and path != `reports/ichi.pdf` → `MISSING (committed artifact absent)` → FAIL (exit 1). This is what catches a fresh-clone where a committed artifact failed to materialize.
  - pinned path PRESENT and sha mismatch → `MISMATCH` → FAIL (exit 1).
  - pinned path PRESENT and sha match → `OK`.
  - Final: `OK_COUNT == PIN_COUNT` (PDF PENDING is decremented from PIN_COUNT) or FAIL.
Distinguish absent from present via `[ -f "$path" ]` BEFORE `sha256sum` (the 05-00 body does this) — never via empty-hash comparison.

Lockfiles that ACTUALLY exist (confirmed): `analysis/uv.lock`, `pnpm-lock.yaml` (repo root). `package-lock.json` and root `uv.lock` DO NOT EXIST — do NOT pin them (CONTEXT decision 6). Subgraph block-pins: blockRange [67378253, 67896653], chainId 42220 (from fit_report.json).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — seeded spot-check module (5 deterministic Blockscout rows, network-optional curl)</name>
  <specialist>DevOps Automator — deterministic seeded draw + URL formatting + network-optional verification helper.</specialist>
  <read_first>
    - data/fits/ichi/bdaf5c7ba5a2/fit_report.json (dataHash a72a4ee…, blockRange, chainId) — read-only source
    - analysis/tests/test_spot_check.py (Wave-0 xfail scaffold — REMOVE xfail as you implement)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decision 5 — seed from run_id, 832→778, Blockscout URL form, network-optional curl)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-RESEARCH.md (Pattern 2 + Pattern 3 + Pitfall 5 off-by-population)
  </read_first>
  <behavior>
    - Test 1 (test_seeded_draw_deterministic): `seeded_spot_check("bdaf5c7ba5a2", panel_path)` returns 5 distinct rows; calling it twice returns identical row indices + txHashes (deterministic via numpy default_rng); asserts `panel_rows == 832`.
    - Test 2 (test_blockscout_urls_wellformed): each returned url matches `^https://celo\.blockscout\.com/tx/0x[0-9a-fA-F]{64}$` (drawn rows have well-formed txHash).
    - Test 3 (test_seed_recorded): the returned object exposes the integer seed used (== int(sha256("bdaf5c7ba5a2")[:8],16)) so MANIFEST can record it.
    - Test 4 (test_curl_logging_network_optional): `verify_url_status(url)` returns a string; on a forced-offline result ("000") it returns "...unverified (no network)" and does NOT raise.
  </behavior>
  <action>
**RED:** Edit `analysis/tests/test_spot_check.py` — remove the Wave-0 xfail decorators; implement the 4 tests per `<behavior>`. Resolve `panel_path` CWD-independently via `Path(__file__).resolve().parents[2] / "data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet"`. Run → MUST fail (module absent). Commit `test(05-02): RED spot_check seeded draw`.

**GREEN:** Add `analysis/src/abrigo_x402/report/spot_check.py` (the `report` package `__init__.py` is created by Plan 05-01; if running before it, create an empty `__init__.py`):
   - `def _seed_from_run_id(run_id: str) -> int: return int(hashlib.sha256(run_id.encode()).hexdigest()[:8], 16)`
   - `def seeded_spot_check(run_id: str, panel_path: Path, k: int = 5) -> dict:` → reads parquet with polars, draws `k` distinct indices via `np.random.default_rng(_seed_from_run_id(run_id)).choice(df.height, size=k, replace=False)`, sorts them, builds rows `{row_index, txHash, blockNumber, url}` with `url = f"https://celo.blockscout.com/tx/{txHash}"`, asserts each drawn txHash matches `0x[0-9a-fA-F]{64}`, returns `{run_id, seed, panel_path, panel_rows: df.height, rows: [...]}`.
   - `def verify_url_status(url: str, timeout: int = 10) -> str:` → runs `curl -s -I -o /dev/null -w "%{http_code}" --connect-timeout 5 --max-time {timeout} {url}` via subprocess; on returncode != 0 / "000" / empty returns `f"{url} -> unverified (no network)"`, else `f"{url} -> HTTP {code}"`. Never raises (wrap in try/except → offline string).
   Run the 4 tests under thread-pinned BLAS → MUST pass. Commit `feat(05-02): spot_check seeded draw + network-optional curl — GREEN`.

DO NOT recompute the fit. DO NOT draw from the 778-filtered events (draw from the 832-row parquet that carries txHash). DO NOT use python's `random` module (use numpy default_rng for cross-version determinism).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_spot_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/test_spot_check.py -x` exits 0 (4 GREEN, no xfail)
    - `cd analysis && uv run python -c "from abrigo_x402.report.spot_check import seeded_spot_check; from pathlib import Path; r=seeded_spot_check('bdaf5c7ba5a2', Path('../data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet')); assert len(r['rows'])==5; assert r['panel_rows']==832; assert all(x['url'].startswith('https://celo.blockscout.com/tx/0x') for x in r['rows']); print(r['seed'])"` prints an integer
    - the one-liner prints the same seed + same txHashes on two consecutive runs (determinism)
  </acceptance_criteria>
  <done>seeded_spot_check returns 5 deterministic Blockscout rows with recorded seed (panel_rows==832, well-formed txHash); verify_url_status is network-optional (connect-timeout bounded) and never raises; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — reports/MANIFEST.md + finalized make verify-reproducibility (3-state rule, canonical parse, clean-checkout verified)</name>
  <specialist>DevOps Automator — sha256 pin manifest + recompute-and-match Makefile gate with the 3-state exit-code rule.</specialist>
  <read_first>
    - Makefile (the verify-reproducibility body from Plan 05-00 — the canonical awk parse + 3-state rule + OK==PIN guard + `MANIFEST ?=` parameterization are ALREADY there; only the SKIP-on-absent-MANIFEST framing changes) — being finalized
    - analysis/tests/test_manifest.py (Wave-0 xfail scaffold + test_correct_lockfile_names + test_missing_committed_artifact_fails guards) — REMOVE xfail as you implement
    - data/fits/ichi/bdaf5c7ba5a2/*.json + residuals.parquet + CORRECTIONS.md (now tracked via 05-00) + run_log.txt + sensitivity_sweep.json (Plan 05-01 output) — the committed artifacts to pin
    - data/fits/ichi/bdaf5c7ba5a2/fit_report.json (dataHash a72a4ee…, blockRange [67378253,67896653], chainId 42220) — pin source
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decision 6 — Option C-hybrid + CORRECT lockfile names + pin panel by sha256, panel now committed)
  </read_first>
  <behavior>
    - Test 1 (test_pins_present): MANIFEST.md pins the panel parquet sha256 (a72a4ee…), the `analysis/uv.lock` SHA, the `pnpm-lock.yaml` SHA, the subgraph block-pins (67378253, 67896653) + chainId 42220, the spot-check seed, AND each committed bdaf5c7ba5a2 artifact (fit_report/gate_report/firing_condition/joint_dist/strip_degenerate/stress_report/sensitivity_sweep.json + residuals.parquet + CORRECTIONS.md + run_log.txt). The string "package-lock.json" and a bare root "uv.lock" pin (a `uv.lock` not prefixed `analysis/`) are ABSENT.
    - Test 2 (test_verify_repro_exit_codes): `make verify-reproducibility MANIFEST=<tmp_copy>` exits 0 (all current pins match) against a COPY of the real manifest placed in `tmp_path`; a tmp-copy variant with one deliberately-wrong sha makes the loop exit 1. NEVER mutate the real reports/MANIFEST.md (the Makefile `MANIFEST ?=` parameterization from 05-00 enables the copy-and-test).
    - Test 3 (test_correct_lockfile_names): KEEP from Wave 0 — analysis/uv.lock + pnpm-lock.yaml exist; package-lock.json + root uv.lock do NOT.
    - Test 4 (test_missing_committed_artifact_fails): KEEP from Wave 0 — using a tmp-copy manifest, a pinned committed-artifact path that is absent (NOT reports/ichi.pdf) → exit 1; a pinned reports/ichi.pdf that is absent → PENDING, exit 0. (Locks BLOCKER-2's contract: PENDING is allow-listed to ichi.pdf ONLY.)
    - Test 5 (test_every_pinned_fits_path_tracked): for every pinned `data/fits/...` and the panel `data/raw/...` path in MANIFEST, `git ls-files <path>` is non-empty (tracked) — so a missing-on-clone pin can never ship (reviewer BLOCKER fix).
  </behavior>
  <action>
**RED:** Edit `analysis/tests/test_manifest.py` — remove xfail decorators; implement test_pins_present + test_verify_repro_exit_codes + test_every_pinned_fits_path_tracked per `<behavior>` (test_correct_lockfile_names + test_missing_committed_artifact_fails carry from Wave 0). Run → MUST fail (MANIFEST absent). Commit `test(05-02): RED manifest pins + verify-repro 3-state exit codes`.

**GREEN:**
   1. Author `reports/MANIFEST.md`. Structure: a prose header (Option C-hybrid: scipy MLE not byte-reproducible → verify checksums committed artifacts, NOT a re-fit; fit provenance is commit `12cf99f` / run_id `bdaf5c7ba5a2` / dataHash `a72a4ee…`. State the panel is COMMITTED (Plan 05-00 root-.gitignore negation) — NOT regenerated via materialize, since the JSONL source cache is not git-tracked), then a `## Pins` section where each checksummed file is a CANONICAL line `<sha256>  <relative-path>` (TWO-space sep — the 05-00 verify loop greps `^[a-f0-9]{64}  <path>` and awk-parses). Compute the ACTUAL sha256 of each at author time via `sha256sum`:
      - panel parquet `data/raw/ichi/0x61Ef.../67378253_67896653.parquet` (now committed)
      - `analysis/uv.lock`
      - `pnpm-lock.yaml`
      - each committed run-dir artifact: `data/fits/ichi/bdaf5c7ba5a2/{fit_report,gate_report,firing_condition,joint_dist,strip_degenerate,stress_report,sensitivity_sweep}.json` + `residuals.parquet` + `CORRECTIONS.md` + `run_log.txt`
      - a documented PLACEHOLDER comment line for `reports/ichi.pdf` (rendered in Plan 05-03; its sha filled in Plan 05-04) — note in prose the PDF pin is finalized at render time and is the ONLY allow-listed PENDING path in the verify gate.
      Add a `## Provenance (non-checksummed)` subsection: subgraph block-pins `blockRange=[67378253,67896653]`, `chainId=42220`, the spot-check seed `int(sha256("bdaf5c7ba5a2")[:8],16)` with its decimal value, and the 832→778 PANEL-04 relationship.
   2. Finalize `make verify-reproducibility`: the canonical awk parse + 3-state rule + OK==PIN guard + `MANIFEST ?=` parameterization are ALREADY in the 05-00 body — the only change is the SKIP-on-absent-MANIFEST early-exit is now a no-op in practice (MANIFEST exists). Confirm the loop body is the 05-00 verbatim body (do NOT re-author a divergent parse). A pinned path that does not yet exist: `reports/ichi.pdf` → PENDING+skip; ANY OTHER missing path → FAIL.
   Run the manifest tests under thread-pinned BLAS + `make verify-reproducibility` → MUST pass / exit 0.
   3. **Clean-checkout verification (reviewer fresh-clone fix):** before declaring done, verify the gate on a CLEAN CHECKOUT of HEAD (exercises the fresh-clone path, not just the working tree where untracked files exist on disk):
      ```bash
      git worktree add /tmp/repro-check HEAD && \
      ( cd /tmp/repro-check && make verify-reproducibility ) ; \
      RC=$? ; git worktree remove --force /tmp/repro-check ; [ "$RC" = 0 ]
      ```
      This MUST exit 0 — proving the panel + CORRECTIONS.md + sensitivity_sweep.json pins all resolve on a clone (i.e. the 05-00 allowlists + 05-01 commit actually tracked them). If it fails, the gitignore/commit wiring is broken — fix before committing.
   Commit `feat(05-02): MANIFEST.md pins + verify-reproducibility 3-state recompute-and-match — GREEN`.

DO NOT pin `package-lock.json` or a bare root `uv.lock` (they do not exist — CONTEXT decision 6). DO NOT wire a `cli.py materialize` regeneration step into verify-reproducibility (the JSONL source cache is not git-tracked — the panel is committed instead). DO NOT re-fit in verify-reproducibility (Pitfall 2 — checksum committed artifacts only). DO NOT mutate the real reports/MANIFEST.md in any test (use the parameterized MANIFEST var against a tmp copy).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/test_manifest.py -x` exits 0 (5 GREEN, no xfail)
    - `make verify-reproducibility` exits 0 (all current pins match; PDF line logged PENDING)
    - `git worktree add /tmp/repro-check HEAD && ( cd /tmp/repro-check && make verify-reproducibility ); RC=$?; git worktree remove --force /tmp/repro-check; [ "$RC" = 0 ]` — clean-checkout gate exits 0 (fresh-clone path proven; panel + CORRECTIONS.md + sensitivity_sweep.json all resolve)
    - `grep -c 'a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a' reports/MANIFEST.md` ≥ 1 (panel sha256 pinned)
    - `grep -c 'analysis/uv.lock' reports/MANIFEST.md` ≥ 1 AND `grep -c 'pnpm-lock.yaml' reports/MANIFEST.md` ≥ 1
    - `! grep -q 'package-lock.json' reports/MANIFEST.md` (wrong lockfile absent)
    - `grep -cE '^[a-f0-9]{64}  data/fits/ichi/bdaf5c7ba5a2/' reports/MANIFEST.md` ≥ 10 (the committed artifacts incl. CORRECTIONS.md + sensitivity_sweep.json pinned)
    - tamper test: a wrong-sha tmp-copy MANIFEST makes the recompute loop exit 1 (test_verify_repro_exit_codes GREEN); the real reports/MANIFEST.md is byte-unchanged after the test run
    - `cd analysis && uv run pytest -x -q` full suite green
  </acceptance_criteria>
  <done>MANIFEST.md pins the panel sha256 + correct lockfiles + committed run-dir artifacts (incl. CORRECTIONS.md + sensitivity_sweep.json) + seed + block-pins + a PDF placeholder; verify-reproducibility uses the canonical parse + 3-state rule (PENDING only for ichi.pdf) + OK==PIN guard and exits 0 on match / 1 on tamper via a tmp-copy MANIFEST; the gate passes on a CLEAN CHECKOUT (fresh-clone path proven); package-lock.json absent; tests GREEN; full suite green.</done>
</task>

</tasks>

<verification>
- `cd analysis && uv run pytest tests/test_spot_check.py tests/test_manifest.py -x` green
- `make verify-reproducibility` exits 0 (PDF line PENDING, rest match); clean-checkout worktree run also exits 0 (fresh-clone proven)
- MANIFEST pins CORRECT lockfiles (analysis/uv.lock + pnpm-lock.yaml), panel sha256, committed artifacts incl. CORRECTIONS.md + sensitivity_sweep.json; package-lock.json absent
- 3-state rule: PENDING only for reports/ichi.pdf; any other missing committed artifact = FAIL; tamper = FAIL (on a tmp copy, never the real file)
- Spot-check draw deterministic from run_id seed (panel_rows==832); Blockscout URLs well-formed; curl network-optional + connect-timeout bounded
- Full suite green under thread-pinned BLAS
</verification>

<success_criteria>
REPORT-02 + REPORT-04 satisfied: deterministic 5-row seeded Blockscout spot-check with recorded re-derivable seed + network-optional curl logging; reproducibility manifest pinning the correct existing lockfiles + the COMMITTED panel sha256 + committed run-dir artifacts (Option C-hybrid, incl. CORRECTIONS.md + sensitivity_sweep.json), with a recompute-and-match verify-reproducibility gate that uses the canonical parse + 3-state missing-file rule (PENDING allow-listed to reports/ichi.pdf only) + OK==PIN guard, exits 0 on match / 1 on tamper, never mutates the real manifest, never re-fits, and PASSES ON A CLEAN CHECKOUT (the fresh-clone contract is genuinely met, not working-tree-only green).
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-02-SUMMARY.md`
</output>
</output>
