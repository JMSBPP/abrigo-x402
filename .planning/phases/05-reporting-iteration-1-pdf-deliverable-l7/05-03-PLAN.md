---
phase: 05-reporting-iteration-1-pdf-deliverable-l7
plan: 03
type: execute
wave: 2
depends_on: [05-00, 05-01, 05-02]
files_modified:
  - reports/ichi.qmd
  - analysis/tests/test_null_result_template.py
  - Makefile
autonomous: true
requirements: [REPORT-01]
specialist:
  name: Technical Writer (research-paper / mathematical-writeup specialist; ROADMAP Phase-5 primary)
  why: "The deliverable is a concise research-paper-style Quarto document carrying typeset equations (Hawkes kernel, branching ratio, LR statistic, KS time-rescaling, Carr-Madan strip) with the methodology-validation + honest-near-miss narrative. CONTEXT mandates the research-paper-capable writing specialist DRIVING the latex-econ-model + notation-clean + latex-doc + read-paper skills. This is the headline per-task assignment."
  skills: [latex-econ-model, notation-clean, latex-doc, read-paper]
must_haves:
  truths:
    - "reports/ichi.qmd is a NEW research-paper-style document (not a renamed null_result.qmd) that {{< include _templates/_evidence_branches.qmd >}}s (CORRECT relative path from reports/ichi.qmd) for the null_strip_unavailable block and carries the dual-signature pattern."
    - "The PDF text states the honest 3/4 verdict verbatim: gate_passes FALSE, KS leg-0 p-value labeled `p=0.0474` (statistic D=0.148; leg-1 p=0.0564, min-leg aggregator), firing_condition=null_strip_unavailable; NONE of the forbidden narrowing strings appear."
    - "The document carries the methodology section with the 3-run comparison naming the three root-cause bugs, the anti-fishing provenance (9 rejected post-hoc changes), the sensitivity sweep, the 5-row spot-check, and the convexity-justified / calibration-caveated 'what unlocks certification' v2-path section."
    - "make report-ichi renders reports/ichi.pdf > 50KB via quarto (markdown fallback rejected; rm -f stale PDF first; render-null-result-pdf retargeted so it no longer collides on reports/ichi.pdf; --execute-param firing_condition=null_strip_unavailable passed); render-touching tests stay quarto_skip-guarded so the green suite is preserved, with a NON-render source-grep companion test that runs in CI."
  artifacts:
    - path: "reports/ichi.qmd"
      provides: "the Iteration-1 deliverable source (REPORT-01)"
      min_lines: 120
      contains: "null_strip_unavailable"
    - path: "Makefile"
      provides: "finalized report-ichi (quarto render + TinyTeX self-install + --execute-param + spot-check curl logging) + retargeted render-null-result-pdf"
      contains: "report-ichi:"
  key_links:
    - from: "reports/ichi.qmd"
      to: "data/fits/ichi/bdaf5c7ba5a2/{fit_report,gate_report,firing_condition,sensitivity_sweep}.json"
      via: "read-only Python code cells (Pattern 1 — never recompute)"
      pattern: "fit_report|gate_report|firing_condition"
    - from: "reports/ichi.qmd"
      to: "reports/_templates/_evidence_branches.qmd"
      via: "{{< include _templates/_evidence_branches.qmd >}} (relative to reports/ichi.qmd) for the null_strip_unavailable evidence block"
      pattern: "include _templates/_evidence_branches"
    - from: "Makefile report-ichi"
      to: "reports/ichi.qmd"
      via: "quarto render → ichi.pdf with --execute-param firing_condition=null_strip_unavailable"
      pattern: "quarto render ichi.qmd"
---

<objective>
Author `reports/ichi.qmd` — the Iteration-1 PDF deliverable source (REPORT-01) — as a concise research-paper-style document with typeset equations and the locked methodology-validation + honest-near-miss narrative, then finalize `make report-ichi` (quarto render + TinyTeX self-install + `--execute-param firing_condition=null_strip_unavailable` + spot-check curl logging + retarget the colliding render-null-result-pdf) and extend the quarto-skip-guarded test for the ichi.qmd dual-signature + verdict-not-narrowed (AF-03) guard, with a NON-render source-grep companion that runs in CI.

Purpose: This is the headline deliverable. Every number is READ from the on-disk `bdaf5c7ba5a2/` artifacts (Pattern 1 — never recompute the fit). The verdict is FIXED and must be stated honestly (gate_passes FALSE, KS leg-0 p-value labeled `p=0.0474` — NOT the statistic D=0.148 — firing null_strip_unavailable); the refusal-to-narrow (citing the 9 rejected post-hoc changes) is a credibility asset. The document carries the methodology section (3-run comparison, three root-cause bugs), the sensitivity sweep (Plan 05-01), the 5-row spot-check (Plan 05-02), and the convexity-justified / calibration-caveated v2-path section.
Output: `reports/ichi.qmd` + finalized `make report-ichi` + a retargeted render-null-result-pdf + an extended quarto-skip-guarded test + a CI-running source-grep companion.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-RESEARCH.md
@data/fits/ichi/bdaf5c7ba5a2/fit_report.json
@data/fits/ichi/bdaf5c7ba5a2/gate_report.json
@data/fits/ichi/bdaf5c7ba5a2/firing_condition.json
@reports/_templates/null_result.qmd
@reports/_templates/_evidence_branches.qmd
@.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/DISPOSITION_MEMO_04_1_1_ks_halt.md
@Makefile

<interfaces>
VERDICT NUMBERS TO EMBED (loaded by .qmd code cells from bdaf5c7ba5a2/, read on disk — these are the values shown):
- eta (branching_ratio) = 0.5999724484494755 -> report "eta ~ 0.600 (lower bound; ~13% downward finite-sample bias at n~700)"
- AIC-min decay beta = 0.001 (1/beta = 1000s); decay_aic_table {0.0001: Inf, 0.001: 9800.78, 0.01: 9833.86, 0.1: 9937.65, 1.0: 10077.41, 10.0: 10117.49}
- LR observed_stat = 561.2948088026606, p_value = 0.0, alpha = 0.01, n_reps = 1000 -> "LR rejects NHPP"
- held_out_loglik hawkes = -1206.23, nhpp = -1320.63 -> "held-out Hawkes wins by ~114 nats"
- KS held-out leg-0: p-VALUE = 0.047350333810196134 (report as "p=0.0474"), STATISTIC D = 0.14799982742727918 (report as "D=0.148"); leg-1 p-VALUE = 0.05643942824856608 (report "p=0.0564"); ks_alpha = 0.05, min-leg aggregator -> ks_held_out_passes = FALSE (knife-edge: leg-0 p 0.0026 below 0.05)
- gate_criteria: lr_rejects=true, eta_floor_met=true, branching_ci_excludes_zero=true, ks_held_out_passes=FALSE, stationary=true -> gate_passes = FALSE (3/4)
- firing_condition = "null_strip_unavailable"
- gate_report.json: all 4 convex-dominance conditions pass (vol_of_vol leg0=1.180, skew=1.849/excess_kurt=3.602, branching_ratio=0.600>=0.2, usdt_depeg_basis_jump) -> any_condition_passed=true
- joint_dist degenerate: n_min=79 <= 101 (DEPEND-01 lag-radius floor) -> Carr-Madan strip unbuildable

KS LABEL DISCIPLINE (reviewer MAJOR-F — verdict-integrity): `0.0474` is the KS leg-0 **p-VALUE**, NOT the statistic (the statistic is D=0.148). The PDF + prose MUST print the LABELED form `p=0.0474` (or `p = 0.0474`), never a bare `0.0474` that could be mistaken for the statistic. Use ONE canonical rounding `0.0474` everywhere (the full `0.047350…` may appear in a footnote). The AF-03 grep asserts the LABELED string `p=0.0474` / `p = 0.0474`, not bare `0.0474`, so a wrong "D=0.0474" or "KS=0.0474" cannot pass.

3-RUN COMPARISON (CONTEXT decision 2 — the v1.0 scientific contribution):
  0afc6af38e24 : synthetic-mislabel : true eta 0.05 (tick normalized-kernel) : -- : bug = mislabeled fixture (expected_branching_ratio=0.5 but spectral_radius=0.05)
  ae9e3ba17900 : real, LS-fallback : (null_lr artifact) : null_lr : bug = projection-trick CI + kernel-blind beta=0.1 -> LS degeneracy
  bdaf5c7ba5a2 : real, scipy_canonical_ll free-beta AIC : 0.600 : 3/4, null_strip_unavailable : (corrected estimator)
  Name the THREE root-cause bugs: (1) mislabeled fixture, (2) projection-trick CI, (3) kernel-blind beta=0.1.

ANTI-FISHING PROVENANCE (DISPOSITION_MEMO_04_1_1_ks_halt.md — the 9 rejected post-hoc changes): cite that none of {relax KS alpha, switch min-leg->leg-average aggregator, change split fraction, drop the failing leg, switch KS->AD/CvM, re-seed/re-fit, add a robustness variant, relabel as "pass with caveat", footnote the miss as confirmatory} were applied. leg-average ~ 0.0519 "would pass" — explicitly NOT taken.

EQUATIONS TO TYPESET (latex-econ-model + notation-clean + latex-doc):
- Hawkes intensity / exp kernel: lambda_i(t) = mu_i + sum_j integral alpha_ij e^{-beta(t-s)} dN_j(s)
- branching ratio: eta = rho(alpha/beta) (spectral radius of the normalized adjacency)
- boundary-correct LR statistic: Lambda = 2(loglik_Hawkes - loglik_NHPP), null = 50:50 chi2(0):chi2(1) mixture
- time-rescaling KS: under correct specification Delta-compensator Lambda(t_k)-Lambda(t_{k-1}) ~ Exp(1); KS vs Exp(1)
- Carr-Madan strip (the UNAVAILABLE target on the degenerate joint): C = e^{-rT}/pi integral Re[e^{-iuk} psi(u)] du with strip suppressed -> null_strip_unavailable

INCLUDE-PATH + EXECUTE-PARAM (reviewer MAJOR-E — render realism). The partial `reports/_templates/_evidence_branches.qmd` dispatches on `firing_condition = "{{< meta params.firing_condition >}}"` and branches `elif firing_condition == "null_strip_unavailable":`, reading substrate JSON via relative paths. Quarto `{{< include >}}` is a TEXTUAL splice relative to the INCLUDING file. From `reports/ichi.qmd`, the partial is at `reports/_templates/_evidence_branches.qmd` → the correct include is `{{< include _templates/_evidence_branches.qmd >}}` (NOT `../_templates/...`, which resolves to a nonexistent repo-root `/_templates/`). The existing proven render path (`render-null-result-pdf`) ALSO passes `--execute-param firing_condition:$$FIRING`; `report-ichi` MUST pass `--execute-param firing_condition=null_strip_unavailable` for parity (belt-and-suspenders with the YAML param) so `{{< meta params.firing_condition >}}` is populated and the partial selects the null_strip_unavailable branch. The partial's relative JSON reads resolve from the render CWD = `reports/`; CONFIRM each referenced JSON path resolves from `reports/` and add a NON-render path-existence smoke test (pure-python, no quarto) so a wrong path is caught in CI, not on the operator's machine.

OUTPUT COLLISION (reviewer MAJOR-A). The legacy `render-null-result-pdf` (Makefile lines 148-154) writes `reports/ichi.pdf` via `--output ichi.pdf` — the SAME path as the new `report-ichi`. After the deliverable PDF is pinned, anyone running `render-null-result-pdf` overwrites it with the generic null-result shell → stale/wrong-document repro trap. RESOLVE in this plan (it owns the finalized render target): retarget render-null-result-pdf's output to a non-colliding name `reports/_diagnostics/null_result_$$FIRING.pdf` (create reports/_diagnostics/ if needed) so `report-ichi` is the SOLE producer of `reports/ichi.pdf`. The Plan-05-00 `report-ichi` already `rm -f reports/ichi.pdf` before render (stale-PDF guard) — keep it.

Existing quarto-skip pattern (reuse verbatim in the new test) — analysis/tests/test_null_result_template.py lines 59-62:
  if not shutil.which("quarto"): pytest.skip("quarto CLI not available ...")
  if not shutil.which("pdftotext") or not shutil.which("pdfinfo"): pytest.skip("pdftotext/pdfinfo not on PATH ...")

report-ichi target (Plan 05-00 skeleton with rm -f + size gate + quarto hard-prereq + idempotent `quarto install tinytex`) — FINALIZE: add `--execute-param firing_condition=null_strip_unavailable` to the render line, add the spot-check curl-logging step (loop the 5 URLs from seeded_spot_check via verify_url_status), keep quarto a HARD build prerequisite (NOT auto-installed); only `quarto install tinytex` self-heals the LaTeX engine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author reports/ichi.qmd — research-paper near-miss deliverable with typeset equations (skills-driven)</name>
  <specialist>Technical Writer (research-paper / mathematical-writeup specialist) DRIVING latex-econ-model + notation-clean + latex-doc + read-paper. Concise research-paper prose; equations typeset; verdict honest.</specialist>
  <read_first>
    - reports/_templates/null_result.qmd (dual-signature shell: title, \pdfinfo HEDGE05-NULL-RESULT-V1, params.firing_condition, the include idiom) — pattern to reuse, NOT clone
    - reports/_templates/_evidence_branches.qmd (the null_strip_unavailable branch already written; READ its JSON-load cells and confirm their relative paths resolve from reports/) — include this block
    - data/fits/ichi/bdaf5c7ba5a2/fit_report.json + gate_report.json + firing_condition.json + sensitivity_sweep.json — numbers the code cells LOAD (read-only, Pattern 1)
    - .planning/phases/04.1.1-.../_artifacts/DISPOSITION_MEMO_04_1_1_ks_halt.md (the 9 rejected post-hoc changes) — anti-fishing provenance to cite
    - .planning/phases/04.1.1-.../04.1.1-DIAGNOSTIC.md (the three root-cause bugs + kernel-normalization mislabel proof) — methodology-section source
    - data/fits/ichi/ae9e3ba17900/README.md + fit_report.json — the superseded LS-null run (3-run comparison row)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decisions 1,2 — fixed verdict, near-miss narrative) + 05-RESEARCH.md (§Skills Integration, Pitfalls 1/4)
    - ~/.claude/skills/latex-econ-model/SKILL.md + notation-clean/SKILL.md + latex-doc/SKILL.md + read-paper/SKILL.md — the skill drivers
  </read_first>
  <action>
**Invoke the project skills as the mechanism** (CONTEXT mandates): `latex-econ-model` to structure + typeset the model section (Hawkes intensity, branching ratio, LR statistic, KS time-rescaling, Carr-Madan strip); `notation-clean --mode both` on the math (Econometrica-standard; no one-shot Greek; no reserved-symbol collisions on beta/r/t/T/k/kappa); `latex-doc check` on the generated math for compile correctness + standard academic ordering; `read-paper` for the locked citations (Filimonov & Sornette 2014, Brown et al. 2002, Daw & Pender 2017, Kirchner 2015, Bacry power-law for the v2 section).

Author `reports/ichi.qmd` as a NEW document (NOT a renamed null_result.qmd — Pitfall 4). Required structure (academic ordering):
   - **YAML front matter:** title "Iteration 1: Falsifiable DGP Estimation for ICHI cKES/USDT on Celo — A Validated Method and an Honest 3/4"; `format: pdf` with the SAME `\pdfinfo{ /HEDGE05Marker (HEDGE05-NULL-RESULT-V1) }` include-in-header dual-signature pattern from null_result.qmd; `execute: freeze:false cache:false`; `params: firing_condition: null_strip_unavailable`.
   - **Abstract:** concise — the free-tier pipeline works end-to-end and CORRECTED a false null; real self-excitation (eta~0.6, LR rejects NHPP p=0.0, held-out Hawkes +114 nats); the held-out KS gate missed by a knife-edge (leg-0 p=0.0474 vs alpha=0.05); verdict gate_passes=FALSE (3/4), firing_condition=null_strip_unavailable. Frame v1.0 as a validated method + an honest 3/4. NO narrowing language.
   - **§1 Introduction:** the thesis (free-tier dominance; null results are valid deliverables) + iteration scope (ICHI cKES/USDT anchor).
   - **§2 Model** (latex-econ-model): typeset the Hawkes intensity, eta=rho(alpha/beta), the boundary-correct LR with 50:50 chi2(0):chi2(1) null, time-rescaling KS (DeltaLambda~Exp(1)), and the Carr-Madan strip integral as the (unavailable) target.
   - **§3 Data:** 832-row panel -> 778 arrival events (PANEL-04 phantom-transfer filter) on cKES/USDT (USDT, never USDC — CLAUDE.md); the spot-check (§7) provenance anchor.
   - **§4 Methodology — the v1.0 contribution** (CONTEXT decision 2): a Python code cell renders the 3-run comparison table (0afc6af38e24 -> ae9e3ba17900 -> bdaf5c7ba5a2), naming the three root-cause bugs (mislabeled fixture, projection-trick CI, kernel-blind beta=0.1). The method corrected its own false null — the headline scientific contribution.
   - **§5 Results:** code cells load fit_report.json + gate_report.json + firing_condition.json (Pattern 1, read-only). Print eta~0.600 (lower bound), the decay-AIC table, LR observed_stat=561.29 p=0.0, held-out LL reversal (+114 nats), the four-criterion gate table (3/4, KS the lone FALSE), the KS knife-edge with EXPLICIT LABELS: leg-0 **p-value = 0.0474** (statistic D = 0.148), leg-1 **p-value = 0.0564**, the locked min-leg aggregator, gate_passes=FALSE, firing_condition=null_strip_unavailable, the 4/4 convex-dominance conditions. Then `{{< include _templates/_evidence_branches.qmd >}}` (CORRECT relative path — NOT `../_templates/...`) for the null_strip_unavailable evidence block.
   - **§6 Anti-fishing provenance:** cite the disposition memo — the 9 rejected post-hoc changes; state leg-average~0.0519 "would pass" was explicitly NOT taken; the refusal-to-narrow is the credibility asset.
   - **§7 Spot-check:** a code cell calls `abrigo_x402.report.spot_check.seeded_spot_check("bdaf5c7ba5a2", panel_path)` and renders the 5 rows + clickable Blockscout URLs + the recorded seed (re-derivable); note 832->778.
   - **§8 Cost-prior sensitivity:** a code cell loads `sensitivity_sweep.json` and renders the 3x3 qualitative convex-dominance grid (rate_per_event {1,5,10} x USD_per_query {2.5e-6,5e-6,7.5e-6}); state the honest finding (conditions cost-prior-INVARIANT -> convexity-driven, not cost-prior-driven) + the modeled-not-paid caveat. Use the artifact's `all_cells_any_condition_passed` flag and NEVER juxtapose it with gate_passes in a way that reads as a pass.
   - **§9 Convexity-justified / calibration-caveated + what unlocks certification (v2 path):** the convex hedge SHAPE is robust (4/4 conditions -> fat tails dominate linear); the priced Carr-Madan strip is uncalibratable on the degenerate joint_dist (n_min=79<101) -> null_strip_unavailable, NOT "no hedge". Name BOTH v2 routes with the tradeoff: power-law kernel (fixes KS via long-memory, BUT extrapolates the tail at n~700) vs more data (cleaner, slow on free-tier). NAMED, NOT executed (deferred).
   - **§10 Conclusion + reproducibility:** point to reports/MANIFEST.md + `make verify-reproducibility`.

FORBIDDEN (AF-03 / disposition memo): do NOT write "pass with caveat", "near-miss positive", "directionally positive", "exploratory positive", or footnote the KS miss as confirmatory. Do NOT print a bare unlabeled "0.0474" or label the statistic as 0.0474 (it is the p-value; the statistic is D=0.148). Do NOT recompute the fit (numbers loaded read-only). Do NOT collapse USDT to USDC. Do NOT introduce a kappa index. gate_passes=FALSE stays FALSE.
  </action>
  <verify>
    <automated>test -f reports/ichi.qmd && grep -q 'null_strip_unavailable' reports/ichi.qmd && grep -q 'include _templates/_evidence_branches.qmd' reports/ichi.qmd && grep -q 'HEDGE05-NULL-RESULT-V1' reports/ichi.qmd && grep -qE 'p ?= ?0\.0474' reports/ichi.qmd && ! grep -qiE 'pass with caveat|near-miss positive|directionally positive|exploratory positive' reports/ichi.qmd && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f reports/ichi.qmd` (the deliverable source exists)
    - `grep -q 'null_strip_unavailable' reports/ichi.qmd`
    - `grep -qE 'p ?= ?0\.0474' reports/ichi.qmd` (the KS knife-edge value is shown LABELED as a p-value — NOT a bare 0.0474; reviewer MAJOR-F)
    - `grep -q 'include _templates/_evidence_branches.qmd' reports/ichi.qmd` AND `! grep -q '\.\./_templates/_evidence_branches' reports/ichi.qmd` (CORRECT relative include path; the wrong `../_templates/` form absent — reviewer MAJOR-E)
    - `grep -q 'HEDGE05-NULL-RESULT-V1' reports/ichi.qmd` (dual-signature marker preserved)
    - `grep -qiE 'bdaf5c7ba5a2' reports/ichi.qmd && grep -qiE 'ae9e3ba17900' reports/ichi.qmd && grep -qiE '0afc6af38e24' reports/ichi.qmd` (3-run comparison present)
    - `! grep -qiE 'pass with caveat|near-miss positive|directionally positive|exploratory positive' reports/ichi.qmd` (no narrowing language — AF-03)
    - `! grep -qE '\bkappa\b|κ' reports/ichi.qmd` (no kappa index — targets the INDEX, not a stray LaTeX glyph; the locked equation set excludes κ)
  </acceptance_criteria>
  <done>reports/ichi.qmd exists as a research-paper-style document with typeset equations, the honest 3/4 verdict + firing_condition, the labeled KS p=0.0474 (statistic D=0.148), the CORRECT-path _evidence_branches.qmd include, the 3-run methodology comparison, anti-fishing provenance, spot-check + sweep code cells, the v2-path section, and the dual-signature marker; no narrowing language; no kappa index.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize make report-ichi (--execute-param + curl spot-check logging) + retarget render-null-result-pdf + extend the quarto-skip-guarded dual-signature + AF-03 verdict-not-narrowed test + non-render source-grep companion</name>
  <specialist>DevOps Automator (build target) + Technical Writer consult on the AF-03 forbidden-string list. Render-pipeline wiring + the verdict-not-narrowed guard tests (CI source-grep + quarto-machine PDF-text).</specialist>
  <read_first>
    - Makefile (the report-ichi target from Plan 05-00 ~line 155 with rm -f + size gate; the render-null-result-pdf target lines 148-154 with `--output ichi.pdf` COLLISION; the spot-check + verify_url_status interfaces) — being finalized
    - analysis/tests/test_null_result_template.py (lines 56-88 quarto-skip + pdftotext/pdfinfo dual-signature pattern) — being extended
    - analysis/src/abrigo_x402/report/spot_check.py (seeded_spot_check + verify_url_status — Plan 05-02 output) — the curl-logging source
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VALIDATION.md (AF-03-guard row: required strings present + forbidden absent)
    - .planning/phases/04.1.1-.../_artifacts/DISPOSITION_MEMO_04_1_1_ks_halt.md (the exact forbidden relabelings) — forbidden-string source
  </read_first>
  <action>
**2a. Finalize `make report-ichi`** in the Makefile (the Plan-05-00 skeleton). (i) Add `--execute-param firing_condition=null_strip_unavailable` to the `quarto render ichi.qmd` line so `{{< meta params.firing_condition >}}` in the included partial is populated and the null_strip_unavailable branch is selected (parity with render-null-result-pdf; belt-and-suspenders with the YAML param) — reviewer MAJOR-E. (ii) Insert, BEFORE `quarto render`, a spot-check curl-logging step that loops the 5 seeded Blockscout URLs and logs HTTP status per row (network-optional), so the build log carries the per-row verification (satisfies SC-2's "or the build script logs the verification per row" clause). Use the report module:
```makefile
	@cd analysis && uv run python -c "from abrigo_x402.report.spot_check import seeded_spot_check, verify_url_status; from pathlib import Path; r=seeded_spot_check('bdaf5c7ba5a2', Path('../data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet')); [print(verify_url_status(x['url'])) for x in r['rows']]" || echo "report-ichi: spot-check curl logging unavailable (continuing)"
```
The render line becomes: `cd reports && quarto render ichi.qmd --to pdf --execute-param firing_condition=null_strip_unavailable --output ichi.pdf`. Keep quarto a HARD prerequisite (the `command -v quarto || exit 1` guard from 05-00); keep `quarto install tinytex` self-heal; keep the `rm -f reports/ichi.pdf` stale-PDF guard + the size>50KB gate + markdown-fallback rejection. Do NOT make the curl step fail the build on offline.

**2b. Retarget the colliding `render-null-result-pdf`** (reviewer MAJOR-A). Change its `--output ichi.pdf` to write to a non-colliding diagnostics path so `report-ichi` is the SOLE producer of `reports/ichi.pdf`. Edit the recipe to `mkdir -p reports/_diagnostics` then `cd reports && quarto render _templates/null_result.qmd --no-cache --execute-param firing_condition:$$FIRING --output _diagnostics/null_result_$$FIRING.pdf` (and update its usage echo accordingly). Leave the rest of the legacy target intact.

**2c. Extend `analysis/tests/test_null_result_template.py`** — add THREE tests:
   - `def test_ichi_qmd_source_not_narrowed():` — NON-render, runs in CI (NO quarto-skip guard). Read `reports/ichi.qmd` source text and assert: required-present `null_strip_unavailable` AND a labeled p-value `p ?= ?0.0474` (regex) AND the CORRECT include path `include _templates/_evidence_branches.qmd`; required-absent the 4 forbidden strings (`pass with caveat`, `near-miss positive`, `directionally positive`, `exploratory positive`) AND the wrong include `../_templates/_evidence_branches`. This catches narrowing / wrong-path introduced in source WITHOUT quarto (reviewer MINOR — promote the one-shot shell grep to an enforced pytest). Also a NON-render path-existence smoke: assert each JSON path the `_evidence_branches.qmd` partial loads resolves relative to `reports/` (read the partial, extract its load paths, assert they exist) — reviewer MAJOR-E.
   - `def test_ichi_pdf_dual_signature():` — quarto-skip-guarded (verbatim shutil.which pattern lines 59-62); renders `reports/ichi.qmd` via `make report-ichi` (or asserts on an already-rendered `reports/ichi.pdf` if present), then `pdftotext reports/ichi.pdf -` contains the dual signature + `pdfinfo` contains the HEDGE05Marker.
   - `def test_ichi_verdict_not_narrowed():` — quarto-skip-guarded; `pdftotext reports/ichi.pdf -` MUST contain the required strings (`null_strip_unavailable`, the labeled `p=0.0474`, and a FALSE gate indication) and MUST NOT contain any forbidden string. Assert-present + assert-absent.
   Use the EXACT quarto-skip pattern (shutil.which guards) from lines 59-62 for the two render tests — do NOT remove or weaken the guard (Pitfall 3). The source-grep test has NO guard (it runs everywhere).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_null_result_template.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'quarto render ichi.qmd' Makefile` AND `grep -q 'execute-param firing_condition=null_strip_unavailable' Makefile` (render wired to ichi.qmd with the param — reviewer MAJOR-E)
    - `grep -q 'seeded_spot_check' Makefile` (curl spot-check logging step present in report-ichi)
    - `! grep -qE 'render _templates/null_result.qmd.*--output ichi.pdf' Makefile` AND `grep -q '_diagnostics/null_result' Makefile` (render-null-result-pdf retargeted off reports/ichi.pdf — reviewer MAJOR-A)
    - `grep -c 'shutil.which("quarto")' analysis/tests/test_null_result_template.py` ≥ 2 (the two render tests keep the skip guard)
    - `grep -q 'test_ichi_qmd_source_not_narrowed' analysis/tests/test_null_result_template.py && grep -q 'test_ichi_pdf_dual_signature' analysis/tests/test_null_result_template.py && grep -q 'test_ichi_verdict_not_narrowed' analysis/tests/test_null_result_template.py` (all three tests added)
    - `cd analysis && uv run pytest tests/test_null_result_template.py::test_ichi_qmd_source_not_narrowed -x` exits 0 (the NON-render source-grep companion runs GREEN in this quarto-less env — catches narrowing + wrong include path in CI)
    - `cd analysis && uv run pytest tests/test_null_result_template.py -q` exits 0 (the two render tests SKIP cleanly here; the source-grep test PASSES; do NOT fail)
    - `cd analysis && uv run pytest -x -q` full suite green incl. the new quarto-skips (baseline preserved)
  </acceptance_criteria>
  <done>make report-ichi renders ichi.qmd with `--execute-param firing_condition=null_strip_unavailable` + per-row curl spot-check logging + rm -f stale guard + size/markdown gates; render-null-result-pdf is retargeted off reports/ichi.pdf (no collision); the CI source-grep companion + the two quarto-skip-guarded PDF tests are added; the source-grep + path-existence checks run GREEN in CI; full suite green.</done>
</task>

</tasks>

<verification>
- `grep -q 'quarto render ichi.qmd' Makefile && grep -q 'execute-param firing_condition=null_strip_unavailable' Makefile && grep -q 'seeded_spot_check' Makefile && grep -q '_diagnostics/null_result' Makefile`
- reports/ichi.qmd: honest verdict (null_strip_unavailable, labeled KS p=0.0474, statistic D=0.148, gate FALSE), CORRECT include path, 3-run comparison, anti-fishing provenance, sweep + spot-check cells, v2-path; no narrowing language; no kappa index; USDT not USDC
- `cd analysis && uv run pytest tests/test_null_result_template.py -q` green (source-grep test runs; the two render tests skip-guarded)
- On a quarto-equipped machine: `make report-ichi` produces reports/ichi.pdf > 50KB (MANUAL — quarto absent in this env; tracked in 05-VALIDATION Manual-Only)
- Full suite green under thread-pinned BLAS
</verification>

<success_criteria>
REPORT-01 satisfied: reports/ichi.qmd is the research-paper-style Iteration-1 deliverable source with typeset equations and the locked methodology-validation + honest-near-miss narrative (KS leg-0 labeled p=0.0474, statistic D=0.148; CORRECT `_templates/_evidence_branches.qmd` include); make report-ichi renders it to a >50KB PDF via quarto with `--execute-param firing_condition=null_strip_unavailable` (markdown rejected, TinyTeX self-install, rm -f stale guard); render-null-result-pdf is retargeted so it no longer collides on reports/ichi.pdf; render-touching tests stay quarto_skip-guarded (baseline preserved) and a NON-render source-grep + path-existence companion runs in CI; the AF-03 verdict-not-narrowed guard is enforced in source (CI) and hard-asserts on the rendered PDF (quarto machine). Documented SC-1 `.qmd`-satisfies-`.ipynb` deviation carries from CONTEXT.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-03-SUMMARY.md` (record which skills were invoked + the notation-clean diff for the 05-04 checkpoint — reviewer MINOR-2).
</output>
</output>
