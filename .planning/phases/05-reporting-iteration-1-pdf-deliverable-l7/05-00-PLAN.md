---
phase: 05-reporting-iteration-1-pdf-deliverable-l7
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - analysis/tests/test_spot_check.py
  - analysis/tests/test_sensitivity_sweep.py
  - analysis/tests/test_manifest.py
  - Makefile
  - .gitignore
  - data/fits/.gitignore
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04]
specialist:
  name: DevOps Automator
  why: "Wave-0 scaffolding is pure build/test infrastructure — new pytest files, Makefile targets, the nested data/fits/.gitignore allowlist + root data/raw panel negation, and committing the canonical run-dir + panel as repro evidence. No prose, no numerics. DevOps Automator owns build-target + repro-manifest + gitignore wiring per CONTEXT."
must_haves:
  truths:
    - "The three new test files collect under pytest (no import/collection errors) and are RED/skip-marked pending Wave-1 implementation."
    - "`make report-ichi` and `make verify-reproducibility` exist as Makefile targets in .PHONY (bodies may be stubs that downstream plans fill); report-ichi rm -f's its stale PDF and renders to a distinct path from render-null-result-pdf (no output collision)."
    - "Fresh-clone reproducibility is wired on BOTH ends: the nested data/fits/.gitignore allowlists the bdaf5c7ba5a2 artifacts (incl. CORRECTIONS.md + the to-be-created sensitivity_sweep.json) so they are git-tracked, NOT -f-smuggled; AND the panel parquet is re-included via a scoped root-.gitignore negation and committed (the JSONL cache is NOT tracked, so materialize-on-absence is impossible — committing the 128KB panel is the chosen path)."
    - "The pre-Phase-5 baseline suite is preserved (new tests are skip/xfail-marked or trivially green at scaffold time); the baseline count is captured at Wave-0 start, not hardcoded."
  artifacts:
    - path: "analysis/tests/test_spot_check.py"
      provides: "REPORT-02 test scaffold (seeded determinism + URL format + network-optional curl logging)"
      contains: "def test_seeded_draw_deterministic"
    - path: "analysis/tests/test_sensitivity_sweep.py"
      provides: "REPORT-03 test scaffold (9-cell pre-reg grid + per-cell qualitative recompute + no-cost-model grep)"
      contains: "def test_schema_and_grid"
    - path: "analysis/tests/test_manifest.py"
      provides: "REPORT-04 test scaffold (pins present + sha256 match + tamper→exit-1 + 3-state PENDING/FAIL/OK)"
      contains: "def test_pins_present"
    - path: "Makefile"
      provides: "report-ichi + verify-reproducibility targets (parameterized MANIFEST path, canonical parse, PDF rm -f)"
      contains: "verify-reproducibility:"
    - path: "data/fits/.gitignore"
      provides: "allowlist negations re-including the bdaf5c7ba5a2 run-dir artifacts (CORRECTIONS.md + sensitivity_sweep.json + the existing 8)"
      contains: "!ichi/bdaf5c7ba5a2/"
    - path: ".gitignore"
      provides: "scoped negation re-including the panel parquet (data/raw/* parent re-include chain) for fresh-clone reproducibility"
      contains: "67378253_67896653.parquet"
  key_links:
    - from: "data/fits/.gitignore"
      to: "data/fits/ichi/bdaf5c7ba5a2/{CORRECTIONS.md,sensitivity_sweep.json,*.json,*.parquet,*.txt}"
      via: "directory-spanning allowlist negation under the nested * ignore (paths relative to data/fits/)"
      pattern: "!ichi/bdaf5c7ba5a2/"
    - from: ".gitignore"
      to: "data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet"
      via: "scoped re-include chain under data/raw/* (panel committed; JSONL siblings stay ignored)"
      pattern: "67378253_67896653.parquet"
    - from: "Makefile .PHONY"
      to: "report-ichi + verify-reproducibility"
      via: ".PHONY declaration"
      pattern: "report-ichi"
---

<objective>
Scaffold Phase 5's Nyquist test surface, Makefile build targets, and the Option C-hybrid reproducibility-evidence commit so Wave 1 and Wave 2 land against a present, collectible, FRESH-CLONE-REPRODUCIBLE structure.

Purpose: Every downstream Phase-5 task (sweep, spot-check, manifest, writeup) references a test file or Makefile target that does not yet exist. This plan creates them all as RED/skip scaffolds + present-but-stub targets, AND closes the fresh-clone reproducibility gap on BOTH ends of the pipeline that both reviewers flagged as a BLOCKER:
  (a) OUTPUT side — the run dir bdaf5c7ba5a2/ is governed by a NESTED `data/fits/.gitignore` (`*` / `!.gitignore` / `!manifest.json`). Only 8 of the 9 on-disk files are tracked; `CORRECTIONS.md` is UNTRACKED+ignored, and the to-be-created `sensitivity_sweep.json` would ALSO be ignored. A root-.gitignore negation CANNOT re-include a path a deeper .gitignore excludes. FIX: add directory-spanning allowlist negations to the NESTED `data/fits/.gitignore`, then `git add` (plain — once allowed, no `-f`).
  (b) INPUT side — the panel parquet (`data/raw/ichi/.../67378253_67896653.parquet`, 128KB) is ignored by root `.gitignore:43 data/raw/*/` and NOT in HEAD. VERIFIED: the JSONL source cache (`pool_events.jsonl` / `vault_state.jsonl` / `fx_snap.jsonl`) is NOT git-tracked (`git ls-files data/raw/` returns only `manifest.json`), so the RESEARCH-suggested `cli.py materialize`-on-absence regeneration path is IMPOSSIBLE offline. CHOSEN FIX: commit the 128KB panel via a scoped root-.gitignore negation (the JSONL siblings stay ignored).
A fresh clone then has the headline numbers + the panel; verify-reproducibility checksums the committed files rather than re-fitting (scipy MLE is not byte-reproducible across BLAS — Pitfall 2 in RESEARCH).
Output: 3 new pytest files (RED/skip), 2 Makefile targets (`report-ichi`, `verify-reproducibility`) with the canonical MANIFEST parse + parameterized path + PDF rm -f, the NESTED `data/fits/.gitignore` allowlist, and a scoped root `.gitignore` panel negation. CORRECTIONS.md + the panel parquet are `git add`ed (plain add, post-allowlist).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-RESEARCH.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VALIDATION.md
@data/fits/ichi/bdaf5c7ba5a2/fit_report.json
@data/fits/ichi/bdaf5c7ba5a2/firing_condition.json
@data/fits/ichi/bdaf5c7ba5a2/gate_report.json
@data/fits/.gitignore
@Makefile

<interfaces>
Existing quarto-skip pattern (reuse verbatim in any render-touching test) — from analysis/tests/test_null_result_template.py:
```python
import shutil, pytest
if not shutil.which("quarto"):
    pytest.skip("quarto CLI not available -- skipping PDF-render dual-signature test")
if not shutil.which("pdftotext") or not shutil.which("pdfinfo"):
    pytest.skip("pdftotext/pdfinfo not on PATH; cannot verify dual signature")
```

GITIGNORE REALITY (verified live, 2026-05-29):
- `git ls-files data/fits/ichi/bdaf5c7ba5a2/` returns EXACTLY 8 files (firing_condition / fit_report / gate_report / joint_dist / residuals.parquet / run_log.txt / stress_report / strip_degenerate). `CORRECTIONS.md` is on disk (1158 B) but UNTRACKED.
- `git check-ignore -v data/fits/ichi/bdaf5c7ba5a2/CORRECTIONS.md` → `data/fits/.gitignore:8:*` (the NESTED ignore governs the run dir). The 8 tracked files survive only because tracked files override .gitignore.
- `git check-ignore -v data/raw/ichi/0x61Ef.../67378253_67896653.parquet` → `.gitignore:43:data/raw/*/` (panel ignored; NOT in HEAD).
- `git ls-files data/raw/` → only `data/raw/manifest.json`. The JSONL cache (pool_events/vault_state/fx_snap.jsonl) is NOT tracked → materialize-on-absence is NOT possible offline.

NESTED `data/fits/.gitignore` ALLOWLIST (paths relative to data/fits/) — empirically validated (re-includes bdaf5c7ba5a2 artifacts, keeps ae9e3ba17900 / 0afc6af38e24 / 000c1cdce376 ignored):
```gitignore
# Phase 5 REPORT-04 (Option C-hybrid): the canonical Iteration-1 run dir bdaf5c7ba5a2
# is committed as reproducibility evidence (<50KB total). scipy MLE is not
# byte-reproducible across BLAS, so verify-reproducibility checksums these
# committed files rather than re-fitting. Directory-spanning allowlist: re-include
# the dir chain, re-ignore the dir contents, then negate the kept artifacts.
!ichi/
ichi/*
!ichi/bdaf5c7ba5a2/
ichi/bdaf5c7ba5a2/*
!ichi/bdaf5c7ba5a2/*.json
!ichi/bdaf5c7ba5a2/*.parquet
!ichi/bdaf5c7ba5a2/*.md
!ichi/bdaf5c7ba5a2/*.txt
```

ROOT `.gitignore` PANEL NEGATION (append after the data/raw block, lines 42-46) — empirically validated (re-includes only the panel parquet; pool_events/vault_state/fx_snap.jsonl stay ignored):
```gitignore
# Phase 5 REPORT-04: commit the 128KB ICHI cKES/USDT panel parquet as the
# fresh-clone reproducibility INPUT. The JSONL source cache is NOT tracked, so
# cli.py materialize-on-absence is impossible offline; committing the panel is
# the chosen path. Scoped re-include chain (only the parquet; JSONL siblings stay ignored).
!data/raw/ichi/
data/raw/ichi/*
!data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/
data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/*
!data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet
```

CANONICAL MANIFEST PIN FORMAT + PARSE (locked here; 05-02 MUST restate identically). Pin lines are STANDARD `sha256sum` format: `<64-hex><TWO spaces><relative-path>`. The verify loop parses with `awk` (NOT `read`/`sed`, which leave a leading space on a two-space line):
```
grep -E "^[a-f0-9]{64}  " "$MANIFEST" | awk '{print $1, $2}'
```
`awk '{print $1,$2}'` splits on runs of whitespace → `$1`=hash, `$2`=path (no leading space). Paths here are whitespace-free, so $2 is the full path. The loop MUST assert `OK_COUNT == PIN_COUNT` so a silently-skipped pin fails (guards the vacuous-PASS mode).

Makefile .PHONY block (lines 6-8) already lists `verify-reproducibility ... render-lr-diagnostic render-null-result-pdf render-strip-diagnostic`. ADD `report-ichi` to .PHONY. `verify-reproducibility` already exists as a STUB at lines 47-48. The legacy `render-null-result-pdf` (lines 148-154) ALREADY writes `reports/ichi.pdf` via `--output ichi.pdf` — an OUTPUT COLLISION with the new `report-ichi`. RESOLVE: `report-ichi` must `rm -f reports/ichi.pdf` immediately before `quarto render` so a stale/wrong-source PDF (e.g. left by render-null-result-pdf) can never satisfy the `test -f`/size gate. (05-03 owns the deeper resolution; the rm -f here is the Wave-0 guard against the stale-PDF repro trap.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the three Phase-5 test files (RED/skip) + Makefile targets + the nested data/fits/.gitignore allowlist + root panel negation + git add the now-trackable evidence</name>
  <specialist>DevOps Automator — build/test infrastructure scaffolding (test stubs, Makefile targets, nested + root .gitignore allowlists, git add), no prose or numerics.</specialist>
  <read_first>
    - Makefile (lines 1-20 .PHONY/help; lines 47-48 verify-reproducibility STUB; lines 89-97 verify-cache-idempotency sha256 idiom; lines 148-154 render-null-result-pdf quarto precedent + the `--output ichi.pdf` COLLISION) — being modified
    - .gitignore (lines 38-46 data/raw block — append the scoped panel negation AFTER it) — being modified
    - data/fits/.gitignore (the nested `*` / `!.gitignore` / `!manifest.json` allowlist — APPEND the bdaf5c7ba5a2 negations) — being modified
    - analysis/tests/test_null_result_template.py (lines 56-88 quarto-skip + pdftotext/pdfinfo pattern) — pattern source
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements) — exact test names
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decisions 4,5,6 — sweep grid, spot-check seed, Option C-hybrid)
  </read_first>
  <action>
**0. Capture the baseline test count FIRST** (before adding any files): `cd analysis && uv run pytest --collect-only -q 2>/dev/null | tail -1` and record the integer in the SUMMARY (do NOT hardcode "207" — that figure is stale; actual is ~203 function defs). All later "suite stays green" checks compare against THIS captured baseline + the new xfails.

**1a. Create `analysis/tests/test_spot_check.py`** (REPORT-02 scaffold). polars/numpy are deps (no importorskip needed). Define these test stubs — each `@pytest.mark.xfail(reason="Wave 1 (Plan 05-02) implements abrigo_x402.report.spot_check", strict=False)` so the suite stays green at scaffold time:
   - `def test_seeded_draw_deterministic():` — will assert `seeded_spot_check(run_id="bdaf5c7ba5a2")` returns the same 5 row indices across two calls.
   - `def test_blockscout_urls_wellformed():` — will assert each URL matches `^https://celo\.blockscout\.com/tx/0x[0-9a-fA-F]{64}$`.
   - `def test_curl_logging_network_optional():` — will assert the curl-logging helper returns a per-row status string and never raises on offline (status "000" → "unverified (no network)").
   Top of file: `import pytest` + a docstring naming REPORT-02 + run_id bdaf5c7ba5a2 + seed-from-run_id derivation. Resolve any panel path via `Path(__file__).resolve().parents[2] / "data/raw/..."` (CWD-independent — MINOR fix shared with 05-01/05-02).

**1b. Create `analysis/tests/test_sensitivity_sweep.py`** (REPORT-03 scaffold). Same xfail-strict=False pattern, reason `"Wave 1 (Plan 05-01) implements abrigo_x402.report.sensitivity_sweep"`:
   - `def test_schema_and_grid():` — will assert `sensitivity_sweep.json` has 9 cells over grid `rate_per_event ∈ {1,5,10}` × `USD_per_query ∈ {2.5e-6, 5e-6, 7.5e-6}` (the PRE-REG-LOCKED grid — NOTE rate is {1,5,10}, NOT {2.5,5,7.5}).
   - `def test_per_cell_conditions_recomputed():` — will assert each cell carries the 4 qualitative convex-dominance booleans + firing_condition, re-evaluated. (05-01 finalizes the exact field semantics; the Wave-0 stub only asserts the 4 boolean keys + firing_condition are present per cell — do NOT bake in a `recomputed: true` literal here; 05-01 renames it to an honest field.)
   - `def test_no_new_cost_model_introduced():` — NOT xfail; runs NOW and must PASS. Use the CANONICAL forbidden-regex locked here (identical in 05-01): `grep -rIE 'kappa|κ|dominance_delta|def +cost_leg|cost_of_convexity' analysis/src/` must find ZERO hits. Implement via `subprocess.run([...], ...)` asserting returncode == 1 (no match). Run from repo root (resolve via `Path(__file__).resolve().parents[2]`). NOTE: `rate_per_event`/`USD_per_query` are NOT in the forbidden set — they are legitimate pre-reg DATA LABELS, not a cost model (05-01 writes them as GRID_* constants); the guard forbids a cost-MODEL (κ / dominance_delta / cost_leg), not the grid axes.

**1c. Create `analysis/tests/test_manifest.py`** (REPORT-04 scaffold). xfail-strict=False, reason `"Wave 1 (Plan 05-02) authors reports/MANIFEST.md + fills verify-reproducibility"`:
   - `def test_pins_present():` — will assert MANIFEST.md pins: panel parquet sha256, `analysis/uv.lock` SHA, `pnpm-lock.yaml` SHA (NOT package-lock.json, NOT root uv.lock), subgraph block-pins [67378253, 67896653] + chainId 42220, and the bdaf5c7ba5a2 artifact sha256s (incl. CORRECTIONS.md) + the PDF.
   - `def test_verify_repro_exit_codes():` — will assert `make verify-reproducibility MANIFEST=<tmp>` exits 0 on match (against a tmp-copy manifest) and 1 after tampering a pinned sha in the tmp copy. (Operates on a COPY via the parameterized MANIFEST var — NEVER mutates the real reports/MANIFEST.md.)
   - `def test_missing_committed_artifact_fails():` — will assert the 3-state rule: a pinned committed-artifact path that is ABSENT (and is NOT reports/ichi.pdf) makes verify-reproducibility exit 1 (FAIL), while an absent reports/ichi.pdf logs PENDING and does NOT fail. (05-02 finalizes the loop; this stub locks the contract.)
   - `def test_correct_lockfile_names():` — NOT xfail; runs NOW and must PASS: assert files `analysis/uv.lock` and `pnpm-lock.yaml` EXIST and `package-lock.json` and root `uv.lock` do NOT exist (locks CONTEXT decision 6).

**1d. Edit `Makefile`** — add `report-ichi` to the `.PHONY` line. Replace the `verify-reproducibility` STUB body (lines 47-48) with a present recompute-and-match skeleton using the CANONICAL awk parse + parameterized MANIFEST path + the 3-state per-line rule + the OK_COUNT==PIN_COUNT assertion. It must exit 0 when MANIFEST is absent (Wave 0 doesn't break) but contain the real loop. Use this verbatim body:
```makefile
# MANIFEST path is parameterized so tests can run against a tmp copy
# (NEVER mutate the real reports/MANIFEST.md). 3-state per-line rule:
#   absent + is reports/ichi.pdf  -> PENDING (skip, not a failure)
#   absent + any other pinned path -> MISSING -> FAIL (exit 1)
#   present + sha mismatch         -> MISMATCH -> FAIL (exit 1)
#   present + sha match            -> OK
# Final guard: OK_COUNT must equal PIN_COUNT (a silently-skipped pin fails).
MANIFEST ?= reports/MANIFEST.md
verify-reproducibility:
	@bash -c 'set -euo pipefail; \
	  MANIFEST="$(MANIFEST)"; \
	  if [ ! -f "$$MANIFEST" ]; then echo "verify-reproducibility: SKIP — $$MANIFEST absent (Wave 1 authors it)"; exit 0; fi; \
	  FAIL=0; PINS=0; OKS=0; \
	  while read -r expected path; do \
	    [ -z "$$path" ] && continue; \
	    PINS=$$((PINS+1)); \
	    if [ ! -f "$$path" ]; then \
	      if [ "$$path" = "reports/ichi.pdf" ]; then echo "PENDING: $$path (not yet rendered)"; PINS=$$((PINS-1)); continue; \
	      else echo "MISSING (committed artifact absent): $$path"; FAIL=1; continue; fi; \
	    fi; \
	    actual=$$(sha256sum "$$path" | cut -d" " -f1); \
	    if [ "$$actual" != "$$expected" ]; then echo "MISMATCH: $$path ($$actual != $$expected)"; FAIL=1; \
	    else echo "OK: $$path"; OKS=$$((OKS+1)); fi; \
	  done < <(grep -E "^[a-f0-9]{64}  " "$$MANIFEST" | awk "{print \$$1, \$$2}"); \
	  if [ "$$OKS" != "$$PINS" ]; then echo "verify-reproducibility: FAIL — OK_COUNT($$OKS) != PIN_COUNT($$PINS)"; exit 1; fi; \
	  [ "$$FAIL" = 0 ] && echo "verify-reproducibility: PASS ($$OKS/$$PINS pins matched)" || { echo "verify-reproducibility: FAIL"; exit 1; }'
```
   And add a NEW `report-ichi` target (after render-null-result-pdf, ~line 155) — present but finalized by Plan 05-03. NOTE the output-collision guard (`rm -f reports/ichi.pdf` before render — render-null-result-pdf also writes reports/ichi.pdf). Use this verbatim body:
```makefile
# Phase 5 REPORT-01 — render the Iteration-1 deliverable. quarto is an operator
# build prerequisite (NOT auto-installed); only TinyTeX self-installs. Plan 05-03
# finalizes the spot-check curl-logging body. The legacy render-null-result-pdf
# target ALSO writes reports/ichi.pdf, so rm -f the stale PDF first — a stale or
# wrong-source PDF must never satisfy the test -f / size gate (repro-trap guard).
report-ichi:
	@command -v quarto >/dev/null 2>&1 || { echo "report-ichi: FAIL — quarto binary required (build prerequisite, not auto-installed)"; exit 1; }
	@quarto install tinytex 2>/dev/null || true
	@test -f reports/ichi.qmd || { echo "report-ichi: FAIL — reports/ichi.qmd absent (Plan 05-03 authors it)"; exit 1; }
	@rm -f reports/ichi.pdf
	cd reports && quarto render ichi.qmd --to pdf --output ichi.pdf
	@test -f reports/ichi.pdf || { echo "report-ichi: FAIL — no PDF emitted (markdown fallback rejected)"; exit 1; }
	@SIZE=$$(stat -c%s reports/ichi.pdf); [ "$$SIZE" -gt 51200 ] || { echo "report-ichi: FAIL — PDF $${SIZE}B < 50KB (SC-1)"; exit 1; }
	@echo "report-ichi: PASS — reports/ichi.pdf ($${SIZE}B)"
```
(NOTE: `quarto install tinytex` is run unconditionally — it is idempotent per Quarto docs — replacing the brittle `quarto list tools | grep tinytex` detection that could misfire on a "not installed" line. MINOR fix from both reviewers.)

**1e. Edit the NESTED `data/fits/.gitignore`** — APPEND the directory-spanning allowlist negations (paths relative to data/fits/) so the bdaf5c7ba5a2 artifacts (incl. CORRECTIONS.md + the to-be-created sensitivity_sweep.json) are re-included while the other run dirs (ae9e3ba17900 / 0afc6af38e24 / 000c1cdce376) stay ignored. Use the VERBATIM block from the <interfaces> "NESTED data/fits/.gitignore ALLOWLIST" above. DO NOT touch the root .gitignore's data/fits handling (there is none — data/fits is governed only by the nested file). Verify with `git check-ignore -q data/fits/ichi/bdaf5c7ba5a2/CORRECTIONS.md` returning NON-zero (NOT ignored) and `git check-ignore -q data/fits/ichi/ae9e3ba17900/fit_report.json` returning ZERO (still ignored).

**1f. Edit the ROOT `.gitignore`** — APPEND (after the data/raw block at line 46) the scoped panel negation chain so the 128KB panel parquet is re-included while the JSONL siblings stay ignored. Use the VERBATIM block from the <interfaces> "ROOT .gitignore PANEL NEGATION" above. Verify with `git check-ignore -q data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` returning NON-zero (NOT ignored) and `git check-ignore -q data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/pool_events.jsonl` returning ZERO (JSONL still ignored).

**1g. `git add` the now-trackable evidence** (PLAIN add — once the allowlists re-include them, no `-f` is needed; -f would mask a misconfigured ignore — MINOR from DevOps):
   - `git add data/fits/ichi/bdaf5c7ba5a2/CORRECTIONS.md` (now un-ignored by 1e)
   - `git add data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` (now un-ignored by 1f; 128KB)
   - The existing 8 run-dir artifacts are ALREADY tracked — do NOT re-add. `sensitivity_sweep.json` is added by Plan 05-01 (plain `git add`, post-allowlist).
   Confirm: `git ls-files data/fits/ichi/bdaf5c7ba5a2/ | wc -l` is now ≥ 9 (8 + CORRECTIONS.md), and `git ls-files data/raw/ | grep -c 67378253_67896653.parquet` == 1.

Do NOT edit any file under analysis/src/ (this plan introduces no source code). Do NOT recompute the fit. Do NOT author MANIFEST.md or ichi.qmd (downstream plans own those). Do NOT commit the JSONL cache (it stays ignored; only the panel parquet is re-included).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_spot_check.py tests/test_sensitivity_sweep.py tests/test_manifest.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/test_spot_check.py tests/test_sensitivity_sweep.py tests/test_manifest.py --collect-only -q` exits 0 with ≥10 tests collected (3 + 3 + 4)
    - `cd analysis && uv run pytest tests/test_sensitivity_sweep.py::test_no_new_cost_model_introduced tests/test_manifest.py::test_correct_lockfile_names -x` exits 0 (the two non-xfail guards pass NOW)
    - `grep -c "report-ichi" Makefile` ≥ 2 (in .PHONY + as a target); `grep -q 'rm -f reports/ichi.pdf' Makefile` (stale-PDF collision guard present)
    - `grep -c "verify-reproducibility:" Makefile` == 1 and `! grep -q "verify-reproducibility: STUB" Makefile` (stub replaced); `grep -q "MANIFEST ?= reports/MANIFEST.md" Makefile` (parameterized path); `grep -q "awk" Makefile` (canonical parse)
    - `git check-ignore -q data/fits/ichi/bdaf5c7ba5a2/CORRECTIONS.md; test $? -ne 0` (CORRECTIONS.md NOT ignored after allowlist) AND `git check-ignore -q data/fits/ichi/ae9e3ba17900/fit_report.json` (exit 0 — other run dir STILL ignored, no scope creep)
    - `git check-ignore -q data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet; test $? -ne 0` (panel NOT ignored) AND `git check-ignore -q data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/pool_events.jsonl` (exit 0 — JSONL still ignored)
    - `git ls-files data/fits/ichi/bdaf5c7ba5a2/ | wc -l` ≥ 9 (8 + CORRECTIONS.md now staged/tracked); `git ls-files data/raw/ | grep -c 67378253_67896653.parquet` == 1 (panel staged)
    - `cd analysis && uv run pytest -x -q` full suite stays green vs the captured baseline (xfail-strict=False keeps unimplemented stubs from breaking)
  </acceptance_criteria>
  <done>Three RED/skip test files collect cleanly (≥10 tests); the two no-cost-model + lockfile-name guards pass NOW; `report-ichi` (with rm -f stale-PDF guard) + `verify-reproducibility` (parameterized MANIFEST, awk parse, 3-state rule, OK==PIN count) targets present; the nested data/fits/.gitignore allowlists the bdaf5c7ba5a2 artifacts and the root .gitignore re-includes the panel (both empirically NOT-ignored, siblings/other-runs still ignored); CORRECTIONS.md + the panel parquet `git add`ed (plain); full suite green vs captured baseline.</done>
</task>

</tasks>

<verification>
- `cd analysis && uv run pytest tests/test_spot_check.py tests/test_sensitivity_sweep.py tests/test_manifest.py --collect-only` exits 0
- `make verify-reproducibility` exits 0 (SKIP path — MANIFEST not yet authored)
- `make report-ichi` exits non-zero with the explicit "quarto binary required" OR "reports/ichi.qmd absent" message (target present, render not yet possible) — EXPECTED at Wave 0
- `git check-ignore` confirms: panel + CORRECTIONS.md NOT ignored; JSONL cache + other run dirs STILL ignored (no scope creep)
- Full suite: `cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest -x` green vs captured baseline
</verification>

<success_criteria>
Wave 0 scaffold complete: all Wave-1/Wave-2 file references exist (3 test files, 2 Makefile targets, nested + root .gitignore allowlists). Fresh-clone reproducibility is wired on BOTH ends — the panel parquet AND CORRECTIONS.md are now git-tracked via allowlist negations (plain add, no -f), and the verify loop has the canonical parse + 3-state PENDING/FAIL/OK rule + OK==PIN guard + parameterized MANIFEST path. The pre-Phase-5 baseline suite is preserved. No source code, no fit recompute, no MANIFEST/qmd authored. `nyquist_compliant` can be flipped to true once Wave 1 lands the implementations behind the xfail tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-00-SUMMARY.md` (record the captured baseline test count).
</output>
</output>
