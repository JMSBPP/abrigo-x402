---
phase: 05-reporting-iteration-1-pdf-deliverable-l7
plan: 04
type: execute
wave: 3
depends_on: [05-00, 05-01, 05-02, 05-03]
files_modified:
  - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md
  - reports/MANIFEST.md
autonomous: false
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04]
specialist:
  name: DevOps Automator (cycle-closure) + Analytics Reporter (numbers-match consult)
  why: "Two sub-roles per CONTEXT decision 7 + the per-task directive: Analytics Reporter verifies every PDF figure equals the on-disk artifact value (the ROADMAP Phase-5 consult) before sign-off; DevOps Automator runs the cycle-closure git workflow (push origin -> PR upstream) per CLAUDE.md. The terminal human checkpoint gates the merge."
must_haves:
  truths:
    - "An Analytics Reporter numbers-match pass confirms every headline figure in the rendered PDF equals the on-disk bdaf5c7ba5a2 artifact value (eta, LR stat/p, KS leg-0/leg-1 p-values labeled, gate_passes, firing_condition, 4/4 conditions, 3-run table)."
    - "The 05-VERIFICATION-pre.md acceptance grid maps REPORT-01..04 + AF-03 verdict-not-narrowed to commands + exit codes; verification_pass is TRI-STATE (`pass` ONLY after a rendered PDF exists AND the AF-03 PDF-text grep ran GREEN; `pending-render` otherwise) — never `pass` on skipped render tests."
    - "reports/MANIFEST.md PDF pin is finalized (the rendered ichi.pdf sha256 filled in) and make verify-reproducibility exits 0 with the PDF line now strict (no longer PENDING), verified on a CLEAN CHECKOUT."
    - "After PDF render + resolved (non-PENDING) PDF pin + AF-03 grep green + clean-checkout verify-reproducibility green + VERIFICATION pass, the cycle closes: push origin -> open PR to upstream (wvs-finance/abrigo-x402:master) with the honest 3/4 verdict in the PR body (NOT a dressed-up pass); a PDF-less PR is HARD-BLOCKED at the checkpoint; the merge is the user's gated action."
  artifacts:
    - path: ".planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md"
      provides: "the Phase-5 acceptance grid (REPORT-01..04 + AF-03), tri-state verification_pass"
      contains: "REPORT-0"
    - path: "reports/MANIFEST.md"
      provides: "finalized (resolved, non-PENDING) PDF pin"
      contains: "reports/ichi.pdf"
  key_links:
    - from: "05-VERIFICATION-pre.md"
      to: "reports/ichi.pdf + sensitivity_sweep.json + MANIFEST.md"
      via: "acceptance grid commands"
      pattern: "report-ichi|verify-reproducibility"
    - from: "git push origin"
      to: "gh pr create --repo wvs-finance/abrigo-x402"
      via: "cycle-closure workflow (CLAUDE.md), gated on a rendered+grepped PDF"
      pattern: "wvs-finance/abrigo-x402"
---

<objective>
Close Iteration 1: verify the PDF numbers match the artifacts (Analytics Reporter consult), author the Phase-5 acceptance grid with a TRI-STATE verification_pass, finalize the MANIFEST PDF pin to a RESOLVED (non-PENDING) sha256, and — HARD-gated on a rendered >50KB PDF + the AF-03 PDF-text grep running GREEN + clean-checkout verify-reproducibility green + VERIFICATION pass — execute the cycle-closure git workflow (push origin -> PR to upstream) with the honest 3/4 verdict in the PR body.

Purpose: This is the terminal plan. It (a) runs the ROADMAP-mandated numbers-match consult so the PDF cannot silently drift from the artifacts, (b) records the acceptance grid for REPORT-01..04 + the AF-03 verdict-not-narrowed guard with a tri-state pass that refuses to certify a never-rendered PDF, (c) finalizes the reproducibility manifest's PDF pin to a real sha256 (PENDING resolved), and (d) closes the development cycle per CLAUDE.md. CRITICAL (reviewer MAJOR-B / MAJOR-1): a PDF-less PR is forbidden — the checkpoint HARD-BLOCKS until reports/ichi.pdf is rendered, its pin resolved, and the AF-03 grep is GREEN (not skipped). Since quarto is absent in this env, that forces the render+verify onto the operator's quarto machine BEFORE the PR opens. AF-03 carries into the PR: never present the 3/4 as a pass.
Output: 05-VERIFICATION-pre.md (tri-state) + finalized (resolved-pin) MANIFEST + a PR opened to upstream (merge is the user's action).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VALIDATION.md
@data/fits/ichi/bdaf5c7ba5a2/fit_report.json
@data/fits/ichi/bdaf5c7ba5a2/gate_report.json
@data/fits/ichi/bdaf5c7ba5a2/firing_condition.json
@reports/MANIFEST.md
@CLAUDE.md

<interfaces>
Cycle-closure git workflow (CLAUDE.md fork/upstream model):
- All pushes go to origin (JMSBPP/abrigo-x402). Never push directly to upstream.
- PRE-FLIGHT (reviewer MINOR): assert `git remote get-url origin | grep -q 'JMSBPP/abrigo-x402'` and `gh auth status` succeed before push/PR.
- The branch name is PINNED (non-optional): `phase-05-iteration-1-pdf` (so the checkpoint verify query matches — reviewer MINOR).
- Open the PR from the feature branch on origin into upstream master:
    git push origin phase-05-iteration-1-pdf
    gh pr create --repo wvs-finance/abrigo-x402 --base master --head JMSBPP:phase-05-iteration-1-pdf --title "Iteration 1: ICHI cKES/USDT DGP estimation — validated method, honest 3/4 (null_strip_unavailable)" --body "<honest 3/4 body — see Task 2>"

NUMBERS-MATCH SOURCE-OF-TRUTH (Analytics Reporter consult — SINGLE canonical roundings, reviewer MAJOR-2/F; every PDF figure must equal these on-disk values):
- eta = 0.5999724 (report "~0.600, lower bound")
- LR observed_stat = 561.2948 (report "561.29"), p = 0.0
- KS held-out leg-0 **p-value = 0.0474** (full 0.047350…; STATISTIC D = 0.148 — the 0.0474 is the P-VALUE, never the statistic), leg-1 **p-value = 0.0564** (min-leg aggregator) -> ks_held_out_passes = FALSE
- gate_passes = FALSE (3/4)
- firing_condition = null_strip_unavailable
- 4/4 convex-dominance conditions pass
- held-out hawkes = -1206.23, nhpp = -1320.63 (+114 nats)
ALL of {this block, the PR body, the AF-03 grep} align on the LABELED `p=0.0474` for KS leg-0 (NOT `0.04735`, NOT a bare `0.0474`, NOT the statistic). The AF-03 grep asserts `p ?= ?0.0474`.

TRI-STATE verification_pass (reviewer MAJOR-1): frontmatter `verification_pass` is one of `pass` | `pending-render`. It is `pass` ONLY when `reports/ichi.pdf` exists AND `pdftotext reports/ichi.pdf | grep` confirms {null_strip_unavailable, labeled p=0.0474, gate FALSE} present and the 4 forbidden strings absent (i.e. the AF-03 PDF-text guard ran GREEN, not skipped). Otherwise it is `pending-render` and the Task-2 checkpoint REFUSES to open the PR. Never `pass` on skipped render tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics Reporter numbers-match consult + author 05-VERIFICATION-pre.md (tri-state) + finalize MANIFEST PDF pin (resolved)</name>
  <specialist>Analytics Reporter (numbers-match consult — ROADMAP Phase-5 consult) + DevOps Automator (acceptance grid + MANIFEST PDF-pin finalize).</specialist>
  <files>.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md, reports/MANIFEST.md</files>
  <read_first>
    - data/fits/ichi/bdaf5c7ba5a2/fit_report.json + gate_report.json + firing_condition.json — the source-of-truth numbers
    - reports/ichi.qmd (Plan 05-03 output) — the document whose code-cell outputs / literals are checked against the artifacts
    - reports/MANIFEST.md (Plan 05-02 output, with the PDF placeholder) — finalize the PDF pin
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VALIDATION.md (Per-Task Verification Map — the acceptance rows) — grid source
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md — the prior-phase VERIFICATION format precedent
  </read_first>
  <action>
**1a. Numbers-match consult (Analytics Reporter).** Cross-check every headline figure ichi.qmd presents against the on-disk artifacts. Because the .qmd loads numbers via read-only code cells (Pattern 1), confirm: (i) the code cells read from `data/fits/ichi/bdaf5c7ba5a2/` (not hardcoded), (ii) any literal numbers in prose match the artifact values exactly using the SINGLE canonical roundings (eta~0.600, LR 561.29 p=0.0, KS leg-0 labeled **p=0.0474** with statistic D=0.148, leg-1 **p=0.0564**, +114 nats, 3/4) — reject any bare or wrongly-labeled `0.0474` and any `0.04735` divergence, (iii) the 3-run comparison rows match (0afc6af38e24 / ae9e3ba17900 / bdaf5c7ba5a2). Record the cross-check as a table in the VERIFICATION doc. If ANY mismatch is found, FIX ichi.qmd (numbers-correctness/label fix, NOT a verdict change) and re-note. If a quarto-equipped machine is available, render once and `pdftotext` to confirm rendered text; otherwise note render-confirmation is `pending-render` (quarto absent — per 05-VALIDATION Manual-Only).

**1b. Finalize the MANIFEST PDF pin (resolved).** If `reports/ichi.pdf` has been rendered (quarto machine), compute its sha256 via `sha256sum` and REPLACE the placeholder line in `reports/MANIFEST.md` with the real CANONICAL pin line `<sha256>  reports/ichi.pdf` so the verify gate treats it STRICTLY (no longer PENDING). Then `make verify-reproducibility` MUST exit 0 with the PDF line now MATCHED (not PENDING), AND the clean-checkout worktree run (05-02 idiom) MUST exit 0. If quarto is absent in THIS env, leave the documented PENDING line, set `verification_pass: pending-render`, and record that the PDF pin is RESOLVED on the operator's quarto machine before the Task-2 checkpoint can proceed (reviewer MAJOR-B: a PENDING PDF pin is NOT a closeable state — Task 2 hard-blocks on it).

**1c. Author `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md`** with frontmatter `verification_pass: <pass|pending-render>` (TRI-STATE — `pass` ONLY if the rendered PDF exists and the AF-03 PDF-text grep ran GREEN; else `pending-render`) + `quarto_skipped: <true|false>` and an acceptance grid mapping each requirement to a command + exit code + verdict:
   - REPORT-01: `make report-ichi` exit 0 + `test $(stat -c%s reports/ichi.pdf) -gt 51200` (if quarto absent → row marked `pending-render` with the documented `.qmd`-satisfies-`.ipynb` SC-1 deviation note + the size>50KB criterion)
   - REPORT-01b: PDF dual-signature greppable (pdftotext/pdfinfo) — quarto-skip-guarded test row
   - REPORT-02: `pytest tests/test_spot_check.py -x` exit 0 + Blockscout URL format + seed re-derivable + network-optional curl logged
   - REPORT-03: `pytest tests/test_sensitivity_sweep.py -x` exit 0 + 9-cell pre-reg grid {1,5,10} x {2.5e-6,5e-6,7.5e-6} + per-cell honest broadcast (evaluated_once/broadcast_to_grid/depends_on_cost_priors:false, no `recomputed` literal) + the canonical no-cost-model grep guard
   - REPORT-04: `pytest tests/test_manifest.py -x` exit 0 + `make verify-reproducibility` exit 0 (incl. the clean-checkout worktree run) + correct lockfiles (analysis/uv.lock + pnpm-lock.yaml; package-lock.json ABSENT) + Option C-hybrid committed artifacts (panel + CORRECTIONS.md + sensitivity_sweep.json all tracked) + the 3-state PENDING/FAIL/OK rule
   - AF-03 verdict-not-narrowed: the CI `test_ichi_qmd_source_not_narrowed` (source-grep, runs here) + the quarto-skip-guarded `test_ichi_verdict_not_narrowed` (PDF-text, on a quarto machine) assert required strings present (null_strip_unavailable, labeled p=0.0474, gate FALSE) + forbidden absent; record the numbers-match table from 1a
   - Regression: full suite green incl. the quarto-skips
   Add a "Numbers-Match Consult" H2 with the cross-check table. Mark render/PDF-size rows `pending-render` where this env lacks quarto, and set the top-level `verification_pass` accordingly.

DO NOT recompute the fit. DO NOT narrow the verdict. DO NOT set `verification_pass: pass` while the PDF is unrendered/the AF-03 PDF grep is skipped (reviewer MAJOR-1). DO NOT push or open the PR in this task (Task 2 owns cycle-closure, hard-gated on this task's `pass`).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_spot_check.py tests/test_sensitivity_sweep.py tests/test_manifest.py tests/test_null_result_template.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/test_spot_check.py tests/test_sensitivity_sweep.py tests/test_manifest.py tests/test_null_result_template.py -q` exits 0 (all green; the render rows SKIP, the source-grep AF-03 companion PASSES)
    - `make verify-reproducibility` exits 0; on a quarto machine the clean-checkout worktree run also exits 0 with the PDF line MATCHED
    - `test -f .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md && grep -qE 'verification_pass: (pass|pending-render)' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md` (tri-state present)
    - `grep -cE 'REPORT-0[1-4]' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md` >= 4 (all four requirements in the grid)
    - `grep -qi 'Numbers-Match' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md` (the consult table present)
    - `grep -q 'null_strip_unavailable' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md && grep -qE 'p ?= ?0\.0474' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md && ! grep -qiE 'pass with caveat|near-miss positive' .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md` (honest verdict; labeled p-value; no narrowing)
    - `grep -q 'reports/ichi.pdf' reports/MANIFEST.md` (PDF pin line present — RESOLVED sha on a quarto machine, or documented PENDING with verification_pass: pending-render)
  </acceptance_criteria>
  <done>Numbers-match consult complete (PDF figures == artifact values on the single canonical roundings, labeled p=0.0474, any mismatch fixed); 05-VERIFICATION-pre.md acceptance grid (REPORT-01..04 + AF-03) authored with TRI-STATE verification_pass (`pass` only if PDF rendered + AF-03 grep green, else `pending-render`); MANIFEST PDF pin resolved (quarto machine) or documented-PENDING (pending-render); verify-reproducibility exits 0 incl. clean-checkout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cycle closure — push origin then open PR to upstream (HARD-gated on a rendered+grepped PDF + resolved pin + clean-checkout verify green)</name>
  <specialist>DevOps Automator — the cycle-closure git workflow (push origin -> PR upstream) per CLAUDE.md; the merge is the user's gated action.</specialist>
  <files>(git refs only — no working-tree file edits; branch on origin + PR on upstream)</files>
  <action>
HARD GATE (reviewer MAJOR-B / MAJOR-1 — a PDF-less PR is forbidden): do NOT proceed unless ALL hold: (i) `reports/ichi.pdf` EXISTS and is >50KB; (ii) the MANIFEST PDF pin is RESOLVED (real sha256, NOT PENDING) and `make verify-reproducibility` exits 0 with the PDF line STRICT, including the clean-checkout worktree run; (iii) the AF-03 PDF-text guard ran GREEN — `pdftotext reports/ichi.pdf - | grep` confirms {null_strip_unavailable, labeled p=0.0474, gate FALSE} present and the 4 forbidden strings absent (NOT skipped); (iv) Task-1 `verification_pass: pass` (NOT `pending-render`). If quarto is absent in the execution env, HALT at this checkpoint with "render reports/ichi.pdf on a quarto machine, resolve the MANIFEST PDF pin, run the AF-03 PDF grep + clean-checkout verify-reproducibility, set verification_pass: pass, then resume" — do NOT open a PDF-less PR.

After the hard gate passes, execute the cycle-closure git workflow per CLAUDE.md §"Git workflow — fork/upstream model": PRE-FLIGHT assert `git remote get-url origin | grep -q 'JMSBPP/abrigo-x402'` and `gh auth status` succeed. Then: (1) if on master, create the PINNED feature branch `phase-05-iteration-1-pdf`; (2) commit the EXACT Phase-5 pathspec (reviewer MINOR — NOT a blanket "all"; explicitly EXCLUDE the unrelated 04.1.1 working-tree changes): `reports/ichi.qmd reports/ichi.pdf reports/MANIFEST.md analysis/src/abrigo_x402/report/ analysis/tests/test_spot_check.py analysis/tests/test_sensitivity_sweep.py analysis/tests/test_manifest.py analysis/tests/test_null_result_template.py Makefile .gitignore data/fits/.gitignore data/fits/ichi/bdaf5c7ba5a2/sensitivity_sweep.json data/fits/ichi/bdaf5c7ba5a2/CORRECTIONS.md data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/` — run `git status --short` first to review and confirm no 04.1.1 file is swept in; end the commit message with the `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` trailer; (3) `git push origin phase-05-iteration-1-pdf` (NEVER push upstream directly); (4) `gh pr create --repo wvs-finance/abrigo-x402 --base master --head JMSBPP:phase-05-iteration-1-pdf --title "Iteration 1: ICHI cKES/USDT DGP estimation — validated method, honest 3/4 (null_strip_unavailable)" --body "<honest 3/4 body>"` where the body states gate_passes=FALSE; eta~0.600 (lower bound); LR rejects NHPP (observed_stat=561.29, p=0.0); held-out Hawkes +114 nats; KS held-out leg-0 **p=0.0474** (knife-edge miss, statistic D=0.148, min-leg aggregator) -> ks_held_out_passes=FALSE; 4/4 convex-dominance conditions pass -> convexity-justified but calibration-caveated (null_strip_unavailable on the degenerate joint_dist n_min=79<101); the methodology corrected a false null (3-run comparison); the v2 path is named (power-law vs more data); end the body with the `🤖 Generated with [Claude Code](https://claude.com/claude-code)` trailer. The PR body is NOT a dressed-up pass (AF-03). Then PAUSE for the user — the merge is the user's gated action. Do NOT merge; do NOT push upstream.
  </action>
  <verify>
    <automated>gh pr list --repo wvs-finance/abrigo-x402 --head phase-05-iteration-1-pdf --json url,baseRefName,headRefName --jq '.[0] | select(.baseRefName=="master" and .headRefName=="phase-05-iteration-1-pdf") | .url' | grep -q https</automated>
  </verify>
  <done>PR opened to wvs-finance/abrigo-x402:master from JMSBPP:phase-05-iteration-1-pdf with the honest 3/4 body (gate_passes=FALSE + null_strip_unavailable + KS labeled p=0.0474, no narrowing strings); the hard gate confirmed a rendered >50KB PDF + resolved PDF pin + AF-03 PDF grep GREEN + clean-checkout verify-reproducibility green + verification_pass:pass BEFORE the PR; push went to origin only; the exact Phase-5 pathspec (incl. the panel + CORRECTIONS.md + sensitivity_sweep.json + reports/ichi.qmd + reports/ichi.pdf + MANIFEST.md) is in the PR diff and no 04.1.1 file was swept in; merge is the user's gated action (checkpoint pauses).</done>
  <read_first>
    - CLAUDE.md (§"Git workflow — fork/upstream model" — push origin, PR to upstream, never push upstream directly)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-VERIFICATION-pre.md (Task-1 output — must be verification_pass:pass, NOT pending-render)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-CONTEXT.md (decision 7 — cycle closure gated on render+repro+VERIFICATION green; AF-03 carries into the merge)
  </read_first>
  <what-built>
The Iteration-1 deliverable is complete: reports/ichi.qmd renders to a >50KB PDF (on a quarto machine), the 5-row spot-check + 9-cell sensitivity sweep + MANIFEST + verify-reproducibility (incl. clean-checkout) are green, and 05-VERIFICATION-pre.md records the honest 3/4 verdict (gate_passes=FALSE, firing_condition=null_strip_unavailable, KS leg-0 labeled p=0.0474) with verification_pass:pass. The automation HARD-BLOCKS a PDF-less PR: it pre-stages the cycle-closure only after the PDF is rendered, its pin resolved (non-PENDING), and the AF-03 PDF grep is GREEN.

Automation steps the executor performs BEFORE pausing (all on origin, never upstream):
  1. Confirm the HARD gate: reports/ichi.pdf exists >50KB; the MANIFEST PDF pin is RESOLVED (real sha256, not PENDING); `make verify-reproducibility` exits 0 with the PDF line strict INCLUDING the clean-checkout worktree run; the AF-03 PDF-text grep ran GREEN (not skipped); 05-VERIFICATION-pre.md is verification_pass:pass. If any fails (e.g. quarto absent here), HALT with the render-on-a-quarto-machine instruction — do NOT open a PDF-less PR.
  2. PRE-FLIGHT: `git remote get-url origin | grep -q 'JMSBPP/abrigo-x402'` and `gh auth status` succeed.
  3. If on master, create the PINNED feature branch `phase-05-iteration-1-pdf`. Commit the EXACT Phase-5 pathspec listed in <action> (review `git status --short` first; exclude unrelated 04.1.1 changes). End the commit message with the Co-Authored-By trailer.
  4. `git push origin phase-05-iteration-1-pdf`.
  5. `gh pr create --repo wvs-finance/abrigo-x402 --base master --head JMSBPP:phase-05-iteration-1-pdf --title "Iteration 1: ICHI cKES/USDT DGP estimation — validated method, honest 3/4 (null_strip_unavailable)" --body "..."` where the body states the HONEST 3/4: gate_passes=FALSE; eta~0.600 (lower bound); LR rejects NHPP (observed_stat=561.29, p=0.0); held-out Hawkes +114 nats; KS held-out leg-0 p=0.0474 (knife-edge miss, statistic D=0.148, min-leg aggregator) -> ks_held_out_passes=FALSE; 4/4 convex-dominance conditions pass -> convexity-justified but calibration-caveated (null_strip_unavailable on the degenerate joint_dist n_min=79<101); the methodology corrected a false null (3-run comparison); the v2 path is named (power-law vs more data). The PR body is NOT a dressed-up pass (AF-03). End the body with the Generated-with trailer.
  </what-built>
  <how-to-verify>
  1. Confirm the PR exists and targets `wvs-finance/abrigo-x402:master` from `JMSBPP:phase-05-iteration-1-pdf`: `gh pr list --repo wvs-finance/abrigo-x402 --head phase-05-iteration-1-pdf`.
  2. Read the PR body and confirm it states gate_passes=FALSE + null_strip_unavailable + KS labeled p=0.0474 (statistic D=0.148) and contains NONE of {"pass with caveat", "near-miss positive", "directionally positive", "exploratory positive"}.
  3. Confirm the push went to origin only (`git remote -v`; `git log origin/phase-05-iteration-1-pdf` exists; upstream was NOT pushed to directly).
  4. Confirm the rendered reports/ichi.pdf + the committed run-dir artifacts (bdaf5c7ba5a2 incl. CORRECTIONS.md + sensitivity_sweep.json) + the panel parquet + reports/ichi.qmd + MANIFEST.md (with the RESOLVED PDF pin) are in the PR diff, and no unrelated 04.1.1 file was swept in.
  5. MERGE is YOUR action — the executor does NOT merge. Review the PR, and if the honest 3/4 framing is correct, merge upstream yourself (or request changes).
  </how-to-verify>
  <resume-signal>Type "merged" once you have merged the PR upstream, or describe any required changes to the PR body / framing.</resume-signal>
</task>

</tasks>

<verification>
- 05-VERIFICATION-pre.md verification_pass tri-state (pass only after rendered+grepped PDF; else pending-render) with the REPORT-01..04 + AF-03 grid + the numbers-match consult table; honest verdict, labeled p=0.0474, no narrowing
- `make verify-reproducibility` exits 0 incl. clean-checkout; MANIFEST PDF pin RESOLVED (non-PENDING) before the PR
- Full suite green incl. the quarto-skips + the CI AF-03 source-grep companion
- PR opened to wvs-finance/abrigo-x402:master from JMSBPP:phase-05-iteration-1-pdf with an honest 3/4 body; HARD-blocked if PDF-less; push to origin only; merge is the user's gated action
</verification>

<success_criteria>
Iteration 1 closed: every PDF figure verified == the on-disk artifact value on the single canonical roundings (KS leg-0 labeled p=0.0474, statistic D=0.148); the Phase-5 acceptance grid records REPORT-01..04 + the AF-03 verdict-not-narrowed guard with a TRI-STATE verification_pass that refuses to certify an unrendered PDF; the reproducibility manifest's PDF pin is RESOLVED (non-PENDING) and verify-reproducibility passes on a clean checkout; and the cycle-closure PR is opened to upstream with the honest 3/4 verdict (gate_passes=FALSE, null_strip_unavailable) — never a dressed-up pass, never a PDF-less PR — with the merge gated to the user.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/05-04-SUMMARY.md`
</output>
</output>
