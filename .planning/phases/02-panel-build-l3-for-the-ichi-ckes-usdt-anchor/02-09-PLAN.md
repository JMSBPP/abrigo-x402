---
phase: 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor
plan: 09
type: execute
wave: 3
depends_on: [02-08]
files_modified:
  - .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md
  - .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-09-SUMMARY.md
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, DEMAND-01]
must_haves:
  truths:
    - "Full Python suite green: cd analysis && uv run pytest exits 0"
    - "Full TS suite green: pnpm -C fetch test --run exits 0"
    - "make lint-artifacts exits 0 (pre-panel-build skip OR post-panel-build pass)"
    - "make schema-frozen-check + make schema-probe both exit 0"
    - "make leak-check exits 0 (Phase 1 invariant carried)"
    - "DEMAND-01 enforce: grep -c 'graph-mainnet' data/raw/_cost_ledger.jsonl returns 0 for Phase 2 commits"
    - "02-VERIFICATION-pre.md exists with frontmatter verification_pass: true and full acceptance grid mapping PANEL-01..04 + DEMAND-01 + ROADMAP SCs"
    - "tsc --noEmit clean across fetch/"
  artifacts:
    - path: ".planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md"
      provides: "Phase 2 substantive acceptance artifact (frontmatter verification_pass: true)"
      contains: "verification_pass: true"
    - path: ".planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-09-SUMMARY.md"
      provides: "Thin pointer to 02-VERIFICATION-pre.md for orchestrator phase-globbing"
  key_links:
    - from: "02-VERIFICATION-pre.md acceptance grid"
      to: "Each requirement ID (PANEL-01..04 + DEMAND-01)"
      via: "Per-requirement command + exit-code + verdict row"
      pattern: "PANEL-0[1234]|DEMAND-01"
---

<objective>
Phase 2 acceptance gate. Runs the full test suite (Python + TS), all Makefile gates (`make lint-artifacts`, `make schema-frozen-check`, `make schema-probe`, `make leak-check`), and the DEMAND-01 enforce check (`grep` on cost-ledger). Produces `.planning/phases/02-…/02-VERIFICATION-pre.md` — the substantive Phase 2 acceptance artifact — and `02-09-SUMMARY.md` thin pointer.

Purpose: This is the close-out artifact `/gsd:verify-work` consumes to confirm Phase 2 is complete. Maps every requirement ID to a runnable command + observable exit code + verdict.
Output: Two markdown files; full acceptance grid; library-drift inventory carried; deferred-items rolled up.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-CONTEXT.md
@.planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-RESEARCH.md
@.planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VALIDATION.md
@.planning/ROADMAP.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-PLAN.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 2 acceptance grid + author 02-VERIFICATION-pre.md + 02-09-SUMMARY.md</name>
  <files>
    .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md,
    .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-09-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-PLAN.md (Phase 1 acceptance gate format precedent)
    - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md (template for the substantive artifact)
    - .planning/ROADMAP.md (Phase 2 §Phase Details — SC-1..SC-4 verbatim)
    - All Phase 2 SUMMARYs from prior plans (02-00 through 02-08)
  </read_first>
  <action>
**Step 1 — execute the full acceptance grid (capture exit codes):**

Run each command sequentially; capture stdout/stderr; record exit code; build a per-row verdict.

```bash
# Tier 1: Build/install/typecheck
pnpm install --frozen-lockfile                                   # expect exit 0
pnpm -C fetch exec tsc --noEmit                                  # expect exit 0
cd analysis && uv sync                                           # expect exit 0

# Tier 2: Test suites
cd analysis && uv run pytest -q                                  # full Python suite — expect exit 0
pnpm -C fetch test --run                                         # full TS suite — expect exit 0

# Tier 3: Makefile gates
make schema-frozen-check                                         # expect exit 0 (baseline e9b214d intact)
make schema-probe                                                # expect exit 0 (PROBE_PASS)
make leak-check                                                  # expect exit 0 (Phase 1 invariant)
make lint-artifacts                                              # expect exit 0 (skip OR pass)

# Tier 4: DEMAND-01 enforce
if [ -f data/raw/_cost_ledger.jsonl ]; then
  count=$(grep -c "graph-mainnet" data/raw/_cost_ledger.jsonl || echo 0)
  test "$count" -eq 0  # expect exit 0
else
  echo "No cost ledger yet — DEMAND-01 vacuously satisfied"
fi

# Tier 5: Per-requirement smoke
cd analysis && uv run pytest tests/test_ingest.py tests/test_decoders.py tests/test_phantom_filter.py tests/test_vault_state.py tests/test_revenue_leg.py tests/test_fx_snap.py tests/test_provenance.py tests/test_panel_e2e.py -x  # expect exit 0
```

If any command fails, STOP and surface to user via orchestrator. Do NOT author a `verification_pass: true` artifact on failure.

**Step 2 — author `02-VERIFICATION-pre.md`** (template from `01-VERIFICATION-pre.md`):

```markdown
---
phase: 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor
verification_pass: true
verified_at: <ISO-8601 timestamp>
verifier: plan-02-09-acceptance-gate
---

# Phase 2 — Panel Build (L3) for ICHI cKES/USDT Anchor — Pre-Verification

**Phase goal:** Materialize the event-level Parquet panel for ICHI on cKES/USDT
with full on-chain provenance, Mento broker mid-rate FX snap, and the
phantom-transfer filter for USDC/USDT fee-abstraction adapters.

## Acceptance Grid

| Requirement / SC | Command | Expected | Actual | Verdict |
|------------------|---------|----------|--------|---------|
| PANEL-01 (ingest) | `cd analysis && uv run pytest tests/test_ingest.py -x` | exit 0; ≥9 tests pass | <actual> | <PASS/FAIL> |
| PANEL-01 (decoders) | `cd analysis && uv run pytest tests/test_decoders.py -x` | exit 0; ≥6 tests pass | <actual> | <PASS/FAIL> |
| PANEL-01 (vault state) | `cd analysis && uv run pytest tests/test_vault_state.py -x && pnpm -C fetch test fetch/tests/vault-state-snap.test.ts --run` | both exit 0 | <actual> | <PASS/FAIL> |
| PANEL-01 (Q96 LP-fee) | `cd analysis && uv run pytest tests/test_revenue_leg.py -x` | exit 0; ≥6 tests pass; worked example matches | <actual> | <PASS/FAIL> |
| PANEL-02 (provenance) | `cd analysis && uv run pytest tests/test_provenance.py -x && make lint-artifacts` | exit 0 | <actual> | <PASS/FAIL> |
| PANEL-03 (FX snap) | `cd analysis && uv run pytest tests/test_fx_snap.py -x && pnpm -C fetch test fetch/tests/mento-historical-rate.test.ts --run && test -f notes/fx_snap_decision.md` | exit 0 | <actual> | <PASS/FAIL> |
| PANEL-04 (phantom filter) | `cd analysis && uv run pytest tests/test_phantom_filter.py -x` | exit 0; ≥8 tests pass | <actual> | <PASS/FAIL> |
| DEMAND-01 (enforce) | `[ -f data/raw/_cost_ledger.jsonl ] && grep -c "graph-mainnet" data/raw/_cost_ledger.jsonl \|\| echo 0` | 0 | <actual> | <PASS/FAIL> |
| End-to-end | `cd analysis && uv run pytest tests/test_panel_e2e.py -x` | exit 0; 97-row panel from 100-block synthetic | <actual> | <PASS/FAIL> |
| Schema frozen | `make schema-frozen-check` | exit 0 (baseline e9b214d intact) | <actual> | <PASS/FAIL> |
| Schema probe | `make schema-probe` | exit 0 (PROBE_PASS) | <actual> | <PASS/FAIL> |
| Leak check | `make leak-check` | exit 0 | <actual> | <PASS/FAIL> |
| tsc clean | `pnpm -C fetch exec tsc --noEmit` | exit 0 | <actual> | <PASS/FAIL> |
| ROADMAP SC-1 (panel exists, zero null blockNumber) | `cd analysis && uv run python -c "import polars as pl; df = pl.read_parquet('data/raw/ichi/<pool>/<range>.parquet'); assert df.null_count().sum_horizontal()[0] == 0"` OR synthetic-only | <actual> | <PASS/FAIL/N/A> |
| ROADMAP SC-2 (PANEL-02 header on every output) | `make lint-artifacts` against any produced panel | exit 0 | <actual> | <PASS/FAIL> |
| ROADMAP SC-3 (FX snap with provenance) | `test -f notes/fx_snap_decision.md && grep -q "Mento broker" notes/fx_snap_decision.md` | exit 0 | <actual> | <PASS/FAIL> |
| ROADMAP SC-4 (phantom filter unit-tested against real fixture) | `cd analysis && uv run pytest tests/test_phantom_filter.py::test_real_fixture_roundtrip_when_captured -x` | exit 0 OR test skipped (no-adapter-traffic-found documented) | <actual> | <PASS/FAIL/SKIP> |

## Summary

- **Total tests** (Python + TS): <N> passing, 0 failing, <M> skipped
- **Wave structure executed:** Wave 0 (02-00) → Wave 1 (02-01..02-07, parallel) → Wave 2 (02-08) → Wave 3 (02-09)
- **Phase deliverables:**
  - `analysis/src/abrigo_x402/` populated with 9 modules
  - `fetch/src/mento/historical-rate.ts` + `fetch/src/vault/state-snap.ts` TS sidecars
  - `notes/fx_snap_decision.md` PANEL-03 decision artifact
  - `protocols/ichi.toml` extended with `[panel] finality_lag_blocks = 120`
  - 3 real/captured fixtures: ICHI vault ABI + Mento exchangeId + USDT phantom-transfer tx

## DEMAND-01 Enforcement

Phase 2 cost-ledger row inventory (from `data/raw/_cost_ledger.jsonl` Phase 2 window):
- `endpoint='forno'` rows: <count> (uncapped per DEMAND-01)
- `endpoint='blockscout'` rows: <count> (uncapped per DEMAND-01)
- `endpoint='graph-mainnet'` rows: **0** (PASS — phase ran without consuming Graph budget)

## Library-Drift Inventory (carried)

From Phase 1 Plan 01-07 → 01-08 carried forward (no Phase 2 drift detected):
- v1 x402 protocol (named networks + X-PAYMENT header) — still applies to any deferred x402 work
- polars 1.41 native Parquet metadata API confirmed runtime-stable in this workspace's uv venv
- Mento SDK 3.2.8 QuoteService confirmed lacking blockNumber parameter — Phase 2 bypassed via raw viem.readContract

## Deferred Items

- Broader phantom-transfer heuristic (Transfer-without-paired-Swap-in-same-tx) — Phase 7 if false negatives surface
- Subgraph-derived LP-fees — Phase 1.5 enrichment if/when a verified Uniswap V3 Celo subgraph is provisioned with GRAPH_API_KEY
- Per-vault aggregate panel (multi-ICHI vault) — v2 per Q-4 lock
- collectFees cross-check accuracy validation — Plan 02-09 OR Phase 7 if a captured collectFees event is feasible

## Outstanding Gaps

(none — Phase 2 closed at 10/10 plans if no gaps surfaced; otherwise list here)

## Phase Goal-Backward Checks

- [x] PANEL-01: event-level Parquet panel with provenance — `data/raw/ichi/panels/<range>.parquet` (or synthetic-only at this stage)
- [x] PANEL-02: 6-key metadata header — `make lint-artifacts` enforces
- [x] PANEL-03: Mento broker mid-rate FX snap + USDT/USD separate column — `notes/fx_snap_decision.md` + fx_snap.py
- [x] PANEL-04: phantom-transfer filter — `tests/test_phantom_filter.py` covers synthetic + real fixture
- [x] DEMAND-01 (enforce): zero graph-mainnet ledger rows from Phase 2

## I11-Style Regex Acceptance

`grep -cE "PANEL-0[1234]|DEMAND-01" 02-VERIFICATION-pre.md` returns ≥10 hits (well above minimum threshold).
```

**Step 3 — author `02-09-SUMMARY.md`** (thin pointer per checker M15):

```markdown
---
phase: 02-panel-build-l3-for-the-ichi-ckes-usdt-anchor
plan: 09
status: complete
---

# Plan 02-09 — Phase 2 Acceptance Gate — SUMMARY

The substantive Phase 2 acceptance artifact lives at
**`.planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md`**
(frontmatter `verification_pass: true`).

See `02-VERIFICATION-pre.md` for the full acceptance grid mapping every requirement
(PANEL-01..04, DEMAND-01 enforce) + ROADMAP SC-1..SC-4 to commands + exit codes
+ verdicts. This SUMMARY is intentionally thin so orchestrator phase-globbing
(`*-SUMMARY.md`) finds it.

**Commit:** <gitCommit short>
**Verified at:** <ISO-8601>
**Phase status:** Complete (10/10 plans landed)
```

**Step 4 — substitute the `<actual>` and `<count>` placeholders** in `02-VERIFICATION-pre.md` with the real command output captured during Step 1. Substitute timestamps, commit hash, counts.

**Step 5 — commit guard:** Before staging `02-VERIFICATION-pre.md`, ensure `02-CONTEXT.md` and `02-RESEARCH.md` already exist (Phase 2 prereq). The pre-commit review-trail hook will only fire on `.planning/**/PLAN.md` paths, so the VERIFICATION-pre.md file is unaffected. The orchestrator (not this plan) handles plan commits.

**Note:** Plan 02-09 makes NO source-code changes. Its only outputs are the two markdown files. The acceptance grid is run live and the results captured at write time.
  </action>
  <verify>
    <automated>cd analysis && uv run pytest -q && pnpm -C fetch test --run && pnpm -C fetch exec tsc --noEmit && make schema-frozen-check && make schema-probe && make leak-check && make lint-artifacts && test -f .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md && grep -q "verification_pass: true" .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md && test "$(grep -cE 'PANEL-0[1234]|DEMAND-01' .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md)" -ge 10 && test "$(grep -c '<actual>' .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md)" -eq 0 && test "$(grep -c '<count>' .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Full Python suite green: `cd analysis && uv run pytest -q` exits 0
    - Full TS suite green: `pnpm -C fetch test --run` exits 0
    - `pnpm -C fetch exec tsc --noEmit` exits 0
    - `make schema-frozen-check && make schema-probe && make leak-check && make lint-artifacts` all exit 0
    - `test -f .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` exits 0
    - `grep -q "verification_pass: true" .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` exits 0
    - `grep -cE "PANEL-0[1234]|DEMAND-01" 02-VERIFICATION-pre.md` returns ≥10
    - `test -f .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-09-SUMMARY.md` exits 0
    - DEMAND-01: `[ -f data/raw/_cost_ledger.jsonl ] && grep -c "graph-mainnet" data/raw/_cost_ledger.jsonl || echo 0` returns 0
    - No unsubstituted `<actual>` placeholders remain in the substantive verification artifact: `grep -c "<actual>" .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` returns 0
    - No unsubstituted `<count>` placeholders remain: `grep -c "<count>" .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` returns 0
    - No unsubstituted `<N>`/`<M>` template placeholders remain in the summary section: `grep -cE "<N>|<M>" .planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` returns 0
  </acceptance_criteria>
  <done>
    Phase 2 acceptance grid runs green; 02-VERIFICATION-pre.md authored with frontmatter `verification_pass: true`; thin SUMMARY pointer for orchestrator phase-globbing; Phase 2 closes at 10/10 plans.
  </done>
</task>

</tasks>

<verification>
- `cd analysis && uv run pytest -q` exits 0
- `pnpm -C fetch test --run` exits 0
- All Makefile gates green
- 02-VERIFICATION-pre.md exists with verification_pass: true
- DEMAND-01: zero graph-mainnet rows
</verification>

<success_criteria>
- Full Phase 2 acceptance grid PASS
- Substantive 02-VERIFICATION-pre.md committed
- Thin 02-09-SUMMARY.md pointer for orchestrator
- /gsd:verify-work 02 can consume the artifact directly
</success_criteria>

<output>
Create `.planning/phases/02-panel-build-l3-for-the-ichi-ckes-usdt-anchor/02-VERIFICATION-pre.md` (substantive) AND `02-09-SUMMARY.md` (thin pointer).
</output>
