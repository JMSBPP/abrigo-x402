---
phase: 04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy
plan: "04"
subsystem: hedge
tags: [hawkes, carr-madan, firing-condition, hedge-orchestrator, convex-dominance, scipy-canonical-ll]

requires:
  - phase: 04.1.1-03
    provides: "corrected scipy_canonical_ll fit_report.json on run_id bdaf5c7ba5a2 (gate 3/4, lr_rejects=true)"
  - phase: 04.1-03
    provides: "Rule-3 orchestrator patches (commit 36881ea): n_min<=101 degenerate-joint_dist guard + quarto_skipped null-stage sentinel"
provides:
  - "6-artifact hedge set on the corrected canonical run_id bdaf5c7ba5a2"
  - "DERIVED firing_condition=null_strip_unavailable (supersedes the LS-degenerate null_lr)"
  - "convexity-justified / calibration-caveated framing on the corrected fit"
affects: [04.1.1-05, phase-5, hedge, firing-condition]

tech-stack:
  added: []
  patterns:
    - "Pattern I thread-pinning on the hedge invocation (consume-only)"
    - "Rule-3 n<101 real-panel path: degenerate joint_dist -> build_failed_upstream strip_degenerate -> null_strip_unavailable firing"

key-files:
  created:
    - .planning/phases/04.1.1-.../_artifacts/hedge_artifacts_manifest.txt
    - data/fits/ichi/bdaf5c7ba5a2/{joint_dist,gate_report,strip_degenerate,stress_report,firing_condition}.json
    - data/fits/ichi/bdaf5c7ba5a2/run_log.txt
  modified: []

key-decisions:
  - "firing_condition recorded AS-OBSERVED as null_strip_unavailable (derived, not pre-committed, not tuned)"
  - "verdict gate_passes=FALSE (3/4, KS knife-edge) NOT flipped; descriptive evidence reported as descriptive only"
  - "convex instrument SHAPE robust (gate 4/4); strip CALIBRATION unavailable (degenerate joint_dist n_min=79<101)"

patterns-established:
  - "Pattern: corrected-fit hedge rerun routes a DIFFERENT firing condition (null_strip_unavailable) than the LS run (null_lr) through the same locked 4-condition tree"

requirements-completed: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05]

duration: 18min
completed: 2026-05-29
---

# Phase 04.1.1 Plan 04: Hedge Production Rerun on Corrected Canonical run_id Summary

**`cli.py hedge --run-id bdaf5c7ba5a2 --stage all` emitted the full 6-artifact set on the corrected `scipy_canonical_ll` fit; the locked decision tree DERIVED `firing_condition=null_strip_unavailable` — convexity-justified (gate 4/4) but calibration-caveated (degenerate joint_dist), superseding the LS-degenerate `null_lr`.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-29T14:38Z
- **Completed:** 2026-05-29T14:43Z
- **Tasks:** 1 completed
- **Files modified:** 0 source; 9 artifacts/docs created

## Accomplishments

- Ran the hedge orchestrator on the corrected canonical run_id `bdaf5c7ba5a2` under thread-pinned BLAS (`OMP=MKL=OpenBLAS=NumExpr=1`); `run_log.txt` carries the `hedge.orchestrator.run_hedge completed` sentinel.
- Emitted the complete 6-artifact set under `data/fits/ichi/bdaf5c7ba5a2/`: `joint_dist.json` (degenerate, n_min=79), `gate_report.json` (any_condition_passed=True, 4/4), `strip_degenerate.json` (build_failed_upstream), `stress_report.json` (divergence NaN), `firing_condition.json`, `run_log.txt`.
- Recorded the DERIVED firing_condition and the divergence-vs-LS story in `_artifacts/hedge_artifacts_manifest.txt`.
- Preserved the Plan 04.1-03 Rule-3 orchestrator patches verbatim (no edit); both fired this run on the n<101 real-panel substrate.

## The DERIVED firing_condition (AS-OBSERVED)

`decide_firing_condition` returned **`null_strip_unavailable`** (condition d). Sequential trace against the live artifacts:

- **(a) cost** — not fired (no `cost_leg_bound` FAIL supplied).
- **(b) lr** — NOT fired: `lr_test.p_value = 0.0 < 0.05` (LR REJECTS NHPP; Hawkes distinguishable).
- **(c) convex** — NOT fired: `gate_report.any_condition_passed = True` (all 4 convex-dominance conditions pass).
- **(d) strip** — FIRED: `strip_degenerate.json` exists in run_dir → `null_strip_unavailable`.

**Divergence vs the old LS run (`ae9e3ba17900`):** the LS run fired `null_lr` (LR p=0.58 did not reject — an artifact of the LS-into-canonical-LL pathology + kernel-blind β=0.1 understating η). On the corrected estimator the LR REJECTS (observed_stat=561.29, p=0.0), so `null_lr` is no longer carried forward. The new firing is `null_strip_unavailable`: the convex SHAPE is justified (gate 4/4: vol_of_vol=1.180, skew=1.849/excess_kurt=3.602, branching_ratio=0.600≥0.2, depeg jump), but the Carr-Madan strip is not emittable on the degenerate joint_dist (held-out n_min=79 < 101 DEPEND-01 lag-radius floor → `build_failed_upstream`). **Convexity-justified, calibration-caveated.**

The four-criterion VERDICT gate remains **gate_passes=FALSE (3/4)** — KS held-out leg-0 p=0.0474 < 0.05 knife-edge miss. The verdict is recorded AS-OBSERVED, NOT flipped (DISPOSITION_MEMO_04_1_1_ks_halt.md, committed 03132cd; CORRECTIONS.md alongside the fit). The descriptive findings (η≈0.6, LR rejects, held-out Hawkes beats NHPP by ~114 nats, all-4 convex gate) are reportable AS DESCRIPTIVE EVIDENCE only.

## Artifact disposition (plainly stated)

| Artifact | Key field | Value |
|---|---|---|
| `firing_condition.json` | firing_condition | `null_strip_unavailable` (derived, not pre-committed) |
| `gate_report.json` | any_condition_passed | `True` (4/4 convex conditions pass) |
| `joint_dist.json` | empirical_copula.family / degenerate_reason | `degenerate` / `n_min=79 <= 101 (DEPEND-01)` |
| `strip_degenerate.json` | char_func_source / reason | `build_failed` / `build_failed_upstream` |
| `stress_report.json` | divergence_pct / divergence_flag | `NaN` / `False` |
| `run_log.txt` | sentinel | `hedge.orchestrator.run_hedge completed`; quarto_skipped=True |

## Deviations from Plan

None — plan executed exactly as written. The hedge ran consume-only; `git diff HEAD -- analysis/ scripts/` is empty. The Rule-3 patches fired as designed (n_min guard → degenerate joint_dist; quarto_skipped sentinel → firing_condition still written). A legitimate strip_degenerate + `null_strip_unavailable` firing is the pre-registered handling for gate_passes=FALSE, not a blocker.

## AF-12 OUT-OF-SCOPE (honored verbatim)

- NO dgp/ or hedge/ source edits (consume only).
- NO new firing conditions (the existing 4: null_cost, null_lr, null_convex, null_strip_unavailable).
- NO new gate criteria (the 4).
- NO threshold/aggregator/tolerance changes (LR α=0.01, KS α=0.05, η-floor 0.2, Q-9 300, Carr-Madan POSITIVITY_TOLERANCE, DIVERGENCE_FLAG_THRESHOLD).
- NO REVERT of the Rule-3 patches (36881ea) — preserved verbatim.
- NO firing_condition pre-commitment — derived by `decide_firing_condition`.
- NO VERIFICATION append (Plan 05).
- NO PANEL-02 schema bump.
- NO Phase 5 PDF authoring/absorption.
- NO synthetic-substrate deletion.
- NO overwrite of `ae9e3ba17900` (still null_lr, least-squares).
- NO verdict flip (gate_passes=FALSE stands).

## Forward pointer

Plan 04.1.1-05 (verification append) consumes `_artifacts/new_run_id.txt` (`bdaf5c7ba5a2`) + the DERIVED firing_condition `null_strip_unavailable` + the gate_passes=FALSE (3/4) verdict, and amends the VERIFICATION-pre.md acceptance grid. Phase 5 PDF renders on the corrected null/near-miss findings, citing the convexity-justified / calibration-caveated framing.

## Self-Check: PASSED
