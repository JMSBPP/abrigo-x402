---
phase: 04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - notes/PRE_REGISTRATION.md
autonomous: true
requirements:
  - DGP-02
  - DGP-03
  - DGP-06
  - DEPEND-01
  - DEPEND-02
  - HEDGE-01
  - HEDGE-02
  - HEDGE-03
  - HEDGE-04
  - HEDGE-05
gap_closure: true
must_haves:
  truths:
    - "notes/PRE_REGISTRATION.md contains a `## Phase 04.1.1 — LL-fit acceptance & fallback chain` section"
    - "The section locks the η-coherence acceptance band [0.283, 0.371] verbatim"
    - "The section locks the synthetic-regression band [0.45, 0.55] on `analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet` verbatim"
    - "The section specifies scipy.optimize.minimize with method='L-BFGS-B' + positivity bounds wrapping `_hawkes_loglik_vectorized` as Fallback A"
    - "The section locks the BLOCKER policy (no Fallback B) if both tick.likelihood + scipy_canonical_ll fail"
    - "The section locks the DECAY_GRID extension to (0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)"
    - "Single atomic commit `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback` predates any Plan 04.1.1-01 commit per git log"
  artifacts:
    - path: notes/PRE_REGISTRATION.md
      provides: "AF-03 pre-registered Phase 04.1.1 LL-fit acceptance criteria + fallback chain (committed BEFORE the LL-fit retry runs)"
      contains: "## Phase 04.1.1 — LL-fit acceptance & fallback chain"
  key_links:
    - from: notes/PRE_REGISTRATION.md
      to: analysis/src/abrigo_x402/dgp/hawkes_fit.py
      via: AF-03 temporal ordering invariant (pre-reg commit MUST precede fit-code commit per git log)
      pattern: "git log --pretty=format:'%H %s %ai' notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/hawkes_fit.py"
---

<objective>
AF-03 pre-registration amendment for Phase 04.1.1. APPEND a new section `## Phase 04.1.1 — LL-fit acceptance & fallback chain` to `notes/PRE_REGISTRATION.md`. Lock — BEFORE Plan 04.1.1-01 runs — (i) the η-coherence acceptance band [0.283, 0.371] (= profile-likelihood CI from `data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci`), (ii) the synthetic-regression band [0.45, 0.55] on `analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet`, (iii) the scipy.optimize.L-BFGS-B fallback wrapping `_hawkes_loglik_vectorized` as Fallback A, (iv) the BLOCKER policy if both tiers fail (no Fallback B), (v) the DECAY_GRID extension to (0.0001, 0.001, 0.01, 0.1, 1.0, 10.0). Commit subject `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback`. Single atomic commit. The git-log temporal ordering of this commit predating Plan 04.1.1-01's `fix(04.1.1-01):` commit is the audit verification of AF-03 compliance.

Purpose: AF-03 anti-fishing discipline. The LL-fit thresholds and fallback chain must be locked before the new fit runs, so the outcome cannot be retroactively rationalized as "the method we always intended". This mirrors the Plan 04-pre AF-03 amendment that preceded all Phase 4 hedge code (commit `2dc3877`).

Output: One commit to `notes/PRE_REGISTRATION.md` adding the new section AFTER the existing `## Carr-Madan Grid Numerical Tolerances` section and BEFORE `## Acceptance Regions`. All 173 existing lines of PRE_REGISTRATION.md are preserved verbatim; this plan ONLY appends a new section.

**AF-12 OUT-OF-SCOPE list (verbatim, every plan body — silent-rescope defense):**
- NO new Hawkes kernel forms (still exponential decay)
- NO new firing conditions (still the existing 4: cost-leg fail, LR indistinguishable, zero convex dominance, null_strip_unavailable)
- NO new gate criteria (still the 4: branching_ci_excludes_zero, eta_floor=0.2, ks_held_out, lr_alpha=0.01)
- NO change to AF-03 pre-registered thresholds (η floor 0.2, LR α 0.01, KS α 0.05, Q-9 floor 300) — the η-coherence band is a NEW lock, not a revision
- NO new requirements (cross-references DGP-01, DGP-02, DGP-03, DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05 only — no new IDs)
- NO re-fetch from Forno/Blockscout
- NO PANEL-02 metadata header changes
- NO Phase 5 PDF wave-1 scaffolding (Phase 5 stays separate)
- NO synthetic-substrate deletion (`0afc6af38e24` archived as "LS-fallback evidence")
- NO overwrite of `ae9e3ba17900` (archived; new run_id is canonical)
- Pattern I thread-pinning mandatory (OMP/MKL/OPENBLAS/NUMEXPR=1) before any numpy import in any new test or script (does not apply here — pure docs commit)
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-CONTEXT.md
@.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-RESEARCH.md
@notes/PRE_REGISTRATION.md
@CLAUDE.md

<interfaces>
<!-- The η-coherence band is read directly from this existing artifact: -->
From data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci:
```json
{
  "alpha": 0.05,
  "ci_width": 0.08751007150998036,
  "eta_hat": 0.3406896551724138,
  "lower": 0.2834212873462106,
  "method": "profile_likelihood",
  "q9_threshold": 0.4,
  "upper": 0.37093135885619094
}
```
→ The η-coherence acceptance band locked by this plan = `[lower, upper]` = `[0.283, 0.371]` (rounded to 3 sig figs).
→ Verbatim quotation in the new section: "η ∈ [0.283, 0.371] (= profile-likelihood CI from `data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci.{lower, upper}` at α=0.05)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Phase 04.1.1 — LL-fit acceptance & fallback chain` section to notes/PRE_REGISTRATION.md</name>
  <files>notes/PRE_REGISTRATION.md</files>
  <read_first>
    - notes/PRE_REGISTRATION.md (the file being modified; READ ENTIRELY to identify the insertion point after `## Carr-Madan Grid Numerical Tolerances` and before `## Acceptance Regions`)
    - .planning/phases/04.1.1-.../04.1.1-CONTEXT.md (carries the AF-12 OUT-OF-SCOPE list; verbatim in commit body)
    - .planning/phases/04.1.1-.../04.1.1-RESEARCH.md (carries Pattern 2 scipy fallback specification + Pitfall 4 AF-03 ordering + DECAY_GRID Pitfall 3)
    - data/fits/ichi/ae9e3ba17900/fit_report.json (source-of-truth for η-coherence band [0.283, 0.371])
    - analysis/src/abrigo_x402/dgp/lr_test.py (canonical `_hawkes_loglik_vectorized` — the scipy fallback's objective function; section references this by full module path)
  </read_first>
  <action>
    APPEND a new section to `notes/PRE_REGISTRATION.md`. Insert AFTER the closing line of `## Carr-Madan Grid Numerical Tolerances` (last line of that section is the "Ordering invariant: …Honored by Plan 04-pre." paragraph) and BEFORE the existing `## Acceptance Regions` H2 header.

    Insert the following section VERBATIM (copy-paste; do NOT paraphrase any threshold values, file paths, or method names):

    ```markdown
    ## Phase 04.1.1 — LL-fit acceptance & fallback chain

    Amendment date: 2026-05-27. This sub-section is added as an AF-03 amendment to lock the acceptance criteria and fallback chain for the canonical maximum-likelihood Hawkes fit on the real ICHI cKES/USDT panel. The pre-existing Phase 04.1 production-rep landed `fit_report.json` at `data/fits/ichi/ae9e3ba17900/` via Pattern F LS-fallback (`fit_method_used = "least-squares"`); the resulting `hawkes_mv_params.branching_ratio = 3.07e-4` is degenerate (boundary_warning=true; baseline ~1.7e-7; adjacency ~1e-5), driving the LR test to non-rejection by construction (`lr_test.p_value = 0.58`). The INDEPENDENT profile-likelihood path on the same panel returns `eta_hat = 0.341` with 95% CI `[0.283, 0.371]` — three orders of magnitude apart. The LS path is the broken estimator; Phase 04.1.1 restores canonical MLE.

    η-coherence acceptance band: η_LL ∈ [0.283, 0.371]. Source: `data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci.lower` = 0.2834212873462106 and `:: branching_ratio_ci.upper` = 0.37093135885619094 at α=0.05 (`branching_ratio_ci.method = "profile_likelihood"`). The 04.1.1 LL-fit's `hawkes_mv_params.branching_ratio` must land inside this band. If inside → SHIP. If LL converges but η outside → NEEDS WORK (different basin; fix is not real). Pre-registered BEFORE Plan 04.1.1-01 fit-retry runs. AF-03 ordering verified by `git log --pretty=format:'%H %s %ai' notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/hawkes_fit.py | head -10` returning the pre-reg commit timestamp strictly less than any commit touching `hawkes_fit.py` under Plan 04.1.1-01.

    Synthetic-regression acceptance band: η_LL ∈ [0.45, 0.55] on `analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet`. This is the single-realization 686-event Wave-0 synthetic fixture (Phase 3 Plan 03-00 origin; ground-truth η=0.5; the only known on-disk substrate where η=0.5 is preserved). The new LL pipeline MUST recover η in [0.45, 0.55] on this fixture as the regression-check criterion. The 3-time-shifted stacked synthetic at `data/fits/ichi/0afc6af38e24/` does NOT preserve branching ratio (its own profile-likelihood path returns η=0.052, not 0.5) and is NOT a valid regression substrate — it remains archived as LS-fallback evidence only.

    DECAY_GRID extension: the Phase 3 hardcoded `DECAY_GRID = (0.01, 0.1, 1.0, 10.0)` in `analysis/src/abrigo_x402/dgp/hawkes_fit.py` is extended to `(0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)`. Rationale: real-panel mean inter-arrival ~666s; β=0.1/s decays in 10s → kernel cannot span typical inter-arrivals → MLE collapses to flat-LL basin. Extending the grid downward to β ∈ {1e-4, 1e-3} adds two grid points that decay over 10⁴/10³ seconds respectively, matching the panel's timescale. AIC-min selection logic over the extended grid is preserved unchanged.

    Fallback A (pre-registered primary fallback): `scipy.optimize.minimize(method="L-BFGS-B")` over the parameter vector `θ = (λ₀_0, λ₀_1, α_00, α_01, α_10, α_11)` with strict positivity bounds `(1e-12, None)` per parameter and decay β fixed at the AIC-min DECAY_GRID value, minimizing `-_hawkes_loglik_vectorized(...)` from `analysis/src/abrigo_x402/dgp/lr_test.py`. Initial point seeded from empirical rate `λ₀ = N/T`, `α_ij = 0.01`. Multi-start (3 starts: `[(λ₀_emp, 0.01), (λ₀_emp/2, 0.1), (λ₀_emp/4, 0.3)]`) is permitted; return the fit with max LL. On success, `fit_method_used = "scipy_canonical_ll"`; `why_not_likelihood` records the tick exception text. Fallback A is library-bug-isolated: it shares no C++ kernel with the broken tick HawkesExpKern MLE; the LL function it wraps is the same closed-form formula already trusted by the LR statistic (Pattern F canonical-LL contract).

    Fallback B (NOT pre-registered — BLOCKER policy): if both tick.likelihood mode AND scipy_canonical_ll fail, raise `RuntimeError` and halt the pipeline. NO silent promotion of `branching_ratio_ci.eta_hat` (profile-likelihood) into `hawkes_mv_params.branching_ratio` is permitted. NO silent reintroduction of LS-fallback is permitted. NO regularized-LS-with-anti-degeneracy-guard is permitted. The BLOCKER must surface to the human reviewer for honest investigation. This is AF-03 anti-fishing discipline at the estimator-method level.

    Ordering invariant: this amendment commit MUST predate every commit under `analysis/src/abrigo_x402/dgp/hawkes_fit.py` from Plan 04.1.1-01 onwards (verifiable via `git log --pretty=format:'%H %s %ai' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/hawkes_fit.py | head -10` — pre-reg commit timestamp strictly less than the next hawkes_fit.py-touching commit). Honored by Plan 04.1.1-00.

    Consumers: `analysis/src/abrigo_x402/dgp/hawkes_fit.py` (Plan 04.1.1-01 implementation site); `analysis/tests/test_hawkes_fit.py` (new tests `test_likelihood_mode_eta_within_profile_ci`, `test_hawkes_likelihood_mode_succeeds_on_synthetic`, `test_likelihood_mode_eta_recovers_synthetic_ground_truth`, `test_scipy_fallback_path_isolated`); `data/fits/ichi/<new-run-id>/fit_report.json` (Plan 04.1.1-01 production-rep output; must satisfy `fit_method_used != "least-squares"` AND `hawkes_mv_params.branching_ratio ∈ [0.283, 0.371]`); `04-VERIFICATION-pre.md` (Plan 04.1.1-03 verification append; 4 new frontmatter fields `ll_fit_rerun_run_id`, `ll_fit_method_used`, `ll_fit_eta_in_profile_ci`, `ls_fallback_artifact_supersession_resolved: true`).

    Out-of-scope (AF-12 silent-rescope defense): NO new Hawkes kernel forms (still exponential decay); NO new firing conditions (still the existing 4); NO new gate criteria (still the 4); NO new requirements (cross-references DGP-01, DGP-02, DGP-03, DEPEND-01, DEPEND-02, HEDGE-01..05 only); NO change to η-floor 0.2 / LR α 0.01 / KS α 0.05 / Q-9 floor 300 (the η-coherence band is a NEW lock, not a revision); NO re-fetch from Forno/Blockscout; NO PANEL-02 schema bump; NO Phase 5 PDF absorption; NO synthetic-substrate deletion; NO overwrite of `ae9e3ba17900`.
    ```

    After the above section ends, leave the existing `## Acceptance Regions` H2 header untouched.

    Run `git diff --stat notes/PRE_REGISTRATION.md` and verify only one file changed and the additions count ≈ 40-50 lines (no deletions; no modifications to lines 1-173 of the existing file).

    Then commit with subject `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback` via:

    ```bash
    git add notes/PRE_REGISTRATION.md
    git commit -m "$(cat <<'EOF'
    docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback

    AF-03 amendment for Phase 04.1.1 LL-fit on the real ICHI cKES/USDT panel.
    Locks BEFORE Plan 04.1.1-01 runs:
    - η-coherence acceptance band [0.283, 0.371] (= profile-likelihood CI from
      data/fits/ichi/ae9e3ba17900/fit_report.json :: branching_ratio_ci).
    - Synthetic-regression band [0.45, 0.55] on
      analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet (NOT the 3-stacked
      0afc6af38e24 substrate which does not preserve branching ratio).
    - DECAY_GRID extension to (0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)
      (real-panel mean inter-arrival ~666s; β=0.1/s decays in 10s = 6× too fast).
    - Fallback A: scipy.optimize.minimize(method="L-BFGS-B") wrapping
      _hawkes_loglik_vectorized with strict positivity bounds; library-bug-isolated.
    - BLOCKER policy if both tick.likelihood + scipy_canonical_ll fail (no
      Fallback B; no silent LS reintroduction; no silent profile-LL promotion).

    Ordering invariant: this commit MUST predate every commit touching
    analysis/src/abrigo_x402/dgp/hawkes_fit.py under Plan 04.1.1-01. Verifiable
    via git log temporal ordering.

    AF-12 OUT-OF-SCOPE: no new kernel forms; no new firing conditions; no new
    gate criteria; no new requirements; no change to η-floor 0.2 / LR α 0.01 /
    KS α 0.05 / Q-9 floor 300; no re-fetch; no PANEL-02 schema bump; no Phase 5
    absorption; no synthetic deletion; no overwrite of ae9e3ba17900.
    EOF
    )"
    ```

    Verify the commit landed via `git log --oneline -1` and that the subject matches exactly `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback`.

    DO NOT touch any other file in this plan. DO NOT edit `analysis/src/`. DO NOT edit `analysis/tests/`. DO NOT edit `data/`. DO NOT edit any other planning artifact. This is a single-file docs commit.
  </action>
  <verify>
    <automated>grep -c "^## Phase 04.1.1 — LL-fit acceptance & fallback chain$" notes/PRE_REGISTRATION.md && grep -c "η-coherence acceptance band: η_LL ∈ \[0.283, 0.371\]" notes/PRE_REGISTRATION.md && grep -c "0.0001, 0.001, 0.01, 0.1, 1.0, 10.0" notes/PRE_REGISTRATION.md && grep -c "fit_method_used = \"scipy_canonical_ll\"" notes/PRE_REGISTRATION.md && grep -c "BLOCKER policy" notes/PRE_REGISTRATION.md && git log --pretty=format:'%s' -1 | grep -c "^docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 04.1.1 — LL-fit acceptance & fallback chain$' notes/PRE_REGISTRATION.md` outputs `1`
    - `grep -c 'η-coherence acceptance band: η_LL ∈ \[0.283, 0.371\]' notes/PRE_REGISTRATION.md` outputs `1`
    - `grep -c 'analysis/tests/fixtures/synthetic_hawkes_eta_05.parquet' notes/PRE_REGISTRATION.md` outputs at least `1`
    - `grep -c '0.0001, 0.001, 0.01, 0.1, 1.0, 10.0' notes/PRE_REGISTRATION.md` outputs `1`
    - `grep -c 'scipy.optimize.minimize(method="L-BFGS-B")' notes/PRE_REGISTRATION.md` outputs `1`
    - `grep -c '_hawkes_loglik_vectorized' notes/PRE_REGISTRATION.md` outputs at least `1`
    - `grep -c 'BLOCKER policy' notes/PRE_REGISTRATION.md` outputs `1`
    - `grep -c 'Fallback B (NOT pre-registered' notes/PRE_REGISTRATION.md` outputs `1`
    - `git log --pretty=format:'%s' -1` outputs exactly `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback`
    - `git diff HEAD~1 --stat -- notes/PRE_REGISTRATION.md` shows only one file changed; insertions ≥ 30 and deletions = 0
    - `git diff HEAD~1 -- analysis/ scripts/ data/ .planning/` produces no output (no spillover edits)
    - Pre-existing 173 lines of `notes/PRE_REGISTRATION.md` are preserved verbatim (verify via `git diff HEAD~1 -- notes/PRE_REGISTRATION.md | grep '^-' | wc -l` returning `1` — the `---` diff header — i.e., zero deleted content lines)
  </acceptance_criteria>
  <done>
    `notes/PRE_REGISTRATION.md` carries a new `## Phase 04.1.1 — LL-fit acceptance & fallback chain` section locking the η-coherence band [0.283, 0.371], the synthetic-regression band [0.45, 0.55], the DECAY_GRID extension, the scipy L-BFGS-B Fallback A specification, and the BLOCKER policy for double-failure. Single atomic `docs(pre-reg:04.1.1):` commit lands. AF-03 temporal ordering is achievable because no Plan 04.1.1-01 commit has been authored yet.
  </done>
</task>

</tasks>

<verification>
- `grep -c '^## Phase 04.1.1 — LL-fit acceptance & fallback chain$' notes/PRE_REGISTRATION.md` → `1`
- `git log --pretty=format:'%H %s' -- notes/PRE_REGISTRATION.md | head -3` shows the new `docs(pre-reg:04.1.1):` commit as the most recent change to the file
- `git log --pretty=format:'%H %ai %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/hawkes_fit.py | head -10` — Plan 04.1.1-00 commit's timestamp is strictly less than any commit touching `hawkes_fit.py` that has not yet landed (Plan 04.1.1-01 runs in Wave 1 after this plan)
- Pre-commit hooks AF-01..AF-12 PASS on this commit (review-trail enforcement requires `.planning/_reviews/04.1.1-00-PLAN_reality_checker.md` and `_code_reviewer.md` with PASS or NEEDS REVISION verdicts before this commit can land — that is enforced by the project's existing hook, not by this plan body)
- Regression test impact: zero (pure docs commit; no source code or test changes)
</verification>

<success_criteria>
- `notes/PRE_REGISTRATION.md` contains exactly one new section `## Phase 04.1.1 — LL-fit acceptance & fallback chain` placed between `## Carr-Madan Grid Numerical Tolerances` and `## Acceptance Regions`
- All five required lockings are textually present in the new section (η-coherence band, synthetic-regression band, DECAY_GRID extension, scipy L-BFGS-B Fallback A, BLOCKER policy)
- Single atomic commit with subject `docs(pre-reg:04.1.1): lock LL-fit η-coherence band + scipy fallback`
- `git diff HEAD~1 -- analysis/ scripts/ data/` produces no output (no spillover)
- AF-03 temporal-ordering invariant is satisfiable: this commit lands BEFORE Plan 04.1.1-01's `fix(04.1.1-01):` commit (the next plan must verify this from its own `git log` invocation)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-00-SUMMARY.md` recording: commit hash, the inserted section's line range in PRE_REGISTRATION.md, the four locked thresholds (η-coherence band, synthetic-regression band, DECAY_GRID, scipy fallback spec), and a forward pointer to Plan 04.1.1-01 noting that its `fix(04.1.1-01):` commit MUST land after this commit per AF-03 temporal ordering.
</output>
</content>
</invoke>