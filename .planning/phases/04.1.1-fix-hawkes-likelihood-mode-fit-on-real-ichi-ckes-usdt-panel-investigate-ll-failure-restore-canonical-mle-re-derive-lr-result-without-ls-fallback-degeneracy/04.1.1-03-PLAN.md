---
phase: 04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy
plan: "03"
type: execute
wave: 3
depends_on: ["04.1.1-02"]
files_modified:
  - .planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/new_run_id.txt
  - .planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/production_rep_fit_summary.txt
autonomous: true
requirements:
  - DGP-02
  - DGP-03
  - DGP-06
gap_closure: true
must_haves:
  truths:
    - "cli.py fit runs on the real panel under thread-pinned BLAS and emits a NEW content-addressed run_id (new gitCommit → new run_id) with fit_method_used='scipy_canonical_ll'"
    - "The new fit_report.json carries the AIC-min β (expected interior β=0.001, η≈0.628 as a LOWER BOUND — assert method + AIC-selection + CI-excludes-zero, NOT a fixed η value), a decay_aic_table over all 6 grid points, and a genuine constrained-MLE branching_ratio_ci (method='constrained_mle_profile')"
    - "The LR bootstrap (α=0.01, Option A) + held-out KS are re-derived on the corrected fit; observed_stat is finite-scale (no 6.05M)"
    - "The four-criterion gate verdict (lr_rejects @ α=0.01, eta_floor_met ≥0.2, ks_held_out_passes, branching_ci_excludes_zero) is recorded as-observed — NO firing_condition is pre-committed; the verdict may be positive OR null"
    - "The new run_id is captured to _artifacts/new_run_id.txt for downstream plans"
  artifacts:
    - path: .planning/phases/04.1.1-.../_artifacts/new_run_id.txt
      provides: "Load-bearing pointer: the new canonical run_id consumed by Plan 04.1.1-04 hedge rerun + Plan 04.1.1-05 verification append"
      contains: ""
    - path: .planning/phases/04.1.1-.../_artifacts/production_rep_fit_summary.txt
      provides: "Recorded fit_method_used, AIC-min β, η (lower bound), branching_ratio_ci.method+lower, lr_test.{observed_stat,p_value,rejects_at_alpha}, ks p_value, four-criterion gate booleans, firing_condition (as-observed)"
      contains: "scipy_canonical_ll"
  key_links:
    - from: analysis/src/abrigo_x402/cli.py
      to: data/fits/ichi/<new-run-id>/fit_report.json
      via: "cli.py fit --pool ... --panel-path ... --out-dir data/fits/ichi (run_fit orchestrator emits fit_report.json + residuals.parquet)"
      pattern: "fit_method_used.*scipy_canonical_ll"
    - from: data/fits/ichi/<new-run-id>/fit_report.json
      to: .planning/phases/04.1.1-.../_artifacts/new_run_id.txt
      via: "run_id captured to artifacts pointer for the hedge rerun + verification append"
      pattern: "[0-9a-f]{12}"
---

<objective>
Production-rep the corrected canonical fit on the real ICHI cKES/USDT panel. Run `cli.py fit` on `data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet` (sha256 `a72a4eeaf2805aaf6b89b993e25d6b9012b8a97d507ca7e40a2dac68ee9f412a`, 832 rows, block_timestamp Int64) under thread-pinned BLAS. Because the v2 source edits (Plans 01/02) changed the gitCommit, `_derive_run_id` yields a NEW content-addressed run_id. The new `fit_report.json` must carry `fit_method_used = "scipy_canonical_ll"`, the AIC-min β (expected interior β=0.001, η≈0.628 — reported as a LOWER BOUND per the ~13% downward bias; do NOT hard-assert a value), a 6-point `decay_aic_table`, and a genuine constrained-MLE `branching_ratio_ci` (`method = "constrained_mle_profile"`). Re-run the LR bootstrap (α=0.01, Option A) + held-out KS on the corrected fit. Record the four-criterion gate verdict (lr_rejects, eta_floor_met, ks_held_out_passes, branching_ci_excludes_zero) AS-OBSERVED — NO firing_condition is pre-committed; the verdict may be positive OR null. Capture the new run_id to `_artifacts/new_run_id.txt`.

Purpose: this is the substantive Phase 04.1.1 deliverable — the real-data fit on the corrected estimator that supersedes the LS-degenerate `ae9e3ba17900`. The four-criterion gate (UNCHANGED Phase-0 thresholds) is the SOLE verdict; the corrected fit clears η-floor + (pending CI) CI-excludes-zero, but LR + KS are NOT yet re-established and could land null.

Output: a new `data/fits/ichi/<new-run-id>/` directory (fit_report.json + residuals.parquet + lr_bootstrap diagnostic PNG); `_artifacts/new_run_id.txt`; `_artifacts/production_rep_fit_summary.txt`.

**AF-12 OUT-OF-SCOPE list (verbatim — silent-rescope defense; NOTE the v2 expansion):**
- v2 EXPANSION (already landed in Plans 01/02): scipy canonical estimator + Option A lr_test + genuine-MLE CI. `lr_test.py` + `profile_likelihood.py` are IN scope for the 04.1.1 phase (authorized in PRE_REGISTRATION §Phase 04.1.1 (v2)); this plan only RUNS them on the real panel — no new source edits here.
- STILL OUT: NO new Hawkes kernel forms; NO new firing conditions (the existing 4); NO new gate criteria (the 4); NO change to η-floor 0.2 / LR α 0.01 / KS α 0.05 / Q-9 floor 300; NO new requirements; NO re-fetch from Forno/Blockscout (panel already on disk); NO PANEL-02 metadata header changes; NO Phase 5 PDF wave-1 scaffolding; NO synthetic-substrate deletion; NO overwrite of `ae9e3ba17900` (new run_id is canonical; old stays archived).
- STILL OUT: NO firing_condition pre-commitment; NO headline flip to "positive" on η alone — the four-criterion gate decides.
- STILL OUT: NO source-code edits in this plan (cli.py fit + run_fit orchestrator consumed unchanged from Plans 01/02; if run_fit raises on the real panel, that is a BLOCKER to surface, NOT a silent patch).
- Pattern I thread-pinning mandatory: prepend `OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1` to the `cli.py fit` invocation and any pytest.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-CONTEXT.md
@.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-DIAGNOSTIC.md
@notes/PRE_REGISTRATION.md
@analysis/src/abrigo_x402/cli.py
@analysis/src/abrigo_x402/dgp/orchestrator.py
@CLAUDE.md

<interfaces>
<!-- cli.py fit invocation (Plan 03-07 surface; unchanged): -->
cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
  uv run python -m abrigo_x402.cli fit \
    --pool 0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F \
    --panel-path ../data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet \
    --out-dir ../data/fits/ichi \
    --bootstrap-reps 1000
# prints JSON: {"run_id": "<12hex>", "fit_report_path": ".../fit_report.json", "residuals_path": "...", "gate_passes": <bool>}

<!-- fit_report.json fields the orchestrator emits (orchestrator.py run_fit): -->
hawkes_mv_params: {baseline, adjacency, decays(AIC-min β), branching_ratio, decay_aic_table, fit_method_used, loglik, ...}
lr_test: {observed_stat, p_value, rejects_at_alpha, n_reps, n_successful_bootstrap, n_failed, seed, alpha}
ks_rescaled_time: {p_value, residuals_hash, per_leg, ks_alpha}
branching_ratio_ci: {method, eta_hat, lower, upper, ci_width, alpha, q9_nullfire_triggered, q9_threshold}   # method now "constrained_mle_profile"
gate_passes: bool
gate_criteria: {lr_rejects, eta_floor_met, ks_held_out_passes, branching_ci_excludes_zero, stationary, eta_floor_threshold, lr_alpha, ks_alpha}
input_diagnostics.hawkes_fit_method_used: "scipy_canonical_ll"

<!-- run_id derivation (orchestrator.py L117): sha256(panel_dataHash + gitCommit + tick_version)[:12]. -->
<!-- Same panel sha256 a72a4ee… + NEW gitCommit (Plans 01/02 edits) + tick 0.8.0.2 → DIFFERENT run_id. -->
<!-- The OLD LS-fallback run_id was ae9e3ba17900 — the new run_id MUST differ from it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cli.py fit on the real panel under thread-pinned BLAS; emit the new run_id + fit_report.json</name>
  <files>data/fits/ichi/&lt;new-run-id&gt;/fit_report.json, data/fits/ichi/&lt;new-run-id&gt;/residuals.parquet, .planning/phases/04.1.1-.../_artifacts/new_run_id.txt</files>
  <read_first>
    - analysis/src/abrigo_x402/cli.py (_cmd_fit L124-149 + the fit subparser L203-231 — flags: --pool, --panel-path, --out-dir, --bootstrap-reps)
    - analysis/src/abrigo_x402/dgp/orchestrator.py (run_fit L222+; _derive_run_id L117; the fit_report.json assembly L427-479 + the four-criterion gate L407-422)
    - .planning/phases/04.1.1-.../04.1.1-DIAGNOSTIC.md (§Q2 expected real-panel η(β) profile: AIC-min β=0.001, η=0.628, ΔAIC=21.9 — for sanity-check expectations, NOT a hard assertion)
    - CLAUDE.md (USDT framing; the real panel is the cKES/USDT anchor pool)
  </read_first>
  <action>
    Confirm the real panel parquet is on disk: `ls -la data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet`. If absent (gitignored / not materialized), this is a BLOCKER — surface to the human; do NOT fabricate a fit.

    Verify AF-03 ordering BEFORE running: `git log --pretty=format:'%H %ai %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/dgp/ | head -8` — the Plan 04.1.1-00-v2 `docs(pre-reg:04.1.1): supersede ... (v2)` commit MUST be older than every Plans-01/02-v2 dgp commit. If not, BLOCKER.

    Run the fit (mkdir the _artifacts dir first if needed):
    ```bash
    mkdir -p ".planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts"
    cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
      uv run python -m abrigo_x402.cli fit \
        --pool 0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F \
        --panel-path ../data/raw/ichi/0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F/67378253_67896653.parquet \
        --out-dir ../data/fits/ichi \
        --bootstrap-reps 1000
    ```
    Capture the printed `run_id` (12 hex). Write it to `_artifacts/new_run_id.txt` (single line, no trailing junk):
    ```bash
    echo "<NEW_RUN_ID>" > ".planning/phases/04.1.1-.../_artifacts/new_run_id.txt"
    ```
    Confirm the new run_id ≠ `ae9e3ba17900` (it MUST differ — new gitCommit). Confirm `data/fits/ichi/<new-run-id>/fit_report.json` exists and `data/fits/ichi/<new-run-id>/residuals.parquet` exists.

    If the bootstrap with `--bootstrap-reps 1000` runs longer than ~10 minutes wall-clock, that indicates Option A did NOT take effect (a scipy fit is being used per null replicate) — STOP and surface as a BLOCKER referencing Plan 04.1.1-02 Task 1 (the null path must be tick-LS). Do NOT silently reduce reps below the AF-04 1000 lock.
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run python -c "import json; rid=open('../.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/new_run_id.txt').read().strip(); assert len(rid)==12 and rid!='ae9e3ba17900', rid; fr=json.load(open(f'../data/fits/ichi/{rid}/fit_report.json')); h=fr['hawkes_mv_params']; assert h['fit_method_used']=='scipy_canonical_ll'; assert len(h['decay_aic_table'])==6; assert fr['input_diagnostics']['hawkes_fit_method_used']=='scipy_canonical_ll'; import os; assert os.path.exists(f'../data/fits/ichi/{rid}/residuals.parquet'); print('OK',rid,'beta=',h['decays'],'eta=',round(h['branching_ratio'],3))"</automated>
  </verify>
  <acceptance_criteria>
    - `_artifacts/new_run_id.txt` contains a 12-hex run_id that is NOT `ae9e3ba17900`
    - `data/fits/ichi/<new-run-id>/fit_report.json` exists; `hawkes_mv_params.fit_method_used == "scipy_canonical_ll"`; `input_diagnostics.hawkes_fit_method_used == "scipy_canonical_ll"`
    - `hawkes_mv_params.decay_aic_table` has all 6 DECAY_GRID keys; the selected `decays` is the AIC-min (expected interior β=0.001 per DIAGNOSTIC §Q2 — sanity-check only, not a hard assert)
    - `branching_ratio_ci.method == "constrained_mle_profile"` (genuine constrained-MLE CI from Plan 02, not the projection trick)
    - `lr_test.observed_stat` is finite-scale: `abs(observed_stat) < 1e5` (the 6.05M LS-into-canonical-LL pathology is gone); `lr_test.n_reps == 1000`; `lr_test.alpha == 0.01`
    - `data/fits/ichi/<new-run-id>/residuals.parquet` exists
    - `ae9e3ba17900` directory is UNCHANGED (no overwrite): `data/fits/ichi/ae9e3ba17900/fit_report.json` still records `fit_method_used == "least-squares"`
  </acceptance_criteria>
  <done>
    A new content-addressed run_id (≠ ae9e3ba17900) is emitted with `fit_method_used == "scipy_canonical_ll"`, AIC-min β + 6-point decay_aic_table, a genuine constrained-MLE branching_ratio_ci, and a finite-scale LR observed_stat. The run_id is captured to `_artifacts/new_run_id.txt`. The LS-degenerate `ae9e3ba17900` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the four-criterion gate verdict + firing_condition AS-OBSERVED (no pre-commitment)</name>
  <files>.planning/phases/04.1.1-.../_artifacts/production_rep_fit_summary.txt</files>
  <read_first>
    - data/fits/ichi/&lt;new-run-id&gt;/fit_report.json (the Task 1 output — read gate_criteria + lr_test + ks_rescaled_time + branching_ratio_ci)
    - notes/PRE_REGISTRATION.md (§Acceptance Regions: four-criterion gate = LR rejects @ α=0.01 AND η≥0.2 AND held-out KS rejects-NHPP-under-Hawkes AND CI excludes zero; §Phase 04.1.1 (v2): verdict UNCHANGED, no headline flip on η alone, result may be null)
    - analysis/src/abrigo_x402/hedge/null_result.py (decide_firing_condition — the four-condition firing tree; consumed by the Plan 04 hedge rerun; firing_condition is DERIVED there, not pre-committed here)
  </read_first>
  <action>
    Read `data/fits/ichi/<new-run-id>/fit_report.json`. Write `_artifacts/production_rep_fit_summary.txt` recording, VERBATIM from the artifact (no editorializing, no firing pre-commitment):
    - `run_id`
    - `hawkes_mv_params.fit_method_used` (must be `scipy_canonical_ll`)
    - `hawkes_mv_params.decays` (AIC-min β) and the full `decay_aic_table`
    - `hawkes_mv_params.branching_ratio` — label it explicitly as "η (LOWER BOUND; ~13% downward finite-sample bias per DIAGNOSTIC §Q1/Q3)"
    - `branching_ratio_ci.{method, eta_hat, lower, upper, ci_width}`
    - `lr_test.{observed_stat, p_value, rejects_at_alpha, n_reps, alpha}`
    - `ks_rescaled_time.{p_value, ks_alpha}`
    - the four gate booleans from `gate_criteria`: `lr_rejects`, `eta_floor_met`, `ks_held_out_passes`, `branching_ci_excludes_zero` (+ `stationary`), and `gate_passes`
    - A one-line VERDICT statement that is PURELY descriptive of the four booleans (e.g. "gate_passes=<bool>: lr_rejects=<>, eta_floor_met=<>, ks_held_out_passes=<>, branching_ci_excludes_zero=<>"). DO NOT assert a firing_condition here — that is decided by `decide_firing_condition` in the Plan 04 hedge rerun. Note explicitly: "firing_condition NOT pre-committed; the four-criterion gate decides; LR + KS were re-established on the corrected fit and the verdict is whatever the booleans say (may be positive OR null)."

    Then commit the production-rep. Stage `_artifacts/new_run_id.txt`, `_artifacts/production_rep_fit_summary.txt`, and the new `data/fits/ichi/<new-run-id>/` artifacts (force-add if data/fits is gitignored, mirroring Plan 04.1-03's force-add convention). Use subject `feat(04.1.1-03): real-panel scipy_canonical_ll production-rep — new run_id <NEW_RUN_ID>` with a 2-way review trail (`.planning/_reviews/04.1.1-03-PLAN_reality_checker.md` + `_code_reviewer.md`, `## VERDICT`).
  </action>
  <verify>
    <automated>grep -c "scipy_canonical_ll" ".planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/production_rep_fit_summary.txt" && grep -ciE "lr_rejects|eta_floor_met|ks_held_out_passes|branching_ci_excludes_zero" ".planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/production_rep_fit_summary.txt" && grep -ci "firing_condition NOT pre-committed" ".planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/_artifacts/production_rep_fit_summary.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `_artifacts/production_rep_fit_summary.txt` records `fit_method_used == scipy_canonical_ll`, the AIC-min β + decay_aic_table, η labeled as a LOWER BOUND, `branching_ratio_ci.method == constrained_mle_profile` + `lower`, the `lr_test.{observed_stat,p_value,rejects_at_alpha}` (α=0.01), the `ks_rescaled_time.p_value`, and all four gate booleans + `gate_passes`
    - The summary contains the line "firing_condition NOT pre-committed" (no firing pre-commitment; gate decides)
    - The summary contains NO sentence asserting "positive result" or "null result" as a foregone conclusion independent of the four booleans
    - The new `data/fits/ichi/<new-run-id>/` artifacts are committed (force-added if gitignored); commit subject is `feat(04.1.1-03): real-panel scipy_canonical_ll production-rep — new run_id <NEW_RUN_ID>`
    - AF-03 ordering preserved (this production-rep commit is after all v2 pre-reg + dgp commits)
  </acceptance_criteria>
  <done>
    The four-criterion gate verdict is recorded exactly as the corrected fit produced it (lr_rejects @ α=0.01, eta_floor_met ≥0.2, ks_held_out_passes, branching_ci_excludes_zero, gate_passes), with η framed as a lower bound and NO firing_condition pre-committed. The production-rep is committed.
  </done>
</task>

</tasks>

<verification>
- New run_id ≠ ae9e3ba17900, captured to `_artifacts/new_run_id.txt`
- `fit_report.json`: fit_method_used == scipy_canonical_ll; 6-point decay_aic_table; branching_ratio_ci.method == constrained_mle_profile; lr_test finite-scale observed_stat @ n_reps=1000, α=0.01
- Four-criterion gate booleans recorded as-observed in `_artifacts/production_rep_fit_summary.txt`; firing_condition NOT pre-committed
- `ae9e3ba17900` untouched (still least-squares)
- AF-03 ordering: production-rep commit after all v2 pre-reg + dgp commits
- Bootstrap completed in tractable time (Option A in effect; not ~143 min)
</verification>

<success_criteria>
- A new canonical run_id with the corrected scipy_canonical_ll fit on the real panel exists and is captured for downstream plans
- The fit carries AIC-min β + genuine constrained-MLE CI + finite-scale LR statistic + re-derived KS
- The four-criterion gate verdict is recorded honestly (may be positive OR null); no firing_condition pre-committed; η is a lower bound
- The LS-degenerate ae9e3ba17900 is preserved unchanged
- AF-12 honored: no new source edits; the existing scipy/Option-A/genuine-CI pipeline is simply RUN on the real panel
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy/04.1.1-03-SUMMARY.md` recording: the new run_id; fit_method_used; AIC-min β + decay_aic_table; η (lower bound) + branching_ratio_ci.{method,lower,upper}; lr_test.{observed_stat,p_value,rejects_at_alpha}; ks p_value; the four gate booleans + gate_passes; an explicit statement that firing_condition was NOT pre-committed; the bootstrap wall-clock time (Option A tractability evidence); and a forward pointer to Plan 04.1.1-04 (hedge rerun consumes `_artifacts/new_run_id.txt`).
</output>
