---
phase: 04.1.1-fix-hawkes-likelihood-mode-fit-on-real-ichi-ckes-usdt-panel-investigate-ll-failure-restore-canonical-mle-re-derive-lr-result-without-ls-fallback-degeneracy
plan: "02b"
subsystem: api
tags: [hawkes, orchestrator, aic-grid, scipy-canonical-ll, pre-registration, tdd]

# Dependency graph
requires:
  - phase: 04.1.1-02
    provides: "scipy_canonical_ll free-β AIC estimator (hawkes_fit.py), lr_test.py Option A, profile_likelihood.py constrained-MLE CI"
provides:
  - "run_fit defaults to free-β AIC (decays=None) so the production cli.py fit path uses the AIC-selected β, not the kernel-blind β=0.1"
  - "AF-12 pre-reg authorization for the orchestrator.run_fit rewire (orchestrator.py added to the v2 expansion set)"
  - "test_run_fit_uses_free_beta_aic_grid: production-path 6-point AIC grid + scipy_canonical_ll assertion"
affects: [04.1.1-03, production-rep, phase-5-pdf]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Production-path wiring closure: orchestrator default flows the canonical estimator's free-β AIC behavior through to HEADLINE/CI/KS/held-out legs via the existing hawkes_decays_t propagation (no duplicate wiring)"
    - "Pattern I thread-pinning carried into the new orchestrator test (deterministic AIC selection under single-threaded BLAS)"

key-files:
  created:
    - analysis/tests/test_dgp_orchestrator.py
  modified:
    - notes/PRE_REGISTRATION.md
    - analysis/src/abrigo_x402/dgp/orchestrator.py

key-decisions:
  - "run_fit decays default 0.1 → None: the production fit now runs the canonical v2 estimator (scipy_canonical_ll, AIC-min over 6-point DECAY_GRID); the AIC-selected β propagates downstream via the existing hawkes_decays_t = float(hawkes_train['decays']) — verified, not duplicated"
  - "Pre-reg authorization (Task 1) committed BEFORE the orchestrator.py source edit (Task 2) — AF-03 ordering"
  - "No estimator-math edits: hawkes_fit.py / lr_test.py / profile_likelihood.py consumed as-is; the rewire ENFORCES the pre-registered canonical-estimator definition, it does not change it; the four-criterion verdict gate is unchanged"

patterns-established:
  - "Wiring-gap closure committed as a docs(pre-reg) authorization + feat source edit pair, never as a silent default flip"

requirements-completed: [DGP-03, DGP-06]

# Metrics
duration: 71min
completed: 2026-05-28
---

# Phase 04.1.1 Plan 02b: Orchestrator run_fit free-β rewire Summary

**`run_fit` default `decays=0.1 → None` so the production `cli.py fit` path runs the canonical free-β AIC grid (`scipy_canonical_ll`, AIC-min over the 6-point DECAY_GRID) and propagates the AIC-selected β to the CI/KS/held-out legs — closing the Plan 04.1.1-03 BLOCKER without touching any estimator math.**

## Performance

- **Duration:** ~71 min
- **Started:** 2026-05-28T13:23:00Z (approx, post-context-load)
- **Completed:** 2026-05-28T14:33:54Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Authorized the orchestrator rewire in `notes/PRE_REGISTRATION.md` §Phase 04.1.1 (v2) as an append-only AF-12 expansion (v1 + v2 sections preserved), committed before the source edit (AF-03 ordering).
- Rewired `run_fit` signature `decays: float = 0.1` → `decays: float | None = None`; the production fit now runs the 6-point AIC grid (`decay_aic_table` carries all of `(0.0001, 0.001, 0.01, 0.1, 1.0, 10.0)`) and selects the AIC-min β instead of the kernel-blind β=0.1 (which understated η ~4×).
- Verified the AIC-selected β propagates to the held-out-LL, KS, eta-CI, and canonical-LL legs through the existing `hawkes_decays_t = float(hawkes_train["decays"])` — no new propagation logic needed.
- Added `test_run_fit_uses_free_beta_aic_grid` (RED→GREEN) asserting the production path's 6-entry AIC table + `fit_method_used == "scipy_canonical_ll"` + selected β == AIC-min β.
- Confirmed SC-5 byte-identity preserved under the free-β default: `test_byte_identical.py` + `test_fit_artifact_provenance.py` (10 passed) still green, since AIC selection is deterministic under thread-pinned BLAS.

## Task Commits

Each task was committed atomically:

1. **Task 1: Pre-reg authorization (AF-03 anchor)** - `3aa9a71` (docs)
2. **Task 2 RED: production-path free-β AIC test** - `d74650f` (test)
3. **Task 2 GREEN: run_fit decays=None rewire** - `2b0e8e1` (feat)

**Plan metadata:** (final docs commit — SUMMARY + STATE + ROADMAP)

## Files Created/Modified
- `notes/PRE_REGISTRATION.md` - Append-only `### Orchestrator production-path rewire (Plan 04.1.1-02b — AF-12 expansion)` subsection authorizing the `run_fit` `decays` default change; verdict gate stated unchanged.
- `analysis/src/abrigo_x402/dgp/orchestrator.py` - `run_fit` signature `decays: float | None = None` + docstring update documenting the free-β AIC default and the diagnostics-only float-pin path.
- `analysis/tests/test_dgp_orchestrator.py` - New `test_run_fit_uses_free_beta_aic_grid` with Pattern I thread-pinning + synthetic Hawkes(η=0.5) panel fixture.

## Decisions Made
- None beyond the plan — executed exactly as written. The fix was the localized one-line default change the BLOCKER diagnosis predicted; downstream propagation was verified (not duplicated).

## Deviations from Plan

None - plan executed exactly as written.

## AF-12 OUT-OF-SCOPE (verbatim)

- v2 EXPANSION (IN scope, THIS plan): `orchestrator.py` `run_fit` free-β AIC rewire — authorized by the pre-reg amendment in Task 1. This COMPLETES the Wave-1/2 rewire (which corrected the estimator functions but left the orchestrator pinned at β=0.1); it enforces the pre-reg v2 canonical-estimator definition, it does not expand it.
- STILL OUT: NO change to the estimator math (hawkes_fit.py / lr_test.py / profile_likelihood.py are consumed as-is — NO edits)
- STILL OUT: NO new Hawkes kernel forms (still exponential decay)
- STILL OUT: NO new firing conditions (still the existing 4)
- STILL OUT: NO new gate criteria (still the 4: branching_ci_excludes_zero, eta_floor=0.2, ks_held_out, lr_alpha=0.01)
- STILL OUT: NO change to AF-03 pre-registered thresholds (η floor 0.2, LR α 0.01, KS α 0.05, Q-9 floor 300)
- STILL OUT: NO new requirements
- STILL OUT: NO production-rep here (that is Plan 04.1.1-03)
- STILL OUT: NO hedge (Plan 04), NO VERIFICATION append (Plan 05)
- STILL OUT: NO re-fetch; NO PANEL-02 bump; NO Phase 5 absorption; NO synthetic deletion; NO overwrite of `ae9e3ba17900`

## Issues Encountered
None. The RED test failed exactly as predicted (`decay_aic_table` had 1 entry `{'0.1': 8380.59}` against the pinned default); GREEN passed once the default became None. The byte-identical / fit-artifact-provenance regression took ~17 min under thread-pinned BLAS (free-β grid is 6× the single-β work) but stayed deterministic and green.

## Next Phase Readiness
- **Plan 04.1.1-03 (production-rep) is now unblocked.** Re-running `cli.py fit` on the real ICHI cKES/USDT panel will record the four-criterion gate verdict on the AIC-selected β (the canonical estimator), not the kernel-blind β=0.1. The non-conforming `000c1cdce376` run dir (β=0.1, uncommitted) should be discarded by Plan 03 and replaced with a free-β run.
- No estimator-math files were touched; the Wave-1/2 corrections remain intact.

## Self-Check: PASSED

- FOUND: `analysis/tests/test_dgp_orchestrator.py`
- FOUND: `analysis/src/abrigo_x402/dgp/orchestrator.py` (signature `decays: float | None = None` at L226)
- FOUND: `notes/PRE_REGISTRATION.md` (`### Orchestrator production-path rewire` subsection)
- FOUND commit: `3aa9a71` (Task 1 pre-reg authorization)
- FOUND commit: `d74650f` (Task 2 RED)
- FOUND commit: `2b0e8e1` (Task 2 GREEN)
- AF-03 ordering: `3aa9a71` (pre-reg) predates `2b0e8e1` (orchestrator) — VERIFIED
- `grep -c "decays: float = 0.1" orchestrator.py` == 0 — VERIFIED
- No edits to hawkes_fit.py / lr_test.py / profile_likelihood.py — VERIFIED (git diff --stat empty)

---
*Phase: 04.1.1-fix-hawkes-likelihood-mode-fit*
*Completed: 2026-05-28*
