---
phase: 01-l1-data-fetch-skeleton-free-tier-discipline
plan: 08
type: execute
wave: 3
depends_on: [01-00, 01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07]
files_modified:
  - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md
  - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-SUMMARY.md
autonomous: true
requirements: [FETCH-01, FETCH-02, FETCH-03, FETCH-04]
must_haves:
  truths:
    - "Full vitest suite (pnpm -C fetch test --run) exits 0 with all Wave-1 + Wave-2 tests green"
    - "pnpm -C fetch exec tsc --noEmit exits 0"
    - "make verify-cache-idempotency exits 0 — two CLI invocations with identical args produce byte-identical payload files (sha256 match)"
    - "make leak-check exits 0 — no protocol-name branches, factory addresses, or magic fee tiers in fetch/src/"
    - "make schema-frozen-check exits 0 — Phase 0 baseline e9b214d preserved"
    - "pnpm -C fetch fetch ichi --dry-run --estimate-budget outputs valid JSON with total_queries < 30000 and exceeds_earmark=false"
    - "Verification summary written to .planning/phases/.../01-VERIFICATION-pre.md with per-requirement PASS/FAIL grid (FETCH-01..04 + each SC-1..SC-6)"
  artifacts:
    - path: ".planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md"
      provides: "Pre-verification summary: per-requirement evidence + transcripts + sha hashes + outstanding gaps"
    - path: ".planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-SUMMARY.md"
      provides: "Convention-compliant thin pointer SUMMARY (per checker M15) — orchestrator phase-globbing routinely scans for *-SUMMARY.md"
  key_links:
    - from: "01-VERIFICATION-pre.md"
      to: "ROADMAP Phase 1 SC-1..SC-6"
      via: "explicit row-per-criterion table with command + output + verdict"
      pattern: "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6"
---

<objective>
End-to-end Phase 1 acceptance run. Re-execute every verification command across all 8 prior plans, record outputs, and write a pre-verification summary at `.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md`. This document is the input to `/gsd:verify-work` and the predecessor of any `01-VERIFICATION.md` gap-closure trigger.

Purpose: Honest acceptance — verify FETCH-01 SC-1..SC-6 against actual command outputs, not against claims. If any criterion fails, document the gap rather than claim success.
Output: 01-VERIFICATION-pre.md with verbatim transcripts.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-CONTEXT.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VALIDATION.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-00-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-01-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-02-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-03-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-04-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-05-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-06-SUMMARY.md
@.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full acceptance command suite + capture transcripts</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 §Phase Details — SC-1..SC-6 verbatim)
    - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VALIDATION.md (per-task verification map)
    - Makefile (existing targets: schema-frozen-check, schema-probe, leak-check, verify-cache-idempotency)
  </read_first>
  <files>
    .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md
  </files>
  <action>
    Execute each acceptance command and capture stdout + stderr + exit code. Write the results into 01-VERIFICATION-pre.md in this exact format:

    ```markdown
    ---
    phase: 01-l1-data-fetch-skeleton-free-tier-discipline
    verification_pass: <true|false>
    generated: <ISO8601-UTC>
    ---

    # Phase 1 — Pre-Verification Summary

    Sampling commands executed in `/home/jmsbpp/apps/d2p/abrigo/abrigo-x402/` immediately after Wave-2 plans landed.

    ## Acceptance grid (FETCH-01..04 + ROADMAP SC-1..SC-6)

    | Criterion | Command | Exit | Verdict | Evidence |
    |---|---|---|---|---|
    | FETCH-01 SC-1 (pnpm install + tsc) | `pnpm install && pnpm -C fetch exec tsc --noEmit` | 0 | PASS | install log; tsc exit 0 |
    | FETCH-01 SC-1 (pins) | `pnpm -C fetch test tests/stack-pins.test.ts --run` | 0 | PASS | 8/8 pin assertions |
    | FETCH-01 SC-1 (analysis pins) | `grep -E "tick==0.8.0.2\|statsmodels==0.14.6\|polars==1.41.0" analysis/pyproject.toml \| wc -l` | 0 | PASS / FAIL | 3 expected |
    | FETCH-02 SC-2 (dry-run JSON) | `pnpm -C fetch fetch ichi --dry-run --estimate-budget \| jq -e '.total_queries < 30000'` | 0 | PASS | <verbatim JSON> |
    | FETCH-02 SC-2 (budget gate) | `pnpm -C fetch test tests/cost-ledger.test.ts --run -t "90k budget gate"` | 0 | PASS | 5 tests |
    | FETCH-03 SC-3 (subgraph wrapper) | `pnpm -C fetch test tests/freshness.test.ts --run -t "subgraphFreshness"` | 0 | PASS | 6 tests |
    | FETCH-03 SC-3 (blockscout wrapper) | `pnpm -C fetch test tests/freshness.test.ts --run -t "blockscoutFreshness"` | 0 | PASS | 3 tests |
    | FETCH-04 SC-4 (byte-identity) | `make verify-cache-idempotency` | 0 | PASS | sha1 == sha2 |
    | FETCH-01 SC-5 (leak gate) | `pnpm -C fetch test tests/protocol-agnostic.test.ts --run && make leak-check` | 0 | PASS | offenders == [] |
    | FETCH-02 SC-6 (cold-backfill estimate) | `pnpm -C fetch fetch ichi --dry-run --estimate-budget \| jq '{vault_count, total_queries, exceeds_earmark}'` | 0 | PASS | <verbatim> |
    | x402 mock round-trip | `pnpm -C fetch test tests/x402_mock.test.ts --run` | 0 | PASS | 3 tests |
    | Schema-frozen baseline (Phase 0) | `make schema-frozen-check` | 0 | PASS | no diff vs e9b214d |
    | Schema-probe (Phase 1 safe) | `make schema-probe` | 0 | PROBE_PASS | per-protocol TOMLs not scanned |

    ## Per-Requirement Evidence

    ### FETCH-01: TS workspace + pinned stack + Blockscout v2 REST client
    - pnpm install: <exit 0>
    - tsc --noEmit: <exit 0>
    - viem version: <verbatim from `pnpm -C fetch ls viem`>
    - @x402/fetch version: <verbatim>
    - @graphprotocol/client-x402 version: <verbatim>
    - @mento-protocol/mento-sdk version: <verbatim>
    - analysis/uv.lock: <ls + first 30 lines>
    - Blockscout v1 client: tests/blockscout-client.test.ts <N tests pass>

    ### FETCH-02: cost-ledger + 90k cap + --force
    - cost-ledger unit tests: 9 pass
    - x402 mock round-trip: 3 pass; ledger row written with endpoint=x402-mock-sepolia
    - dry-run JSON output verbatim: <full JSON>
    - exceeds_earmark: false; total_queries: <N>

    ### FETCH-03: subgraph + blockscout freshness
    - subgraphFreshness: 6 tests pass (lag=99, lag=101, missing _meta, missing block.number, custom threshold, single getBlockNumber call)
    - blockscoutFreshness: 3 tests pass (lag=99, lag=101, custom threshold)
    - block_consensus check: <grep -r "block_consensus" fetch/> → no hits (orchestrator finding #1 honored)

    ### FETCH-04: content-addressed cache + paid-step-idempotent
    - cacheKeyHash: 6 unit tests pass (case-invariance, chainId-sensitive, blockRange-sensitive, no fetchTimestamp)
    - manifest: 5 unit tests pass (read empty, append, idempotent on dup, findByCacheKey null, zod reject)
    - writeCachePayload: 2 unit tests pass (byte-identity, dataHash match)
    - `make verify-cache-idempotency`: PASS

    ## SC-4 Parquet → JSONL pivot (checker I6 — recognized concession)

    ROADMAP.md SC-4 verbiage says "Parquet". Phase 1 ships **JSONL append** (`fs.appendFile`) instead of streaming Parquet because:
    - hyparquet-writer has a documented high-frequency-small-write risk (RESEARCH.md §J fallback).
    - JSONL append is atomic per POSIX line, simpler, and byte-stable.
    - The byte-identity invariant (FETCH-04 SC-4) holds for JSONL: `sha256sum` on the `.jsonl` file is byte-stable across reruns. The SC-4 byte-identity test (`make verify-cache-idempotency`) is updated to find `.jsonl` files.
    - Phase 2 / Phase 5 batch-converts JSONL → Parquet via polars when the ledger is read; the on-disk Phase 1 representation does not need to be Parquet.

    Files affected: `data/raw/_cost_ledger.jsonl`, `data/raw/<protocol>/<prefix>/<hash>.jsonl`.
    ROADMAP.md SC-4 should be read as "byte-identical cache payload on second run" — file extension is implementation detail.

    ## Subgraph hunt verdict (orchestrator finding #2)
    - Phase 1 default: Blockscout-only — no [subgraphs.uniswap_v3] block added to protocols/ichi.toml at Phase 1
    - getSubgraphClient throws MissingGraphApiKeyError without GRAPH_API_KEY env (verified by test)
    - Phase 1.5 enrichment task pending: provision GRAPH_API_KEY → re-attempt _meta freshness check against candidate IDs Messari `9nh6Ums...ALYMqa` + Uniswap-tagged `t3uzAbri...iq1o`

    ## Outstanding gaps (if any)
    <list any FAIL rows here; if all PASS, write "None — Phase 1 closed.">

    ## Next step
    Run `/gsd:verify-work 01-l1-data-fetch-skeleton-free-tier-discipline` to produce the canonical 01-VERIFICATION.md or close-out Phase 1.
    ```

    The executor MUST run each command listed in the table, capture exit codes, and fill the verdict + evidence cells from real output (not inferred). Treat any non-zero exit as a FAIL row and surface it in the "Outstanding gaps" section — DO NOT silently retry until green. The summary is honest acceptance.

    If `make verify-cache-idempotency` requires a live Blockscout call (which would consume free-tier requests), the executor MAY skip the network leg and instead invoke the cache-idempotency unit test (`pnpm -C fetch test tests/cache.test.ts -t "byte-identity" --run`) as a Tier-1 substitute. Document the substitution explicitly.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md &amp;&amp; grep -q "verification_pass:" .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md &amp;&amp; grep -cE "FETCH-0[1234]|SC-[1-6]" .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md | awk '$1 &gt;= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    01-VERIFICATION-pre.md exists; frontmatter has verification_pass field; all 6 SCs documented with command + exit code + verdict; any failures surfaced honestly in Outstanding gaps.
  </done>
</task>

</tasks>

<verification>
1. 01-VERIFICATION-pre.md exists at `.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md`.
2. The file's frontmatter `verification_pass: true|false` field is set based on the grid.
3. All 6 ROADMAP SCs (SC-1..SC-6) have explicit rows with exit codes and verdicts.
4. Outstanding gaps section is either "None — Phase 1 closed" or a concrete list with paths to remediation.
</verification>

<success_criteria>
Honest pre-verification: all FETCH-01..04 satisfied with primary evidence (test counts, exit codes, transcripts). Phase 1 ready for `/gsd:verify-work` and Phase 2 hand-off.
</success_criteria>

<output>
Two artifacts (per checker M15 — convention requires a *-SUMMARY.md):

1. `.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md` — the substantive document (full grid, evidence, transcripts).
2. `.planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-SUMMARY.md` — a thin pointer summary so orchestrator globbing finds this plan's output. Contents:

```yaml
---
phase: 01-l1-data-fetch-skeleton-free-tier-discipline
plan: 08
generated: <ISO8601-UTC>
---

one_liner: "Phase 1 acceptance — see 01-VERIFICATION-pre.md for full grid"

key_files:
  created:
    - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-VERIFICATION-pre.md
    - .planning/phases/01-l1-data-fetch-skeleton-free-tier-discipline/01-08-SUMMARY.md

deviations: "01-VERIFICATION-pre.md is the substantive artifact; this SUMMARY is a pointer for orchestrator globbing (per checker M15)."

next_step: "Run /gsd:verify-work 01-l1-data-fetch-skeleton-free-tier-discipline"
```
</output>
