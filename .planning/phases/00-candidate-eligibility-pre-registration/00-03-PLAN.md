---
phase: 00-candidate-eligibility-pre-registration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/Q9_DECISION.md
autonomous: true
requirements:
  - REPRO-04
must_haves:
  truths:
    - "Q9_DECISION.md exists and is committed BEFORE Phase 6 fetch starts"
    - "V3-anchor-only primary panel is committed as headline"
    - "V3+V4+Broker unified fallback is pre-registered with NUMERIC trigger threshold (not deferred to Phase 6 ad-hoc decision)"
    - "Cross-class permutation test specification is fully detailed (1000 reps, null hypothesis, p-value threshold)"
    - "Phase 2 code obligation is documented (unified-panel pooling code lives in analysis/src/ from Phase 2 onwards, dead-code-exercised) — REPRO-02 invariant"
    - "Numeric trigger threshold matches notes/PRE_REGISTRATION.md Q-9 Fallback Pre-Registration section"
  artifacts:
    - path: "notes/Q9_DECISION.md"
      provides: "cCOP panel construction decision (V3-only primary + unified fallback) with pooling-assumption argument structure and REPRO-02 dead-code-exercise obligation"
      min_lines: 60
  key_links:
    - from: "notes/Q9_DECISION.md"
      to: "notes/PRE_REGISTRATION.md `## Q-9 Fallback Pre-Registration`"
      via: "shared numeric trigger threshold"
      pattern: "PRE_REGISTRATION"
    - from: "notes/Q9_DECISION.md"
      to: "REPRO-02 invariant"
      via: "analysis/src/ dead-code-exercise commitment"
      pattern: "REPRO-02|dead-code"
---

<objective>
Author `notes/Q9_DECISION.md` — the cCOP panel construction decision document. Locks V3-anchor-only as the primary panel + V3+V4+Broker unified as the pre-registered fallback (with numeric trigger threshold + cross-class permutation test specification). Defends against AF-03 spec-swap by committing the fallback path BEFORE Phase 6 fetch.

Purpose: Lock REPRO-04 (cCOP panel construction decision component). Enforces the REPRO-02 invariant by mandating that V3+V4+Broker pooling code AND cross-class permutation test code live in `analysis/src/` from Phase 2 onwards, dead-code-exercised by synthetic unit tests in Iteration 1.
Output: `notes/Q9_DECISION.md`, committed to git BEFORE Phase 6 fetch starts (which is forward-looking; analysis/src/ doesn't exist yet in Phase 0).
</objective>

<context>
@.planning/phases/00-candidate-eligibility-pre-registration/00-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/CANDIDATES.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@./CLAUDE.md
</context>

<tasks>

<task id="1" type="auto">
  <name>Task 1: Author notes/Q9_DECISION.md with V3-only primary + unified fallback pre-registration</name>
  <files>notes/Q9_DECISION.md</files>
  <read_first>
    - .planning/phases/00-candidate-eligibility-pre-registration/00-CONTEXT.md (Q-9 cCOP decision locked: V3-only primary + unified fallback; Phase 2 code obligation; numeric trigger threshold Claude's Discretion)
    - .planning/research/CANDIDATES.md (§7 Hidden-Volume Audit; §7.5 open question #9 cCOP additive channels Broker + V4; §7.3 corrected counts cCOP/USDT ~580–625/30d V3 + ~185 Broker + ~90 V4 ≈ ~900 unified)
    - .planning/research/PITFALLS.md (§4 four-criterion gate; §5 cross-leg dependence)
    - .planning/research/FEATURES.md (AF-03 spec-swap; AF-12 silent re-scope; D-03 pre-registration; TS-12 parameter-driven re-runnability — REPRO-02 source)
    - .planning/REQUIREMENTS.md (REPRO-04 verbatim; REPRO-02 invariant)
    - notes/PRE_REGISTRATION.md (Q-9 Fallback Pre-Registration section — Q9_DECISION.md must match thresholds verbatim)
  </read_first>
  <action>
Create `notes/Q9_DECISION.md` with the following structure. Use these EXACT H2 headers:

# Header:
  - Title: `# Q-9 Decision: cCOP Panel Construction (Iteration 2 / Steer on cCOP/USDT)`
  - Committed: `**Committed:** 2026-05-25`
  - Status: "Locked — V3-anchor-only primary panel + V3+V4+Broker unified fallback. Both paths pre-registered. Spec swap after seeing data is forbidden (AF-03)."
  - Cross-reference: "This decision is also recorded in `notes/PRE_REGISTRATION.md` `## Q-9 Fallback Pre-Registration` — both files MUST agree on numeric trigger threshold values."

## Background
  - The question (per CANDIDATES.md §7 Hidden-Volume Audit + REQUIREMENTS.md REPRO-04):
    "cCOP panel construction — V3-anchor-only (~580–625 swaps/30d) OR V3+V4+Broker unified (~900 events/30d adding Mento V2 Broker ~185/30d + Uniswap V4 PoolManager cCOP routing ~90/30d)."
  - Stakes: AF-03 spec-swap-after-seeing-results vs. AF-12 silent-re-scope. The fallback choice MUST be pre-registered to avoid an ad-hoc Phase-6 spec swap after seeing V3-only fit results.

## Primary Decision: V3-Anchor-Only

**Headline panel: cCOP/USDT Uniswap V3 Swap stream on pool `0x2AC5baA668A8A58FD0e302B9896717484fd217B0`.**

  - Event class: Uniswap V3 `Swap` events (topic `0xc42079f9...`).
  - Volume: ~580–625 swaps/30d per CANDIDATES.md §7.3 (corrected count post-thinness-retraction).
  - Why primary: No pooling assumption to defend at the headline. Single-event-class panel construction is the cleanest reproducibility surface for the per-iteration swap-surface (REPRO-01 / REPRO-02).
  - Cost of choice: Ignores ~38% of cCOP cashflow signal (~375 swap-equivalents/30d from Broker + V4 channels per CANDIDATES §7.3). Acceptable v1 tradeoff: simpler model, fewer assumptions, lower spec-risk.

## Pre-Registered Fallback: V3+V4+Broker Unified

**Switch trigger (BOTH conditions MUST hold):**

  1. **Sample-size or precision floor failure**: The V3-only sample fails one of:
     - V3-only sample size < 300 events over the fitted window (Hawkes branching-ratio identifiability floor per PITFALLS §4)
     - Hawkes branching-ratio profile-likelihood CI width > 0.4 (conventional precision floor — wider CI means η is not usefully bounded)
  2. **Cross-class permutation test passes (fails to reject pooling assumption)**: Cross-class permutation test (1000 reps; null hypothesis = three event classes V3 Swap + V4 PoolManager + Mento V2 Broker share common arrival-process structure; alternative = at least one class differs) returns p > 0.05.

**If both hold:** Switch to V3+V4+Broker unified panel.
  - V3 Swap events on `0x2AC5baA668A8A58FD0e302B9896717484fd217B0`
  - V4 PoolManager events on `0x288dc841A52FCA2707c6947B3A777c5E56cd87BC` filtered to cCOP-counterparty (~90 V4 swap-equivalents/30d)
  - Mento V2 Broker mint/burn events on `0x777A8255cA72412f0d706dc03C9D1987306B4CaD` filtered to cCOP-counterparty (~185 swap-equivalents/30d)
  - Total: ~900 events/30d per CANDIDATES §7.3.

**If V3-only sample passes precision floor OR permutation test rejects pooling:** Stay on V3-anchor-only; the fit is interpreted with the V3-only signal scope explicitly stated.

**Why this is pre-registration, not Phase-6 ad-hoc decision:** AF-03 (spec-swap-after-seeing-results) forbids changing the headline specification after the data fit. The fallback path is committed HERE with explicit numeric trigger conditions; Phase 6 only OBSERVES whether the conditions hold, never DECIDES whether to switch.

## Cross-Class Permutation Test Specification

The pooling assumption (V3 + V4 + Broker share a common arrival-process structure) is the load-bearing claim if the fallback fires. The permutation test specification:

  - **Null hypothesis:** The inter-event-time distributions of V3 Swap events, V4 PoolManager cCOP-touching events, and Mento V2 Broker cCOP mint/burn events are draws from the same arrival process (common λ(t) or common Hawkes self-excitation structure).
  - **Test statistic:** Cross-class Kolmogorov-Smirnov D statistic between the three pairwise distributions of inter-event times: max(D_{V3,V4}, D_{V3,Broker}, D_{V4,Broker}).
  - **Permutation procedure:** 1000 permutations randomly relabeling event-class labels across the pooled event stream while preserving timestamps; recompute D under each permutation; build empirical null distribution.
  - **Decision rule:** Reject the pooling assumption if observed D > 95th percentile of permuted D distribution (p < 0.05). FAIL to reject (p > 0.05) → pooling assumption is statistically permissible, fallback may proceed.
  - **Implementation: `analysis/src/abrigo_x402/panel/cross_class_permutation.py`** with deterministic seed for reproducibility. Test in `analysis/tests/test_cross_class_permutation.py` validates against synthetic data with known null + alternative distributions.

## Phase 2 Code Obligation (REPRO-02 Invariant)

Per REQUIREMENTS.md REPRO-02 ("Iteration 2 must run the same Phase 2–5 pipeline end-to-end on Steer-on-cCOP/USDT with no edits to fetch/src or analysis/src"):

**Both the unified-panel pooling code AND the cross-class permutation test code MUST live in `analysis/src/` from Phase 2 onwards, dead-code-exercised by synthetic unit tests in Iteration 1.**

Specifically:
  - `analysis/src/abrigo_x402/panel/unified.py` — builds the V3+V4+Broker unified panel from three event-class inputs. Exists in Phase 2 (Iteration 1), dead-code-exercised by `analysis/tests/test_unified_panel_synthetic.py` against synthetic three-class fixtures.
  - `analysis/src/abrigo_x402/panel/cross_class_permutation.py` — performs the permutation test specified above. Exists in Phase 2, dead-code-exercised by `analysis/tests/test_cross_class_permutation.py`.
  - `analysis/src/abrigo_x402/panel/__init__.py` — exports `build_v3_anchor_panel()` AND `build_unified_panel()` AND `cross_class_permutation_test()`. Both signatures present in Phase 2.

This obligation is enforced by the Phase 1 SC-5 protocol-agnosticism contract test + the REPRO-01 leak-gate `grep -ri "ichi" fetch/src analysis/src` (must return 0 before Phase 6 starts).

## Pooling-Assumption Argument Structure

If the fallback fires, the report (Iteration 2 `reports/steer.pdf` or `reports/steer_null_result.pdf`) MUST include the following argument structure:

  1. State the pooling assumption: "We pool V3 Swap + V4 PoolManager + Mento V2 Broker events as a unified arrival process for cCOP."
  2. Justify empirically: "The cross-class permutation test (1000 reps) returned p = [X] > 0.05, failing to reject the common-arrival-structure null."
  3. State the consequence: "Headline DGP parameters (μ, α, β, η) are estimated on the unified panel."
  4. Quantify the alternative: "Under V3-only panel, the same fit produces parameters (μ', α', β', η') with branching-ratio profile-likelihood CI width [W'] > 0.4 (precision floor failure trigger)."
  5. State the sensitivity: "The convex-dominance / hedge-strip conclusions are reported separately under V3-only and unified panels in the cost-leg sensitivity sweep (REPORT-03 / D-09)."

This argument structure must be templated into the Phase 5 / Phase 6 report build so it cannot be silently omitted.

## Sources

  - CANDIDATES.md §7 Hidden-Volume Audit (channels: V3 + V4 + Broker)
  - CANDIDATES.md §7.3 (corrected event counts: V3 ~580–625, V4 ~90, Broker ~185)
  - CANDIDATES.md §7.5 Q9 (open question #9: include all three event classes / V3 only / V3 + covariates)
  - PITFALLS.md §4 (Hawkes branching-ratio identifiability + profile-likelihood CIs)
  - PITFALLS.md §5 (cross-leg dependence; multivariate Hawkes specification)
  - FEATURES.md AF-03 (spec-swap discipline); AF-12 (silent re-scope); D-03 (pre-registration)
  - REQUIREMENTS.md REPRO-02 (parameter-driven re-runnability invariant)
  - REQUIREMENTS.md REPRO-04 (cCOP panel construction decision)
  - notes/PRE_REGISTRATION.md `## Q-9 Fallback Pre-Registration` (must match this document verbatim on numeric thresholds)

---
*Q-9 Decision Locked: 2026-05-25*

After authoring, verify the numeric trigger threshold values (sample < 300 events; CI width > 0.4; permutation test p > 0.05) match notes/PRE_REGISTRATION.md `## Q-9 Fallback Pre-Registration` verbatim. If PRE_REGISTRATION.md committed different values, update it via amendment (these two files MUST agree).

Commit to git with message: `docs(00): commit Q9_DECISION.md (REPRO-04 decision + REPRO-02 dead-code-exercise obligation)` — push to `origin = JMSBPP/abrigo-x402` per CLAUDE.md Git workflow.
  </action>
  <verify>
    <automated>test -f notes/Q9_DECISION.md && grep -c "## Primary Decision: V3-Anchor-Only" notes/Q9_DECISION.md && grep -c "## Pre-Registered Fallback: V3\\+V4\\+Broker Unified" notes/Q9_DECISION.md && grep -c "## Cross-Class Permutation Test Specification" notes/Q9_DECISION.md && grep -c "## Phase 2 Code Obligation" notes/Q9_DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f notes/Q9_DECISION.md` exits 0
    - `grep -c "^## Background" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Primary Decision: V3-Anchor-Only" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Pre-Registered Fallback" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Cross-Class Permutation Test Specification" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Phase 2 Code Obligation" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Pooling-Assumption Argument Structure" notes/Q9_DECISION.md` returns 1
    - `grep -c "^## Sources" notes/Q9_DECISION.md` returns 1
    - `grep -c "0x2AC5baA668A8A58FD0e302B9896717484fd217B0" notes/Q9_DECISION.md` returns >= 1 (cCOP/USDT V3 pool)
    - `grep -c "0x288dc841A52FCA2707c6947B3A777c5E56cd87BC" notes/Q9_DECISION.md` returns >= 1 (V4 PoolManager)
    - `grep -c "0x777A8255cA72412f0d706dc03C9D1987306B4CaD" notes/Q9_DECISION.md` returns >= 1 (Mento V2 BrokerProxy)
    - `grep -c "300 events" notes/Q9_DECISION.md` returns >= 1 (sample-size trigger)
    - `grep -c "0.4" notes/Q9_DECISION.md` returns >= 1 (CI width threshold)
    - `grep -c "1000" notes/Q9_DECISION.md` returns >= 1 (permutation reps)
    - `grep -c "REPRO-02" notes/Q9_DECISION.md` returns >= 1
    - `grep -c "REPRO-04" notes/Q9_DECISION.md` returns >= 1
    - `grep -c "AF-03" notes/Q9_DECISION.md` returns >= 1 (spec-swap defense)
    - `grep -c "dead-code-exercise" notes/Q9_DECISION.md` OR `grep -c "dead.code.exercised" notes/Q9_DECISION.md` returns >= 1
    - `grep -c "analysis/src/" notes/Q9_DECISION.md` returns >= 1
    - `wc -l notes/Q9_DECISION.md` shows >= 60 lines
    - `git log --oneline -- notes/Q9_DECISION.md` returns at least one commit
    - Sanity cross-check (deterministic): `comm -3 <(grep -E "(300 events|0\.4|p > 0\.05|p < 0\.05)" notes/Q9_DECISION.md | sort -u) <(grep -E "(300 events|0\.4|p > 0\.05|p < 0\.05)" notes/PRE_REGISTRATION.md | sort -u) | wc -l` returns 0 (asymmetric difference empty → both files contain the same threshold strings verbatim)
  </acceptance_criteria>
  <done>
Q9_DECISION.md exists with all 8 required H2 sections, V3-anchor-only primary headline, V3+V4+Broker unified fallback with explicit numeric trigger conditions (sample < 300 OR CI width > 0.4 AND permutation p > 0.05), cross-class permutation test fully specified (1000 reps, K-S D statistic, decision rule), Phase 2 code obligation explicit (analysis/src/abrigo_x402/panel/unified.py + cross_class_permutation.py + __init__.py with dead-code-exercise), pooling-assumption argument structure templated for Phase 5/6 report build, all addresses cited (cCOP/USDT pool, V4 PoolManager, Mento V2 BrokerProxy), AF-03 spec-swap defense explicit, REPRO-02 + REPRO-04 references explicit, ≥60 lines, committed to git, numeric thresholds match PRE_REGISTRATION.md verbatim.
  </done>
</task>

</tasks>

<verification>
- All acceptance criteria pass
- Numeric thresholds in Q9_DECISION.md match PRE_REGISTRATION.md (cross-check)
- Phase 2 code obligation is explicit enough for the executor of a future Phase 2 plan to understand WHAT files must exist with WHAT exports
</verification>

<success_criteria>
- ROADMAP SC-3 satisfied (Q-9 cCOP panel construction decision committed before Phase 6 fetch starts)
- REPRO-04 (decision component) fully addressed
- REPRO-02 invariant enforced via dead-code-exercise obligation
- AF-03 spec-swap defense is structurally complete (fallback path is pre-committed with explicit numeric triggers)
</success_criteria>

<output>
After completion, create `.planning/phases/00-candidate-eligibility-pre-registration/00-03-SUMMARY.md` documenting: (a) the final numeric trigger threshold values committed (sample size floor, CI width floor, permutation p-value threshold); (b) the cross-check status against PRE_REGISTRATION.md (must match); (c) the git commit hash of Q9_DECISION.md.
</output>
