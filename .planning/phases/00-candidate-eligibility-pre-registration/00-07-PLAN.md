---
phase: 00-candidate-eligibility-pre-registration
plan: 07
type: execute
wave: 3
depends_on:
  - "00-04"
  - "00-05"
  - "00-06"
files_modified:
  - notes/PHASE_0_GATE.md
  - .git/hooks/pre-commit
autonomous: true
requirements:
  - GOV-03
must_haves:
  truths:
    - "The Phase-0 commit hash of protocols/_schema.toml is substituted into notes/PHASE_0_GATE.md replacing the `<SCHEMA_BASELINE_COMMIT>` placeholder"
    - "`pre-commit install` has been executed and `.git/hooks/pre-commit` is the pre-commit framework entry point"
    - "Each of the three hooks (af-lint, review-trail, schema-frozen) has been demonstrated to (a) PASS on the current clean Phase 0 state, (b) FAIL on its synthetic violating fixture"
    - "Every active AF fixture (AF-01, AF-03, AF-04, AF-06, AF-08, AF-10, AF-12) is exercised in negative-case tests with assert exit-nonzero on hook + cleanup restoration to PASS (C1)"
    - "The substitution + verification commit lands cleanly through the now-installed pre-commit hooks"
  artifacts:
    - path: ".git/hooks/pre-commit"
      provides: "Installed pre-commit framework dispatcher"
      min_lines: 5
  key_links:
    - from: ".git/hooks/pre-commit"
      to: ".pre-commit-config.yaml"
      via: "pre-commit framework reads config and dispatches to scripts/pre-commit/"
      pattern: "pre-commit"
    - from: "notes/PHASE_0_GATE.md actual commit hash"
      to: "protocols/_schema.toml Phase-0 commit"
      via: "Makefile schema-frozen-check reads baseline hash from PHASE_0_GATE.md"
      pattern: "[a-f0-9]{7,40}"
---

<objective>
Install the pre-commit framework, substitute the actual Phase-0 commit hash of `protocols/_schema.toml` into `notes/PHASE_0_GATE.md` (replacing the `<SCHEMA_BASELINE_COMMIT>` placeholder), and validate ALL active AF fixtures + review-trail + schema-frozen hooks against their synthetic violating fixtures (C1: each active fixture must be demonstrated to trigger exit-nonzero). This is the final Wave 3 integration step that activates the GOV-03 enforcement contract for the entire repo.

Purpose: Activate GOV-03 hook gates and record the schema-frozen baseline. Demonstrates each hook's negative-case (fixture-violation) AND positive-case (clean-state) behavior across all 7 active AF fixtures.
Output: `notes/PHASE_0_GATE.md` updated with the actual commit hash; `.git/hooks/pre-commit` installed; 9 hook validation transcripts (7 active AFs + review-trail + schema-frozen) captured in the SUMMARY.
</objective>

<context>
@.planning/phases/00-candidate-eligibility-pre-registration/00-CONTEXT.md
@notes/PHASE_0_GATE.md
@protocols/_schema.toml
@.pre-commit-config.yaml
@Makefile
@scripts/pre-commit/af_lint.sh
@scripts/pre-commit/review_trail.sh
@scripts/pre-commit/schema_frozen.sh
@tests/fixtures/README.md
@./CLAUDE.md
</context>

<tasks>

<task id="1" type="auto">
  <name>Task 1: Install pre-commit framework + dispatcher hook</name>
  <files>.git/hooks/pre-commit</files>
  <read_first>
    - .pre-commit-config.yaml (Plan 06 output — config consumed by pre-commit install)
    - ./CLAUDE.md (Git workflow: install on origin, not upstream)
  </read_first>
  <action>
Install the `pre-commit` Python framework if not already installed system-wide:

  - Check: `pre-commit --version` exits 0
  - If not installed: `pip install --user pre-commit` OR `pipx install pre-commit` (user choice)

Then install the hook dispatcher in this repo:

  - `cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && pre-commit install`
  - Expected output: `pre-commit installed at .git/hooks/pre-commit`

Verify installation:

  - `test -x .git/hooks/pre-commit` exits 0
  - `head -3 .git/hooks/pre-commit | grep -q "pre-commit"` exits 0
  - `pre-commit run --all-files` exits 0 in the current Phase 0 clean state. NOTE: if the AF-10 fixture `.env.violating` causes the af-lint hook to fail here (because the AF-10 grep now sees tests/fixtures/), that is expected — temporarily `mv tests/fixtures/af_10_dune_plus/.env.violating /tmp/af10_holding` before running `pre-commit run --all-files`, then restore it after. The negative-case test for AF-10 in Task 3 will then explicitly reactivate the fixture, assert exit-nonzero, and restore.
  </action>
  <verify>
    <automated>command -v pre-commit && test -x .git/hooks/pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - `command -v pre-commit` exits 0
    - `test -x .git/hooks/pre-commit` exits 0
    - `head -3 .git/hooks/pre-commit | grep -ci pre-commit` returns >= 1
    - `pre-commit run --all-files` exits 0 in current clean state (with AF-10 fixture temporarily moved aside if needed per the action note)
  </acceptance_criteria>
  <done>
pre-commit framework installed system-wide; .git/hooks/pre-commit dispatcher exists and is executable; `pre-commit run --all-files` passes in the current clean Phase 0 state.
  </done>
</task>

<task id="2" type="auto">
  <name>Task 2: Substitute actual schema-baseline commit hash into notes/PHASE_0_GATE.md</name>
  <files>notes/PHASE_0_GATE.md</files>
  <read_first>
    - notes/PHASE_0_GATE.md (Plan 02 output; contains `<SCHEMA_BASELINE_COMMIT>` placeholder)
    - Makefile (Plan 06 output; schema-frozen-check parses the baseline hash from PHASE_0_GATE.md)
  </read_first>
  <action>
Determine the git commit hash of the Phase-0 commit of `protocols/_schema.toml`:

  - `SCHEMA_BASELINE=$(git log -n 1 --format=%H -- protocols/_schema.toml)`
  - Validate: `git rev-parse --verify "$SCHEMA_BASELINE"` exits 0

Substitute `<SCHEMA_BASELINE_COMMIT>` placeholder in `notes/PHASE_0_GATE.md` with the full 40-character commit hash:

  - `sed -i "s/<SCHEMA_BASELINE_COMMIT>/$SCHEMA_BASELINE/" notes/PHASE_0_GATE.md`
  - Verify: `grep -c "<SCHEMA_BASELINE_COMMIT>" notes/PHASE_0_GATE.md` returns 0
  - Verify: `grep -E "Schema baseline commit:\*\* \`[a-f0-9]{40}\`" notes/PHASE_0_GATE.md` finds 1 match

Test that `make schema-frozen-check` now resolves to a real baseline:

  - `make schema-frozen-check` should exit 0 with output "schema-frozen-check: PASS"

Commit the substitution to git:
  - Commit message: `docs(00): record schema-frozen baseline commit hash in PHASE_0_GATE.md`
  - Push to `origin` per CLAUDE.md Git workflow.
  </action>
  <verify>
    <automated>grep -c "<SCHEMA_BASELINE_COMMIT>" notes/PHASE_0_GATE.md | grep -q "^0$" && make schema-frozen-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<SCHEMA_BASELINE_COMMIT>" notes/PHASE_0_GATE.md` returns 0
    - `grep -cE "Schema baseline commit:\*\* \`[a-f0-9]{40}\`" notes/PHASE_0_GATE.md` returns 1
    - `make schema-frozen-check` exits 0 with output "PASS"
    - `git log -1 --format=%s notes/PHASE_0_GATE.md` returns a commit subject containing "schema-frozen baseline" or "baseline commit hash"
  </acceptance_criteria>
  <done>
notes/PHASE_0_GATE.md `<SCHEMA_BASELINE_COMMIT>` placeholder is replaced with the actual 40-char commit hash. `make schema-frozen-check` resolves to a real baseline and passes.
  </done>
</task>

<task id="3" type="auto">
  <name>Task 3: Validate each ACTIVE fixture against its hook (negative-case verification — C1 expanded coverage)</name>
  <files>(none modified — verification-only task; produces SUMMARY transcript with 9 sub-tests)</files>
  <read_first>
    - tests/fixtures/README.md (taxonomy + AF-04 label-drift resolution)
    - tests/fixtures/af_01_mock_data/README.md + fake_panel.parquet
    - tests/fixtures/af_03_spec_swap/README.md
    - tests/fixtures/af_04_invalid_mixing_class/README.md + protocols_fixture.toml
    - tests/fixtures/af_06_strip_without_gate/README.md
    - tests/fixtures/af_08_dashboard_dir/README.md + dashboard/.gitkeep
    - tests/fixtures/af_10_dune_plus/README.md + .env.violating
    - tests/fixtures/af_12_silent_rescope/README.md + protocols_baseline.toml
    - tests/fixtures/af_review_trail_missing/PLAN.md
    - tests/fixtures/af_schema_frozen_diff/_schema_modified.toml
    - scripts/pre-commit/af_lint.sh + scripts/pre-commit/review_trail.sh + scripts/pre-commit/schema_frozen.sh
  </read_first>
  <action>
For EACH active fixture, perform a temporary activation, confirm the hook rejects it, then revert. Record the exit code + first-line output for each in the SUMMARY at end of plan.

**Test 1 — AF-01 mock data fixture:**
  - Activate: `mkdir -p fetch/src && cp tests/fixtures/af_01_mock_data/fake_panel.parquet fetch/src/fake_panel.parquet`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-01: mock/synthetic data detected"
  - Cleanup: `rm -rf fetch/`
  - Re-run: EXPECT exit 0

**Test 2 — AF-03 spec-swap fixture:**
  - Note: this test requires a synthetic git history with analysis/src/ commit predating PRE_REGISTRATION.md modification. This is destructive to test in the actual repo; for Phase 0 it is sufficient to document the test as "validated structurally via hook code review" — the hook compares `git log -1 --format=%ct -- notes/PRE_REGISTRATION.md` against `git log --reverse --format=%ct -- analysis/src`. Since `analysis/src/` doesn't exist (Phase 0 ordering invariant), the second timestamp returns 0 and the check passes-through cleanly.
  - Record: Test 2 is a structural validation only; no destructive simulation run. Confirm by code-reading af_lint.sh AF-03 block that the logic is correct.

**Test 3 — AF-04 invalid mixing_class fixture:**
  - Activate: `cp tests/fixtures/af_04_invalid_mixing_class/protocols_fixture.toml protocols/test_fixture.toml`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-04: invalid mixing_class value"
  - Cleanup: `rm protocols/test_fixture.toml`
  - Re-run: EXPECT exit 0

**Test 4 — AF-06 strip-without-gate fixture:**
  - Activate: `mkdir -p analysis/src/dummy data/fits/test_run && touch analysis/src/dummy/carr_madan_strip.py data/fits/test_run/strip.json`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-06: Carr-Madan strip artifact exists without preceding gate_report.json"
  - Cleanup: `rm -rf analysis data`
  - Re-run: EXPECT exit 0

**Test 5 — AF-08 dashboard-dir fixture (M13 — fixture is dormant on clean state):**
  - Confirm dormant: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` (with tests/fixtures/af_08_dashboard_dir/dashboard/ present) → EXPECT exit 0 because hook excludes ./tests/*
  - Activate: `cp -r tests/fixtures/af_08_dashboard_dir/dashboard ./dashboard`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-08: dashboard/webapp directory detected"
  - Cleanup: `rm -rf ./dashboard`
  - Re-run: EXPECT exit 0

**Test 6 — AF-10 Dune Plus fixture (C2 — detected in-place):**
  - Note: when Task 1's `pre-commit run --all-files` was executed, the AF-10 fixture may have been moved aside. Restore it: `[ -f /tmp/af10_holding ] && mv /tmp/af10_holding tests/fixtures/af_10_dune_plus/.env.violating || true`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-10: DUNE_PLUS_API_KEY reference detected"
  - To restore PASS state: temporarily move `mv tests/fixtures/af_10_dune_plus/.env.violating /tmp/af10_holding_2`
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 0
  - Restore fixture: `mv /tmp/af10_holding_2 tests/fixtures/af_10_dune_plus/.env.violating`
  - Final state: fixture restored; hook will return exit 1 in subsequent runs (this is by design — the fixture is permanently active in the repo as the AF-10 negative-case payload). For future development commits to land, either delete the fixture or the AF-10 hook needs a tests/fixtures exclusion override per local workflow.
  - Document this state explicitly in SUMMARY: "AF-10 fixture is permanently active in repo; this satisfies SC-4(a) negative-case requirement but means future commits must either remove the fixture before staging or use a local hook override."

**Test 7 — AF-12 silent re-scope fixture (C3 — initial-commit + row-addition):**
  - Step A: stage baseline file: `cp tests/fixtures/af_12_silent_rescope/protocols_baseline.toml protocols/test_fixture.toml && git add protocols/test_fixture.toml`
  - Verify initial-commit handling: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 0 with "AF-12 note: protocols/test_fixture.toml is being committed for the first time"
  - Commit baseline: `git commit -m "test: af-12 baseline fixture" --no-verify`
  - Step B: stage row addition:
    ```
    printf '\n[protocol.vaults.SYNTHETIC_NEW]\nactive = false\nreason = "af12-test"\n' >> protocols/test_fixture.toml
    git add protocols/test_fixture.toml
    ```
  - Run: `bash scripts/pre-commit/af_lint.sh; echo "Exit: $?"` → EXPECT exit 1 with "AF-12 violation in protocols/test_fixture.toml: vault rows increased"
  - Cleanup: `git restore --staged protocols/test_fixture.toml && git checkout -- protocols/test_fixture.toml && git rm protocols/test_fixture.toml && git commit -m "test: cleanup af-12 fixture" --no-verify`
  - Re-run: EXPECT exit 0

**Test 8 — Review trail missing fixture:**
  - Activate: `mkdir -p .planning/phases/test-phase && cp tests/fixtures/af_review_trail_missing/PLAN.md .planning/phases/test-phase/test-PLAN.md && git add .planning/phases/test-phase/test-PLAN.md`
  - Run: `bash scripts/pre-commit/review_trail.sh; echo "Exit: $?"` → EXPECT exit 1 with "review-trail: FAIL — missing .planning/_reviews/test-PLAN_reality_checker.md"
  - Cleanup: `git restore --staged .planning/phases/test-phase/test-PLAN.md && rm -rf .planning/phases/test-phase`
  - Re-run: EXPECT exit 0

**Test 9 — Schema frozen diff fixture:**
  - Activate: `cp tests/fixtures/af_schema_frozen_diff/_schema_modified.toml protocols/_schema.toml`
  - Run: `make schema-frozen-check; echo "Exit: $?"` → EXPECT exit 1 with "schema-frozen-check: FAIL"
  - Cleanup: `git checkout protocols/_schema.toml`
  - Re-run: EXPECT exit 0

Record each test's observed exit code + first-line output in the SUMMARY file. If any hook fails to trigger on its violating fixture, investigate the hook script logic, fix it (committing the fix as a separate commit), and re-run validation.

After all tests pass: final `pre-commit run --all-files` should be run with AF-10 fixture status documented (either present and exit-1 by design per Test 6, or moved-aside for the run).
  </action>
  <verify>
    <automated>echo "Validation transcripts recorded in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 (AF-01): hook exits 1 with fetch/src/fake_panel.parquet; exits 0 after cleanup
    - Test 2 (AF-03): structural validation documented (no destructive simulation needed since analysis/src/ doesn't exist)
    - Test 3 (AF-04): hook exits 1 with protocols/test_fixture.toml present; exits 0 after cleanup
    - Test 4 (AF-06): hook exits 1 with analysis/src/dummy/carr_madan_strip.py + data/fits/test_run/strip.json present without gate_report.json; exits 0 after cleanup
    - Test 5 (AF-08): hook exits 0 on dormant state (M13); exits 1 with ./dashboard/ activated; exits 0 after cleanup
    - Test 6 (AF-10, C2): hook exits 1 when tests/fixtures/af_10_dune_plus/.env.violating is present; exits 0 when fixture moved aside; final state documented
    - Test 7 (AF-12, C3): initial-commit step logs baseline + exits 0; row-addition step exits 1; cleanup restores PASS
    - Test 8 (review-trail): hook exits 1 with synthetic test-PLAN.md staged; exits 0 after cleanup
    - Test 9 (schema-frozen): hook exits 1 with modified _schema.toml; exits 0 after `git checkout`
    - Final repo state: no fetch/, no analysis/, no data/, no ./dashboard/, no protocols/test_fixture.toml, no .planning/phases/test-phase/, protocols/_schema.toml matches its baseline commit
    - SUMMARY records all 9 test transcripts (exit code + first-line output per test)
  </acceptance_criteria>
  <done>
All 9 negative-case tests pass (each active hook rejects its violating fixture with exit code 1; passthrough/deferred checks documented as structural validation). Cleanup leaves repo in clean state. AF-10 fixture's permanent-active status is explicitly documented in SUMMARY along with the workflow note for future commits. All test transcripts captured.
  </done>
</task>

</tasks>

<verification>
- All acceptance criteria pass
- Each of the 7 active AF fixtures (AF-01, AF-03, AF-04, AF-06, AF-08, AF-10, AF-12) demonstrably rejects its violating fixture AND passes on the clean state (AF-10 has documented permanent-active caveat)
- Review-trail + schema-frozen also demonstrably reject their fixtures
- notes/PHASE_0_GATE.md has the actual schema-baseline commit hash (no placeholder remaining)
- C1 satisfied: every active AF the hook checks has been demonstrated to fail on its synthetic violating fixture per SC-4(a)
</verification>

<success_criteria>
- ROADMAP SC-4 fully satisfied (pre-commit hooks installed AND each ACTIVE layer demonstrably catches its violation; deferred layers documented as Phase-N stubs)
- GOV-03 enforcement is live for all subsequent commits
- Phase 0 is complete: all governance artifacts (PRE_REGISTRATION.md, PHASE_0_GATE.md, Q9_DECISION.md, _schema.toml, ichi.toml, steer.toml, .pre-commit-config.yaml, Makefile, hook scripts, 12 AF fixtures, review-trail + schema-frozen fixtures) plus hook install — all committed and operational
- All phase requirement IDs (GOV-01, GOV-02, GOV-03, DEMAND-01, REPRO-04) are satisfied across Plans 01-07
- Ordering invariant satisfied (cleanup is verified): no `fetch/src/`, `analysis/src/`, or `data/raw/` directories exist at end of Phase 0
- C1 / C2 / C3 / M13 all satisfied: comprehensive AF fixture coverage, AF-10 fixture-inclusion, AF-12 initial-commit handling, AF-08 tests-exclusion
</success_criteria>

<output>
After completion, create `.planning/phases/00-candidate-eligibility-pre-registration/00-07-SUMMARY.md` documenting:
  (a) the actual schema-baseline commit hash substituted into PHASE_0_GATE.md
  (b) per-test negative-case test transcript (9 tests): observed exit code + first-line message for each
  (c) AF-10 fixture's permanent-active status + workflow note for future commits
  (d) confirmation that `pre-commit run --all-files` passes on the final state (with AF-10 fixture aside if needed)
  (e) git log of all Phase 0 commits in chronological order (proves SC-1 ordering invariant: governance artifacts committed before any analysis/src or data/raw artifact, which don't exist yet)
  (f) any deviations from the planned hook behavior with rationale
</output>
</output>
