---
phase: 00-candidate-eligibility-pre-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/PRE_REGISTRATION.md
autonomous: true
requirements:
  - GOV-01
  - REPRO-04
must_haves:
  truths:
    - "PRE_REGISTRATION.md exists and is committed BEFORE any commit under analysis/src/ or data/raw/"
    - "Kernel forms (Hawkes exponential + Kirchner INAR(p) NHPP) are fully specified with citations"
    - "All prior parameters have numeric values committed (rate_per_event grid, USD_per_query, α=0.01, η≥0.2)"
    - "REPRO-03 two-tier threshold (PASS ≥100k/mo / STRADDLE 30-100k / FAIL <30k) is committed with marginal-demand and below-window flag semantics"
    - "Q-9 fallback (V3-only primary; unified fallback with numeric trigger threshold) is pre-registered, not deferred to Phase 6"
    - "Q-7 TVL/event-count floor numeric values are committed"
    - "Deferred substrate section enumerates COPM, cXOF/USDm, BRLm/EURm, ~38 non-anchor ICHI vaults with reconsideration triggers"
    - "USDT (not USDC) depeg + USDT/USDC basis is the condition-4 framing per CLAUDE.md non-negotiables"
  artifacts:
    - path: "notes/PRE_REGISTRATION.md"
      provides: "Pre-fit governance document with kernel forms, priors, test statistics, acceptance regions, decision rules, fallbacks, deferred substrate"
      min_lines: 150
  key_links:
    - from: "notes/PRE_REGISTRATION.md"
      to: "SOMNIA_DRAFT.md §FUNCTIONAL FORM"
      via: "convex-dominance condition 4 citation (USDT depeg + USDT/USDC basis)"
      pattern: "USDT.*depeg.*basis|USDT/USDC basis"
    - from: "notes/PRE_REGISTRATION.md"
      to: "PITFALLS §4 boundary-correct LR test"
      via: "50:50 chi-squared(0):chi-squared(1) mixture citation"
      pattern: "50:50|χ²\\(0\\)|chi.?squared.*mixture"
---

<objective>
Author `notes/PRE_REGISTRATION.md` — the most substantive Phase 0 governance artifact. Commits all pre-fit numerical thresholds, test specifications, decision rules, fallback strategies, and deferred-substrate enumeration BEFORE any data fetch or estimation runs. This document is the audit-trail anchor for AF-03 (spec-swap-after-seeing-results) defense.

Purpose: Lock GOV-01 (pre-registration discipline) and REPRO-04 (Q-9 decision component). All downstream phases consume thresholds from this file.
Output: `notes/PRE_REGISTRATION.md`, committed to git BEFORE any `analysis/src/` or `data/raw/` artifact exists (verifiable via `git log --oneline -- notes/PRE_REGISTRATION.md` predating `analysis/src/` commits, which don't exist yet in Phase 0 — forward-looking ordering invariant).
</objective>

<context>
@.planning/phases/00-candidate-eligibility-pre-registration/00-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@.planning/research/CANDIDATES.md
@.planning/research/SUMMARY.md
@../abrigo-analytics/notes/SOMNIA_DRAFT.md
@./CLAUDE.md
</context>

<tasks>

<task id="1" type="auto">
  <name>Task 1: Author notes/PRE_REGISTRATION.md with all locked thresholds, fallbacks, and deferred substrate</name>
  <files>notes/PRE_REGISTRATION.md</files>
  <read_first>
    - .planning/phases/00-candidate-eligibility-pre-registration/00-CONTEXT.md (every locked decision and prior value — load-bearing source of truth)
    - .planning/research/PITFALLS.md (especially §1 thinness-retraction, §3 in-sample optimism, §4 LR-boundary correction, §6 cost-leg stipulation)
    - .planning/research/FEATURES.md (TS-04 demand-window gate, TS-15 null-result template, AF-01..AF-12 anti-feature catalogue, D-03 pre-registration)
    - .planning/research/CANDIDATES.md (§4.1 ICHI PASS verbatim, §4.2 Steer CONDITIONAL, §6 Q4/Q6/Q7 open questions, §7 hidden-volume audit and thinness retraction)
    - .planning/research/SUMMARY.md (statistical sources synthesis)
    - .planning/ROADMAP.md (Phase 0 SC-1 verbatim wording and ordering invariant; HEDGE-05 firing scope)
    - ../abrigo-analytics/notes/SOMNIA_DRAFT.md (§FUNCTIONAL FORM convex-dominance conditions including condition 4 USDT depeg + USDT/USDC basis; §ARRIVAL PROCESS NHPP/Hawkes spec)
    - ./CLAUDE.md (domain non-negotiables: USDT depeg + USDT/USDC basis, NOT USDC depeg; x402-on-Celo not Base)
  </read_first>
  <action>
Create `notes/PRE_REGISTRATION.md` as a substantive (≥150 lines) governance document with EXACTLY the following H2 sections in order. Use these exact section header strings (literal markdown):

# Header (top of file, before first H2):
  - Title: `# Pre-Registration: abrigo-x402 Iteration 1 (ICHI on cKES/USDT) + Iteration 2 (Steer on cCOP/USDT)`
  - Gathered: `**Committed:** 2026-05-25`
  - Status: `**Status:** Locked — no downstream phase may revise without an AF-03 audit-trail entry`
  - Discipline statement: "This document fixes all pre-fit numerical thresholds, test specifications, decision rules, and fallback paths BEFORE any vault-level estimation runs. Spec swap after seeing results is forbidden per FEATURES.md AF-03. Pre-commit hook enforces this contract (see `.pre-commit-config.yaml` review-trail enforcement)."

## Kernel Forms
  - NHPP specification: "Non-homogeneous Poisson process fit via Kirchner 2015 INAR(p) bin-count estimator on `statsmodels.tsa.api.VAR` with non-negativity projection. Bin width selection: AIC-min over grid `{1m, 5m, 15m, 1h}` per FEATURES.md AF-04 (canonical label: 'Hand-tuned bin width for INAR(p)' — no hand-tuned bin width allowed; AIC-min over the pre-registered grid is the locked selection rule). Source: Kirchner 2015 arxiv.org/abs/1509.02017."
  - Hawkes specification: "Bivariate (revenue leg × cost leg) Hawkes process via `tick.hawkes.HawkesExpKern` with exponential kernel and full off-diagonal excitation matrix `[[α_RR, α_RC], [α_CR, α_CC]]`. Diagonal-only shortcut is forbidden (DGP-02). Stationarity condition `||α/β||_∞ < 1` enforced. Source: Daw & Pender 2017 arxiv.org/pdf/1707.05143v3."
  - Power-law kernel (deferred): "Exponential kernel is the v1 headline; power-law kernel (Bacry et al.) is v2 robustness sweep (DGP-V2-01)."

## Prior Parameters
  - rate_per_event: "Three-point grid (1, 5, 10) queries/event. Central value 5 = headline; (1, 5, 10) = REPORT-03 sensitivity sweep replayed in Phase 5. Rationale: 1 = single subgraph poll per swap (minimum plausible); 5 = typical multi-component vault dashboard polling; 10 = heavy multi-vault aggregation."
  - USD_per_query: "$5e-6/query headline. Sensitivity sweep ±50%: ($2.5e-6, $5e-6, $7.5e-6) per REPORT-03 / FEATURES D-09. Defensible interpretation: opportunity-cost-of-free-tier-exhaustion at paid-tier marginal rate."
  - LR-test α-level: "α = 0.01 (defensive single-tier headline). Pre-committed for bootstrap-LR rig per DGP-03. Rationale: 50:50 χ²(0):χ²(1) mixture right-tail thickness + PITFALLS §4 boundary issues + AF-02 hand-tuned-p-value concern."
  - Branching-ratio η floor: "η ≥ 0.2 (conventional 'meaningful self-excitation' threshold). Pre-registered as a co-requirement alongside LR rejection at α = 0.01 for any Hawkes-positive claim."

## Test Statistics
  - Bootstrap LR: "Bootstrap likelihood-ratio test with 50:50 χ²(0):χ²(1) mixture null distribution. 1000 bootstrap reps simulating from fitted NHPP, refitting both models per rep. Vanilla `statsmodels.likelihood_ratio_test` is forbidden (boundary error). Sources: Filimonov & Sornette 2014 arxiv.org/pdf/1403.5227; arxiv.org/pdf/2410.05008; Wheatley ETH thesis."
  - Time-rescaling KS: "Brown et al. 2002 time-rescaling theorem applied to Hawkes residuals. Conventional threshold p > 0.05 for fail-to-reject under Hawkes calibration."
  - Profile-likelihood η-CI: "Profile likelihood for branching-ratio confidence intervals per PITFALLS §4 (Hessian-based MLE standard errors known to under-cover for Hawkes with small samples — Ogata 1978). EM-based estimator is acceptable alternative."
  - Cross-correlogram + permutation null: "DEPEND-01 spec: cross-correlogram between dK_revenue(t) and dK_cost(t) arrivals at lags from -T to +T with permutation null (1000 reps). Empirical copula fit follows; vine copula fallback only if BIC prefers."

## Acceptance Regions
  - Hawkes-positive claim requires ALL FOUR of the PITFALLS §4 four-criterion gate:
    1. Bootstrap LR rejects NHPP at α = 0.01
    2. η ≥ 0.2 (conventional self-excitation floor)
    3. Held-out KS test rejects NHPP rescaling under Hawkes-fitted parameters
    4. Branching-ratio profile-likelihood CI excludes zero
  - If ANY of the four fails: published as null-result per FEATURES TS-15 / HEDGE-05 firing condition (b) "NHPP-vs-Hawkes indistinguishable at conventional α".

## Decision Rules
  - HEDGE-05 firing conditions (per FEATURES TS-15) — any one fires null-result emission:
    (a) Phase-0 cost-leg gate fails (REPRO-03 threshold below window) — applies to Steer in Iteration 2 leading-firing-condition.
    (b) NHPP-vs-Hawkes indistinguishable per the four-criterion gate above.
    (c) Zero convex-dominance conditions hold per HEDGE-01 (vol-of-vol > 0; positive skew / fat tails; Hawkes self-excitation; USDT depeg + USDT/USDC basis jump — tail-risk condition).
  - Each firing produces `reports/{protocol}_null_result.pdf` (or memo-only for Phase 0 firings per ROADMAP HEDGE-05 scope).

## REPRO-03 Threshold
  - Two-tier semantics (NOT the binary ROADMAP draft):
    - **PASS**: primary-source evidence shows ≥ 100k Graph queries/mo attributable to Steer's Celo deployment. Proceed with Iteration 2 estimation.
    - **STRADDLE**: primary-source bound lands in [30k, 100k]/mo. Fires HEDGE-05 null with `marginal-demand` flag in `notes/steer_cost_leg_bound.md`. `reports/steer_null_result.pdf` ships documented as marginal-demand.
    - **FAIL**: primary-source bound < 30k/mo. Fires HEDGE-05 null with `below-window` flag. `reports/steer_null_result.pdf` ships documented as below-window.
  - Rationale: 100k/mo = Graph free-tier ceiling (DEMAND-01 canonical demand-window lower bound). 30k floor reflects paid-tier breakeven-ish band where Steer would *start* paying for queries but is structurally far from saturating the demand window.
  - Threshold committed BEFORE Phase 6 first execution. Phase-0 pre-validation (PHASE_0_GATE.md) attempts to resolve this for Steer at Phase 0 via primary-source enumeration (Blockscout + Steer docs + DefiLlama TVL extrapolation).

## Q-9 Fallback Pre-Registration
  - **Primary panel: V3-anchor-only** (cCOP/USDT Uniswap V3 pool `0x2AC5baA668A8A58FD0e302B9896717484fd217B0`, ~580–625 swaps/30d per CANDIDATES §7.3). No pooling assumption to defend at the headline level.
  - **Pre-registered fallback: V3+V4+Broker unified.** Switch is triggered IF AND ONLY IF BOTH conditions hold:
    1. V3-only sample size < 300 events over the fitted window (Hawkes branching-ratio identifiability floor per PITFALLS §4) OR Hawkes branching-ratio profile-likelihood CI width > 0.4 (conventional precision floor).
    2. Cross-class permutation test (1000 reps; null = three event classes V3 Swap / V4 PoolManager / Mento V2 Broker share common arrival-process structure) returns p > 0.05 — i.e., fails to reject the pooling assumption.
  - If both conditions hold: switch to unified panel (V3 + V4 PoolManager cCOP routing ~90/30d + Mento V2 Broker cCOP mint/burn ~185/30d ≈ ~900 events/30d total per CANDIDATES §7.3).
  - This switch is PRE-COMMITTED, not Phase-6 ad-hoc decision — AF-03 defense.
  - **Phase 2 code obligation:** The V3+V4+Broker pooling code AND the cross-class permutation test code MUST live in `analysis/src/` from Phase 2 onwards, dead-code-exercised by synthetic unit tests in Iteration 1. REPRO-02 invariant.

## Q-7 Floor
  - Pools below the floor are EXCLUDED from any aggregate (deferred substrate per Q-4 / SYNTH-V2-01).
  - **Threshold: TVL < $10k OR events < 30 per 30 days** (whichever is binding).
  - Affected pools (per CANDIDATES §2):
    - cXOF/USDm `0xAA97F0689660eA15b7d6f84F2E5250B63f2b381a` (TVL ~$11k — at floor; flagged)
    - BRLm/EURm `0xb6c8f9490314394CFc6EDacb8717bFDC1EB8dab5` (small; below floor)
  - Flagged in Deferred substrate below as v2-reconsideration candidates.

## Deferred Substrate
  - **COPM (Minteo) vaults** — `0xC92E8Fc2947E32F2B574CCA9F2F12097A71d5606` token; ICHI vaults `0x9F2bB8B7dFF141e1e35d05D6B8215BA8634fFce8`, `0xB52CfF57Cf94717193C63fbcdd50d09EdEe3FBF5`. Reason: v2-deferred per CONTEXT.md (Minteo controlled-broadening flag retained but not active in v1). Reconsideration trigger: v2 substrate expansion to Minteo-fintech mixing class.
  - **cXOF/USDm pool** (`0xAA97F0689660eA15b7d6f84F2E5250B63f2b381a`, ~$11k TVL) — at-or-below Q-7 floor. Reconsideration: Phase-7-or-v2 if TVL grows ≥ $20k.
  - **BRLm/EURm pool** (`0xb6c8f9490314394CFc6EDacb8717bFDC1EB8dab5`, < $10k TVL) — below floor. Reconsideration: Phase-7-or-v2 if events/30d ≥ 60.
  - **~38 non-anchor ICHI Celo vaults** — listed in `protocols/ichi.toml` with `active = false; reason = "v2-deferred"`. Activated by toggling boolean flags in future iteration; row additions forbidden by AF-12 silent-re-scope defense.
  - **Real x402-on-Celo settlement infrastructure** — x402-foundation monorepo lists no Celo facilitator as of 2026-05-25. Cost leg is MODELED, not paid, in Iterations 1+2. Forward-looking research finding.

## Sources
  - Kirchner 2015 INAR(p): arxiv.org/abs/1509.02017
  - Daw & Pender 2017 bivariate Hawkes: arxiv.org/pdf/1707.05143v3
  - Chen et al. 2017 NHPP-vs-Hawkes LR test: arxiv.org/pdf/1702.06055v2
  - Brown et al. 2002 time-rescaling KS test: cited in PITFALLS §4
  - Filimonov & Sornette 2014 LR boundary correction: arxiv.org/pdf/1403.5227
  - arxiv 2410.05008 LR over-rejection: arxiv.org/pdf/2410.05008
  - Wheatley ETH thesis robust Hawkes: ethz.ch/.../wheatleythesis.pdf
  - Hernandez Cruz et al. 2024 stablecoin depeg calibration (cited as historical USDC depeg comparison only; condition-4 framing is USDT not USDC per CLAUDE.md): arxiv.org/pdf/2407.11716v1
  - Ma et al. 2014 Carr–Madan strip: arxiv.org/pdf/1406.5430v1
  - SOMNIA_DRAFT.md §FUNCTIONAL FORM (convex-dominance conditions, condition 4 reparameterized USDT depeg + USDT/USDC basis per CLAUDE.md non-negotiables)
  - SOMNIA_DRAFT.md §ARRIVAL PROCESS (NHPP/Hawkes specification)
  - CANDIDATES.md §7 Hidden-Volume Audit (thinness retraction; cKES/USDT ~4,440/30d; cCOP/USDT ~580–625/30d on V3 anchor)

Specific token / pool / factory addresses that MUST appear verbatim:
  - ICHI factory: `0x9FAb4bdD4E05f5C023CCC85D2071b49791D7418F`
  - Steer factory: `0x116Dba5DcE9CcDA828218b7eB46406810632014C`
  - cKES/USDT pool (Iter-1 anchor): `0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F`
  - cCOP/USDT pool (Iter-2 anchor): `0x2AC5baA668A8A58FD0e302B9896717484fd217B0`
  - USDT canonical Celo: `0x48065fbBE25f71C9282ddf5e1cD6D6A887483D5e` (per CANDIDATES §1)
  - Mento V2 BrokerProxy: `0x777A8255cA72412f0d706dc03C9D1987306B4CaD`
  - Uniswap V4 PoolManager Celo: `0x288dc841A52FCA2707c6947B3A777c5E56cd87BC`

USDT framing must be unambiguous. Use phrases like "USDT depeg + USDT/USDC basis risk" and "tail-risk reframe: USDT (not USDC)". Every mention of the convex-dominance condition 4 MUST cite USDT, not USDC. The single permitted reference to "USDC depeg" is in the historical citation of Hernandez Cruz et al. 2024 (used as methodology source, not as condition-4 framing).

After authoring, commit to git with message: `docs(00): commit PRE_REGISTRATION.md (GOV-01 + REPRO-04 decision)` — push to `origin = JMSBPP/abrigo-x402` per CLAUDE.md Git workflow.
  </action>
  <verify>
    <automated>test -f notes/PRE_REGISTRATION.md && grep -c "## Kernel Forms" notes/PRE_REGISTRATION.md && grep -c "## Prior Parameters" notes/PRE_REGISTRATION.md && grep -c "## Test Statistics" notes/PRE_REGISTRATION.md && grep -c "## Acceptance Regions" notes/PRE_REGISTRATION.md && grep -c "## Decision Rules" notes/PRE_REGISTRATION.md && grep -c "## REPRO-03 Threshold" notes/PRE_REGISTRATION.md && grep -c "## Q-9 Fallback Pre-Registration" notes/PRE_REGISTRATION.md && grep -c "## Q-7 Floor" notes/PRE_REGISTRATION.md && grep -c "## Deferred Substrate" notes/PRE_REGISTRATION.md && grep -c "## Sources" notes/PRE_REGISTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f notes/PRE_REGISTRATION.md` exits 0
    - `grep -c "^## Kernel Forms$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Prior Parameters$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Test Statistics$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Acceptance Regions$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Decision Rules$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## REPRO-03 Threshold$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Q-9 Fallback Pre-Registration$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Q-7 Floor$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Deferred Substrate$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "^## Sources$" notes/PRE_REGISTRATION.md` returns 1
    - `grep -c "marginal-demand" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -c "below-window" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -c "USDT" notes/PRE_REGISTRATION.md` returns >= 5 (USDT depeg + USDT/USDC basis must be visible per CLAUDE.md)
    - `grep -c "USDT depeg" notes/PRE_REGISTRATION.md` returns >= 1 (I5: USDT depeg framing is the condition-4 headline per CLAUDE.md)
    - `grep -c "USDT/USDC basis" notes/PRE_REGISTRATION.md` returns >= 1 (I5: basis-risk co-framing per CLAUDE.md condition-4)
    - `grep -c "USDC depeg" notes/PRE_REGISTRATION.md` returns <= 1 (I5: at most one historical/comparison reference allowed; never as condition-4 headline)
    - `grep -ciE "(η ≥ 0.2|eta >= 0.2|η.*≥.*0\\.2)" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -ciE "(α = 0.01|alpha = 0.01|α.*=.*0\\.01)" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -c "Kirchner" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -c "Brown" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -ci "filimonov" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -ci "daw.*pender" notes/PRE_REGISTRATION.md` returns >= 1
    - `grep -c "0x9FAb4bdD4E05f5C023CCC85D2071b49791D7418F" notes/PRE_REGISTRATION.md` returns >= 1 (ICHI factory)
    - `grep -c "0x116Dba5DcE9CcDA828218b7eB46406810632014C" notes/PRE_REGISTRATION.md` returns >= 1 (Steer factory)
    - `grep -c "0x61Ef8708fc240DC7f9F2c0d81c3124Df2fd8829F" notes/PRE_REGISTRATION.md` returns >= 1 (cKES/USDT pool)
    - `grep -c "0x2AC5baA668A8A58FD0e302B9896717484fd217B0" notes/PRE_REGISTRATION.md` returns >= 1 (cCOP/USDT pool)
    - `grep -c "COPM" notes/PRE_REGISTRATION.md` returns >= 1 (Deferred substrate)
    - `grep -c "cXOF/USDm" notes/PRE_REGISTRATION.md` returns >= 1 (Deferred substrate)
    - `grep -c "BRLm/EURm" notes/PRE_REGISTRATION.md` returns >= 1 (Deferred substrate)
    - `wc -l notes/PRE_REGISTRATION.md` shows >= 150 lines
    - `git log --oneline -- notes/PRE_REGISTRATION.md` returns at least one commit
    - `test ! -d analysis/src` exits 0 (M10: analysis/src/ MUST NOT exist yet — deterministic existence check per SC-1 ordering invariant)
    - `test ! -d data/raw` exits 0 (M10: data/raw/ MUST NOT exist yet — deterministic existence check per SC-1 ordering invariant)
    - `test ! -d fetch/src` exits 0 (M10: fetch/src/ MUST NOT exist yet — deterministic existence check per SC-1 ordering invariant)
  </acceptance_criteria>
  <done>
PRE_REGISTRATION.md exists with all 11 required H2 sections, all numeric thresholds (α=0.01, η≥0.2, rate_per_event grid (1,5,10), USD_per_query $5e-6 ±50%, REPRO-03 two-tier 30k/100k, Q-7 TVL<$10k OR events<30/30d, Q-9 fallback trigger sample<300 OR CI width>0.4), USDT framing (not USDC) consistently applied per CLAUDE.md non-negotiables, all required citations (Kirchner, Daw & Pender, Chen et al., Brown, Filimonov & Sornette, Wheatley, Hernandez Cruz, SOMNIA_DRAFT.md), all required addresses (ICHI factory, Steer factory, cKES/USDT pool, cCOP/USDT pool), Deferred substrate enumerates COPM + cXOF/USDm + BRLm/EURm + ~38 non-anchor ICHI vaults with reconsideration triggers, document is ≥150 lines, committed to git, no `analysis/src/`, `data/raw/`, or `fetch/src/` directories exist (ordering invariant satisfied via deterministic test commands).
  </done>
</task>

</tasks>

<verification>
- Run all 31 acceptance criteria above
- Confirm `git log --oneline -- notes/PRE_REGISTRATION.md` returns at least one commit
- Confirm no `analysis/src/`, `data/raw/`, or `fetch/src/` directories exist in the repo yet (ordering invariant per SC-1, verified via `test ! -d` rather than vacuous git log)
- Confirm USDT framing is consistent: at-most-one `USDC depeg` reference, allowed only as historical Hernandez Cruz comparison citation
</verification>

<success_criteria>
- All acceptance criteria pass
- PRE_REGISTRATION.md is ≥150 lines and committed to git
- ROADMAP SC-1 (Phase 0) is satisfied by this plan in conjunction with Plan 02 (PHASE_0_GATE.md) and Plan 03 (Q9_DECISION.md)
- GOV-01 (pre-registration discipline) fully addressed
- REPRO-04 (Q-9 decision component) addressed via Q-9 Fallback Pre-Registration section
</success_criteria>

<output>
After completion, create `.planning/phases/00-candidate-eligibility-pre-registration/00-01-SUMMARY.md` documenting: (a) the actual numeric values committed for Q-7 floor and Q-9 fallback trigger; (b) the git commit hash of the PRE_REGISTRATION.md commit; (c) any deviations from the planned section structure with rationale.
</output>
</output>
