---
phase: 04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6
plan: 09
type: execute
wave: 4
depends_on: [pre, "00", "01", "02", "03", "04", "05", "06", "07", "08"]
files_modified:
  - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
autonomous: false
requirements: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05]

must_haves:
  truths:
    - "04-VERIFICATION-pre.md exists with frontmatter verification_pass: true if AND ONLY IF every row of the acceptance grid passes"
    - "Acceptance grid maps every requirement (DEPEND-01, DEPEND-02, HEDGE-01..05) + every ROADMAP §Phase 4 SC (SC-1..6) + the PR-amendment ordering invariant to {command, expected exit code, observed verdict}"
    - "AF-03 git-ordering check passes: amendment commit predates every hedge/* and dependence/* commit"
    - "SC-2 grep gate green: ! grep -i '^[^#]*usdc' analysis/src/abrigo_x402/hedge/falsification.py exits 0"
    - "Carr-Madan anti-pattern grep green: ! grep -E 'scipy\\.integrate\\.quad|np\\.trapz' analysis/src/abrigo_x402/hedge/carr_madan_strip.py exits 0"
    - "Canonical-LL anti-pattern grep green: ! grep -rE 'loglik_in_sample_raw' analysis/src/abrigo_x402/hedge/ exits 0"
    - "Non-citation grep green: ! grep -E 'port from Hernandez Cruz|methodological_port' notes/usdt_depeg_calibration.md exits 0"
    - "Full Phase 2+3+4 test suite green; total test count >= 50; failures == 0"
    - "make phase-4-acceptance exits 0"
    - "make lint-artifacts exits 0"
    - "Manual production-rep documented: one-shot run of `python -m abrigo_x402.cli hedge --run-id <phase-3-run-id> --stage all` recorded with the actual firing condition (or strip emission) on real data, written into the Notes/Caveats column of 04-VERIFICATION-pre.md"
    - "Acceptance grid row 13a (char_func production-grade gate, iter-3 Sobol QMC labels): if strip.json was emitted, the row verifies strip.json :: char_func_source ∈ {'gaussian_copula_latent_mvn','t_copula_latent_mvt','clayton_sobol_qmc','frank_sobol_qmc','gumbel_sobol_qmc'} AND the family-suffix component of char_func_source matches joint_dist.json :: empirical_copula.family (gaussian->gaussian_copula_latent_mvn, t->t_copula_latent_mvt, clayton->clayton_sobol_qmc, frank->frank_sobol_qmc, gumbel->gumbel_sobol_qmc). Silent-proxy ship fails the row (value 'gaussian_proxy_pooled_sigma' is REMOVED in Plan 04-08 iter-2 Path A; any occurrence in strip.json fails the gate). If strip_degenerate.json was emitted with char_func_source == 'build_failed', the row records the helper exception message + reason='build_failed_upstream' and verifies that the orchestrator did NOT silently fall back to a Gaussian proxy. If strip_degenerate.json was emitted with reason='positivity_fail_after_2_12', the row records the Sobol-QMC label and the FFT noise-floor diagnostic."
    - "Acceptance grid row 13b (char_func helper test pass): pytest tests/test_char_func_from_winner.py -x exits 0 (the new 04-08 helper has its own ≥7-test coverage incl. iter-3 Sobol noise-floor + power-of-2 N guard; row 13b verifies the test file lands and is green)"
    - "iter-3 Acceptance grid row 13c (fourth firing condition wiring): decide_firing_condition handles `null_strip_unavailable` (verified by `grep -q 'null_strip_unavailable' analysis/src/abrigo_x402/hedge/null_result.py`); _evidence_branches.qmd has the fourth branch (verified by `grep -q 'null_strip_unavailable' reports/_templates/_evidence_branches.qmd`); orchestrator passes run_dir param (verified by `grep -q 'run_dir=run_dir' analysis/src/abrigo_x402/hedge/orchestrator.py`)"
    - "iter-3 Acceptance grid row 16 mtime guard (replaces honor-system): row 16 acceptance is `test -f data/fits/ichi/<run_id>/run_log.txt && test $(stat -c %Y data/fits/ichi/<run_id>/run_log.txt) -gt $(stat -c %Y .planning/phases/04-.../04-09-PLAN.md)` — the run_log.txt MUST be newer than the plan file (proving the run happened AFTER the plan landed). Plus the file MUST contain the orchestrator's stdout 'hedge.orchestrator.run_hedge completed' line (greppable). Plus the <run_id> directory MUST contain newly-created joint_dist.json + (strip.json | strip_degenerate.json) whose mtimes are also > plan-file mtime."
    - "iter-3 Acceptance grid quarto_skipped flag: the grid output now carries a separate boolean field `quarto_skipped: bool` distinct from the unified `verification_pass: bool`. When Quarto isn't available: quarto_skipped=true AND verification_pass=true ONLY when (a) the rest of the grid is green AND (b) a 04-VERIFICATION-pre.md note documents the Quarto skip. The single `verification_pass: true` value with a silent Quarto skip is no longer accepted."
  artifacts:
    - path: ".planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md"
      provides: "Phase 4 acceptance grid + verification_pass verdict + manual production-rep documentation"
      contains: "verification_pass:"
  key_links:
    - from: "04-VERIFICATION-pre.md acceptance grid"
      to: "make phase-4-acceptance"
      via: "each row's command runs against the actual codebase HEAD"
      pattern: "phase-4-acceptance"
---

<objective>
Phase 4 acceptance gate. Produce `04-VERIFICATION-pre.md` with a complete acceptance grid covering every Phase 4 requirement + every ROADMAP §Phase 4 SC + the AF-03 PR-amendment ordering invariant + a documented manual production-rep run on the real ICHI panel (or the closest available `data/fits/ichi/<run_id>/` substrate from Phase 3).

Mirrors Phase 3 Plan 03-08 (Pattern K). Frontmatter `verification_pass: true` if every grid row PASSes; otherwise NEEDS WORK with the failing rows.

Output: single VERIFICATION file + acceptance-grid run output + manual production-rep transcript embedded in Notes/Caveats column.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-CONTEXT.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-RESEARCH.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VALIDATION.md
@.planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-VERIFICATION-pre.md
@notes/PRE_REGISTRATION.md
@notes/usdt_depeg_calibration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 4 acceptance gate + collect grid evidence</name>
  <files>(no file changes — collection step)</files>
  <read_first>
    - .planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-VERIFICATION-pre.md (Pattern K template — Phase 4 mirrors this format)
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VALIDATION.md §"Per-Task Verification Map" (13-row req→test map — Phase 4 grid is a superset)
    - Makefile (phase-4-acceptance target — runs the full grid)
  </read_first>
  <action>
    Run the full acceptance gate and capture output for each grid row:

    ```bash
    # ROW: PR-amendment ordering invariant (AF-03)
    git log --pretty=format:'%H %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/hedge/ analysis/src/abrigo_x402/dependence/ > /tmp/p4_git_order.txt
    PRE_REG_HASH=$(git log --reverse --pretty=format:'%H' -- notes/PRE_REGISTRATION.md | head -1)
    FIRST_HEDGE_HASH=$(git log --reverse --pretty=format:'%H' -- analysis/src/abrigo_x402/hedge/ analysis/src/abrigo_x402/dependence/ 2>/dev/null | head -1)
    # PRE_REG_HASH timestamp must be earlier than FIRST_HEDGE_HASH timestamp
    PRE_TS=$(git show -s --format=%ct $PRE_REG_HASH)
    HEDGE_TS=$(git show -s --format=%ct $FIRST_HEDGE_HASH)
    [ $PRE_TS -lt $HEDGE_TS ] && echo "AF-03 PASS" || echo "AF-03 FAIL"

    # ROW: SC-2 grep gate (USDT framing)
    ! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py && echo "SC-2 PASS" || echo "SC-2 FAIL"

    # ROW: Carr-Madan anti-pattern grep
    ! grep -E "scipy\.integrate\.quad|scipy\.integrate\.fixed_quad|scipy\.integrate\.romberg|np\.trapz" analysis/src/abrigo_x402/hedge/carr_madan_strip.py && echo "CM-anti-pattern PASS" || echo "CM-anti-pattern FAIL"

    # ROW: Canonical-LL anti-pattern grep
    ! grep -rE "loglik_in_sample_raw" analysis/src/abrigo_x402/hedge/ && echo "Pattern-F PASS" || echo "Pattern-F FAIL"

    # ROW: Non-citation discipline
    ! grep -E "port from Hernandez Cruz|methodological_port" notes/usdt_depeg_calibration.md && echo "Non-cite PASS" || echo "Non-cite FAIL"

    # ROW: Hardcoded jump-params gate
    ! grep -rE "lambda_J\s*=\s*0\.|mu_J\s*=\s*-?0\.|sigma_J\s*=\s*0\." analysis/src/abrigo_x402/hedge/falsification.py analysis/src/abrigo_x402/hedge/carr_madan_strip.py && echo "Hardcoded-params PASS" || echo "Hardcoded-params FAIL"

    # ROW 13a: char_func production-grade gate (NO silent Gaussian proxy)
    ! grep -q "gaussian_proxy_pooled_sigma" analysis/src/abrigo_x402/hedge/orchestrator.py && echo "char_func-no-proxy PASS" || echo "char_func-no-proxy FAIL"
    grep -qE "gaussian_copula_latent_mvn|t_copula_latent_mvt|clayton_sobol_qmc|frank_sobol_qmc|gumbel_sobol_qmc" analysis/src/abrigo_x402/hedge/orchestrator.py && echo "char_func-source-labels PASS" || echo "char_func-source-labels FAIL"
    grep -q "_build_char_func_from_winner" analysis/src/abrigo_x402/hedge/orchestrator.py && echo "char_func-helper PASS" || echo "char_func-helper FAIL"
    grep -q "Sobol\|scipy.stats.qmc" analysis/src/abrigo_x402/hedge/orchestrator.py && echo "iter-3 sobol-qmc PASS" || echo "iter-3 sobol-qmc FAIL"

    # ROW 13b: char_func helper test pass
    cd analysis && uv run pytest tests/test_char_func_from_winner.py -x 2>&1 | tail -1 > /tmp/p4_char_func.txt
    cd ..

    # ROW 13c (iter-3): fourth firing condition wiring
    grep -q "null_strip_unavailable" analysis/src/abrigo_x402/hedge/null_result.py && echo "iter-3 firing-d-decide PASS" || echo "iter-3 firing-d-decide FAIL"
    grep -q "null_strip_unavailable" reports/_templates/_evidence_branches.qmd && echo "iter-3 firing-d-evidence PASS" || echo "iter-3 firing-d-evidence FAIL"
    grep -q "run_dir=run_dir" analysis/src/abrigo_x402/hedge/orchestrator.py && echo "iter-3 firing-d-rundir-passthrough PASS" || echo "iter-3 firing-d-rundir-passthrough FAIL"

    # ROW 16 mtime guard (iter-3): manual production-rep verification (replaces honor-system)
    # The grid row's command is built later from the actual run_id; here we record the
    # plan-file mtime so the Task 2 author can compare against run_log.txt mtime.
    PLAN_MTIME=$(stat -c %Y .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-09-PLAN.md)
    echo "plan_file_mtime=${PLAN_MTIME}" > /tmp/p4_plan_mtime.txt

    # ROW: Full test suite (Phase 2+3+4)
    cd analysis && uv run pytest tests/ 2>&1 | tail -3 > /tmp/p4_pytest.txt
    cd ..

    # ROW: make lint-artifacts
    make lint-artifacts > /tmp/p4_lint.txt 2>&1
    echo "lint exit: $?"

    # ROW: make phase-4-acceptance
    make phase-4-acceptance > /tmp/p4_accept.txt 2>&1
    echo "phase-4-accept exit: $?"

    # ROW: regex acceptance count
    grep -cE "DEPEND-0[12]|HEDGE-0[1-5]|SC-[1-6]" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
    # (Will be evaluated after Task 2 lands the file; for now ensure scaffolding exists)
    ```

    Collect outputs in `/tmp/p4_*.txt` for Task 2 to embed verbatim into the acceptance grid.
  </action>
  <verify>
    <automated>cd analysis &amp;&amp; uv run pytest tests/ 2>&amp;1 | tail -1 | grep -qE "[0-9]+ passed" &amp;&amp; cd .. &amp;&amp; make lint-artifacts &amp;&amp; make phase-4-acceptance</automated>
  </verify>
  <acceptance_criteria>
    - `cd analysis && uv run pytest tests/` exits 0
    - `make lint-artifacts` exits 0
    - `make phase-4-acceptance` exits 0
    - AF-03 ordering check passes (PRE_REG amendment commit predates first hedge/dependence commit)
    - All four grep gates green (SC-2, CM-anti-pattern, canonical-LL, non-citation)
    - char_func gates green: NO `gaussian_proxy_pooled_sigma` in orchestrator.py; ≥1 valid `*_sobol_qmc|*_latent_mvn|*_latent_mvt` source_label present; `_build_char_func_from_winner` helper present
    - **iter-3 Issue 1 (Sobol QMC):** `grep -q "Sobol\|scipy.stats.qmc" analysis/src/abrigo_x402/hedge/orchestrator.py` exits 0
    - **iter-3 fourth firing condition:** grep gates for null_strip_unavailable (in null_result.py + _evidence_branches.qmd) and run_dir=run_dir passthrough in orchestrator.py all exit 0
    - **iter-3 Issue 4 (mtime):** /tmp/p4_plan_mtime.txt exists with `plan_file_mtime=<unix_ts>` content (used by Task 2 to compare against run_log.txt mtime)
  </acceptance_criteria>
  <done>All acceptance-gate commands run; outputs captured in /tmp/p4_*.txt for Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (human-verify): Manual production-rep on the real Phase-3 ICHI run_id + author 04-VERIFICATION-pre.md</name>
  <files>.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md</files>
  <read_first>
    - .planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-VERIFICATION-pre.md (Pattern K format template — 04-VERIFICATION-pre.md mirrors)
    - /tmp/p4_pytest.txt + /tmp/p4_lint.txt + /tmp/p4_accept.txt + /tmp/p4_git_order.txt + /tmp/p4_char_func.txt (Task 1 outputs)
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VALIDATION.md §"Manual-Only Verifications" — the row "Real ICHI panel run" is THIS task's manual production-rep step
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-CONTEXT.md `<specifics>` — STRADDLE expectation means HEDGE-05 firing on real panel is the likely outcome
  </read_first>
  <what-built>
    The full Phase 4 implementation: 7 Wave-1 module implementations + Wave-2 orchestrator with BIC-winner-derived char_func + CLI subcommand + Quarto null-result rendering + lint-artifacts five-track extension + four pre-commit gates + PRE_REGISTRATION amendment.
  </what-built>
  <action>
    (CHECKPOINT — see how-to-verify and resume-signal below. Executor runs the steps in
    how-to-verify, populates the grid, and pauses for human approval.)
  </action>
  <how-to-verify>
    1. **Identify a real Phase-3 run_id** to test against:
       ```bash
       ls data/fits/ichi/ 2>/dev/null | head -5
       ```
       Pick the most recent run_id (12-hex directory). If none exist (e.g., no real ICHI panel has been fit yet), use the synthetic substrate from Phase 3 fixtures and document that substitution explicitly in the VERIFICATION file.

    2. **Run the full hedge pipeline once**:
       ```bash
       cd analysis && uv run python -m abrigo_x402.cli hedge --run-id <run-id> --stage all 2>&1 | tee /tmp/p4_real_run.txt
       ```
       Expected outputs:
       - `data/fits/ichi/<run-id>/joint_dist.json` exists with all REQUIRED_JOINT_DIST_KEYS
       - `data/fits/ichi/<run-id>/gate_report.json` exists with all REQUIRED_GATE_REPORT_KEYS
       - `data/fits/ichi/<run-id>/(strip.json OR strip_degenerate.json)` exists. If strip.json: `char_func_source` is one of the BIC-winner labels; if strip_degenerate.json with `char_func_source == "build_failed"`, record the exception message.
       - `data/fits/ichi/<run-id>/stress_report.json` exists with all REQUIRED_STRESS_REPORT_KEYS
       - Either `reports/ichi.pdf` exists (HEDGE-05 fired) OR `firing_condition: null` in the printed result (positive-result path).

    3. **Record the headline scientific finding**:
       - Did HEDGE-05 fire? If yes, which condition (null_cost, null_lr, null_convex)?
       - If positive-result: which gate condition(s) passed? What is the divergence_pct in stress_report.json? What is `char_func_source` in strip.json (the BIC winner)?

    4. **Author `04-VERIFICATION-pre.md`** mirroring Phase 3 03-VERIFICATION-pre.md format. The grid MUST have at minimum 18 rows (was 17; iter-3 added row 13c for fourth firing condition wiring):

       | Row | Requirement / SC | Command | Expected | Observed | Verdict | Notes/Caveats |
       |-----|------------------|---------|----------|----------|---------|---------------|
       | 1 | PR-amendment ordering invariant (AF-03) | `git log --pretty=format:'%H %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/hedge/` | PRE_REG commit predates hedge commits by timestamp | (paste git log head) | PASS / FAIL | — |
       | 2 | DEPEND-01 cross-correlogram + perm null + copula | `pytest tests/test_cross_correlogram.py tests/test_permutation_null.py tests/test_copula_bic.py -x` | exit 0 | (paste pytest tail) | PASS / FAIL | — |
       | 3 | DEPEND-02 joint_dist.json schema | `make lint-artifacts` | exit 0 | exit 0 | PASS / FAIL | — |
       | 4 | HEDGE-01 four-condition gate | `pytest tests/test_falsification.py -x && ! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py` | exit 0 + 0 | (paste) | PASS / FAIL | SC-2 enforced |
       | 5 | HEDGE-02 Carr-Madan strip + 2^11→2^12 escalation | `pytest tests/test_carr_madan_strip.py -x` | exit 0 (5 passed) | (paste) | PASS / FAIL | PRE_REG 0.001 tolerance honored |
       | 6 | HEDGE-03 USDT depeg literature_range_stipulation + LHS | `pytest tests/test_usdt_depeg_lhs.py -x && grep -q "literature_range_stipulation" notes/usdt_depeg_calibration.md && ! grep -E "port from Hernandez Cruz" notes/usdt_depeg_calibration.md` | exit 0 + 0 + 0 | (paste) | PASS / FAIL | Non-citation discipline locked |
       | 7 | HEDGE-04 three-way stress | `pytest tests/test_stress_test.py -x` | exit 0 (5 passed) | (paste) | PASS / FAIL | Divergence flag at 30% |
       | 8 | HEDGE-05 null-result template firing | `pytest tests/test_null_result_template.py -x` | exit 0 (4 passed; PDF test may skip if no quarto) | (paste) | PASS / FAIL | 3 fixtures parametrized |
       | 9 | SC-1 PANEL-02 header on all 4 new artifacts | `make lint-artifacts` | exit 0 | exit 0 | PASS / FAIL | — |
       | 10 | SC-2 USDT (not USDC) framing | `! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py` | exit 0 | (paste) | PASS / FAIL | pre-commit hook active |
       | 11 | SC-3 Carr-Madan 2^11→2^12 escalation + abort | `pytest tests/test_carr_madan_strip.py -x` | exit 0 (escalation + degenerate paths exercised) | (paste) | PASS / FAIL | — |
       | 12 | SC-4 stress_report 3-scenario + divergence_flag | `pytest tests/test_stress_test.py::test_divergence_flag_thresholding -x` | exit 0 | (paste) | PASS / FAIL | — |
       | 13 | SC-5 HEDGE-05 PDF auto-fires + dual signature | manual: run `make render-null-result-pdf FIRING=null_lr` then `pdftotext reports/ichi.pdf - \| grep "HEDGE-05 NULL RESULT"` AND `pdfinfo reports/ichi.pdf \| grep HEDGE05` | both grep exit 0 | (paste) | PASS / FAIL / SKIP-NO-QUARTO | iter-3: when SKIP-NO-QUARTO, the frontmatter MUST also carry `quarto_skipped: true` and the rest of the grid MUST be green; bare `verification_pass: true` with no quarto_skipped field is REJECTED. |
       | 13a | char_func production-grade gate (iter-3 Sobol QMC labels; NO silent Gaussian proxy) | `! grep -q "gaussian_proxy_pooled_sigma" analysis/src/abrigo_x402/hedge/orchestrator.py && grep -qE "gaussian_copula_latent_mvn\|t_copula_latent_mvt\|clayton_sobol_qmc\|frank_sobol_qmc\|gumbel_sobol_qmc" analysis/src/abrigo_x402/hedge/orchestrator.py && grep -q "_build_char_func_from_winner" analysis/src/abrigo_x402/hedge/orchestrator.py && grep -q "Sobol\|scipy.stats.qmc" analysis/src/abrigo_x402/hedge/orchestrator.py` | exit 0 + 0 + 0 + 0 | (paste) | PASS / FAIL | If strip.json was emitted on the manual rep run: paste `jq '.char_func_source' strip.json` AND `jq '.empirical_copula.family' joint_dist.json` — verify the family-suffix matches (gaussian->`gaussian_copula_latent_mvn`, t->`t_copula_latent_mvt`, clayton/frank/gumbel-><family>_sobol_qmc). If strip_degenerate.json emitted: `reason` field ∈ {`build_failed_upstream`, `positivity_fail_after_2_12`} — both route to firing condition (d) null_strip_unavailable. |
       | 13b | char_func helper unit tests | `cd analysis && uv run pytest tests/test_char_func_from_winner.py -x` | exit 0 (≥7 passed incl. iter-3 sobol noise-floor + power-of-2 N guard; skips OK if a copulae family unavailable) | (paste pytest tail) | PASS / FAIL | — |
       | 13c | iter-3 fourth firing condition (`null_strip_unavailable`) wiring | `grep -q "null_strip_unavailable" analysis/src/abrigo_x402/hedge/null_result.py && grep -q "null_strip_unavailable" reports/_templates/_evidence_branches.qmd && grep -q "run_dir=run_dir" analysis/src/abrigo_x402/hedge/orchestrator.py && grep -q "build_failed_upstream" analysis/src/abrigo_x402/hedge/orchestrator.py && grep -q "positivity_fail_after_2_12" analysis/src/abrigo_x402/hedge/carr_madan_strip.py` | exit 0 + 0 + 0 + 0 + 0 | (paste) | PASS / FAIL | iter-3 added: decide_firing_condition handles firing (d); template has fourth branch; orchestrator passes run_dir; both `reason` values wired. |
       | 14 | SC-6 USDT calibration documented + non-citation | `test -f notes/usdt_depeg_calibration.md && grep -q "literature_range_stipulation" notes/usdt_depeg_calibration.md && ! grep -qE "port from Hernandez Cruz" notes/usdt_depeg_calibration.md` | exit 0 + 0 + 0 | (paste) | PASS / FAIL | — |
       | 15 | Full Phase 2+3+4 test suite green | `cd analysis && uv run pytest tests/ -x` | exit 0 | (paste pytest tail with N passed) | PASS / FAIL | — |
       | 16 | Manual production-rep on real ICHI panel (iter-3 mtime guard) | `cd analysis && uv run python -m abrigo_x402.cli hedge --run-id <run-id> --stage all 2>&1 \| tee data/fits/ichi/<run-id>/run_log.txt` THEN `test -f data/fits/ichi/<run-id>/run_log.txt && test $(stat -c %Y data/fits/ichi/<run-id>/run_log.txt) -gt $(stat -c %Y .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-09-PLAN.md) && grep -q "run_hedge completed\|hedge.orchestrator" data/fits/ichi/<run-id>/run_log.txt && test -f data/fits/ichi/<run-id>/joint_dist.json && (test -f data/fits/ichi/<run-id>/strip.json \|\| test -f data/fits/ichi/<run-id>/strip_degenerate.json)` | json output with firing_condition + artifacts; run_log.txt mtime > plan-file mtime; greppable orchestrator completion line; joint_dist.json + (strip.json OR strip_degenerate.json) all newly created | (paste headline: firing_condition=X, artifacts=[...], char_func_source=Y matching family=Z, run_log.txt mtime=<ts>, plan mtime=<ts>) | PASS / DOCUMENTED-NULL | iter-3 replaces honor-system: file-mtime comparison proves the rep was executed AFTER this plan landed; greppable orchestrator-completion sentinel; required artifact presence with mtime > plan-file mtime. Record headline finding here. |

    5. **Set frontmatter `verification_pass` AND iter-3 `quarto_skipped` flags:**
       - If every row is PASS AND quarto was invoked on row 13: `verification_pass: true` + `quarto_skipped: false`
       - If every row is PASS AND row 13 was SKIP-NO-QUARTO: `verification_pass: true` + `quarto_skipped: true` AND a "Quarto Availability Note" block at the end of the file documents the local environment (e.g., "Quarto not installed on verifier's machine; PDF render path not exercised on this rep; row 13 grep gates verified statically against the .qmd template only")
       - Otherwise: `verification_pass: false` with explicit list of failing rows.
       - Bare `verification_pass: true` with row 13 = SKIP-NO-QUARTO but no `quarto_skipped` field is REJECTED (iter-3 Issue 4 fix).

    6. **Add regex acceptance footer**:
       ```
       grep -cE "DEPEND-0[12]|HEDGE-0[1-5]|SC-[1-6]" 04-VERIFICATION-pre.md
       grep -cE "null_strip_unavailable" 04-VERIFICATION-pre.md   # iter-3: fourth firing condition documented
       ```
       must return ≥ 13 hits on the first grep + ≥ 1 hit on the second (iter-3 fourth firing condition formally surfaced in the grid).

    7. **Embed manual production-rep transcript** at the end of the file (or in Notes/Caveats of row 16):
       ```
       ## Manual Production-Rep Transcript

       Run: `cd analysis && uv run python -m abrigo_x402.cli hedge --run-id <id> --stage all`
       Date: 2026-05-27
       run_id: <id>
       Substrate: data/fits/ichi/<id>/{fit_report.json, residuals.parquet}

       Outcome:
         - firing_condition: <null_cost|null_lr|null_convex|None>
         - artifacts written: [joint_dist.json, gate_report.json, ...]
         - BIC winner (from joint_dist.empirical_copula.family): <gaussian|t|clayton|frank|gumbel>
         - char_func_source (from strip.json, if emitted): <*_latent_mvn|*_latent_mvt|*_sobol_qmc|build_failed>  # iter-3 labels
         - reason (from strip_degenerate.json, if emitted): <build_failed_upstream|positivity_fail_after_2_12>  # iter-3 fourth firing condition routing
         - iter-3 mtime check: run_log.txt mtime <ts> vs plan-file mtime <ts> (former MUST be greater)
         - iter-3 quarto_skipped flag: <true|false>

       Headline scientific finding:
         <one paragraph summarizing whether HEDGE-05 fired, which condition,
          and the supporting evidence from the four artifacts. If strip.json
          emitted, also state which copula family won BIC and whether the
          char_func construction succeeded.>
       ```

    8. **Commit**:
       ```bash
       git add .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
       git commit -m "docs(04-09): Phase 4 acceptance gate — VERIFICATION-pre.md with 17-row grid (incl. char_func gate row 13a/13b) + manual production-rep on real ICHI panel"
       ```
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md &amp;&amp; grep -q "verification_pass:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md &amp;&amp; grep -q "quarto_skipped:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md &amp;&amp; grep -q "null_strip_unavailable" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md &amp;&amp; [ $(grep -cE "DEPEND-0[12]|HEDGE-0[1-5]|SC-[1-6]" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md) -ge 13 ]</automated>
  </verify>
  <done>04-VERIFICATION-pre.md exists with frontmatter verification_pass + iter-3 quarto_skipped flags set (true or false documented); 18-row grid populated (incl. row 13a iter-3 Sobol-QMC char_func gate + row 13b helper-test gate + row 13c iter-3 fourth-firing-condition wiring); row 16 mtime guard exercised (run_log.txt mtime > plan-file mtime); regex acceptance count >=13 on req/SC tokens AND >=1 on null_strip_unavailable; manual production-rep transcript embedded with iter-3 fields (sobol_qmc label, reason field, plan_mtime comparison, quarto_skipped); commit landed with docs(04-09): prefix; human approval received via resume-signal.</done>
  <resume-signal>
    Reply with one of:
    - "approved verification_pass:true" + brief headline (e.g., "HEDGE-05 fired on null_lr per STRADDLE expectation; BIC winner=clayton; char_func_source=clayton_mc_empirical")
    - "approved verification_pass:false" + list of failing rows
    - "needs work: <issue>" with description of the blocker
  </resume-signal>
</task>

</tasks>

<verification>
After Task 2 commit:

```bash
test -f .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
grep -q "verification_pass:" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
grep -cE "DEPEND-0[12]|HEDGE-0[1-5]|SC-[1-6]" .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md
# expect: >= 13

# char_func gate spot-checks (independent of the grid commit):
! grep -q "gaussian_proxy_pooled_sigma" analysis/src/abrigo_x402/hedge/orchestrator.py
grep -qE "gaussian_copula_latent_mvn|t_copula_latent_mvt|clayton_mc_empirical|frank_mc_empirical|gumbel_mc_empirical" analysis/src/abrigo_x402/hedge/orchestrator.py
grep -q "_build_char_func_from_winner" analysis/src/abrigo_x402/hedge/orchestrator.py

# Re-run the full gate as evidence of replay-ability:
make phase-4-acceptance
cd analysis && uv run pytest tests/ -x
```
</verification>

<success_criteria>
- `04-VERIFICATION-pre.md` exists with frontmatter `verification_pass: true` (if all rows PASS) or `false` (if any FAIL; the file documents the failures explicitly) AND iter-3 `quarto_skipped: bool` flag distinct from verification_pass
- 18-row acceptance grid (iter-3 added row 13c for fourth firing condition wiring) covers DEPEND-01/02 + HEDGE-01..05 + SC-1..6 + PR-amendment ordering invariant + char_func production-grade gate (row 13a, iter-3 sobol_qmc labels) + char_func helper-test gate (row 13b) + iter-3 fourth-firing-condition wiring (row 13c) + manual production-rep (row 16 with iter-3 mtime guard)
- `grep -cE "DEPEND-0[12]|HEDGE-0[1-5]|SC-[1-6]"` returns >= 13 hits; `grep -q "null_strip_unavailable"` returns >= 1 hit (iter-3)
- AF-03 ordering invariant verified via git log (PRE_REG amendment commit predates all hedge/* + dependence/* commits by timestamp)
- All four pre-commit grep gates verified green at HEAD
- char_func gates verified green (iter-3 Sobol QMC labels): NO `gaussian_proxy_pooled_sigma` in orchestrator.py; ≥1 valid `*_sobol_qmc|*_latent_mvn|*_latent_mvt` label present; `Sobol` or `scipy.stats.qmc` import present; `_build_char_func_from_winner` helper present; `test_char_func_from_winner.py` green
- iter-3 fourth firing condition wiring verified green: `null_strip_unavailable` present in null_result.py + _evidence_branches.qmd; orchestrator passes `run_dir=run_dir` to decide_firing_condition; reasons `build_failed_upstream` (orchestrator) + `positivity_fail_after_2_12` (carr_madan_strip) both wired
- iter-3 row 16 mtime guard verified: run_log.txt mtime > plan-file mtime; orchestrator completion line greppable; required artifacts present and newer than plan-file
- Full Phase 2+3+4 test suite verified green
- Manual production-rep transcript embedded with the actual firing condition or strip emission + char_func_source aligned to BIC winner + iter-3 reason field (if strip_degenerate) + iter-3 mtime comparison + iter-3 quarto_skipped flag
- Single commit with `docs(04-09):` prefix
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-09-SUMMARY.md` recording:
- Commit hash of the VERIFICATION-pre.md
- Verbatim copy of the 17-row grid verdicts (or pointers to each row's exit code evidence)
- Whether `verification_pass: true` or `false`
- Manual production-rep headline finding (one paragraph)
- If strip.json was emitted: state which BIC winner family + char_func_source label was observed (validates Plan 04-08 Path A architecture)
- If `false`: enumerate failing rows and forward-link to gap-closure planning via `/gsd:plan-phase 4 --gaps`
- Full Phase 4 plan list (04-pre through 04-09) with their commit-pair hashes for forward audit trail
</output>
