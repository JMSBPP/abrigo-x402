---
phase: 04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6
plan: 00
type: execute
wave: 0
depends_on: [pre]
files_modified:
  - analysis/src/abrigo_x402/dependence/__init__.py
  - analysis/src/abrigo_x402/dependence/cross_correlogram.py
  - analysis/src/abrigo_x402/dependence/permutation_null.py
  - analysis/src/abrigo_x402/dependence/copula.py
  - analysis/src/abrigo_x402/hedge/__init__.py
  - analysis/src/abrigo_x402/hedge/falsification.py
  - analysis/src/abrigo_x402/hedge/carr_madan_strip.py
  - analysis/src/abrigo_x402/hedge/stress_test.py
  - analysis/src/abrigo_x402/hedge/usdt_depeg.py
  - analysis/src/abrigo_x402/hedge/null_result.py
  - analysis/src/abrigo_x402/hedge/orchestrator.py
  - analysis/tests/test_cross_correlogram.py
  - analysis/tests/test_permutation_null.py
  - analysis/tests/test_copula_bic.py
  - analysis/tests/test_falsification.py
  - analysis/tests/test_carr_madan_strip.py
  - analysis/tests/test_stress_test.py
  - analysis/tests/test_usdt_depeg_lhs.py
  - analysis/tests/test_null_result_template.py
  - analysis/tests/test_joint_dist_provenance.py
  - analysis/tests/test_gate_report_provenance.py
  - analysis/tests/test_stress_report_provenance.py
  - analysis/tests/test_byte_identical_phase_4.py
  - analysis/tests/test_required_keys_sync.py
  - analysis/tests/conftest.py
  - analysis/tests/fixtures/hedge_05_null_cost/fit_report.json
  - analysis/tests/fixtures/hedge_05_null_cost/gate_report.json
  - analysis/tests/fixtures/hedge_05_null_cost/cost_leg_bound.md
  - analysis/tests/fixtures/hedge_05_null_lr/fit_report.json
  - analysis/tests/fixtures/hedge_05_null_lr/gate_report.json
  - analysis/tests/fixtures/hedge_05_null_lr/cost_leg_bound.md
  - analysis/tests/fixtures/hedge_05_null_convex/fit_report.json
  - analysis/tests/fixtures/hedge_05_null_convex/gate_report.json
  - analysis/tests/fixtures/hedge_05_null_convex/cost_leg_bound.md
  - reports/_templates/null_result.qmd
  - scripts/lint_artifacts.py
  - Makefile
  - analysis/pyproject.toml
  - analysis/README.md
  - .pre-commit-config.yaml
autonomous: true
requirements: [DEPEND-01, DEPEND-02, HEDGE-01, HEDGE-02, HEDGE-03, HEDGE-04, HEDGE-05]

must_haves:
  truths:
    - "Every Wave-1 plan (04-01..04-07) can import the dependence/* or hedge/* module it owns and find a NotImplementedError stub at the canonical Wave-1 symbol surface"
    - "Every Wave-1 plan can import its test file and find pytest.skip-with-reason markers (no syntax errors, no missing fixtures)"
    - "Three HEDGE-05 fixture triplets exist (hedge_05_{null_cost,null_lr,null_convex}/{fit_report.json,gate_report.json,cost_leg_bound.md}); each forces exactly one HEDGE-05 firing condition"
    - "reports/_templates/null_result.qmd is a valid Quarto template with dual signature markers (visible H1 + \\pdfinfo HEDGE05-NULL-RESULT-V1) and three firing-condition branches"
    - "scripts/lint_artifacts.py knows about five new JSON artifacts (joint_dist.json, gate_report.json, stress_report.json, strip.json, strip_degenerate.json) — recognises paths and exposes lint helpers but defers schema enforcement until Wave 2 lands actual artifacts"
    - "Full Phase 2+3+4 test discovery succeeds (pytest --collect-only returns 0 exit code; no import errors)"
    - "make lint-artifacts exits 0 on the empty tree (no false-positive failures from absent data/fits/<run_id>/{joint_dist,gate_report,stress_report,strip,strip_degenerate}.json files)"
    - "make phase-4-acceptance Makefile target exists and is invokable (may exit non-zero pre-Wave-3, but exists)"
    - "analysis/pyproject.toml dependencies include copulae==0.8.0 and dev includes jupyter; pyvinecopulib is NOT installed (lazy per RESEARCH Open Question 3)"
    - "Pre-commit config has SC-2 grep gate, Carr-Madan anti-pattern gate (scipy.integrate.quad/np.trapz), canonical-LL gate (loglik_in_sample_raw), hardcoded-jump-params gate"
    - "Scaffold symbol names MATCH the canonical Wave-1 names: cross_correlogram_event_index, permutation_null_max_abs_rho, fit_5_families_bic, evaluate_condition_1_vol_of_vol, evaluate_condition_2_skew_fat_tails, evaluate_condition_3_hawkes_self_excitation, evaluate_condition_4_usdt_depeg, compute_strip, run_three_way_stress, load_calibration, generate_lhs_samples, decide_firing_condition, render_null_result_pdf, run_hedge"
    - "REQUIRED_JOINT_DIST_KEYS, REQUIRED_GATE_REPORT_KEYS, REQUIRED_STRESS_REPORT_KEYS, REQUIRED_STRIP_KEYS, STRIP_DEGENERATE_KEYS, HEDGE05_SIGNATURE='HEDGE05-NULL-RESULT-V1' constants are forward-declared in their owning modules"
    - "iter-3 Issue 2: copulae==0.8.0 install verified at scaffold commit time (`uv run python -c \"import copulae\"` exits 0); analysis/INSTALL_TROUBLESHOOTING.md documents 0.7.10 + test.pypi.org fallback paths"
    - "iter-3 Issue 2: tests/test_required_keys_sync.py is a NON-skip-marked test that asserts every REQUIRED_*_KEYS tuple in dependence/hedge modules equals the corresponding frozenset in scripts/lint_artifacts.py — catches Wave-0 mismatched-tuple regression at scaffold time"
  artifacts:
    - path: "analysis/src/abrigo_x402/dependence/__init__.py"
      provides: "dependence submodule namespace"
    - path: "analysis/src/abrigo_x402/dependence/cross_correlogram.py"
      provides: "cross_correlogram_event_index stub (NotImplementedError)"
    - path: "analysis/src/abrigo_x402/dependence/permutation_null.py"
      provides: "permutation_null_max_abs_rho stub"
    - path: "analysis/src/abrigo_x402/dependence/copula.py"
      provides: "fit_5_families_bic + REQUIRED_JOINT_DIST_KEYS tuple"
    - path: "analysis/src/abrigo_x402/hedge/falsification.py"
      provides: "evaluate_condition_{1..4} stubs + REQUIRED_GATE_REPORT_KEYS tuple"
    - path: "analysis/src/abrigo_x402/hedge/carr_madan_strip.py"
      provides: "compute_strip stub + REQUIRED_STRIP_KEYS + STRIP_DEGENERATE_KEYS tuples"
    - path: "analysis/src/abrigo_x402/hedge/stress_test.py"
      provides: "run_three_way_stress stub + REQUIRED_STRESS_REPORT_KEYS tuple"
    - path: "analysis/src/abrigo_x402/hedge/usdt_depeg.py"
      provides: "load_calibration + generate_lhs_samples stubs"
    - path: "analysis/src/abrigo_x402/hedge/null_result.py"
      provides: "decide_firing_condition + render_null_result_pdf stubs + HEDGE05_SIGNATURE constant"
    - path: "analysis/src/abrigo_x402/hedge/orchestrator.py"
      provides: "run_hedge stub (Wave 2 implements)"
    - path: "analysis/tests/fixtures/hedge_05_null_cost/"
      provides: "Phase-0 cost-leg-fail firing fixture triplet"
    - path: "analysis/tests/fixtures/hedge_05_null_lr/"
      provides: "DGP-03 LR-indistinguishable firing fixture triplet"
    - path: "analysis/tests/fixtures/hedge_05_null_convex/"
      provides: "HEDGE-01 zero-convex-condition firing fixture triplet"
    - path: "reports/_templates/null_result.qmd"
      provides: "Quarto template with dual signature markers + three firing-condition branches"
    - path: "scripts/lint_artifacts.py"
      provides: "Five-track lint extension (parquet + fit_report + joint_dist + gate_report + stress_report + strip)"
    - path: "Makefile"
      provides: "render-null-result-pdf + render-strip-diagnostic + phase-4-acceptance targets"
    - path: "analysis/pyproject.toml"
      provides: "copulae==0.8.0 dep + jupyter dev-dep"
    - path: ".pre-commit-config.yaml"
      provides: "Four new grep gates (SC-2 usdc, Carr-Madan integration anti-pattern, canonical-LL, hardcoded-jump-params)"
  key_links:
    - from: "analysis/tests/conftest.py"
      to: "analysis/tests/fixtures/hedge_05_*/"
      via: "null_result_fixture_triplet parametrized fixture"
      pattern: "@pytest.fixture.*hedge_05"
    - from: "analysis/src/abrigo_x402/hedge/null_result.py :: HEDGE05_SIGNATURE"
      to: "reports/_templates/null_result.qmd"
      via: "PDF metadata injection via \\pdfinfo"
      pattern: "HEDGE05-NULL-RESULT-V1"
    - from: "scripts/lint_artifacts.py"
      to: "analysis/src/abrigo_x402/{dependence,hedge}/*.py REQUIRED_*_KEYS tuples"
      via: "Pattern G sync (frozenset mirror)"
      pattern: "REQUIRED_(JOINT_DIST|GATE_REPORT|STRESS_REPORT|STRIP)_KEYS"
---

<objective>
Wave 0 scaffold for Phase 4 cross-leg dependence + falsification + Carr-Madan strip. Create:
1. All `analysis/src/abrigo_x402/dependence/*` + `analysis/src/abrigo_x402/hedge/*` module stubs at the canonical Wave-1 symbol surface (raising `NotImplementedError` in bodies; constants and tuples forward-declared).
2. All 12 `analysis/tests/test_*.py` skip-marked stubs at canonical Wave-1 surface.
3. `conftest.py` extension with three new fixtures (`joint_dist_fixture`, `gate_report_fixture`, `null_result_fixture_triplet`).
4. Three HEDGE-05 fixture triplets at `analysis/tests/fixtures/hedge_05_{null_cost,null_lr,null_convex}/` — each forcing exactly one firing condition.
5. `reports/_templates/null_result.qmd` Quarto template with dual signature markers + three firing-condition branches.
6. `scripts/lint_artifacts.py` five-track extension (parquet + fit_report + joint_dist + gate_report + stress_report + strip + strip_degenerate).
7. `Makefile` new targets (`render-null-result-pdf`, `render-strip-diagnostic`, `phase-4-acceptance`).
8. `analysis/pyproject.toml` adds `copulae==0.8.0` + `jupyter` dev-dep; defers `pyvinecopulib` per RESEARCH Open Question 3.
9. `analysis/README.md` system-dependency block (quarto + texlive-luatex).
10. `.pre-commit-config.yaml` four new grep gates (SC-2 usdc, Carr-Madan integration anti-pattern, canonical-LL, hardcoded-jump-params).

Purpose: establish the file tree and import graph so Wave 1 plans (04-01..04-07) can land in parallel without colliding on shared files or fixture conventions. Mirrors Phase 3 Plan 03-00 (which produced 9 dgp/* stubs + 9 test stubs + 2 locked synthetic fixtures + lint extension).

Output: ~30 source files + 9 fixture files + 1 Quarto template + 4 config-file edits, no production logic. `pytest --collect-only` exits 0; `make lint-artifacts` exits 0; `make phase-4-acceptance` is invokable.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-CONTEXT.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-RESEARCH.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VALIDATION.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-pre-PLAN.md
@.planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-00-PLAN.md
@.planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-08-SUMMARY.md
@analysis/src/abrigo_x402/dgp/orchestrator.py
@analysis/src/abrigo_x402/dgp/lr_test.py
@analysis/src/abrigo_x402/provenance.py
@analysis/tests/conftest.py
@scripts/lint_artifacts.py
@analysis/pyproject.toml
@notes/PRE_REGISTRATION.md

<interfaces>
From analysis/src/abrigo_x402/dgp/orchestrator.py (Pattern G reference — Phase 4 mirrors this):
```python
REQUIRED_FIT_REPORT_KEYS: tuple[str, ...] = (
    "chainId", "contractAddress", "blockRange",
    "fetchTimestamp", "dataHash", "gitCommit",
    "run_id", "tick_lib_version",
    "nhpp_inar_params", "hawkes_mv_params", "lr_test",
    "ks_rescaled_time", "held_out_loglik", "branching_ratio_ci",
    "baseline_stationarity_check", "input_diagnostics",
    "gate_passes", "gate_criteria",
)
# Pre-write guard:
missing = [k for k in REQUIRED_FIT_REPORT_KEYS if k not in fit_report]
if missing:
    raise KeyError(f"...: missing required SC-1 keys: {missing}")
```

From analysis/src/abrigo_x402/dgp/lr_test.py (canonical-LL sources Phase 4 must import):
```python
def _hawkes_loglik_vectorized(baseline, adjacency, decays, leg_0, leg_1, ...) -> float: ...
def _nhpp_pointprocess_loglik(baseline, leg_0, leg_1, ...) -> float: ...
def parametric_bootstrap_lr(...) -> dict: ...
```

From analysis/src/abrigo_x402/provenance.py:
```python
def git_commit_short() -> str: ...
def with_header(df: pl.DataFrame, output_path: Path | str, **meta) -> Path: ...
def assert_has_header(path: Path | str) -> None: ...
```

From scripts/lint_artifacts.py (extension target — Phase 4 adds new frozensets):
```python
FIT_REPORT_REQUIRED_KEYS = frozenset({...})  # PANEL-02 header keys
FIT_REPORT_SC1_KEYS = frozenset({...})       # SC-1 schema keys
def lint_fit_report_json(path: Path) -> list[str]: ...
def lint_fit_reports(root: Path) -> list[tuple[Path, list[str]]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dependence/* and hedge/* module skeletons + test stubs + lint extension + Makefile targets + pyproject deps + pre-commit gates + Quarto template + conftest fixtures</name>
  <files>
    analysis/src/abrigo_x402/dependence/__init__.py,
    analysis/src/abrigo_x402/dependence/cross_correlogram.py,
    analysis/src/abrigo_x402/dependence/permutation_null.py,
    analysis/src/abrigo_x402/dependence/copula.py,
    analysis/src/abrigo_x402/hedge/__init__.py,
    analysis/src/abrigo_x402/hedge/falsification.py,
    analysis/src/abrigo_x402/hedge/carr_madan_strip.py,
    analysis/src/abrigo_x402/hedge/stress_test.py,
    analysis/src/abrigo_x402/hedge/usdt_depeg.py,
    analysis/src/abrigo_x402/hedge/null_result.py,
    analysis/src/abrigo_x402/hedge/orchestrator.py,
    analysis/tests/test_cross_correlogram.py,
    analysis/tests/test_permutation_null.py,
    analysis/tests/test_copula_bic.py,
    analysis/tests/test_falsification.py,
    analysis/tests/test_carr_madan_strip.py,
    analysis/tests/test_stress_test.py,
    analysis/tests/test_usdt_depeg_lhs.py,
    analysis/tests/test_null_result_template.py,
    analysis/tests/test_joint_dist_provenance.py,
    analysis/tests/test_gate_report_provenance.py,
    analysis/tests/test_stress_report_provenance.py,
    analysis/tests/test_byte_identical_phase_4.py,
    analysis/tests/test_required_keys_sync.py,
    analysis/tests/conftest.py,
    analysis/INSTALL_TROUBLESHOOTING.md,
    reports/_templates/null_result.qmd,
    scripts/lint_artifacts.py,
    Makefile,
    analysis/pyproject.toml,
    analysis/README.md,
    .pre-commit-config.yaml
  </files>
  <read_first>
    - analysis/src/abrigo_x402/dgp/orchestrator.py (verbatim REQUIRED_FIT_REPORT_KEYS tuple shape + pre-write KeyError guard — Pattern G template to mirror)
    - analysis/src/abrigo_x402/dgp/lr_test.py (canonical-LL source for Wave-1 condition-3 imports)
    - analysis/src/abrigo_x402/provenance.py (with_header / assert_has_header API to reuse verbatim)
    - analysis/tests/conftest.py (existing panel_fixture + synthetic_hawkes_fixture + synthetic_nhpp_fixture; extend, do not replace)
    - scripts/lint_artifacts.py (FIT_REPORT_SC1_KEYS / FIT_REPORT_REQUIRED_KEYS / lint_fit_report_json shape — Wave-0 mirrors for joint_dist + gate_report + stress_report + strip + strip_degenerate)
    - .planning/phases/03-dgp-estimation-l4-with-boundary-correct-lr-test/03-00-PLAN.md (Wave-0 scaffold template; Phase 4 mirrors its frontmatter shape, must_haves shape, and the "skip-marked test stub" discipline)
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VALIDATION.md (Wave 0 Requirements section — full checklist of scaffold deliverables)
    - .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-RESEARCH.md §"Architecture Patterns" Pattern 1 (REQUIRED_KEYS + lint sync) + Pattern 5 (Carr-Madan polymorphic payoff signature)
    - notes/PRE_REGISTRATION.md §Carr-Madan Grid Numerical Tolerances (committed by Plan 04-pre — verify it exists before starting this task; if `grep -q "Carr-Madan Grid Numerical Tolerances" notes/PRE_REGISTRATION.md` fails, abort and run Plan 04-pre first)
  </read_first>
  <action>
    Execute in this order:

    **STEP 1 — Verify Plan 04-pre landed** (AF-03 prerequisite check):
    ```bash
    grep -q "Carr-Madan Grid Numerical Tolerances" notes/PRE_REGISTRATION.md || { echo "ABORT: Plan 04-pre not landed"; exit 1; }
    git log -1 --pretty=format:'%s' -- notes/PRE_REGISTRATION.md | grep -q "AF-03 amendment" || { echo "ABORT: PRE_REG amendment commit missing"; exit 1; }
    ```

    **STEP 2 — Create `analysis/src/abrigo_x402/dependence/` package** (4 files):

    `dependence/__init__.py`:
    ```python
    """Cross-leg dependence inference (Phase 4 L5)."""
    from abrigo_x402.dependence.cross_correlogram import cross_correlogram_event_index
    from abrigo_x402.dependence.permutation_null import permutation_null_max_abs_rho
    from abrigo_x402.dependence.copula import fit_5_families_bic, REQUIRED_JOINT_DIST_KEYS

    __all__ = [
        "cross_correlogram_event_index",
        "permutation_null_max_abs_rho",
        "fit_5_families_bic",
        "REQUIRED_JOINT_DIST_KEYS",
    ]
    ```

    `dependence/cross_correlogram.py`:
    ```python
    """Bowsher-2007 event-index lag-domain cross-correlogram. Substrate: residuals.parquet :: rescaled_dt per leg."""
    import numpy as np

    def cross_correlogram_event_index(
        leg_0_rescaled_dt: np.ndarray,
        leg_1_rescaled_dt: np.ndarray,
        max_lag: int = 50,
    ) -> dict:
        """Return {lags: list[int] of length 2*max_lag+1, values: list[float] of pearson rho per lag}.

        Bowsher-2007 event-index convention: for each event in leg_0, look at index-h-shifted
        event in leg_1, compute Pearson rho across the shifted pairs. CONTEXT.md locks max_lag=50
        and the substrate as rescaled_dt (Phase 3 DGP-05 output), per PITFALLS §4 stationarity discipline.
        """
        raise NotImplementedError("Plan 04-01 implements DEPEND-01 cross-correlogram")
    ```

    `dependence/permutation_null.py`:
    ```python
    """1000-rep within-window shuffle on rescaled_dt; max|rho(h)| statistic; deterministic seed."""
    import numpy as np

    def permutation_null_max_abs_rho(
        leg_0_rescaled_dt: np.ndarray,
        leg_1_rescaled_dt: np.ndarray,
        max_lag: int = 50,
        n_reps: int = 1000,
        seed: int = 20260527,
    ) -> dict:
        """Return {n_reps, p_value, max_abs_rho_observed, max_abs_rho_null_dist: list[float]}.

        Test statistic: max|rho(h)| over h in -max_lag..+max_lag.
        Permutation: within-window shuffle of leg_1_rescaled_dt via default_rng(seed).
        p_value: empirical fraction of perm-max exceeding observed-max.
        """
        raise NotImplementedError("Plan 04-02 implements DEPEND-01 permutation null")
    ```

    `dependence/copula.py`:
    ```python
    """5-family BIC menu (Gaussian + t + Clayton + Frank + Gumbel) via copulae==0.8.0.
    Vine fallback via pyvinecopulib==0.7.6 ONLY if ΔBIC >= 5 in favor of vine (deferred install).
    """
    import numpy as np

    REQUIRED_JOINT_DIST_KEYS: tuple[str, ...] = (
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "cross_correlogram",   # {lags: [...], values: [...]}
        "permutation_null",    # {n_reps: int, p_value: float, max_abs_rho_observed: float}
        "empirical_copula",    # {family, params, bic, all_candidates_bic: {gaussian, t, clayton, frank, gumbel}}
        "vine_fallback_used",  # bool
    )

    VINE_FALLBACK_DELTA_BIC_THRESHOLD: float = 5.0  # CONTEXT.md locked

    def fit_5_families_bic(u_data: np.ndarray) -> dict:
        """Fit Gaussian + Student-t + Clayton + Frank + Gumbel copulae on PIT-uniform (N,2) data.

        Returns: {winner: str, all_candidates: {family: {params, log_lik, bic}}, vine_fallback_used: bool}.
        BIC = -2 * log_lik + k * log(n). Winner = argmin(bic).
        vine_fallback_used: only True if a vine pair-copula construction yields bic <= bivariate_bic_min - 5
        (per PITFALLS Pitfall 2 — vine library defer per RESEARCH Open Question 3).
        """
        raise NotImplementedError("Plan 04-03 implements DEPEND-01 5-family BIC ranking")
    ```

    **STEP 3 — Create `analysis/src/abrigo_x402/hedge/` package** (7 files):

    `hedge/__init__.py`:
    ```python
    """Falsification gate + Carr-Madan replicating strip + null-result rendering (Phase 4 L6)."""
    from abrigo_x402.hedge.falsification import (
        evaluate_condition_1_vol_of_vol,
        evaluate_condition_2_skew_fat_tails,
        evaluate_condition_3_hawkes_self_excitation,
        evaluate_condition_4_usdt_depeg,
        REQUIRED_GATE_REPORT_KEYS,
    )
    from abrigo_x402.hedge.carr_madan_strip import (
        compute_strip,
        REQUIRED_STRIP_KEYS,
        STRIP_DEGENERATE_KEYS,
    )
    from abrigo_x402.hedge.stress_test import run_three_way_stress, REQUIRED_STRESS_REPORT_KEYS
    from abrigo_x402.hedge.usdt_depeg import load_calibration, generate_lhs_samples
    from abrigo_x402.hedge.null_result import decide_firing_condition, render_null_result_pdf, HEDGE05_SIGNATURE
    from abrigo_x402.hedge.orchestrator import run_hedge

    __all__ = [
        "evaluate_condition_1_vol_of_vol", "evaluate_condition_2_skew_fat_tails",
        "evaluate_condition_3_hawkes_self_excitation", "evaluate_condition_4_usdt_depeg",
        "REQUIRED_GATE_REPORT_KEYS", "compute_strip", "REQUIRED_STRIP_KEYS", "STRIP_DEGENERATE_KEYS",
        "run_three_way_stress", "REQUIRED_STRESS_REPORT_KEYS",
        "load_calibration", "generate_lhs_samples",
        "decide_firing_condition", "render_null_result_pdf", "HEDGE05_SIGNATURE",
        "run_hedge",
    ]
    ```

    `hedge/falsification.py`:
    ```python
    """Four-condition convex-dominance gate per SOMNIA_DRAFT §FUNCTIONAL FORM.
    Condition 4 is USDT depeg + USDT/USDC basis (SC-2 — NOT USDC alone).
    """
    REQUIRED_GATE_REPORT_KEYS: tuple[str, ...] = (
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "vol_of_vol_gt_zero",         # {passed: bool, evidence: dict}
        "positive_skew_fat_tails",    # {passed: bool, evidence: dict}
        "hawkes_self_excitation",     # {passed: bool, evidence: dict}
        "usdt_depeg_basis_jump",      # {passed: bool, evidence: dict (incl. sensitivity_fragile + sensitivity_summary)}
        "any_condition_passed",       # bool — HEDGE-05 firing condition (c) consumes this
    )

    def evaluate_condition_1_vol_of_vol(residuals_df) -> dict:
        """Return {passed: bool, evidence: {...}}. Condition 1: vol-of-vol > 0 per leg."""
        raise NotImplementedError("Plan 04-04 implements HEDGE-01 condition 1")

    def evaluate_condition_2_skew_fat_tails(residuals_df) -> dict:
        """Return {passed: bool, evidence: {...}}. Condition 2: positive skew / fat tails per leg."""
        raise NotImplementedError("Plan 04-04 implements HEDGE-01 condition 2")

    def evaluate_condition_3_hawkes_self_excitation(fit_report: dict) -> dict:
        """Return {passed: bool, evidence: {...}}. Reads fit_report :: hawkes_mv_params :: branching_ratio
        + gate_criteria.eta_floor_met from Phase 3. Reuses canonical _hawkes_loglik_vectorized for any
        re-evaluation (Pattern F — NOT tick.score())."""
        raise NotImplementedError("Plan 04-04 implements HEDGE-01 condition 3")

    def evaluate_condition_4_usdt_depeg(calibration: dict, lhs_samples) -> dict:
        """Return {passed: bool, evidence: {source: 'literature_range_stipulation', base_triple: {...},
        sensitivity_fragile: bool, sensitivity_summary: {n_samples: 64, n_flips: int, flip_examples: [...]}}}.
        DEPEG framing: USDT depeg + USDT/USDC basis jump (SC-2 — NOT USDC alone)."""
        raise NotImplementedError("Plan 04-04 implements HEDGE-01 condition 4")
    ```

    `hedge/carr_madan_strip.py`:
    ```python
    """FFT-based Carr-Madan static-replication strip on convergence-tested grid.
    Per RESEARCH Pattern 5 — polymorphic payoff signature for v2.0 streaming-tokenization.
    Per PRE_REGISTRATION §Carr-Madan Grid Numerical Tolerances: 0.1% positivity tolerance,
    2^11->2^12 escalation, abort to strip_degenerate.json after 2^12 fails.
    """
    from typing import Callable
    import numpy as np

    REQUIRED_STRIP_KEYS: tuple[str, ...] = (
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "strip_prices",            # list[float] per strike
        "strikes",                  # list[float]
        "n_grid_used",              # int — 2^11 or 2^12
        "escalated_to_2_12",        # bool
        "negative_mass_fraction",   # float
        "positivity_tolerance",     # float — 0.001 from PRE_REG amendment
    )

    STRIP_DEGENERATE_KEYS: tuple[str, ...] = (
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "max_negative_value",
        "total_negative_mass",
        "characteristic_function_decay_rate",
        "recommended_method",     # "COS" | "PROJ" | "none"
    )

    POSITIVITY_TOLERANCE: float = 0.001  # PRE_REGISTRATION amendment 2026-05-27

    def compute_strip(
        payoff: Callable[[np.ndarray], np.ndarray],   # f(S_T) — v2.0 polymorphism per RESEARCH Pattern 5
        char_func: Callable[[np.ndarray], np.ndarray],
        n_grid: int = 2**11,
        positivity_tolerance: float = POSITIVITY_TOLERANCE,
        max_escalations: int = 1,
    ) -> dict:
        """Return strip dict or strip_degenerate dict. Uses np.fft.fft / np.fft.ifft ONLY —
        NEVER scipy.integrate.quad / fixed_quad / romberg / np.trapz."""
        raise NotImplementedError("Plan 04-05 implements HEDGE-02 Carr-Madan strip")
    ```

    `hedge/stress_test.py`:
    ```python
    """Three-way joint-distribution stress: independence + fitted_joint + comonotone (Frechet upper bound).
    Comonotone via shared uniform U_2 = U_1; reproducible from empirical marginals alone (CONTEXT.md locked).
    """
    import numpy as np

    REQUIRED_STRESS_REPORT_KEYS: tuple[str, ...] = (
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "independence_price",
        "fitted_joint_price",
        "comonotone_price",
        "divergence_pct",         # (max - min) / mean * 100
        "divergence_flag",        # bool — True if divergence_pct > 30
        "comonotone_method",      # "empirical_body_parametric_tail" per RESEARCH Pitfall 6
    )

    DIVERGENCE_FLAG_THRESHOLD_PCT: float = 30.0  # CONTEXT.md locked — flag-only, no hard-fail

    def run_three_way_stress(
        payoff,
        marginal_cdf_leg_0,
        marginal_cdf_leg_1,
        fitted_copula,
        n_samples: int = 10_000,
        seed: int = 20260527,
    ) -> dict:
        """Independence: marginal CDF leg_0 x marginal CDF leg_1.
        Fitted joint: empirical copula sample (copulae.fit().random(N)).
        Comonotone: shared U ~ U(0,1), U_2 = U_1, inverse-transform per leg (Frechet upper bound)."""
        raise NotImplementedError("Plan 04-07 implements HEDGE-04 three-way stress test")
    ```

    `hedge/usdt_depeg.py`:
    ```python
    """USDT depeg jump-leg calibration: literature-range stipulation (Merton 1976 ballpark) + N=64 LHS.
    CONTEXT.md (commit e600d3a): evidence_source = 'literature_range_stipulation'. Hernandez Cruz 2024
    and Wu & Liu 2026 do NOT publish jump-diffusion parameters and MUST NOT be cited as parameter sources.
    """
    import numpy as np
    from scipy.stats import qmc

    DEFAULT_LAMBDA_J: float = 0.05        # jumps per year (Merton 1976 stablecoin-class ballpark)
    DEFAULT_MU_J: float = -0.05           # log-return per jump
    DEFAULT_SIGMA_J: float = 0.02         # log-return std per jump
    LHS_N_SAMPLES: int = 64
    LHS_BOUND_RATIO: float = 0.5          # ±50% per CONTEXT.md
    LHS_SEED: int = 20260527

    def load_calibration(calibration_path: str = "notes/usdt_depeg_calibration.md") -> dict:
        """Return {evidence_source: 'literature_range_stipulation', base_triple: {lambda_J, mu_J, sigma_J}}.
        Reads the markdown doc and extracts the stipulated triple."""
        raise NotImplementedError("Plan 04-06 implements HEDGE-03 calibration loader")

    def generate_lhs_samples(
        base_lambda_J: float = DEFAULT_LAMBDA_J,
        base_mu_J: float = DEFAULT_MU_J,
        base_sigma_J: float = DEFAULT_SIGMA_J,
        n_samples: int = LHS_N_SAMPLES,
        bound_ratio: float = LHS_BOUND_RATIO,
        seed: int = LHS_SEED,
    ) -> np.ndarray:
        """Return (n_samples, 3) array of (lambda_J, mu_J, sigma_J) samples on ±bound_ratio bracket.
        Implementation: scipy.stats.qmc.LatinHypercube(d=3, seed=seed) + qmc.scale(samples,
        l_bounds=base*(1-bound_ratio), u_bounds=base*(1+bound_ratio))."""
        raise NotImplementedError("Plan 04-06 implements HEDGE-03 LHS sampler")
    ```

    `hedge/null_result.py`:
    ```python
    """HEDGE-05 firing decision + Quarto null-result PDF rendering.
    Dual signature markers: visible H1 + \\pdfinfo HEDGE05-NULL-RESULT-V1 (PDF metadata)."""
    from pathlib import Path

    HEDGE05_SIGNATURE: str = "HEDGE05-NULL-RESULT-V1"

    def decide_firing_condition(
        fit_report: dict,
        gate_report: dict,
        cost_leg_bound_path: Path | None = None,
    ) -> str | None:
        """Decision tree (per CONTEXT.md):
        1. If cost_leg_bound_path exists AND parses to verdict='FAIL' -> return 'null_cost'
        2. Elif fit_report['lr_test']['p_value'] >= 0.05 -> return 'null_lr'
        3. Elif gate_report['any_condition_passed'] is False -> return 'null_convex'
        4. Else -> return None (no firing; positive result path)
        """
        raise NotImplementedError("Plan 04-08 implements HEDGE-05 firing decision")

    def render_null_result_pdf(
        firing_condition: str,
        fit_report: dict,
        gate_report: dict,
        output_path: Path = Path("reports/ichi.pdf"),
        template: Path = Path("reports/_templates/null_result.qmd"),
    ) -> Path:
        """Invoke `quarto render <template> --param firing_condition:<firing_condition> --no-cache --output <output_path>`.
        Inject HEDGE05_SIGNATURE into PDF metadata via \\pdfinfo block in template.
        Per Pitfall 3: always pass --no-cache for determinism."""
        raise NotImplementedError("Plan 04-08 implements HEDGE-05 PDF render")
    ```

    `hedge/orchestrator.py`:
    ```python
    """Hedge CLI entry: wires dependence + falsification + strip + stress + null_result modules.
    Single `hedge` subcommand with --stage flag per CONTEXT.md Claude's Discretion."""
    from pathlib import Path

    def run_hedge(run_id: str, stage: str = "all", run_dir_root: Path = Path("data/fits/ichi")) -> dict:
        """stage ∈ {'dependence', 'gate', 'strip', 'stress', 'null', 'all'}.
        Writes (per stage): joint_dist.json | gate_report.json | (strip.json | strip_degenerate.json) |
        stress_report.json | reports/ichi.pdf.
        Returns a dict with paths to all written artifacts + firing_condition (None if no fire)."""
        raise NotImplementedError("Plan 04-08 implements hedge orchestrator + CLI subcommand")
    ```

    **STEP 4 — Create 12 skip-marked test stubs** at canonical Wave-1 surface:

    Pattern for each `analysis/tests/test_<module>.py`:
    ```python
    """Wave-1 will replace skip markers with real tests. Plan 04-NN owns this file."""
    import pytest

    pytestmark = pytest.mark.skip(reason="Plan 04-NN (Wave 1) implements")

    # Import-only smoke (validates the module symbol surface for Wave 1 to land against):
    def test_smoke_imports():
        from abrigo_x402.<module> import <symbol>  # imports MUST succeed even if NotImplementedError
    ```

    Specific test files + Wave-1-plan ownership + imports:
    - `test_cross_correlogram.py` -> Plan 04-01 -> `from abrigo_x402.dependence.cross_correlogram import cross_correlogram_event_index`
    - `test_permutation_null.py` -> Plan 04-02 -> `from abrigo_x402.dependence.permutation_null import permutation_null_max_abs_rho`
    - `test_copula_bic.py` -> Plan 04-03 -> `from abrigo_x402.dependence.copula import fit_5_families_bic, REQUIRED_JOINT_DIST_KEYS, VINE_FALLBACK_DELTA_BIC_THRESHOLD`
    - `test_falsification.py` -> Plan 04-04 -> `from abrigo_x402.hedge.falsification import evaluate_condition_1_vol_of_vol, evaluate_condition_2_skew_fat_tails, evaluate_condition_3_hawkes_self_excitation, evaluate_condition_4_usdt_depeg, REQUIRED_GATE_REPORT_KEYS`
    - `test_carr_madan_strip.py` -> Plan 04-05 -> `from abrigo_x402.hedge.carr_madan_strip import compute_strip, REQUIRED_STRIP_KEYS, STRIP_DEGENERATE_KEYS, POSITIVITY_TOLERANCE`
    - `test_usdt_depeg_lhs.py` -> Plan 04-06 -> `from abrigo_x402.hedge.usdt_depeg import load_calibration, generate_lhs_samples, DEFAULT_LAMBDA_J, DEFAULT_MU_J, DEFAULT_SIGMA_J, LHS_N_SAMPLES`
    - `test_stress_test.py` -> Plan 04-07 -> `from abrigo_x402.hedge.stress_test import run_three_way_stress, REQUIRED_STRESS_REPORT_KEYS, DIVERGENCE_FLAG_THRESHOLD_PCT`
    - `test_null_result_template.py` -> Plan 04-08 -> `from abrigo_x402.hedge.null_result import decide_firing_condition, render_null_result_pdf, HEDGE05_SIGNATURE`
    - `test_joint_dist_provenance.py` -> Plan 04-03 -> imports REQUIRED_JOINT_DIST_KEYS + provenance.assert_has_header
    - `test_gate_report_provenance.py` -> Plan 04-04 -> imports REQUIRED_GATE_REPORT_KEYS + provenance.assert_has_header
    - `test_stress_report_provenance.py` -> Plan 04-07 -> imports REQUIRED_STRESS_REPORT_KEYS + provenance.assert_has_header
    - `test_byte_identical_phase_4.py` -> Plan 04-08 -> first 4 lines MUST be `os.environ.setdefault(...)` thread-pinning per Pattern I (copy verbatim from analysis/tests/test_byte_identical.py)
    - `test_required_keys_sync.py` -> Plan 04-00 (iter-3 addition; runs at scaffold time, NOT skip-marked) -> asserts every `REQUIRED_*_KEYS` tuple in `analysis/src/abrigo_x402/{dependence,hedge}/*.py` equals the corresponding `*_REQUIRED_KEYS` frozenset in `scripts/lint_artifacts.py`. Real test at Wave 0 (no skip marker); catches the Wave-0 mismatched-tuple regression before Wave 1 lands.

    Specifically `test_required_keys_sync.py` MUST contain (no skip marker):
    ```python
    import sys
    from pathlib import Path
    sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "scripts"))
    from abrigo_x402.dependence.copula import REQUIRED_JOINT_DIST_KEYS
    from abrigo_x402.hedge.falsification import REQUIRED_GATE_REPORT_KEYS
    from abrigo_x402.hedge.stress_test import REQUIRED_STRESS_REPORT_KEYS
    from abrigo_x402.hedge.carr_madan_strip import REQUIRED_STRIP_KEYS, STRIP_DEGENERATE_KEYS
    from lint_artifacts import (
        JOINT_DIST_REQUIRED_KEYS, GATE_REPORT_REQUIRED_KEYS,
        STRESS_REPORT_REQUIRED_KEYS, STRIP_REQUIRED_KEYS, STRIP_DEGENERATE_REQUIRED_KEYS,
    )

    def test_joint_dist_keys_sync():
        assert frozenset(REQUIRED_JOINT_DIST_KEYS) == JOINT_DIST_REQUIRED_KEYS

    def test_gate_report_keys_sync():
        assert frozenset(REQUIRED_GATE_REPORT_KEYS) == GATE_REPORT_REQUIRED_KEYS

    def test_stress_report_keys_sync():
        assert frozenset(REQUIRED_STRESS_REPORT_KEYS) == STRESS_REPORT_REQUIRED_KEYS

    def test_strip_keys_sync():
        assert frozenset(REQUIRED_STRIP_KEYS) == STRIP_REQUIRED_KEYS

    def test_strip_degenerate_keys_sync():
        assert frozenset(STRIP_DEGENERATE_KEYS) == STRIP_DEGENERATE_REQUIRED_KEYS
    ```

    For `test_byte_identical_phase_4.py` specifically, copy the verbatim thread-pinning header from `analysis/tests/test_byte_identical.py:1-15` BEFORE any other code (the first 4 executable lines MUST be the four `os.environ.setdefault` calls for OMP/MKL/OpenBLAS/NumExpr).

    **STEP 5 — Extend `analysis/tests/conftest.py`**:

    Append three new fixtures (do NOT remove existing fixtures):
    ```python
    import pytest
    from pathlib import Path

    FIXTURES_DIR = Path(__file__).parent / "fixtures"

    @pytest.fixture
    def joint_dist_fixture():
        """Minimal valid joint_dist.json dict for provenance + lint tests."""
        return {
            "chainId": 42220,
            "contractAddress": "0x" + "00"*20,
            "blockRange": [0, 100],
            "fetchTimestamp": "2026-05-27T00:00:00+00:00",
            "dataHash": "sha256:" + "0"*64,
            "gitCommit": "0"*40,
            "run_id": "0"*12,
            "cross_correlogram": {"lags": [-1, 0, 1], "values": [0.1, 1.0, 0.1]},
            "permutation_null": {"n_reps": 1000, "p_value": 0.5, "max_abs_rho_observed": 0.1},
            "empirical_copula": {"family": "gaussian", "params": [0.1], "bic": -100.0,
                                 "all_candidates_bic": {"gaussian": -100.0, "t": -98.0,
                                                         "clayton": -90.0, "frank": -85.0, "gumbel": -80.0}},
            "vine_fallback_used": False,
        }

    @pytest.fixture
    def gate_report_fixture():
        """Minimal valid gate_report.json dict for provenance + lint tests."""
        return {
            "chainId": 42220, "contractAddress": "0x" + "00"*20, "blockRange": [0, 100],
            "fetchTimestamp": "2026-05-27T00:00:00+00:00",
            "dataHash": "sha256:" + "0"*64, "gitCommit": "0"*40, "run_id": "0"*12,
            "vol_of_vol_gt_zero": {"passed": False, "evidence": {}},
            "positive_skew_fat_tails": {"passed": False, "evidence": {}},
            "hawkes_self_excitation": {"passed": False, "evidence": {}},
            "usdt_depeg_basis_jump": {"passed": False, "evidence": {
                "source": "literature_range_stipulation",
                "base_triple": {"lambda_J": 0.05, "mu_J": -0.05, "sigma_J": 0.02},
                "sensitivity_fragile": False,
                "sensitivity_summary": {"n_samples": 64, "n_flips": 0, "flip_examples": []},
            }},
            "any_condition_passed": False,
        }

    @pytest.fixture(params=["null_cost", "null_lr", "null_convex"])
    def null_result_fixture_triplet(request):
        """Parametrized fixture yielding paths to the three hedge_05_* fixture triplets."""
        return FIXTURES_DIR / f"hedge_05_{request.param}"
    ```

    **STEP 6 — Create three HEDGE-05 fixture triplets** at `analysis/tests/fixtures/hedge_05_{null_cost,null_lr,null_convex}/`:

    For `hedge_05_null_cost/`:
    - `fit_report.json`: synthetic fit_report with `lr_test.p_value = 0.001` (LR rejects — Hawkes wins) + `gate_passes: true` + `gate_criteria.eta_floor_met: true` so DGP+gate pass; the firing condition is the cost_leg_bound.
    - `gate_report.json`: synthetic gate_report with `any_condition_passed: true`.
    - `cost_leg_bound.md`: hand-authored markdown with frontmatter `verdict: FAIL` and explanation text.

    For `hedge_05_null_lr/`:
    - `fit_report.json`: synthetic fit_report with `lr_test.p_value = 0.45` (LR cannot reject NHPP at α=0.05).
    - `gate_report.json`: synthetic gate_report with `any_condition_passed: true` (other conditions don't matter; LR firing dominates).
    - `cost_leg_bound.md`: hand-authored markdown with frontmatter `verdict: PASS`.

    For `hedge_05_null_convex/`:
    - `fit_report.json`: synthetic fit_report with `lr_test.p_value = 0.001` (LR rejects).
    - `gate_report.json`: synthetic gate_report with all four conditions `passed: false` and `any_condition_passed: false`.
    - `cost_leg_bound.md`: hand-authored markdown with frontmatter `verdict: PASS`.

    Each `fit_report.json` MUST have the full PANEL-02 header + all keys in REQUIRED_FIT_REPORT_KEYS (use sensible synthetic values; this is a fixture, not real data — `chainId: 42220`, `contractAddress: "0x" + "00"*20`, `blockRange: [0, 100]`, etc).

    Each `gate_report.json` MUST have the full PANEL-02 header + all keys in REQUIRED_GATE_REPORT_KEYS.

    Each `cost_leg_bound.md` follows the format:
    ```markdown
    ---
    verdict: FAIL  # or PASS
    protocol: ichi-fixture
    threshold_queries_per_month: 100000
    measured_queries_per_month: 5000  # FAIL case: below threshold; PASS case: 150000
    ---

    # Cost-Leg Bound Memo (FIXTURE — synthetic for HEDGE-05 firing test)

    This fixture forces firing condition (a) when verdict=FAIL.
    ```

    **STEP 7 — Create `reports/_templates/null_result.qmd`** Quarto template:

    ```yaml
    ---
    title: "HEDGE-05 Null Result"
    format:
      pdf:
        documentclass: article
        include-in-header:
          text: |
            \pdfinfo{
              /HEDGE05Marker (HEDGE05-NULL-RESULT-V1)
            }
    execute:
      freeze: false
      cache: false
    params:
      firing_condition: null  # "null_cost" | "null_lr" | "null_convex"
    ---

    # HEDGE-05 NULL RESULT — `r params$firing_condition`

    <!-- Dual signature: visible H1 above + \pdfinfo HEDGE05-NULL-RESULT-V1 marker in PDF metadata. -->
    <!-- Greppable via: pdftotext reports/ichi.pdf - | grep "HEDGE-05 NULL RESULT" -->
    <!-- AND: pdfinfo reports/ichi.pdf | grep "HEDGE05Marker" -->

    ```{python}
    #| label: load-substrate
    import json
    from pathlib import Path

    firing_condition = "{{< meta params.firing_condition >}}"
    # Substrate loaded by render_null_result_pdf at invocation time via --param + injected files.
    ```

    ::: {.callout-note}
    ## Firing Condition

    This null-result PDF was generated because firing condition **`{python} firing_condition`** tripped.
    :::

    ## Evidence Block

    {{< include _evidence_branches.qmd >}}

    <!-- Wave 2 (Plan 04-08) implements the three conditional branches via Quarto conditional content
         keyed off `params$firing_condition`. Wave 0 scaffolds only the dual-signature shell. -->

    ## Pre-Registration Reference

    This null result is a valid completion per [PRE_REGISTRATION.md §Decision Rules](../../notes/PRE_REGISTRATION.md#decision-rules).
    Null results are explicitly publishable per AF-03 discipline.
    ```

    The `_evidence_branches.qmd` partial may be a placeholder for Wave 2 — create as an empty file `reports/_templates/_evidence_branches.qmd` with a Wave-2 TODO comment.

    **STEP 8 — Extend `scripts/lint_artifacts.py`**:

    Append to the existing file (keep existing FIT_REPORT_REQUIRED_KEYS / FIT_REPORT_SC1_KEYS / lint_fit_report_json / lint_fit_reports unchanged):

    ```python
    # ----- Phase 4 extensions (mirror REQUIRED_*_KEYS tuples from owning modules) -----

    # Sync source: analysis/src/abrigo_x402/dependence/copula.py :: REQUIRED_JOINT_DIST_KEYS
    JOINT_DIST_REQUIRED_KEYS = frozenset({
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "cross_correlogram", "permutation_null", "empirical_copula", "vine_fallback_used",
    })

    # Sync source: analysis/src/abrigo_x402/hedge/falsification.py :: REQUIRED_GATE_REPORT_KEYS
    GATE_REPORT_REQUIRED_KEYS = frozenset({
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "vol_of_vol_gt_zero", "positive_skew_fat_tails",
        "hawkes_self_excitation", "usdt_depeg_basis_jump",
        "any_condition_passed",
    })

    # Sync source: analysis/src/abrigo_x402/hedge/stress_test.py :: REQUIRED_STRESS_REPORT_KEYS
    STRESS_REPORT_REQUIRED_KEYS = frozenset({
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "independence_price", "fitted_joint_price", "comonotone_price",
        "divergence_pct", "divergence_flag", "comonotone_method",
    })

    # Sync source: analysis/src/abrigo_x402/hedge/carr_madan_strip.py :: REQUIRED_STRIP_KEYS
    STRIP_REQUIRED_KEYS = frozenset({
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "strip_prices", "strikes", "n_grid_used", "escalated_to_2_12",
        "negative_mass_fraction", "positivity_tolerance",
    })

    # Sync source: analysis/src/abrigo_x402/hedge/carr_madan_strip.py :: STRIP_DEGENERATE_KEYS
    STRIP_DEGENERATE_REQUIRED_KEYS = frozenset({
        "chainId", "contractAddress", "blockRange",
        "fetchTimestamp", "dataHash", "gitCommit", "run_id",
        "max_negative_value", "total_negative_mass",
        "characteristic_function_decay_rate", "recommended_method",
    })

    def lint_joint_dist_json(path: Path) -> list[str]: ...    # mirror lint_fit_report_json shape
    def lint_gate_report_json(path: Path) -> list[str]: ...
    def lint_stress_report_json(path: Path) -> list[str]: ...
    def lint_strip_json(path: Path) -> list[str]: ...
    def lint_strip_degenerate_json(path: Path) -> list[str]: ...

    def lint_phase_4_artifacts(root: Path) -> list[tuple[Path, list[str]]]:
        """Walk data/fits/**/{joint_dist,gate_report,stress_report,strip,strip_degenerate}.json
        and apply the appropriate linter to each. Returns aggregated errors."""
        ...
    ```

    Update `main(argv)` to invoke `lint_phase_4_artifacts(repo_root)` after `lint_fit_reports(repo_root)` and merge errors.

    **STEP 9 — Extend `Makefile`**:

    Add three new targets (keep existing targets unchanged):

    ```makefile
    .PHONY: render-null-result-pdf render-strip-diagnostic phase-4-acceptance

    render-null-result-pdf:
    	@if [ -z "$$FIRING" ]; then echo "Usage: make render-null-result-pdf FIRING={null_cost|null_lr|null_convex}"; exit 1; fi
    	cd reports && quarto render _templates/null_result.qmd --no-cache --param firing_condition:$$FIRING --output ../reports/ichi.pdf

    render-strip-diagnostic:
    	cd analysis && uv run python -m abrigo_x402.cli hedge --stage strip --run-id $$RUN_ID

    phase-4-acceptance:
    	@echo "=== Phase 4 acceptance gate ==="
    	cd analysis && uv run pytest tests/test_cross_correlogram.py tests/test_permutation_null.py tests/test_copula_bic.py tests/test_falsification.py tests/test_carr_madan_strip.py tests/test_stress_test.py tests/test_usdt_depeg_lhs.py tests/test_null_result_template.py tests/test_joint_dist_provenance.py tests/test_gate_report_provenance.py tests/test_stress_report_provenance.py tests/test_byte_identical_phase_4.py -x
    	@! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py
    	@! grep -E "scipy\.integrate\.quad|np\.trapz" analysis/src/abrigo_x402/hedge/carr_madan_strip.py
    	@! grep -rE "loglik_in_sample_raw" analysis/src/abrigo_x402/hedge/
    	@! grep -E "port from Hernandez Cruz" notes/usdt_depeg_calibration.md 2>/dev/null || true
    	$(MAKE) lint-artifacts
    	@echo "=== Phase 4 acceptance: PASS ==="
    ```

    **STEP 10 — Extend `analysis/pyproject.toml`** + **verify copulae install** (iter-3 fix for Issue 2):

    Add to `[project] dependencies`:
    ```toml
    "copulae==0.8.0",
    ```

    Add to `[dependency-groups] dev`:
    ```toml
    "jupyter",
    ```

    Do NOT add `pyvinecopulib` (deferred per RESEARCH Open Question 3 — install in a follow-up plan if Wave-1 empirical fit triggers ΔBIC≥5 vine fallback).

    **iter-3 install verification (mandatory after `uv sync`):**
    ```bash
    cd analysis && uv sync
    cd analysis && uv run python -c "import copulae; print(copulae.__version__)" \
      || { echo "ABORT: copulae install failed — see analysis/INSTALL_TROUBLESHOOTING.md"; exit 1; }
    ```

    **iter-3 documented fallback paths** (write to `analysis/INSTALL_TROUBLESHOOTING.md`):
    ```markdown
    # Copulae Install Troubleshooting (Phase 4)

    Primary: `copulae==0.8.0` via `uv sync`. If that fails:

    1. **Wheel unavailable on platform** — try the prior minor: `uv add copulae==0.7.10` (relaxes
       numpy upper bound; verify `import copulae` still succeeds and the 5-family API
       `NormalCopula/StudentCopula/ClaytonCopula/FrankCopula/GumbelCopula` is intact — Plan 04-03
       smoke test catches API drift).
    2. **Nightly wheels** — `uv pip install --index-url https://test.pypi.org/simple/ copulae`
       (use ONLY as a transient unblock; document in the run log).
    3. **Total failure (0.7.x and 0.8.x both unavailable)** — workflow BLOCKS. A v1.1 follow-up plan
       must implement a minimal 5-family BIC fitter directly on `scipy.stats.multivariate_normal`
       (Gaussian + t) + hand-rolled Archimedean log-likelihoods (Clayton/Frank/Gumbel via the
       generator-function MLE). NOT in Phase 4 scope; surface the block immediately.
    ```

    **STEP 11 — Extend `analysis/README.md`** (or create if absent):

    Add a `## System Dependencies (Phase 4)` section:
    ```markdown
    ## System Dependencies (Phase 4)

    Phase 4 requires Quarto + a LaTeX engine for null-result PDF rendering:

    ```bash
    # Arch
    sudo pacman -S quarto texlive-luatex texlive-latexextra

    # Ubuntu / Debian
    sudo apt-get install quarto texlive-luatex

    # Alternative (cross-platform, self-managed):
    quarto install tinytex
    ```

    Verify: `quarto --version && lualatex --version`
    ```

    **STEP 12 — Extend `.pre-commit-config.yaml`** with four new local grep gates:

    ```yaml
    - repo: local
      hooks:
        - id: sc-2-usdc-literal-gate
          name: SC-2 grep gate (no usdc literals outside comments in hedge/falsification.py)
          entry: bash -c '! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py'
          language: system
          pass_filenames: false
          always_run: true

        - id: carr-madan-anti-pattern-gate
          name: Carr-Madan integration anti-pattern grep gate
          entry: bash -c '! grep -E "scipy\.integrate\.quad|scipy\.integrate\.fixed_quad|scipy\.integrate\.romberg|np\.trapz" analysis/src/abrigo_x402/hedge/carr_madan_strip.py 2>/dev/null'
          language: system
          pass_filenames: false
          always_run: true

        - id: canonical-ll-gate
          name: Canonical-LL contract gate (no loglik_in_sample_raw in hedge/*)
          entry: bash -c '! grep -rE "loglik_in_sample_raw" analysis/src/abrigo_x402/hedge/ 2>/dev/null'
          language: system
          pass_filenames: false
          always_run: true

        - id: hardcoded-jump-params-gate
          name: Hardcoded jump-diffusion params gate (params only live in usdt_depeg.py + notes/usdt_depeg_calibration.md)
          entry: bash -c '! grep -rE "lambda_J\s*=\s*0\.|mu_J\s*=\s*-?0\.|sigma_J\s*=\s*0\." analysis/src/abrigo_x402/hedge/falsification.py analysis/src/abrigo_x402/hedge/carr_madan_strip.py 2>/dev/null'
          language: system
          pass_filenames: false
          always_run: true
    ```

    **STEP 13 — Validate the scaffold** (run before commit):
    ```bash
    cd analysis && uv sync                      # picks up copulae==0.8.0 + jupyter
    cd analysis && uv run pytest --collect-only # MUST exit 0
    make lint-artifacts                          # MUST exit 0 (no malformed JSON because none yet)
    cd analysis && uv run python -c "from abrigo_x402.dependence import *; from abrigo_x402.hedge import *; print('OK')"  # MUST print OK
    pre-commit run --all-files                   # MAY exit non-zero (some existing files may fail new gates); document any failures and either fix or note as deferred
    ```

    Commit the scaffold (single commit covering all ~30 files):
    ```bash
    git add analysis/src/abrigo_x402/dependence/ analysis/src/abrigo_x402/hedge/ \
            analysis/tests/test_cross_correlogram.py analysis/tests/test_permutation_null.py \
            analysis/tests/test_copula_bic.py analysis/tests/test_falsification.py \
            analysis/tests/test_carr_madan_strip.py analysis/tests/test_stress_test.py \
            analysis/tests/test_usdt_depeg_lhs.py analysis/tests/test_null_result_template.py \
            analysis/tests/test_joint_dist_provenance.py analysis/tests/test_gate_report_provenance.py \
            analysis/tests/test_stress_report_provenance.py analysis/tests/test_byte_identical_phase_4.py \
            analysis/tests/conftest.py analysis/tests/fixtures/hedge_05_null_cost/ \
            analysis/tests/fixtures/hedge_05_null_lr/ analysis/tests/fixtures/hedge_05_null_convex/ \
            reports/_templates/null_result.qmd reports/_templates/_evidence_branches.qmd \
            scripts/lint_artifacts.py Makefile analysis/pyproject.toml analysis/README.md \
            .pre-commit-config.yaml
    git commit -m "scaffold(04): Phase 4 dependence + hedge module skeletons + HEDGE-05 fixtures + lint extension + pre-commit gates"
    ```
  </action>
  <verify>
    <automated>cd analysis &amp;&amp; uv run pytest --collect-only -q 2>&amp;1 | tail -5 &amp;&amp; cd .. &amp;&amp; make lint-artifacts &amp;&amp; cd analysis &amp;&amp; uv run python -c "from abrigo_x402.dependence import cross_correlogram_event_index, permutation_null_max_abs_rho, fit_5_families_bic, REQUIRED_JOINT_DIST_KEYS; from abrigo_x402.hedge import evaluate_condition_1_vol_of_vol, evaluate_condition_2_skew_fat_tails, evaluate_condition_3_hawkes_self_excitation, evaluate_condition_4_usdt_depeg, REQUIRED_GATE_REPORT_KEYS, compute_strip, REQUIRED_STRIP_KEYS, STRIP_DEGENERATE_KEYS, POSITIVITY_TOLERANCE, run_three_way_stress, REQUIRED_STRESS_REPORT_KEYS, DIVERGENCE_FLAG_THRESHOLD_PCT, load_calibration, generate_lhs_samples, DEFAULT_LAMBDA_J, decide_firing_condition, render_null_result_pdf, HEDGE05_SIGNATURE, run_hedge; assert HEDGE05_SIGNATURE == 'HEDGE05-NULL-RESULT-V1'; assert POSITIVITY_TOLERANCE == 0.001; assert DIVERGENCE_FLAG_THRESHOLD_PCT == 30.0; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 source files exist: `analysis/src/abrigo_x402/dependence/{__init__,cross_correlogram,permutation_null,copula}.py` + `analysis/src/abrigo_x402/hedge/{__init__,falsification,carr_madan_strip,stress_test,usdt_depeg,null_result,orchestrator}.py`
    - All 12 test files exist as skip-marked stubs in `analysis/tests/`
    - `pytest --collect-only -q` exits 0 (no collection errors)
    - Three fixture directories exist: `analysis/tests/fixtures/hedge_05_{null_cost,null_lr,null_convex}/` each with three files (fit_report.json, gate_report.json, cost_leg_bound.md)
    - `python -c "from abrigo_x402.dependence import *; from abrigo_x402.hedge import *; print('OK')"` prints OK
    - `grep -q "REQUIRED_JOINT_DIST_KEYS" analysis/src/abrigo_x402/dependence/copula.py` exits 0
    - `grep -q "REQUIRED_GATE_REPORT_KEYS" analysis/src/abrigo_x402/hedge/falsification.py` exits 0
    - `grep -q "REQUIRED_STRESS_REPORT_KEYS" analysis/src/abrigo_x402/hedge/stress_test.py` exits 0
    - `grep -q "REQUIRED_STRIP_KEYS" analysis/src/abrigo_x402/hedge/carr_madan_strip.py` exits 0
    - `grep -q "STRIP_DEGENERATE_KEYS" analysis/src/abrigo_x402/hedge/carr_madan_strip.py` exits 0
    - `grep -q 'HEDGE05_SIGNATURE.*=.*"HEDGE05-NULL-RESULT-V1"' analysis/src/abrigo_x402/hedge/null_result.py` exits 0
    - `grep -q 'POSITIVITY_TOLERANCE.*=.*0\.001' analysis/src/abrigo_x402/hedge/carr_madan_strip.py` exits 0
    - `grep -q "evaluate_condition_4_usdt_depeg" analysis/src/abrigo_x402/hedge/falsification.py` exits 0
    - `grep -q "literature_range_stipulation" analysis/src/abrigo_x402/hedge/usdt_depeg.py` exits 0 (in docstring/comment ok)
    - `! grep -i "^[^#]*usdc" analysis/src/abrigo_x402/hedge/falsification.py` exits 0 (SC-2 grep gate green at scaffold time)
    - `! grep -E "scipy\.integrate\.quad|np\.trapz" analysis/src/abrigo_x402/hedge/carr_madan_strip.py` exits 0
    - `! grep -rE "loglik_in_sample_raw" analysis/src/abrigo_x402/hedge/` exits 0
    - `grep -q "copulae==0.8.0" analysis/pyproject.toml` exits 0
    - `grep -q "jupyter" analysis/pyproject.toml` exits 0
    - `! grep -q "pyvinecopulib" analysis/pyproject.toml` (NOT installed at scaffold time — RESEARCH Open Question 3)
    - `grep -q "render-null-result-pdf" Makefile` exits 0
    - `grep -q "render-strip-diagnostic" Makefile` exits 0
    - `grep -q "phase-4-acceptance" Makefile` exits 0
    - `grep -q "HEDGE05-NULL-RESULT-V1" reports/_templates/null_result.qmd` exits 0
    - `grep -q "HEDGE-05 NULL RESULT" reports/_templates/null_result.qmd` exits 0
    - `grep -q "pdfinfo" reports/_templates/null_result.qmd` exits 0
    - `grep -q "JOINT_DIST_REQUIRED_KEYS\|GATE_REPORT_REQUIRED_KEYS\|STRESS_REPORT_REQUIRED_KEYS\|STRIP_REQUIRED_KEYS\|STRIP_DEGENERATE_REQUIRED_KEYS" scripts/lint_artifacts.py` returns >=5 hits
    - `grep -q "sc-2-usdc-literal-gate\|carr-madan-anti-pattern-gate\|canonical-ll-gate\|hardcoded-jump-params-gate" .pre-commit-config.yaml` returns >=4 hits
    - `test -f analysis/tests/fixtures/hedge_05_null_cost/cost_leg_bound.md && grep -q "verdict: FAIL" analysis/tests/fixtures/hedge_05_null_cost/cost_leg_bound.md` exits 0
    - `test -f analysis/tests/fixtures/hedge_05_null_lr/fit_report.json && python -c "import json; d=json.load(open('analysis/tests/fixtures/hedge_05_null_lr/fit_report.json')); assert d['lr_test']['p_value'] >= 0.05"` exits 0
    - `test -f analysis/tests/fixtures/hedge_05_null_convex/gate_report.json && python -c "import json; d=json.load(open('analysis/tests/fixtures/hedge_05_null_convex/gate_report.json')); assert d['any_condition_passed'] is False"` exits 0
    - First four executable lines of `analysis/tests/test_byte_identical_phase_4.py` are the four `os.environ.setdefault(...)` calls (Pattern I thread-pinning)
    - `make lint-artifacts` exits 0
    - **iter-3 Issue 2 acceptance:** `cd analysis && uv run python -c "import copulae; print(copulae.__version__)"` exits 0 (copulae install verified)
    - **iter-3 Issue 2 acceptance:** `test -f analysis/INSTALL_TROUBLESHOOTING.md && grep -q "copulae==0.7.10" analysis/INSTALL_TROUBLESHOOTING.md && grep -q "test.pypi.org" analysis/INSTALL_TROUBLESHOOTING.md` exits 0 (fallback paths documented)
    - **iter-3 Issue 2 acceptance:** `cd analysis && uv run pytest tests/test_required_keys_sync.py -x` exits 0 with 5 passed (scaffold-time REQUIRED_*_KEYS ↔ frozenset sync verification — catches the Wave-0 mismatched-tuple regression before Wave 1)
    - Single git commit with prefix `scaffold(04):`
  </acceptance_criteria>
  <done>
    All Wave-0 deliverables landed: ~30 source files (modules + tests + fixtures + template + configs); pytest collection green; lint-artifacts green; symbol surface exposes every canonical Wave-1 name needed by Plans 04-01..04-08; pre-commit gates active; PRE_REGISTRATION amendment honored (this commit is strictly later than Plan 04-pre's commit on the same branch). Wave 1 plans (04-01..04-07) can now land in parallel.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

```bash
# Symbol surface validation
cd analysis && uv run python -c "from abrigo_x402.dependence import *; from abrigo_x402.hedge import *; print('OK')"

# Test collection green
cd analysis && uv run pytest --collect-only -q | tail -3

# Lint-artifacts no-op green
make lint-artifacts

# Frontmatter validation (gsd-tools)
node "/home/jmsbpp/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-00-PLAN.md --schema plan

# AF-03 ordering invariant (commits in order)
git log --pretty=format:'%H %s' -- notes/PRE_REGISTRATION.md analysis/src/abrigo_x402/hedge/ analysis/src/abrigo_x402/dependence/ | head -5
# Expect: 04-pre commit appears LAST in chronological order (git log shows newest first)
```
</verification>

<success_criteria>
- All ~30 scaffold files exist on disk at canonical Wave-1 symbol surface
- `pytest --collect-only` exits 0 with no import errors
- `make lint-artifacts` exits 0 on empty tree
- `from abrigo_x402.dependence import *` and `from abrigo_x402.hedge import *` both succeed
- All four pre-commit grep gates installed and active
- `copulae==0.8.0` + `jupyter` (dev) installed via `uv sync`; `pyvinecopulib` NOT installed
- Three HEDGE-05 fixture triplets each force exactly one firing condition (verifiable via JSON parse + verdict check)
- AF-03 ordering invariant preserved: Plan 04-pre commit predates this commit
- Single git commit with `scaffold(04):` prefix
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-00-SUMMARY.md` recording:
- Commit hash of the scaffold
- List of 30 files created/modified
- Verification output of `pytest --collect-only`, `make lint-artifacts`, the symbol-surface python -c
- AF-03 ordering confirmation (04-pre commit hash + this commit hash + relative timestamp)
- Forward reference: "Plans 04-01..04-07 may now land in parallel; Plan 04-08 (Wave 2) depends on all of Wave 1; Plan 04-09 (Wave 3) is the acceptance gate."
</output>
