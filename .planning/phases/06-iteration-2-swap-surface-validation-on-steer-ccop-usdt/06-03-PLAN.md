---
phase: 06-iteration-2-swap-surface-validation-on-steer-ccop-usdt
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - data/raw/steer/
  - data/fits/steer/
  - reports/steer_null_result.pdf
autonomous: false
requirements: [REPRO-02, REPRO-04, HEDGE-05]
must_haves:
  truths:
    - "The frozen Phase 2-5 pipeline runs end-to-end on protocols/steer.toml via config-swap with ZERO edits to fetch/src or analysis/src"
    - "materialize writes data/raw/steer/<pool>/ (not data/raw/ichi/) using the Plan 01 namespace generalization"
    - "decide_firing_condition returns null_cost from the verdict: FAIL cost-leg doc, fired from INSIDE the completed run"
    - "reports/steer_null_result.pdf headlines the cost-leg null_cost evidence + includes the DGP/gate output as support + a REPRO-02 zero-edit attestation"
    - "The Q9 trigger is EVALUATED + logged; the V3-only fit is reported with the pre-registered signal-scope caveat (fallback deferred, not authored)"
  artifacts:
    - path: "reports/steer_null_result.pdf"
      provides: "Iteration-2 null-result deliverable (REPORT-01 re-fires)"
      min_lines: 0
    - path: "data/fits/steer/"
      provides: "Steer run artifacts (fit_report.json, gate_report.json, firing_condition.json)"
  key_links:
    - from: "protocols/steer.toml"
      to: "data/raw/steer/ + data/fits/steer/"
      via: "config-swap drives materialize/fit/hedge"
      pattern: "steer"
    - from: "notes/steer_cost_leg_bound.md"
      to: "reports/steer_null_result.pdf"
      via: "cli.py hedge --cost-leg-bound -> decide_firing_condition null_cost -> render_null_result_pdf"
      pattern: "null_cost"
---

<objective>
Run the FROZEN Phase 2-5 pipeline on `protocols/steer.toml` (config-swap only) and emit `reports/steer_null_result.pdf` via the Plan-01-fixed generic renderer — proving the REPRO-02 parameter-driven swap-surface invariant. `null_cost` fires from INSIDE the completed run (the verdict: FAIL cost-leg doc from Plan 02), mirroring ICHI's `null_strip_unavailable`. The full pipeline runs REGARDLESS of the cost-leg fail — the run is what demonstrates the zero-edit invariant.

Purpose: Decision 3 resolves the SC-2-vs-SC-4 tension toward the full re-run: the cost-leg verdict is a FIELD in the run output, not a pre-fetch short-circuit. The deliverable headlines the disqualifying cost-leg evidence with the full DGP/Hawkes/gate output as support + an explicit REPRO-02 attestation. Honest and complete, not a stub.
Output: `data/raw/steer/<pool>/<range>.parquet`, `data/fits/steer/<run_id>/` artifacts, and `reports/steer_null_result.pdf` (firing_condition null_cost, dual-signature, >50KB).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-CONTEXT.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-RESEARCH.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VALIDATION.md

<interfaces>
<!-- Verified CLI surface on HEAD (2026-05-29). Run from repo root unless noted. -->

# (cost-leg doc already emitted by Plan 02: notes/steer_cost_leg_bound.md verdict: FAIL)

# fetch (already protocol-agnostic; cli.ts accepts 'steer'; parquet-writer namespaces data/raw/steer/)
pnpm -C fetch fetch steer --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --from <N> --to <M>

# materialize (Plan 01 generalized the namespace -> writes data/raw/steer/<pool>/)
uv run python -m abrigo_x402.cli materialize --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 \
    --from-block <N> --to-block <M> --protocol-toml protocols/steer.toml

# fit (overridable out-dir)
uv run python -m abrigo_x402.cli fit --pool 0x2AC5... \
    --panel-path data/raw/steer/0x2AC5.../<range>.parquet --out-dir data/fits/steer

# hedge (passes the FAIL-verdict doc -> decide_firing_condition returns null_cost FIRST in the tree)
uv run python -m abrigo_x402.cli hedge --run-id <steer-run> --stage all \
    --run-dir-root data/fits/steer \
    --cost-leg-bound notes/steer_cost_leg_bound.md \
    --reports-pdf reports/steer_null_result.pdf

# Pattern I: thread-pinned BLAS (OMP/MKL/OpenBLAS/NumExpr=1) before numpy — set in env for every fit/hedge.
# steer.toml anchor pool: 0x2AC5baA668A8A58FD0e302B9896717484fd217B0; panel_construction=v3-anchor-only;
#   swaps_per_30d_observed=600 (2.0x the 300 floor -> sample trigger likely does NOT fire).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config-swap run of the frozen pipeline on Steer cCOP/USDT (fetch -> materialize -> fit)</name>
  <files>data/raw/steer/, data/fits/steer/, _artifacts/steer_run_id.txt</files>
  <specialist>Data Engineer — Steer protocol-spec-driven fetch + panel materialization re-run via the swap surface (ROADMAP Phase 6 primary agent for the data-fetch leg).</specialist>
  <read_first>
    - protocols/steer.toml (anchor pool, vault enumeration, block range fields, panel_construction)
    - analysis/src/abrigo_x402/cli.py (materialize/fit args — CONSUME ONLY; do NOT edit; Plan 01 already generalized materialize)
    - fetch/src/cli.ts (steer command path) + fetch/src/cache/parquet-writer.ts (data/raw/steer/ namespace)
    - notes/Q9_DECISION.md (V3-anchor-only — the panel construction the fit must honor)
    - notes/steer_cost_leg_bound.md (the Plan 02 verdict: FAIL doc — already on disk)
  </read_first>
  <action>
    Run the frozen pipeline against `protocols/steer.toml`. STRICT discipline: NO edits to fetch/src or analysis/src — this is pure consume-only execution. Set thread-pinned BLAS (`OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1`) before every Python invocation (Pattern I).
    1. Fetch the cCOP/USDT V3 anchor pool (`0x2AC5baA668A8A58FD0e302B9896717484fd217B0`) over the steer.toml block range, respecting the cost-ledger 90k free-tier cap: `pnpm -C fetch fetch steer --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --from <N> --to <M>`. If the network is unavailable in the execution env, DEFER the live fetch to the Task 3 human-verify checkpoint (operator runs the recorded command sequence on a networked machine) — record the exact command + expected `data/raw/steer/<pool>/<range>.parquet` output here in the SUMMARY. (There is no separate Task 4 — Task 3 is the checkpoint that absorbs an offline-fetch contingency.)
    2. `materialize --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --from-block <N> --to-block <M> --protocol-toml protocols/steer.toml` -> confirm output lands at `data/raw/steer/<pool>/<range>.parquet` (NOT data/raw/ichi/ — proves the Plan 01 fix). CRITICAL (M6): `--protocol-toml` DEFAULTS to `protocols/ichi.toml`; you MUST pass `--protocol-toml protocols/steer.toml` explicitly or the run writes the wrong namespace and reads the wrong spec.
    3. `fit --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --panel-path data/raw/steer/0x2AC5baA668A8A58FD0e302B9896717484fd217B0/<range>.parquet --out-dir data/fits/steer` -> produces `data/fits/steer/<run_id>/fit_report.json` (+ residuals). CRITICAL (M6): `fit` requires `--pool <cCOP/USDT anchor pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0>`. Capture the new run_id to `_artifacts/steer_run_id.txt`.
    Confirm the Q9 trigger is EVALUATED + logged in fit_report (sample-size + CI-width); the V3-only ~600/30d sample should keep the sample trigger from firing. Do NOT author any Q9 fallback module — if both triggers fire, record "fallback deferred per pre-registration; V3-only reported with signal-scope caveat" (Plan 02 pre-registered this).
  </action>
  <verify>
    <automated>test -d data/raw/steer && ls data/fits/steer/*/fit_report.json >/dev/null 2>&1 && git diff --quiet -- fetch/src analysis/src && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/raw/steer/<pool>/<range>.parquet` exists (namespace is steer, NOT ichi)
    - `ls data/fits/steer/*/fit_report.json` resolves; run_id recorded to `_artifacts/steer_run_id.txt`
    - `git diff --stat -- fetch/src analysis/src` is EMPTY (zero source edits during the run)
    - fit_report logs the Q9 trigger evaluation (sample-size + CI-width fields present)
    - No `data/raw/ichi/0x2AC5...` artifact exists (the materialize fix held)
  </acceptance_criteria>
  <done>Steer panel materialized under data/raw/steer/ and fit_report.json produced under data/fits/steer/ with zero frozen-source edits; Q9 trigger evaluated/logged.</done>
</task>

<task type="auto">
  <name>Task 2: Hedge stage + render reports/steer_null_result.pdf (null_cost from inside the run)</name>
  <files>data/fits/steer/, reports/steer_null_result.pdf</files>
  <specialist>Analytics Reporter — Phase 2-5 estimation re-run + the hedge-stage firing decision + the null-result deliverable rendering (ROADMAP Phase 6 estimation-re-run agent).</specialist>
  <read_first>
    - analysis/src/abrigo_x402/hedge/null_result.py (decide_firing_condition condition (a) first; render_null_result_pdf — Plan-01-fixed, CONSUME ONLY)
    - analysis/src/abrigo_x402/hedge/orchestrator.py (run_hedge run_dir_root / cost_leg_bound_path / reports_pdf signature)
    - notes/steer_cost_leg_bound.md (verdict: FAIL — drives null_cost)
    - reports/_templates/null_result.qmd (the template the renderer targets)
    - _artifacts/steer_run_id.txt (the run_id from Task 1)
  </read_first>
  <action>
    Run the hedge stage on the Steer run_id, passing the cost-leg doc so `decide_firing_condition` returns `null_cost` (condition (a) is evaluated FIRST in the sequential tree — fires from inside the completed run):
    `uv run python -m abrigo_x402.cli hedge --run-id <steer-run> --stage all --run-dir-root data/fits/steer --cost-leg-bound notes/steer_cost_leg_bound.md --reports-pdf reports/steer_null_result.pdf` (thread-pinned BLAS).
    This produces the Phase-4 artifact set under `data/fits/steer/<run_id>/` (gate_report.json, firing_condition.json, etc.) and renders `reports/steer_null_result.pdf` via the fixed generic renderer.
    The PDF DEPTH (Decision 4): cost-leg `null_cost` headline (the gating verdict, 30k-100k STRADDLE vs the 100k line) + the full DGP/Hawkes/gate output as supporting material (since the pipeline actually ran) + an explicit REPRO-02 zero-edit attestation line. Honest and complete, not a stub. The PDF MUST NOT relabel the null as a near-miss/partial-positive (AF-03 — Plan 04's content-check enforces the forbidden-narrowing strings).
    Verify firing_condition.json records `null_cost`; verify the PDF carries the visible H1 + HEDGE05-NULL-RESULT-V1 dual signature and is >50KB.
    NO edits to analysis/src or fetch/src.
  </action>
  <verify>
    <automated>test -f reports/steer_null_result.pdf && [ "$(wc -c < reports/steer_null_result.pdf)" -gt 51200 ] && grep -rq "null_cost" data/fits/steer/*/firing_condition.json && git diff --quiet -- fetch/src analysis/src && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/fits/steer/<run_id>/firing_condition.json` records `null_cost`
    - `reports/steer_null_result.pdf` exists and is > 50KB
    - `pdftotext reports/steer_null_result.pdf - | grep -i null_cost` matches (cost-leg headline present)
    - `pdfinfo reports/steer_null_result.pdf | grep HEDGE05` matches (dual-signature marker)
    - `pdftotext reports/steer_null_result.pdf - | grep -Ei "pass with caveat|near-miss positive|directionally positive|exploratory positive|positive result"` returns NOTHING (AF-03 no narrowing — the SHARED 5-string set, M7, identical to the ichi gate / Plan 04 / verify-reproducibility)
    - `git diff --stat -- fetch/src analysis/src` EMPTY (zero source edits)
  </acceptance_criteria>
  <done>null_cost fires from inside the completed Steer run; reports/steer_null_result.pdf renders >50KB with the cost-leg headline + DGP support + dual signature + no forbidden narrowing; zero frozen-source edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Steer null PDF + the live cCOP/USDT provenance</name>
  <files>reports/steer_null_result.pdf</files>
  <action>Human verification checkpoint (no autonomous action) — pause for the operator to open the rendered PDF and run the network-dependent provenance spot-check. The automated render + content gates ran in Task 2; this gate confirms the human-facing quality (headline, typeset equations, live Blockscout 200s) before the phase acceptance gate. See &lt;how-to-verify&gt; for the exact steps.</action>
  <verify>Manual — see &lt;how-to-verify&gt; (operator opens reports/steer_null_result.pdf + spot-checks cCOP/USDT Blockscout URLs).</verify>
  <done>Operator confirms the null PDF headline + typeset + live provenance, or describes issues to fix.</done>
  <what-built>The frozen Phase 2-5 pipeline ran on protocols/steer.toml (config-swap, zero source edits); reports/steer_null_result.pdf was rendered via the fixed generic renderer with a null_cost headline + DGP/gate support + REPRO-02 attestation. Network-dependent fetch + math typesetting need human eyes.</what-built>
  <how-to-verify>
    1. Open `reports/steer_null_result.pdf`. Confirm: the cost-leg null_cost (30k-100k STRADDLE) is the HEADLINE; the DGP/Hawkes/gate output appears as supporting material; a REPRO-02 zero-edit attestation line is present; equations typeset cleanly; size > 50KB; no near-miss/positive relabeling.
    2. Spot-check 3-5 cCOP/USDT panel rows: run their Blockscout URLs (`https://celo.blockscout.com/tx/0x...`) and confirm HTTP 200 (the run logs status per row if network was available during fetch).
    3. If the fetch step ran offline (Task 1 deferred to operator), run the recorded `pnpm -C fetch fetch steer ...` + materialize/fit/hedge sequence on a networked machine and re-confirm the PDF.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. PDF headline wrong, narrowing language present, fetch incomplete).</resume-signal>
</task>

</tasks>

<verification>
- `git diff --stat -- fetch/src analysis/src` EMPTY across the whole plan (the REPRO-02 invariant under live run)
- `data/raw/steer/<pool>/<range>.parquet` + `data/fits/steer/<run_id>/{fit_report,gate_report,firing_condition}.json` exist
- `firing_condition.json` == null_cost; `reports/steer_null_result.pdf` > 50KB, dual-signature, cost-leg headline, no narrowing
- Q9 trigger evaluated + logged; V3-only reported with the pre-registered signal-scope caveat (no fallback module authored). If both Q9 switch-trigger conditions fire: record SC-5 SKIP — V3-anchor-only (panel_construction=v3-anchor-only); unified fallback pre-registered deferred; q9_pooling_test.json absent; no REPRO-02 violation.
- Portability note: system grep is ugrep 7.5.0 (GNU-compatible for the -Ei flags in the no-narrowing gate); the operator runs the acceptance greps via the same shell.
</verification>

<success_criteria>
- The frozen pipeline runs end-to-end on Steer via config-swap with ZERO fetch/src or analysis/src edits
- materialize writes data/raw/steer/ (Plan 01 fix held); null_cost fires from inside the completed run
- reports/steer_null_result.pdf is an honest, complete null-result deliverable (headline + support + attestation)
- REPRO-04 honored on V3-anchor-only; Q9 fallback deferred per pre-registration
</success_criteria>

<out_of_scope>
AF-12 OUT-OF-SCOPE (verbatim — do NOT do any of these in this plan):
- NO κ complexity index; NO closed-form f(κ).
- NO new firing conditions (only the existing null_cost path fires).
- NO substitute-candidate naming or execution (future milestone, AF-03).
- NO Q9 unified-panel / cross-class-permutation module authoring (deferred — if both triggers fire, document fallback-unavailable, do NOT write code).
- NO verdict narrowing / relabel of the null as near-miss/partial-positive (AF-03).
- NO edits to fetch/src or analysis/src — pure consume-only run (any needed fix was a Plan-01 baseline commit BEFORE this window).
- NO re-estimation of the Steer demand band.
- NO _schema.toml edit; NO PANEL-02 schema bump.
- NO USDC substituted for USDT (cCOP/USDT pair).
- NO overwrite/deletion of ICHI run artifacts.
- NO push to upstream; pushes (when asked) go to origin only.
</out_of_scope>

<output>
After completion, create `.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-03-SUMMARY.md` recording: the exact config-swap command sequence run, the Steer run_id, the data/raw/steer + data/fits/steer artifact paths, the firing_condition (null_cost), the Q9 trigger evaluation outcome (fired or not; fallback deferred), the PDF size + signature confirmation, and the `git diff fetch/src analysis/src` empty confirmation.
</output>
