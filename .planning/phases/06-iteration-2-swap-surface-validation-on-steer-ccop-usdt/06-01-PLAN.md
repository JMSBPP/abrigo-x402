---
phase: 06-iteration-2-swap-surface-validation-on-steer-ccop-usdt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/src/abrigo_x402/hedge/null_result.py
  - analysis/src/abrigo_x402/cli.py
  - analysis/tests/test_null_result_template.py
  - scripts/lint_artifacts.py
  - reports/_templates/null_result.qmd
  - reports/_templates/_evidence_branches.qmd
  - notes/PRE_REGISTRATION.md
  - _artifacts/repro_02_baseline_sha.txt
autonomous: true
requirements: [REPRO-01, REPRO-02]
must_haves:
  truths:
    - "render_null_result_pdf renders a >5KB PDF for FIRING=null_cost with no papermill dependency, and the firing string appears in the PDF BODY/evidence section (not only the title line)"
    - "cli.py materialize writes data/raw/<protocol>/ derived from the loaded --protocol-toml, not a hardcoded data/raw/ichi/"
    - "lint_artifacts.py column-presence lint fires for any data/raw/<protocol>/ panel (Steer included), not only data/raw/ichi/"
    - "After these baseline-maintenance commits land, this HEAD is the new REPRO-02 baseline; the exact post-Plan-01 HEAD sha is written verbatim (single 40-char line) to _artifacts/repro_02_baseline_sha.txt"
    - "PRE_REGISTRATION.md records the REPRO-01 scoped-grep re-scope as an explicit AF-12 note (not silent)"
  artifacts:
    - path: "analysis/src/abrigo_x402/hedge/null_result.py"
      provides: "Fixed generic null-result renderer (top-level pandoc metadata via -M, repo-root anchored, no papermill)"
      contains: "firing_condition"
    - path: "reports/_templates/null_result.qmd"
      provides: "Template H1 + meta shortcode reading top-level firing_condition (no params. prefix)"
      contains: "firing_condition"
    - path: "reports/_templates/_evidence_branches.qmd"
      provides: "Evidence branch reading top-level firing_condition (no params. prefix)"
      contains: "firing_condition"
    - path: "analysis/src/abrigo_x402/cli.py"
      provides: "Protocol-derived materialize namespace"
      contains: "spec.protocol.name"
    - path: "scripts/lint_artifacts.py"
      provides: "Generalized data/raw/<protocol>/ column-presence guard (Steer panels linted)"
      contains: "data/raw/"
    - path: "_artifacts/repro_02_baseline_sha.txt"
      provides: "The single 40-char post-Plan-01 HEAD sha = the REPRO-02 baseline, read directly by Plan 04 Task 1"
      contains: ""
    - path: "notes/PRE_REGISTRATION.md"
      provides: "AF-12 REPRO-01 scoped-grep re-scope note"
      contains: "REPRO-01"
  key_links:
    - from: "analysis/src/abrigo_x402/hedge/null_result.py"
      to: "reports/_templates/null_result.qmd + reports/_templates/_evidence_branches.qmd"
      via: "quarto render with -M firing_condition:X — ALL THREE interpolation points read top-level metadata (no params. prefix)"
      pattern: "-M.*firing_condition"
    - from: "analysis/src/abrigo_x402/cli.py materialize"
      to: "data/raw/<protocol>/"
      via: "namespace derived from loaded protocol spec"
      pattern: "spec\\.protocol\\.name"
---

<objective>
Land the THREE generic, leak-clean, pre-Iteration-2 BASELINE-MAINTENANCE edits to `analysis/src` BEFORE the REPRO-02 empty-diff window opens, plus the AF-12 pre-registration note that re-scopes REPRO-01's literal `ichi` grep to its SC-5 algorithmic-leak intent. These are the ONLY permitted `analysis/src` edits in Phase 6; every subsequent plan runs against a frozen tree.

Purpose: An honest REPRO-02 pass requires that the renderer fix + the materialize-namespace generalization + the genuine-coupling scrub (incl. the lint_artifacts.py column-presence guard) land as STANDALONE maintenance commits, so the iteration-1-complete -> iteration-2-complete empty-diff is measured from THIS re-baselined HEAD forward. Editing `analysis/src` during the iteration-2 diff window and still claiming REPRO-02 pass is a dressed-up pass — forbidden (critical_locked_decisions, AF-03-adjacent).
Output: a fixed generic `render_null_result_pdf` (first production-usable, firing string in PDF body), a protocol-driven `materialize` namespace, a generalized `data/raw/<protocol>/` column lint, scrubbed genuine `ichi` couplings, an AF-12 REPRO-01 re-scope note, and `_artifacts/repro_02_baseline_sha.txt` pinning this HEAD. This HEAD becomes the REPRO-02 baseline used by Plan 04's attestation.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-CONTEXT.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-RESEARCH.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VALIDATION.md
@.planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/_artifacts/FOLLOWUP_generic_null_result_renderer_bug.md

<interfaces>
<!-- Verified directly on HEAD (2026-05-29). Executor uses these — no re-exploration needed. -->

null_result.py render_null_result_pdf (current, broken — papermill + cwd-relative template):
  cmd = ["quarto","render",str(template),"--no-cache","--to","pdf",
         "-P", f"firing_condition:{firing_condition}",   # papermill — ABSENT in env
         "--output", output_path.name, "--output-dir", str(output_path.parent)]
  subprocess.run(cmd, check=True, capture_output=True, text=True)   # cwd-relative
  _VALID_FIRING_CONDITIONS includes "null_cost".
  REPO-ROOT DEPTH (verified): Path(__file__).resolve().parents[4] == repo root. USE parents[4].

THE THREE TEMPLATE INTERPOLATION POINTS (all read the params.* namespace — verified on HEAD):
  (1) reports/_templates/null_result.qmd:18  — H1: `r params$firing_condition`   (R-inline)
  (2) reports/_templates/null_result.qmd:29  — `firing_condition = "{{< meta params.firing_condition >}}"`
  (3) reports/_templates/_evidence_branches.qmd:16 — `firing_condition = "{{< meta params.firing_condition >}}"`
  CRITICAL: `-M firing_condition:X` sets TOP-LEVEL pandoc metadata. It is readable as
  `{{< meta firing_condition >}}` (NO `params.` prefix) — it does NOT populate `params.firing_condition`.
  The proven `make report-ichi` (Makefile:224-227) passes NO -M/-P flag and relies on the quarto
  `params:` YAML default. So a `-M` flag without ALSO converting the THREE points to the no-prefix
  form would render a BLANK firing condition in the body/evidence — the exact B1 trap.
  CHOSEN MECHANISM (single, consistent across all three): convert ALL THREE to the no-prefix
  `{{< meta firing_condition >}}` shortcode form (the H1 R-inline at point (1) becomes a
  `{{< meta firing_condition >}}` shortcode too) AND pass `-M firing_condition:X` from the renderer.
  Templates live in reports/_templates/ — OUTSIDE frozen dirs, free to edit.

cli.py materialize (current, hardcoded):
  L15 docstring: "data/raw/ichi/<pool>/<from_block>_<to_block>.parquet"
  L67: pool_dir = repo_root / "data" / "raw" / "ichi" / pool
  protocol_spec.load_protocol(...).protocol.name exposes the slug. fit/hedge already accept
  --out-dir/--run-dir-root overrides; only materialize needs generalizing.

scripts/lint_artifacts.py:344 (current — genuine functional coupling, NOT just a comment):
  `if "data/raw/ichi" in str(p):`  gates the block_timestamp/column-presence lint to ICHI only.
  A Steer panel at data/raw/steer/ SILENTLY SKIPS the column lint. Generalize to match ANY
  `data/raw/<protocol>/` so Steer panels get the same column-presence contract (M1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the generic null-result renderer (-M top-level metadata + repo-root anchoring + all THREE template points)</name>
  <specialist>Backend Architect — owns the Python subprocess/path-resolution + dependency-removal fix on the rendering boundary; closest fit for de-papermilling a quarto invocation and re-anchoring CWD.</specialist>
  <read_first>
    - analysis/src/abrigo_x402/hedge/null_result.py (the function being fixed, lines 134-200)
    - reports/_templates/null_result.qmd (the TWO points: line 18 `r params$firing_condition`, line 29 `{{< meta params.firing_condition >}}`)
    - reports/_templates/_evidence_branches.qmd (the THIRD point: line 16 `{{< meta params.firing_condition >}}`)
    - analysis/tests/test_null_result_template.py (the skip-guarded test to un-skip)
    - .planning/phases/05-reporting-iteration-1-pdf-deliverable-l7/_artifacts/FOLLOWUP_generic_null_result_renderer_bug.md (acceptance criteria)
    - Makefile lines 196-203 (render-null-result-pdf with --execute-param) AND lines 224-227 (report-ichi: the PROVEN no-flag params-default render — read the comment explaining why no --execute-param)
  </read_first>
  <action>
    In `analysis/src/abrigo_x402/hedge/null_result.py :: render_null_result_pdf`:
    1. Add `REPO_ROOT = Path(__file__).resolve().parents[4]` (VERIFIED = repo root).
    2. Resolve template to `template_abs = (REPO_ROOT / template).resolve()` and output dir to `(REPO_ROOT / output_path.parent).resolve()`.
    3. Replace the `-P firing_condition:{firing_condition}` flag (papermill) with `-M firing_condition:{firing_condition}` (TOP-LEVEL pandoc metadata — NO papermill).
    4. Pass `cwd=REPO_ROOT` to `subprocess.run(...)`.

    CRITICAL (B1 — fix ALL THREE template interpolation points so the body is not blank).
    `-M` populates TOP-LEVEL metadata (`{{< meta firing_condition >}}`, NO `params.` prefix). Convert all three points to the no-prefix form so they resolve under `-M`:
    - reports/_templates/null_result.qmd line 18 (H1): change `\`r params$firing_condition\`` to the shortcode `{{< meta firing_condition >}}`.
    - reports/_templates/null_result.qmd line 29: change `"{{< meta params.firing_condition >}}"` to `"{{< meta firing_condition >}}"`.
    - reports/_templates/_evidence_branches.qmd line 16: change `"{{< meta params.firing_condition >}}"` to `"{{< meta firing_condition >}}"`.
    (Optionally keep the `params: firing_condition: null` YAML default as a harmless fallback; it no longer feeds the body.) Templates are outside frozen dirs — free to edit; record the conversions in SUMMARY.

    Render once via the function (or `make render-null-result-pdf FIRING=null_cost` after Plan 02 updates the Makefile target), `pdftotext` the output, and confirm the firing string appears in BOTH the H1 title line AND the evidence/body section (the `## Evidence: ... (null_cost)` branch text from _evidence_branches.qmd). If the body still renders blank, the `params.`->no-prefix conversion was incomplete — fix before proceeding.
    Un-skip `test_pdf_dual_signature_when_quarto_available` in `analysis/tests/test_null_result_template.py` (quarto IS installed) so it RUNS and PASSES; keep the `shutil.which("quarto")` skip-guard wrapper so the no-quarto baseline stays green elsewhere.
    Do NOT introduce any `ichi`/`steer` literal — the renderer stays generic (args are `firing_condition` + `output_path`).
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/test_null_result_template.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "papermill" analysis/src/abrigo_x402/hedge/null_result.py` == 0
    - `grep -cE -- "(^|[^A-Za-z])-P([^A-Za-z]|$)" analysis/src/abrigo_x402/hedge/null_result.py` == 0 (the -P flag is gone)
    - `grep -cE -- "-M.*firing_condition" analysis/src/abrigo_x402/hedge/null_result.py` >= 1
    - `grep -c "parents\[4\]" analysis/src/abrigo_x402/hedge/null_result.py` >= 1
    - `grep -c "params.firing_condition" reports/_templates/null_result.qmd` == 0 AND `grep -c "params.firing_condition" reports/_templates/_evidence_branches.qmd` == 0 (all three points converted to no-prefix)
    - `grep -c "params\$firing_condition" reports/_templates/null_result.qmd` == 0 (the R-inline H1 reference converted)
    - `grep -rni "ichi\|steer" analysis/src/abrigo_x402/hedge/null_result.py | wc -l` == 0 (renderer stays generic)
    - `pytest tests/test_null_result_template.py::test_pdf_dual_signature_when_quarto_available` RUNS (not skipped) and PASSES
    - SUBSTRATE-CONTENT assertion (B1): after rendering a null_cost PDF, `pdftotext <out>.pdf - | grep -A5 -i 'firing' | grep -qi null_cost` succeeds AND `pdftotext <out>.pdf - | grep -qi "Evidence.*null_cost"` succeeds — the firing string is present in the BODY/evidence section, not only the title line.
  </acceptance_criteria>
  <done>render_null_result_pdf renders a PDF for null_cost from repo root with no papermill; all three template points read top-level metadata so the body shows null_cost (not blank); the dual-signature test passes; the renderer carries no protocol identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize materialize namespace + generalize the lint column-guard + scrub genuine ichi couplings</name>
  <specialist>Data Engineer — owns the data-namespace/parquet-path layer and protocol-spec-driven materialization; closest fit for deriving data/raw/PROTOCOL/ from the loaded TOML.</specialist>
  <read_first>
    - analysis/src/abrigo_x402/cli.py (materialize subcommand, lines 1-90; the L67 hardcode + L15 docstring)
    - analysis/src/abrigo_x402/protocol_spec.py (load_protocol -> spec.protocol.name slug)
    - fetch/src/cache/parquet-writer.ts (reference: fetch side already namespaces by data/raw/PROTOCOL/)
    - scripts/lint_artifacts.py (the ICHI_PANEL_REQUIRED_COLUMNS frozenset — a REPRO-01 hit; AND the L344 `if "data/raw/ichi" in str(p):` column-lint guard — the M1 genuine coupling)
    - notes/Q9_DECISION.md (panel_construction lock — do NOT alter panel semantics, only the namespace)
  </read_first>
  <action>
    In `analysis/src/abrigo_x402/cli.py :: materialize`:
    1. After the spec is loaded, derive `protocol_slug = spec.protocol.name`.
    2. Replace L67 `pool_dir = repo_root / "data" / "raw" / "ichi" / pool` with `pool_dir = repo_root / "data" / "raw" / protocol_slug / pool`.
    3. Update the L15 docstring `data/raw/ichi/<pool>/...` -> `data/raw/<protocol>/<pool>/...`.
    A Steer materialize then writes `data/raw/steer/<pool>/...` with NO further code edit (Plan 03 relies on this).

    M1 — GENERALIZE the lint_artifacts.py column-presence guard (genuine functional coupling, NOT a comment):
    - At scripts/lint_artifacts.py:344, replace `if "data/raw/ichi" in str(p):` with a guard that matches ANY `data/raw/<protocol>/` panel — e.g. `if re.search(r"data/raw/[^/]+/", str(p)):` (import `re` if not already). This makes Steer panels (data/raw/steer/) get the SAME block_timestamp/column-presence contract as ICHI; without this M1 fix a Steer panel SILENTLY skips the column lint AND escapes the scoped leak-grep.
    - Update the adjacent comment (lines 341-343) to say the lint applies to any data/raw/<protocol>/ panel, not "ICHI only".

    SCRUB remaining GENUINE `ichi` couplings outside `protocols/*.toml` that are not bare iter-1-example comments:
    - Run `grep -rni "ichi" analysis/src fetch/src scripts/lint_artifacts.py` and triage each hit.
    - SCRUB load-bearing functional couplings to generic terms: rename `ICHI_PANEL_REQUIRED_COLUMNS` -> `LP_AGGREGATOR_PANEL_REQUIRED_COLUMNS` (update all references); rename any `ichi`-named ABI fixture reference to a protocol-slug-derived path or a generic name; change docstrings saying "ICHI vault" -> "the LP-aggregator vault".
    - LEAVE in place: comments citing ICHI purely as the iteration-1 worked EXAMPLE (reconciled by the scoped grep), and anything inside `protocols/ichi.toml`/`protocols/_schema.toml` (outside searched roots), and the CLI-overridable `data/fits/ichi` defaults (reconciled by the scoped grep).
    Do NOT add any protocol-name conditional branch (`if config.name == ...`) — SC-5 lint must still pass.
  </action>
  <verify>
    <automated>cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spec.protocol.name\|protocol_slug" analysis/src/abrigo_x402/cli.py` >= 1
    - `grep -cE 'raw.*/.*ichi.*/.*pool|"ichi" / pool' analysis/src/abrigo_x402/cli.py` == 0 (hardcoded namespace gone)
    - `grep -c '"data/raw/ichi" in str' scripts/lint_artifacts.py` == 0 (M1: the ichi-only column-lint guard generalized)
    - `grep -cE 'data/raw/\[\^/\]\+/|data/raw/.*protocol' scripts/lint_artifacts.py` >= 1 (the generalized guard present)
    - `grep -c "ICHI_PANEL_REQUIRED_COLUMNS" scripts/lint_artifacts.py` == 0 (renamed)
    - `cd fetch && pnpm test protocol-agnostic` passes (SC-5 algorithmic-leak gate stays green)
    - `make leak-check` exits 0 (no new protocol-name branch / addr / fee-tier literal introduced)
    - Full analysis suite green (no regression from the rename or the lint generalization)
  </acceptance_criteria>
  <done>materialize derives its namespace from the loaded protocol; the lint column-guard fires for any data/raw/<protocol>/ panel (Steer included); genuine ichi couplings scrubbed to generic terms; SC-5 lint + leak-check + full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: AF-12 pre-registration note re-scoping REPRO-01 to its SC-5 intent + pin the REPRO-02 baseline sha</name>
  <specialist>Reality Checker — the re-scope of a requirement's acceptance is exactly the evidence-based, "must not be silent" call this agent guards; it writes the transparent AF-12 note so the narrowing is recorded, not hidden.</specialist>
  <read_first>
    - notes/PRE_REGISTRATION.md (append-only; §Pre-Registration Discipline (AF-03 Audit Trail), §Verification Hooks)
    - .planning/REQUIREMENTS.md (REPRO-01 wording, line 65)
    - .planning/ROADMAP.md §Phase 6 SC-3 (the two-layer leak gate; SC-5 algorithmic-leak lint is authoritative)
    - fetch/tests/protocol-agnostic.test.ts (the authoritative SC-5 forbidden-pattern set)
  </read_first>
  <action>
    APPEND (do not delete prior content) a new section to `notes/PRE_REGISTRATION.md`:
    `## Phase 6 — REPRO-01 scoped-grep re-scope (AF-12 transparency note)`.
    State explicitly and verbatim-greppable:
    - The literal `grep -ri "ichi" fetch/src analysis/src == 0` was an OVER-STRICT proxy; the load-bearing algorithmic-leak gate is the SC-5 protocol-agnosticism lint (no protocol-name branches, no factory-address literals, no magic fee-tier literals).
    - REPRO-01's INTENT is satisfied by: (1) the SC-5 lint passing (`pnpm test protocol-agnostic`), AND (2) a SCOPED `ichi` grep that excludes comments/docstrings and the CLI-overridable `data/fits/ichi` default, after the genuine functional couplings (materialize namespace, ICHI_PANEL_REQUIRED_COLUMNS frozenset, the lint_artifacts.py data/raw/ichi column-guard, ABI fixture name) were scrubbed/generalized in Plan 06-01 Task 2.
    - Bare-comment ICHI references that cite Iteration-1 as the worked example are PERMITTED and explicitly in-scope for the scoped grep's exclusion.
    - This narrowing is RECORDED here BEFORE the Phase 6 leak-gate verdict is claimed (AF-12 discipline — not silent).
    Include the EXACT scoped-grep command string the Plan 02 `make leak-check` extension will run (M5: byte-identical to what the Makefile recipe runs), so the note and the gate agree.
    Carry an AF-12 OUT-OF-SCOPE line: this note changes NO numeric threshold, introduces NO new requirement, and does not re-scope REPRO-02/03/04.

    THEN pin the REPRO-02 baseline (M3): after Plan 01's source edits are committed, write the post-Plan-01 HEAD sha to `_artifacts/repro_02_baseline_sha.txt` as a SINGLE 40-char line and NOTHING else (`git rev-parse HEAD > _artifacts/repro_02_baseline_sha.txt`). This is the file Plan 04 Task 1 reads directly for the empty-diff base (replacing the fragile grep-the-summary approach). NOTE the ordering: this note + the baseline-sha file are the LAST commit of Plan 01, so the sha they pin IS the post-baseline-fix HEAD.
  </action>
  <verify>
    <automated>grep -q "REPRO-01 scoped-grep re-scope" notes/PRE_REGISTRATION.md && grep -q "SC-5" notes/PRE_REGISTRATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REPRO-01 scoped-grep re-scope" notes/PRE_REGISTRATION.md` >= 1
    - `grep -c "SC-5" notes/PRE_REGISTRATION.md` >= 1 (intent reconciled to the authoritative gate)
    - The note contains the exact scoped-grep command string that Plan 02 wires into `make leak-check` (byte-identical, M5)
    - `_artifacts/repro_02_baseline_sha.txt` exists and is a SINGLE line matching `^[0-9a-f]{40}$` (M3 — Plan 04 reads this file directly)
    - `git log -1 --format=%H -- notes/PRE_REGISTRATION.md` predates the Plan 02 leak-check-extension commit and the Plan 03 fetch/run commits (AF-12 ordering — proven in Plan 04)
    - No numeric threshold in PRE_REGISTRATION.md changed (diff is pure addition below the prior content)
  </acceptance_criteria>
  <done>PRE_REGISTRATION.md carries an explicit, greppable AF-12 note re-scoping REPRO-01 to SC-5 intent + the exact scoped-grep command; _artifacts/repro_02_baseline_sha.txt pins the single-line post-Plan-01 HEAD sha for Plan 04, recorded before any Phase 6 leak-gate verdict.</done>
</task>

</tasks>

<verification>
- `cd analysis && OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 uv run pytest tests/` — full analysis suite green (incl. the now-running renderer dual-signature test)
- `cd fetch && pnpm test protocol-agnostic` — SC-5 algorithmic-leak gate green
- `make leak-check` — exit 0 (no protocol-name branch / factory-addr / fee-tier literal introduced)
- `grep -c "papermill" analysis/src/abrigo_x402/hedge/null_result.py` == 0 and `grep -cE -- "-M.*firing_condition" .../null_result.py` >= 1
- All THREE template points read top-level metadata (B1): `grep -c "params.firing_condition" reports/_templates/null_result.qmd reports/_templates/_evidence_branches.qmd` == 0
- `grep -c "spec.protocol.name\|protocol_slug" analysis/src/abrigo_x402/cli.py` >= 1; `grep -c '"data/raw/ichi" in str' scripts/lint_artifacts.py` == 0 (M1)
- `_artifacts/repro_02_baseline_sha.txt` is a single 40-char sha; after this plan's commits, HEAD == that sha and is the declared REPRO-02 baseline (M3); Plan 04 reads this file.
- Portability note: system `grep` is ugrep 7.5.0 (GNU-compatible for the flags used here); executors run the acceptance greps via the same shell.
</verification>

<success_criteria>
- render_null_result_pdf renders a >5KB null_cost PDF from repo root with no papermill, firing string in the body (FOLLOWUP bug closed)
- materialize writes data/raw/<protocol>/ derived from the loaded --protocol-toml; lint column-guard fires for any data/raw/<protocol>/ panel
- genuine ichi functional couplings scrubbed; SC-5 lint + leak-check + full suite green
- PRE_REGISTRATION.md carries the AF-12 REPRO-01 re-scope note + exact scoped-grep command before any verdict
- _artifacts/repro_02_baseline_sha.txt pins the single-line post-Plan-01 HEAD sha (Plan 04's diff base)
- These edits are STANDALONE baseline-maintenance commits; the iteration-2 REPRO-02 diff window opens AFTER this HEAD
</success_criteria>

<out_of_scope>
AF-12 OUT-OF-SCOPE (verbatim — do NOT do any of these in this plan):
- NO κ complexity index; NO closed-form f(κ).
- NO new firing conditions (the existing 4 stay; this plan only FIXES the renderer wiring).
- NO substitute-candidate naming or execution (future milestone, AF-03).
- NO Q9 unified-panel / cross-class-permutation module authoring (deferred — Plan 02 pre-registers the deferral).
- NO verdict narrowing / relabel of any null as a near-miss/partial-positive.
- NO Steer fetch or pipeline run (that is Plan 03).
- NO numeric-threshold revision in PRE_REGISTRATION.md (α, η-floor, REPRO-03 band, Q-7, Q-9 trigger all locked).
- NO _schema.toml edit; NO PANEL-02 schema bump.
- NO re-estimation of the Steer demand band (it is pre-committed in steer.toml; Phase 6 only OBSERVES).
- NO USDC substituted for USDT anywhere.
- NO push to upstream; pushes (when asked) go to origin only.
</out_of_scope>

<output>
After completion, create `.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-01-SUMMARY.md` recording: the renderer fix (incl. the three-point params.->top-level metadata conversion and the body-content pdftotext proof), the materialize namespace generalization, the lint_artifacts.py M1 column-guard generalization, the scrub triage table (each ichi hit -> scrubbed / generalized / left-as-example), the AF-12 note + the exact scoped-grep command, and the exact post-Plan-01 HEAD sha written to _artifacts/repro_02_baseline_sha.txt that becomes the REPRO-02 baseline. Note that system grep is ugrep 7.5.0 (GNU-compatible).
</output>
