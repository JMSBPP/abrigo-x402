---
phase: 06-iteration-2-swap-surface-validation-on-steer-ccop-usdt
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - notes/PRE_REGISTRATION.md
  - notes/steer_cost_leg_bound.md
  - scripts/cost_leg_check.py
  - scripts/test_cost_leg_check.py
  - Makefile
autonomous: true
requirements: [REPRO-03, REPRO-04, HEDGE-05]
must_haves:
  truths:
    - "The straddle decision rule is written to PRE_REGISTRATION.md and applied to the pre-committed 30k-100k band with NO re-estimation"
    - "scripts/cost_leg_check.py reads steer.toml's [protocol.repro_03_verdict] band and writes notes/steer_cost_leg_bound.md with verdict: FAIL"
    - "make iteration-2-full target exists, is .PHONY, and runs the cost-leg check FIRST as a deterministic recipe"
    - "make leak-check carries the EXACT scoped ichi grep (byte-identical to the Plan 01 AF-12 PRE_REGISTRATION command string) and returns 0 after the Plan 01 scrub"
    - "The Q9 unified-panel fallback is pre-registered as DEFERRED with a signal-scope caveat (V3-only ~600/30d >> 300 floor)"
  artifacts:
    - path: "scripts/cost_leg_check.py"
      provides: "Pre-registered straddle adjudication, OUTSIDE frozen dirs"
      contains: "FAIL"
    - path: "notes/steer_cost_leg_bound.md"
      provides: "verdict: FAIL frontmatter consumed by decide_firing_condition -> null_cost"
      contains: "verdict: FAIL"
    - path: "Makefile"
      provides: "iteration-2-full target (deterministic recipe) + scoped-ichi leak-check layer"
      contains: "iteration-2-full"
  key_links:
    - from: "scripts/cost_leg_check.py"
      to: "notes/steer_cost_leg_bound.md"
      via: "writes YAML frontmatter verdict: FAIL"
      pattern: "verdict:\\s*FAIL"
    - from: "notes/steer_cost_leg_bound.md"
      to: "decide_firing_condition (null_result.py)"
      via: "_parse_cost_leg_bound_verdict reads frontmatter verdict == FAIL -> null_cost"
      pattern: "null_cost"
---

<objective>
Pre-register the cost-leg STRADDLE decision rule and build the REPRO-03 first-step machinery + `make iteration-2-full`/`leak-check` wiring — ALL outside the frozen `fetch/src`/`analysis/src` dirs so REPRO-02's empty-diff invariant is preserved. The straddle rule must be committed BEFORE the verdict is observed (AF-03 commit-ordering).

DEPENDS ON 06-01 (wave 2): 06-01 Task 3 writes the exact scoped-ichi grep command into PRE_REGISTRATION.md AND scrubs the genuine couplings; this plan's leak-check (Task 3) wires that byte-identical command and relies on the scrub having landed (else the grep would still hit). 06-01 is the standalone baseline-fix commit set; 06-02 builds on top — they MUST NOT run in parallel (both append to PRE_REGISTRATION.md, and the AF-03 ordering requires 06-01 Task 3 to predate this plan's leak-check commit).

Purpose: Steer's data-demand is a STRADDLE (30k-100k vs the 100k Graph free-tier line). The pre-registered conservative-fail rule (not strictly ABOVE 100k -> FAIL -> HEDGE-05 condition (a) `null_cost`) is the honest D-08 negative-control. Phase 6 only OBSERVES the pre-committed band — it never re-estimates demand to chase pass/fail (AF-03).
Output: `notes/PRE_REGISTRATION.md` straddle-rule + Q9-fallback-deferred sections, `scripts/cost_leg_check.py` (+ test) writing `notes/steer_cost_leg_bound.md` (verdict: FAIL), and the `make iteration-2-full` (deterministic recipe) + scoped `leak-check` targets. None of this touches frozen source.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-CONTEXT.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-RESEARCH.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VALIDATION.md

<interfaces>
<!-- Verified on HEAD (2026-05-29). -->

_parse_cost_leg_bound_verdict (null_result.py:42-62) — the frontmatter contract:
  re.match(r"---\n(.*?)\n---", text, re.DOTALL) ; yaml.safe_load ; fm.get("verdict").upper() == "FAIL"
  So notes/steer_cost_leg_bound.md MUST begin with a YAML frontmatter block whose first lines are:
    ---
    verdict: FAIL
    ---
  Anything else (no frontmatter / no verdict key) -> None -> condition (a) does NOT fire.

protocols/steer.toml [protocol.repro_03_verdict] (verified):
  result = "STRADDLE"
  celo_attributable_queries_per_mo_lower_bound = 30000
  celo_attributable_queries_per_mo_upper_bound = 100000
  demand_window_lower_bound_queries_per_mo = 100000   # Graph free-tier ceiling (DEMAND-01)
  Top-level: phase_0_repro_03_verdict = "STRADDLE", phase_0_repro_03_flag = "marginal-demand",
  panel_construction = "v3-anchor-only", swaps_per_30d_observed = 600, above_hawkes_floor = true.

PRE_REGISTRATION.md already has: §REPRO-03 Threshold (PASS/STRADDLE/FAIL — STRADDLE fires null with
  marginal-demand flag) and §Q-9 Fallback Pre-Registration. This plan APPENDS the explicit
  straddle DECISION RULE (conservative-fail) + the Q9-fallback-deferred caveat (append-only).
  06-01 Task 3 has ALREADY appended the REPRO-01 scoped-grep re-scope note + the exact scoped-grep
  command string — this plan's leak-check Task 3 wires that SAME command byte-identically (M5).

Makefile leak-check (verified lines 161-173) currently greps ONLY: protocol-name branches,
  factory-addr literals (0x9FAb...418F, 0x116Dba...014C), magic fee tiers. NO bare-ichi layer.
  iteration-2-full does NOT exist (only a help-text stub at line 4). report-ichi (224-227) is the
  proven SOURCE_DATE_EPOCH determinism + QUARTO_PYTHON venv pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register the straddle decision rule + the Q9 fallback-deferred caveat</name>
  <specialist>Reality Checker — primary on REPRO-03's binding cost-leg gate (per ROADMAP Phase 6 agent assignment); the conservative-fail straddle rule and the honest deferral note are exactly its evidence-first charter.</specialist>
  <read_first>
    - notes/PRE_REGISTRATION.md (§REPRO-03 Threshold ~line 97; §Q-9 Fallback Pre-Registration ~line 109; §Pre-Registration Discipline ~line 158; AND the 06-01-Task-3 REPRO-01 scoped-grep re-scope note appended at the tail)
    - protocols/steer.toml ([protocol.repro_03_verdict] band + panel_construction + swaps_per_30d_observed)
    - notes/Q9_DECISION.md (V3-anchor-only primary + the two-condition switch trigger: sample<300 OR CI>0.4, AND cross-class permutation p>0.05)
    - .planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-CONTEXT.md (Decisions 1 + collision 3)
  </read_first>
  <action>
    APPEND (append-only — preserve all prior content, including 06-01's REPRO-01 re-scope note) two sections to `notes/PRE_REGISTRATION.md`:

    (A) `## Phase 6 — Steer cost-leg STRADDLE decision rule (REPRO-03, pre-registered BEFORE the check)`:
    - State the rule verbatim and greppable: "A STRADDLE (the primary-source band is NOT strictly ABOVE the 100k/mo Graph free-tier lower bound) -> cost-leg FAILS -> fire HEDGE-05 condition (a) null_cost." 
    - Record that the rule is applied to the EXISTING pre-committed 30k-100k band in steer.toml [protocol.repro_03_verdict] AS-IS — NO re-estimation, NO fresh enumeration to narrow the band (AF-03). Phase 6 only OBSERVES the verdict.
    - State that Steer is the intended D-08 negative control; the null firing is the validation, not a disappointment.
    - State the resolution-on-fail policy: SUBSTITUTE a replacement candidate, NOT named or executed in this milestone; the AF-03 guardrail that any future substitute MUST be pre-registered before its own data is seen.

    (B) `## Phase 6 — Q-9 unified-panel fallback: DEFERRED with signal-scope caveat (REPRO-04)`:
    - Record that the V3-anchor-only path is honored (steer.toml panel_construction="v3-anchor-only"); V3-only observed ~600 swaps/30d is 2.0x the 300-event Hawkes floor, so the sample-size switch trigger likely does NOT fire.
    - Record that the Phase-0 SC-3 unified-panel + cross-class-permutation dead-code modules were NEVER built (no analysis/src/abrigo_x402/panel/ package exists).
    - PRE-REGISTER the disposition: IF both Q9 switch-trigger conditions fire during the Steer fit, the unified-panel fallback is DEFERRED (a documented "fallback-unavailable" note), NOT authored as reactive mid-iteration code — authoring it would violate REPRO-02's empty-diff. The V3-only fit is reported with an explicit signal-scope caveat. State explicitly that with this V3-anchor-only path, ROADMAP SC-5 (the Q9 pooling test, only required IF unified mode is selected) is SKIPPED — not omitted, not failed — and `q9_pooling_test.json` is therefore absent by design.
    - This is AF-03 clean: pre-registered, admits the gap, before the fit runs.
    Carry an AF-12 OUT-OF-SCOPE line on both sections: changes NO numeric threshold, names NO substitute, authors NO Q9 module.
  </action>
  <verify>
    <automated>grep -q "STRADDLE decision rule" notes/PRE_REGISTRATION.md && grep -q "null_cost" notes/PRE_REGISTRATION.md && grep -qi "fallback.*deferred\|deferred.*fallback" notes/PRE_REGISTRATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "not strictly ABOVE\|not strictly above" notes/PRE_REGISTRATION.md` >= 1 (the conservative-fail rule, greppable)
    - `grep -c "null_cost" notes/PRE_REGISTRATION.md` >= 1
    - `grep -ciE "fallback.*deferred|deferred.*fallback" notes/PRE_REGISTRATION.md` >= 1
    - `grep -c "v3-anchor-only\|600" notes/PRE_REGISTRATION.md` >= 1 (signal-scope caveat present)
    - `grep -c "SC-5" notes/PRE_REGISTRATION.md` >= 1 with the SKIP-with-reason note (q9_pooling_test.json absent by design)
    - Diff to PRE_REGISTRATION.md is pure addition (no prior numeric threshold altered; 06-01's REPRO-01 note preserved)
    - This commit's timestamp predates the Task-2 cost_leg_check run that emits the verdict (AF-03 — proven by git log in Plan 04)
  </acceptance_criteria>
  <done>PRE_REGISTRATION.md carries the greppable conservative-fail straddle rule + the Q9-fallback-deferred signal-scope caveat (incl. the SC-5 SKIP-with-reason), both pre-registered before any verdict/fit.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/cost_leg_check.py + test — applies the straddle rule, writes verdict: FAIL</name>
  <specialist>DevOps Automator — owns scripts/-level glue + Makefile wiring that runs OUTSIDE the frozen source tree; the cost-leg first-step tool is precisely this CI-shaped glue.</specialist>
  <read_first>
    - analysis/src/abrigo_x402/hedge/null_result.py lines 42-62 (_parse_cost_leg_bound_verdict — the frontmatter contract this file MUST satisfy)
    - protocols/steer.toml [protocol.repro_03_verdict] (the band to read; do NOT re-estimate)
    - notes/PRE_REGISTRATION.md (the straddle rule from Task 1 — the tool implements it, does not redefine it)
    - scripts/ (existing layout; e.g. scripts/lint_artifacts.py for the in-repo script style)
  </read_first>
  <action>
    Create `scripts/cost_leg_check.py` (pure stdlib + tomllib; no analysis/src import so it stays outside the frozen tree):
    - `--protocol <path>` (default protocols/steer.toml) and `--out <path>` (default notes/steer_cost_leg_bound.md).
    - Read `[protocol.repro_03_verdict]` -> `result`, `celo_attributable_queries_per_mo_lower_bound`, `..._upper_bound`, `demand_window_lower_bound_queries_per_mo`.
    - Apply the PRE-REGISTERED rule: verdict = "FAIL" unless the band is STRICTLY ABOVE the demand-window lower bound (i.e. `lower_bound > demand_window_lower_bound`). A STRADDLE (band includes or sits below 100k) -> FAIL. Do NOT re-derive the band; read it verbatim.
    - Write `--out` with YAML frontmatter EXACTLY matching the parser contract: first lines `---\nverdict: FAIL\nflag: marginal-demand\nfiring_condition: null_cost\nband_lower: 30000\nband_upper: 100000\nfree_tier_ceiling: 100000\nrule: "not strictly > 100k -> FAIL (pre-registered, AF-03)"\n---` followed by a body documenting the STRADDLE provenance copied from steer.toml (channel sources + synthesis), the pre-registration reference, and the "substitute pending (future milestone)" disposition.
    Create `scripts/test_cost_leg_check.py` (pytest): run the tool against protocols/steer.toml into a tmp path, then assert (1) `_parse_cost_leg_bound_verdict(out)` returns "FAIL" (import the parser from abrigo_x402.hedge.null_result for the contract check — test-only import is fine, it does not make the script depend on analysis/src), (2) the frontmatter contains `firing_condition: null_cost`, (3) a strictly-above synthetic band (lower=120000) yields "PASS" (rule symmetry), (4) the output begins with `---` on line 1 (regex-match safe).
    RUN the tool to actually emit `notes/steer_cost_leg_bound.md` (verdict: FAIL) — this is the REPRO-03 first-step artifact consumed by Plan 03.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && python scripts/cost_leg_check.py --protocol protocols/steer.toml --out notes/steer_cost_leg_bound.md && cd analysis && OMP_NUM_THREADS=1 uv run pytest ../scripts/test_cost_leg_check.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `head -3 notes/steer_cost_leg_bound.md` shows `---` then `verdict: FAIL`
    - `grep -c "firing_condition: null_cost" notes/steer_cost_leg_bound.md` >= 1
    - `python -c "from pathlib import Path; import sys; sys.path.insert(0,'analysis/src'); from abrigo_x402.hedge.null_result import _parse_cost_leg_bound_verdict as p; print(p(Path('notes/steer_cost_leg_bound.md')))"` prints `FAIL`
    - `scripts/test_cost_leg_check.py` passes (incl. the strictly-above->PASS symmetry case)
    - `grep -rn "import abrigo_x402\|from abrigo_x402" scripts/cost_leg_check.py | wc -l` == 0 (the script itself does not import frozen source -> stays outside the diff window)
  </acceptance_criteria>
  <done>scripts/cost_leg_check.py applies the pre-registered straddle rule to the verbatim band and emits notes/steer_cost_leg_bound.md (verdict: FAIL, null_cost); the parser-contract test passes.</done>
</task>

<task type="auto">
  <name>Task 3: make iteration-2-full (deterministic recipe) + EXACT scoped-ichi leak-check layer + drop --execute-param</name>
  <specialist>DevOps Automator — Makefile target authoring + the leak-check gate extension are this agent's core CI-shaped-glue charter.</specialist>
  <read_first>
    - Makefile (leak-check lines 161-173; render-null-result-pdf lines 196-203 with --execute-param; report-ichi lines 212-230 + verify-reproducibility for the SOURCE_DATE_EPOCH/QUARTO_PYTHON determinism pattern)
    - notes/PRE_REGISTRATION.md (the 06-01-Task-3 REPRO-01 scoped-grep note — the leak-check ichi layer MUST be byte-identical to the command string recorded there; M5)
    - scripts/cost_leg_check.py (Task 2 — invoked FIRST by iteration-2-full)
    - 06-CONTEXT.md Decision 3 (full pipeline runs regardless; iteration-2-full wires cost-leg-check FIRST then the pipeline)
    - .planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-03-PLAN.md interfaces (the exact fetch/materialize/fit/hedge command surface iteration-2-full sequences)
  </read_first>
  <action>
    Edit `Makefile`:

    (A) M4 — ADD a DETERMINISTIC `iteration-2-full` target (NOT "echoes OR invokes"). Add `iteration-2-full` to the `.PHONY` list (line 6-9). The recipe runs literal commands in this exact order, with the Pattern-I BLAS env prefix on every Python invocation:
    ```
    BLAS = OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1
    iteration-2-full:
    	@echo "iteration-2-full: STEP 1 — cost-leg check FIRST (REPRO-03 first-step, AF-03 ordering)"
    	python scripts/cost_leg_check.py --protocol protocols/steer.toml --out notes/steer_cost_leg_bound.md
    	@echo "iteration-2-full: STEP 2 — fetch Steer cCOP/USDT V3 anchor pool"
    	pnpm -C fetch fetch steer --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --from $$STEER_FROM --to $$STEER_TO
    	@echo "iteration-2-full: STEP 3 — materialize panel (-> data/raw/steer/)"
    	cd analysis && $(BLAS) uv run python -m abrigo_x402.cli materialize --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --from-block $$STEER_FROM --to-block $$STEER_TO --protocol-toml ../protocols/steer.toml
    	@echo "iteration-2-full: STEP 4 — fit (-> data/fits/steer/)"
    	cd analysis && $(BLAS) uv run python -m abrigo_x402.cli fit --pool 0x2AC5baA668A8A58FD0e302B9896717484fd217B0 --panel-path ../data/raw/steer/0x2AC5baA668A8A58FD0e302B9896717484fd217B0/$$STEER_RANGE.parquet --out-dir ../data/fits/steer
    	@echo "iteration-2-full: STEP 5 — hedge + render null PDF (null_cost from inside the run)"
    	cd analysis && $(BLAS) uv run python -m abrigo_x402.cli hedge --run-id $$STEER_RUN --stage all --run-dir-root ../data/fits/steer --cost-leg-bound ../notes/steer_cost_leg_bound.md --reports-pdf ../reports/steer_null_result.pdf
    ```
    Define `BLAS` once near the top of the Makefile (or inline the four vars on each Python line if a top-level var is undesirable). Document in a comment that the cost-leg check runs BEFORE any Steer fetch (AF-03 ordering) and that the block-range / run-id env vars (STEER_FROM/STEER_TO/STEER_RANGE/STEER_RUN) are supplied by the operator at invocation (Plan 03 records the concrete values). The recipe is a deterministic command sequence — it does NOT branch on env availability.

    (B) M5 — EXTEND `leak-check` with the scoped `ichi` layer. The command MUST be BYTE-IDENTICAL to the string 06-01 Task 3 recorded in PRE_REGISTRATION.md. "Exclude docstrings" is not line-grep-expressible, so use a concrete, tested command with an EXPLICIT allowlist of the remaining permitted-comment/CLI-default files. Concretely, after the Plan 01 scrub, run (and this exact string is the one pinned in PRE_REGISTRATION.md):
    ```
    grep -rniE 'ichi' fetch/src analysis/src \
      | grep -vE '<EXPLICIT_ALLOWLIST>'
    ```
    where `<EXPLICIT_ALLOWLIST>` enumerates the exact remaining lines/files that legitimately mention ichi as the iteration-1 worked example or the CLI-overridable `data/fits/ichi` default (the executor determines the allowlist from the post-scrub `grep -rniE ichi fetch/src analysis/src` output, then pins BOTH the grep and the allowlist verbatim into PRE_REGISTRATION.md via a coordinated edit, so the note and the recipe agree). The layer EXITS 1 with the offending lines if ANY genuine functional `ichi` coupling remains; else continue to the existing protocol-name/addr/fee checks. Both checks must pass.

    (C) UPDATE `render-null-result-pdf` to DROP `--execute-param` and instead use `-M firing_condition:$$FIRING` (matching the Plan 01 renderer fix), rendering from repo root, mirroring `report-ichi`'s `SOURCE_DATE_EPOCH` + `QUARTO_PYTHON` determinism.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && make leak-check && grep -q "iteration-2-full:" Makefile && ! grep -q "execute-param" Makefile && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "iteration-2-full:" Makefile` >= 1 and `iteration-2-full` is in the `.PHONY` list
    - The `iteration-2-full` recipe is a deterministic command sequence (NOT "echo OR invoke") that runs `scripts/cost_leg_check.py` as STEP 1 BEFORE any fetch/materialize/fit/hedge step, with the Pattern-I BLAS prefix on every Python line
    - `make leak-check` exits 0 (post Plan-01 scrub) AND the recipe contains the EXACT scoped `ichi` grep + allowlist byte-identical to the PRE_REGISTRATION.md command string (M5); `diff <(grep-extract from Makefile) <(grep-extract from PRE_REGISTRATION.md)` is empty
    - `grep -c "execute-param" Makefile` == 0 and `grep -cE -- "-M.*firing_condition" Makefile` >= 1
    - `make render-null-result-pdf FIRING=null_cost` renders a >5KB PDF (quarto present) OR fails only on a missing-quarto guard
  </acceptance_criteria>
  <done>make iteration-2-full is a deterministic cost-leg-check-first recipe; make leak-check carries the scoped-ichi layer byte-identical to the pre-reg command; render-null-result-pdf uses -M (no papermill).</done>
</task>

</tasks>

<verification>
- `python scripts/cost_leg_check.py --protocol protocols/steer.toml --out notes/steer_cost_leg_bound.md` writes verdict: FAIL; `_parse_cost_leg_bound_verdict` returns FAIL
- `pytest scripts/test_cost_leg_check.py` green (FAIL + the strictly-above->PASS symmetry case)
- `make leak-check` exit 0 with the EXACT scoped-ichi layer (byte-identical to PRE_REGISTRATION.md); `make iteration-2-full` is a deterministic recipe running the cost-leg check first
- `make render-null-result-pdf FIRING=null_cost` uses -M (no papermill), renders >5KB
- PRE_REGISTRATION.md straddle rule + Q9-fallback-deferred (incl. SC-5 SKIP-with-reason) sections committed BEFORE the verdict (AF-03 ordering)
- `git diff -- fetch/src analysis/src` is EMPTY for this plan (only notes/, scripts/, Makefile touched)
- Portability note: system grep is ugrep 7.5.0 (GNU-compatible for the -rniE/-vE flags used in the scoped layer); executors run the recipe greps via the same shell.
</verification>

<success_criteria>
- The pre-registered conservative-fail straddle rule is recorded before the verdict; null_cost fires from the pre-committed band with NO re-estimation
- scripts/cost_leg_check.py (outside frozen dirs) emits notes/steer_cost_leg_bound.md verdict: FAIL consumed by decide_firing_condition -> null_cost
- make iteration-2-full (deterministic, cost-leg-check-first) + scoped leak-check layer + -M renderer target all wired
- Q9 unified-panel fallback pre-registered as DEFERRED with signal-scope caveat (REPRO-04 honored on the V3-anchor-only path; SC-5 SKIP-with-reason recorded)
- ZERO edits to fetch/src or analysis/src in this plan
</success_criteria>

<out_of_scope>
AF-12 OUT-OF-SCOPE (verbatim — do NOT do any of these in this plan):
- NO κ complexity index; NO closed-form f(κ).
- NO new firing conditions (only WIRES the existing null_cost path).
- NO substitute-candidate naming or execution (future milestone, AF-03).
- NO Q9 unified-panel / cross-class-permutation module authoring (pre-register deferral instead).
- NO verdict narrowing / relabel of the null as near-miss/partial-positive.
- NO re-estimation of the Steer demand band (read the pre-committed steer.toml band verbatim; OBSERVE only).
- NO edits to fetch/src or analysis/src (the cost-leg check lives in scripts/ + Makefile to preserve REPRO-02).
- NO numeric-threshold revision in PRE_REGISTRATION.md.
- NO _schema.toml edit.
- NO Steer fetch or pipeline run here (that is Plan 03 — iteration-2-full is the recipe, executed in Plan 03).
- NO CI wiring (no CI exists; enforcement is pre-commit/manual — explicitly Deferred).
- NO USDC substituted for USDT.
- NO push to upstream; pushes (when asked) go to origin only.
</out_of_scope>

<output>
After completion, create `.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-02-SUMMARY.md` recording: the straddle-rule + Q9-deferral (incl. SC-5 SKIP-with-reason) pre-reg text, the cost_leg_check.py contract + the emitted verdict: FAIL doc, the deterministic iteration-2-full + scoped-leak-check (byte-identical command) + -M renderer Makefile wiring, and confirmation that git diff fetch/src analysis/src is empty for this plan. Note that system grep is ugrep 7.5.0 (GNU-compatible).
</output>
