---
phase: 06-iteration-2-swap-surface-validation-on-steer-ccop-usdt
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - Makefile
  - reports/MANIFEST.md
  - .planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VERIFICATION-pre.md
autonomous: true
requirements: [REPRO-01, REPRO-02, REPRO-03, REPRO-04, HEDGE-05]
must_haves:
  truths:
    - "git diff $(cat _artifacts/repro_02_baseline_sha.txt) HEAD -- fetch/src analysis/src is EMPTY across the iteration boundary (REPRO-02); the diff base is read from the sha file, not grepped from a SUMMARY"
    - "verify-reproducibility content-checks reports/steer_null_result.pdf (size + null_cost + HEDGE05 + the shared 5-string AF-03 forbidden-narrowing guard)"
    - "AF-03 commit-ordering is proven: the straddle rule + REPRO-01 re-scope notes predate the verdict + run commits"
    - "06-VERIFICATION-pre.md maps REPRO-01..04 + HEDGE-05(a) + SC-1..5 to commands + verdicts; verification_pass is CONDITIONAL (true only if PDF>50KB AND null_cost observed AND empty-diff holds; pending-fetch otherwise), with the null recorded AS-OBSERVED"
  artifacts:
    - path: ".planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VERIFICATION-pre.md"
      provides: "Phase 6 acceptance grid + REPRO-02 empty-diff attestation"
      contains: "REPRO-02"
    - path: "Makefile"
      provides: "steer PDF content-check in verify-reproducibility"
      contains: "steer_null_result"
    - path: "_artifacts/repro_02_baseline_sha.txt"
      provides: "Read by Task 1 as the REPRO-02 diff base (written by Plan 01 Task 3)"
      contains: ""
  key_links:
    - from: "_artifacts/repro_02_baseline_sha.txt -> git diff $BASE HEAD"
      to: "fetch/src + analysis/src"
      via: "empty-diff attestation reading the post-Plan-01 baseline sha from the pinned file"
      pattern: "fetch/src"
    - from: "Makefile verify-reproducibility"
      to: "reports/steer_null_result.pdf"
      via: "size + null_cost + HEDGE05 + forbidden-narrowing content-check"
      pattern: "steer_null_result"
---

<objective>
Attest the REPRO-02 empty-diff invariant, extend `verify-reproducibility` to content-check `reports/steer_null_result.pdf`, and author the Phase 6 acceptance gate (`06-VERIFICATION-pre.md`) mapping REPRO-01..04 + HEDGE-05(a) to commands + verdicts — with the null recorded AS-OBSERVED (never narrowed to a pass).

Purpose: This is the honest-pass gate. REPRO-02 must show an EMPTY `git diff` between the iteration-1-complete baseline (the post-baseline-fix HEAD declared by Plan 01, anchored to the PR#1 merge `87991ac` iteration-1-complete marker) and the iteration-2-complete HEAD. The AF-03 commit-ordering (rule predates verdict) must be proven by git log. A dressed-up pass (frozen-dir edits inside the window) is forbidden — this plan catches it.
Output: a REPRO-02 empty-diff attestation, the steer PDF content-check, and `06-VERIFICATION-pre.md` with the full acceptance grid.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-CONTEXT.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-RESEARCH.md
@.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VALIDATION.md
@.planning/phases/04-cross-leg-dependence-l5-falsification-carr-madan-strip-l6/04-VERIFICATION-pre.md

<interfaces>
<!-- Verified on HEAD (2026-05-29). -->

# Iteration-1-complete marker (REPRO-02 baseline anchor):
#   PR#1 merge 87991ac (iteration-1-complete per CLAUDE.md cycle-closure).
#   Last frozen-dir touch before Phase 6 baseline fixes: b68352e (feat 05-02 spot_check).
#   The Plan 01 baseline-fix commits (renderer/-M, materialize namespace, scrub) land AFTER b68352e;
#   Plan 01 Task 3 writes the exact post-baseline-fix HEAD sha (single 40-char line) to
#   _artifacts/repro_02_baseline_sha.txt -> that is the REPRO-02 baseline. READ THAT FILE DIRECTLY (M3):
#     BASE=$(cat _artifacts/repro_02_baseline_sha.txt)   # do NOT grep-the-SUMMARY for a hex token
#   (Confirmed by DevOps review: the diff base is the post-Plan-01 sha, NOT 87991ac. 87991ac..HEAD is
#    EMPTY today and goes NON-empty only after Plan 01's source edits land; so the base MUST be the
#    post-Plan-01 sha for the empty-diff to mean "no edits during the iteration-2 window".)
#   Attestation command: git diff "$BASE" HEAD -- fetch/src analysis/src  (expect EMPTY)

# verify-reproducibility PDF content-check (Makefile lines 82-97) — the ichi pattern to mirror:
#   size > 51200; pdftotext greps firing_condition string + verdict; pdfinfo greps HEDGE05;
#   AF-03 forbidden-narrowing loop greps for "near-miss positive"/"directionally positive"/etc.
#   The steer check mirrors this with the firing string "null_cost".

# 04-VERIFICATION-pre.md is the structural template for the acceptance grid (frontmatter
#   verification_pass + an N-row grid mapping requirement IDs -> command -> exit code -> verdict).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REPRO-02 empty-diff attestation + AF-03 commit-ordering proof</name>
  <specialist>Model QA Specialist — verifies the re-run is byte-equivalent on the pipeline side and that Steer's cCOP results aren't contaminated by ICHI's priors (ROADMAP Phase 6 sub-task audit assignment); the empty-diff + ordering proof is its charter.</specialist>
  <read_first>
    - _artifacts/repro_02_baseline_sha.txt (the SINGLE 40-char post-Plan-01 HEAD sha = the REPRO-02 baseline; READ THIS FILE, M3 — do NOT grep 06-01-SUMMARY.md for a hex token)
    - .planning/phases/06-.../06-01-SUMMARY.md (context: which Plan 01 commits produced that sha)
    - .planning/phases/06-.../06-02-SUMMARY.md + 06-03-SUMMARY.md (the run + verdict commit shas)
    - notes/PRE_REGISTRATION.md (the straddle rule + REPRO-01 re-scope notes whose ordering must be proven)
    - CLAUDE.md (the 87991ac iteration-1-complete marker + push origin / PR upstream discipline)
  </read_first>
  <action>
    Produce the REPRO-02 attestation:
    1. Determine the REPRO-02 baseline sha by reading the file Plan 01 Task 3 wrote (M3): `BASE=$(cat _artifacts/repro_02_baseline_sha.txt)`. This is the single 40-char post-Plan-01 HEAD sha — do NOT grep 06-01-SUMMARY.md for a hex token (that grabs the wrong token). Run `git diff "$BASE" HEAD -- fetch/src analysis/src` and confirm it is EMPTY. If NON-empty, the iteration-2 window illegally edited frozen source -> STOP and record a BLOCKER (do NOT narrow the verdict).
    2. Prove AF-03 commit-ordering with `git log --oneline -- notes/PRE_REGISTRATION.md notes/steer_cost_leg_bound.md`: the straddle-rule pre-reg commit (Plan 02 Task 1) and the REPRO-01 re-scope note (Plan 01 Task 3) MUST predate the cost-leg verdict emission (Plan 02 Task 2) and the Steer run/hedge commits (Plan 03). Record the sha ordering.
    3. Confirm REPRO-01: `make leak-check` exit 0 (scoped-ichi layer) + `cd fetch && pnpm test protocol-agnostic` green; record the scoped-grep output.
    Write the attestation block (baseline sha, the empty-diff command + result, the ordering proof) into a `_artifacts/repro_02_attestation.txt` for the VERIFICATION grid to cite.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && BASE=$(cat _artifacts/repro_02_baseline_sha.txt) && git diff --quiet "$BASE" HEAD -- fetch/src analysis/src && make leak-check && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git diff <baseline-sha> HEAD -- fetch/src analysis/src` is EMPTY (REPRO-02 honest pass)
    - `git log` shows the straddle-rule + REPRO-01-re-scope commits predate the verdict + run commits (AF-03 ordering)
    - `make leak-check` exit 0 + `cd fetch && pnpm test protocol-agnostic` green (REPRO-01 two-layer gate)
    - `BASE=$(cat _artifacts/repro_02_baseline_sha.txt)` is a single 40-char sha matching `^[0-9a-f]{40}$` (M3 — the diff base is read from the file, not grepped from a SUMMARY)
    - `_artifacts/repro_02_attestation.txt` records the baseline sha + the empty-diff command/result + the ordering proof
  </acceptance_criteria>
  <done>The REPRO-02 empty-diff is attested from the post-baseline-fix HEAD; AF-03 ordering proven; REPRO-01 two-layer gate green; attestation written.</done>
</task>

<task type="auto">
  <name>Task 2: Extend verify-reproducibility with the steer PDF content-check + MANIFEST entry</name>
  <specialist>DevOps Automator — the verify-reproducibility content-check + MANIFEST extension is the reproducibility-gate glue this agent owns.</specialist>
  <read_first>
    - Makefile lines 66-97 (verify-reproducibility — the ichi PDF content-check to mirror)
    - reports/MANIFEST.md (the Iteration-1 manifest to extend with the steer PDF entry)
    - reports/steer_null_result.pdf (the artifact to content-check)
    - 06-CONTEXT.md Decision 4 (depth: cost-leg headline + DGP support + attestation)
  </read_first>
  <action>
    Extend the `Makefile :: verify-reproducibility` target so that when `reports/steer_null_result.pdf` exists it is content-checked, mirroring the ichi pattern: size > 51200 bytes; `pdftotext` greps for the `null_cost` string AND a cost-leg/STRADDLE evidence string; `pdfinfo` greps for the `HEDGE05` marker; and the SAME AF-03 forbidden-narrowing loop (`"near-miss positive"`, `"directionally positive"`, `"pass with caveat"`, `"exploratory positive"`, `"positive result"`) must find NOTHING. Keep the ichi PDF check intact; add the steer check additively (the Makefile target may skip-gracefully if the steer PDF is absent, like the ichi PENDING path — this is for iteration-1-only checkouts ONLY). NOTE (M2): a skip in the Makefile target does NOT mean the PHASE passes — Task 3 sets `verification_pass: true` only when the steer PDF actually exists+>50KB+null_cost+empty-diff; absence => `pending-fetch`. Preserve `SOURCE_DATE_EPOCH` determinism semantics.
    Add a `reports/steer_null_result.pdf` row to `reports/MANIFEST.md` (content-checked, not byte-pinned — PDFs vary by /Producer; mirror the ichi PDF manifest treatment).
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && make verify-reproducibility && grep -q "steer_null_result" Makefile && grep -q "steer_null_result" reports/MANIFEST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "steer_null_result" Makefile` >= 1 (steer content-check wired into verify-reproducibility)
    - The steer check greps `null_cost` + the HEDGE05 marker + runs the AF-03 forbidden-narrowing loop
    - `grep -c "steer_null_result" reports/MANIFEST.md` >= 1
    - `make verify-reproducibility` exits 0 (both ichi PENDING/OK + steer content-check pass)
  </acceptance_criteria>
  <done>verify-reproducibility content-checks the steer null PDF (size + null_cost + HEDGE05 + AF-03 no-narrowing); MANIFEST carries the steer PDF entry.</done>
</task>

<task type="auto">
  <name>Task 3: Author 06-VERIFICATION-pre.md acceptance grid (null AS-OBSERVED)</name>
  <specialist>Model QA Specialist — the phase acceptance grid mapping requirements to evidence is the model/spec-audit charter; pairs with the Code Reviewer on the PLAN-side review per ROADMAP Phase 6.</specialist>
  <read_first>
    - .planning/phases/04-.../04-VERIFICATION-pre.md (structural template: frontmatter + acceptance grid)
    - .planning/REQUIREMENTS.md (REPRO-01..04 + HEDGE-05 acceptance wording)
    - .planning/ROADMAP.md §Phase 6 SC-1..5 (the success criteria to map)
    - _artifacts/repro_02_attestation.txt (Task 1) + reports/steer_null_result.pdf + data/fits/steer/<run_id>/firing_condition.json
  </read_first>
  <action>
    Author `.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VERIFICATION-pre.md` with:
    - Frontmatter — `verification_pass` is CONDITIONAL (M2 — do NOT pre-bake true). Set `verification_pass: true` ONLY IF ALL of the following are observed THIS session:
        (1) `reports/steer_null_result.pdf` exists AND `wc -c` > 51200 (>50KB);
        (2) `null_cost` is observed in `data/fits/steer/<run_id>/firing_condition.json` AND in the in-session `make verify-reproducibility` steer content-check report;
        (3) the REPRO-02 empty-diff holds (`git diff "$BASE" HEAD -- fetch/src analysis/src` empty, BASE from _artifacts/repro_02_baseline_sha.txt).
      IF the Steer fetch was deferred/blocked (Plan 03 Task 3 offline contingency) OR the PDF is absent/<=50KB OR the empty-diff does not hold, set `verification_pass: false` and `verdict: pending-fetch` (NOT pass) — record honestly which gate is unmet. Do NOT "skip gracefully if absent" and still pass.
      Other frontmatter: `created: 2026-05-29` (or run date), `firing_condition: null_cost`, `repro_02_empty_diff: <observed bool>`, `iteration_1_complete_marker: "87991ac"`, `repro_02_baseline_sha: <the sha from _artifacts/repro_02_baseline_sha.txt>`, `quarto_skipped: false` (quarto present).
    - An acceptance grid (rows) mapping each of REPRO-01, REPRO-02, REPRO-03, REPRO-04, HEDGE-05(a) AND ROADMAP SC-1..5 to: the automated command + exit code + verdict (PASS / SKIP-with-reason / pending-fetch). Include: REPRO-01 -> `make leak-check` + `pnpm test protocol-agnostic` + the AF-12 scoped-grep note; REPRO-02 -> the empty-diff attestation (cite repro_02_attestation.txt); REPRO-03 -> the straddle rule predates the verdict (git-log ordering) + verdict: FAIL doc; REPRO-04 -> panel_construction=v3-anchor-only honored + Q9 trigger evaluated + fallback-deferred note; HEDGE-05(a) -> firing_condition.json null_cost + PDF content-check.
    - The SC-5 grid row (minor) MUST be recorded EXPLICITLY as: `SKIP — V3-anchor-only (panel_construction=v3-anchor-only); unified fallback pre-registered deferred; q9_pooling_test.json absent; no REPRO-02 violation` — NOT PASS, NOT omitted.
    - A section recording the verdict AS-OBSERVED: Steer cost-leg STRADDLE 30k-100k -> null_cost; this is the intended D-08 negative control; NOT narrowed to a pass. State the "substitute pending (future milestone)" disposition + the AF-03 future-substitute guardrail. The forbidden-narrowing string set (M7, shared 5): "pass with caveat", "near-miss positive", "directionally positive", "exploratory positive", "positive result" — none may appear describing this verdict.
    - A REPRO-02 attestation section quoting the empty-diff command + result + the baseline (post-Plan-01) sha + the 87991ac iteration-1-complete marker.
    Do NOT mark any row PASS without a runnable command + observed exit. If any row fails or is unmet, record it honestly (do NOT flip the verdict).
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/d2p/abrigo/abrigo-x402 && test -f .planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VERIFICATION-pre.md && grep -cE "REPRO-0[1-4]|HEDGE-05" .planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-VERIFICATION-pre.md | python3 -c "import sys; sys.exit(0 if int(sys.stdin.read().strip())>=5 else 1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION-pre.md` exists; `verification_pass` is true ONLY IF the three M2 gates (PDF >50KB + null_cost observed in firing_condition.json AND the in-session gate report + empty-diff) all hold; otherwise `verification_pass: false` with `verdict: pending-fetch`
    - B3 (coverage gate fails explicitly, not a silent no-op): the >= 5-distinct-ID check uses `python3 -c "...sys.exit(0 if int(...)>=5 else 1)"` (or `| { read n; [ "$n" -ge 5 ]; }`) — NOT `| awk '$1>=5'` (which exits 0 even when false)
    - The grid maps ALL of REPRO-01, REPRO-02, REPRO-03, REPRO-04, HEDGE-05 (>= 5 distinct ID mentions) to commands + verdicts
    - The grid maps ROADMAP SC-1..4 with PASS or SKIP-with-reason AND the SC-5 row recorded VERBATIM as the SKIP-with-reason string above (minor)
    - The verdict is recorded AS-OBSERVED (null_cost; D-08 negative control) with NONE of the shared 5 forbidden-narrowing strings (M7)
    - The REPRO-02 attestation section quotes the empty-diff command + the post-Plan-01 baseline sha + the 87991ac marker
  </acceptance_criteria>
  <done>06-VERIFICATION-pre.md maps REPRO-01..04 + HEDGE-05 + SC-1..5 to runnable commands + verdicts; verification_pass is CONDITIONAL on the PDF+null_cost+empty-diff gates (pending-fetch otherwise); the SC-5 row is the explicit SKIP-with-reason; the coverage gate fails explicitly (B3); the null is recorded AS-OBSERVED with no narrowing.</done>
</task>

</tasks>

<verification>
- `BASE=$(cat _artifacts/repro_02_baseline_sha.txt) && git diff "$BASE" HEAD -- fetch/src analysis/src` EMPTY (REPRO-02 honest pass; M3 base read from the pinned file)
- `make leak-check` + `cd fetch && pnpm test protocol-agnostic` green (REPRO-01 two-layer)
- `make verify-reproducibility` exit 0 with the steer PDF content-check (size + null_cost + HEDGE05 + the shared 5-string no-narrowing guard)
- `06-VERIFICATION-pre.md` exists; verification_pass is CONDITIONAL — true ONLY IF the steer PDF >50KB AND null_cost is observed (firing_condition.json + the in-session gate report) AND the empty-diff holds; otherwise verification_pass:false + verdict:pending-fetch (M2). Maps REPRO-01..04 + HEDGE-05 + SC-1..5, null AS-OBSERVED; SC-5 is the explicit SKIP-with-reason row
- The coverage gate fails EXPLICITLY (B3): the >=5-ID check is `python3 -c "...sys.exit(...)"`, NOT `awk '$1>=5'` (which is a silent no-op)
- AF-03 ordering proven: straddle rule + REPRO-01 re-scope predate the verdict + run commits
- Portability note: system grep is ugrep 7.5.0 (GNU-compatible for the flags used); executors run the acceptance greps via the same shell
</verification>

<success_criteria>
- REPRO-02 empty-diff attested from the post-Plan-01 baseline sha (read from _artifacts/repro_02_baseline_sha.txt; honest, not dressed-up)
- verify-reproducibility content-checks the steer null PDF with the shared 5-string AF-03 forbidden-narrowing guard
- The Phase 6 acceptance grid records the null_cost verdict AS-OBSERVED as the D-08 negative control; verification_pass is CONDITIONAL (pending-fetch if the PDF/null_cost/empty-diff gates are unmet) — never pre-baked
- The coverage gate is an explicit-fail test (B3), not a silent awk no-op
- Substitute-pending disposition + AF-03 future-substitute guardrail recorded; no substitute named/executed
</success_criteria>

<out_of_scope>
AF-12 OUT-OF-SCOPE (verbatim — do NOT do any of these in this plan):
- NO κ complexity index; NO closed-form f(κ).
- NO new firing conditions.
- NO substitute-candidate naming or execution (record disposition only; future milestone, AF-03).
- NO Q9 unified-panel / cross-class-permutation module authoring.
- NO verdict narrowing / relabel of the null as near-miss/partial-positive (the grid records AS-OBSERVED).
- NO edits to fetch/src or analysis/src (this plan touches only Makefile, reports/, .planning/).
- NO re-estimation of the Steer demand band.
- NO _schema.toml edit; NO PANEL-02 schema bump.
- NO USDC substituted for USDT.
- NO push to upstream from inside the plan; the cycle-closure push origin / PR-to-upstream is a USER-gated step after verification passes (CLAUDE.md cycle-closure ritual).
</out_of_scope>

<output>
After completion, create `.planning/phases/06-iteration-2-swap-surface-validation-on-steer-ccop-usdt/06-04-SUMMARY.md` recording: the REPRO-02 baseline sha + empty-diff result, the AF-03 ordering proof, the verify-reproducibility steer-PDF extension, the 06-VERIFICATION-pre.md grid outcome (all PASS, null AS-OBSERVED), and the cycle-closure next-step (USER-gated push origin -> PR upstream -> merge upstream after verification passes).
</output>
